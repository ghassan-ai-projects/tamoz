# Candidates — rank 10 gems/tamoz-agent-session/lib/tamoz/agent/session.rb
LOC: 803  Verdict: CANDIDATES
## C1: step-down the 111-line constructor
- where: `Tamoz::Agent::Session#initialize` (~lines 66–176)
- principle: 2/3/5 (short, one thing, step-down); §4 "one responsibility per method"
- evidence: one method does input validation (87–103), source/profile gating (105, 124),
  routing→version selection (119–123), and full graph assembly (125–174) before `freeze`.
- proposal: extract private `validate_session_options!(model:, max_plan_attempts:, max_repair_attempts:, model_call_safety:, routing:)` (lines 87–98, error messages byte-identical; keep the durable-checkpointer `ConfigurationError` check inline — it is the boundary assertion), and private `build_definitions(node_arguments)` returning the frozen hash now literal at 153–170; `initialize` keeps assignment order, `@apps = @definitions.transform_values {...}.freeze`, and `freeze` exactly where they are. Residual length is the 21-kwarg signature plus data plumbing — the cohesive core, not a split target.
- risk: medium
- within_file: yes
## C2: outcome reads state twice over; share view's status rule
- where: `Tamoz::Agent::Session#outcome` (~lines 734–779) vs `#view` (line 639)
- principle: 2 (46 lines > ceiling), 5 (step-down); §4 one responsibility
- evidence: `status = if blocked ... elsif snapshot.status == :paused ... else snapshot.status` (754–765) is provably `blocked ? :blocked : snapshot.status` — exactly what `view` already writes at 639.
- proposal: extract private `verification_result(state)` (the `Result.new` block, 741–752) and private `lifecycle_status(status, blocked:)` used by both `view` and `outcome`. Pure read-side presentation; no durable write touched.
- risk: low
- within_file: yes
## C3: dedupe the durable effect-writer boilerplate
- where: `Tamoz::Agent::Session#resolve_effect` (~661–678) and `#effect` (~680–690)
- principle: 2 (duplication), 5 (step-down); §4
- evidence: both open the writer identically: `store.open_writer(thread_id: thread, namespace:, owner_id: owner_id || SecureRandom.uuid, ttl: store.writer_ttl) { |writer| ... writer.effects ... }`.
- proposal: extract private `with_effect_writer(thread, namespace:, owner_id:)` that opens the writer and yields `writer.effects`; call sites keep `effects.resolve(...)` / `effects.fetch(...)`. Routing through the durable effect journal is preserved verbatim — same `open_writer` semantics, ttl, owner minting, and ordering; nothing bypasses or reorders the journal.
- risk: low
- within_file: yes
## C4: one tail for start/resume/continue
- where: `Tamoz::Agent::Session#start` (~577), `#resume` (~589), `#continue` (~603)
- principle: 2 (triplicated delivery tail), 5; §4
- evidence: each ends `run_context = build_run_context(context:, emitter:); runner_for(thread).deliver(..., owner_id: owner_id || SecureRandom.uuid, context: run_context); outcome(thread:, request_id:)`.
- proposal: extract private `deliver_turn(payload, thread:, request_id:, operation: nil, owner_id:, emitter:, context:)` returning the outcome; the public methods keep their exact signatures and become guard + one call. `recover` keeps its own path (different runner verb). Name avoids `dispatch` (collides with the adaptive_dispatch graph node).
- risk: low
- within_file: yes
## C5: rename verify_mcp_source! to validate_mcp_source!
- where: `Tamoz::Agent::Session#verify_mcp_source!` (~180–194, sole call site 105)
- principle: §3.1 vocabulary — the check is `required.reject { |method| mcp.respond_to?(method) }` raising `ArgumentError, "mcp source must respond to #{missing}"`: a shape/schema check with bad input named by field = `validate_*`, not `verify_*` (evidence-vs-tamper).
- proposal: rename the private method to `validate_mcp_source!`; message bytes unchanged. Grep across gems/ and test/: only in-file references (105/180/194). Note: two non-normative prose mentions in `docs/repo-quality-audit-2026-08-20/incorrect-existing-usage.md` go stale.
- risk: low
- within_file: yes
## C6: routing→graph-version case becomes a frozen table
- where: `Tamoz::Agent::Session#initialize` routing case (~119–123)
- principle: owner directive "simple solution"; §3 constants over inline case
- evidence: `case routing.to_sym when :experimental then CURRENT when :adaptive then ADAPTIVE else COMPACTION end`.
- proposal: `GRAPH_VERSION_BY_ROUTING = { legacy: COMPACTION, experimental: CURRENT, adaptive: ADAPTIVE }.freeze` next to `ROUTINGS`; assign via `.fetch(routing.to_sym)`. Equivalent because line 98 already validated membership.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `Session.build_definition` (~420–575, 156 lines) is documented public surface (gem README) yet only called internally; §6 tolerates long declarative registries, so no split proposed — but narrowing its visibility is a public-surface (compatibility) decision for the owner. Related known note: review L8 (definition compiled per instance).
- `verify_profile_binding!` (~206) is the lone raiser spelled `verify_` beside the five `enforce_*_binding!` peers doing the same compare-and-refuse shape; §3.1 favors `enforce_profile_binding!`, but the name is cited as an authority pointer in other gems' comments (`tamoz-tools/capability_host.rb:16`, `tamoz-agent-capabilities/capability_binding.rb:22`) — fixing those is a cross-gem edit, so owner call.
- Constructor takes 19 keyword parameters (Reek context-names this exact initializer); reshaping that surface is an API-compatibility decision, out of scope for behavior-preserving candidates.
