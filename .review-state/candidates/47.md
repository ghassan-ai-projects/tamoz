# Candidates — rank 47 gems/tamoz-agent-healing/lib/tamoz/agent/healing/failure_record.rb
LOC: 432  Verdict: CANDIDATES
## C1: internal-validators-are-public
- where: `FailureRecord` validator block (`validate_boolean!` … `validate_non_empty_string!`) (~lines 277–376)
- principle: standard §6 narrow public APIs; principle 4 (public surface reads as domain)
- evidence: 14 leaf validators sit ABOVE the `private` keyword at line 378 (`def validate_hash!(value, name)`, `def validate_allowed_keys!(value, allowed, name)`, `def forbid_raw_text!(value)`…), while their composed counterparts (`validate_id`, `validate_member`, `validate_optional_digest`, `validate_retryability`, `validate_trusted_context`, `validate_message_ref`) are private below it. Grep across gems/, test/, apps/: zero callers of any public leaf validator outside this file; tests exercise validation only through `new`/`from_h`.
- proposal: move the `private` divider above line 277 so all validator helpers share one visibility. No call-site change; public-API test does not pin these names.
- risk: low
- within_file: yes
## C2: single-source-never-mutate
- where: `FailureRecord#never_mutate?` / `#never_mutate_class` (~lines 141–151)
- principle: 2 (one thing); safety invariant kept in one place
- evidence: `never_mutate?` computes `NEVER_MUTATE_CATEGORIES.include?(category) || capability_absent` while `never_mutate_class` re-derives the same answer as a second precedence chain (`return :capability_absent if capability_absent; return category if NEVER_MUTATE_CATEGORIES.include?(category)`). Two hand-synced implementations of one load-bearing invariant.
- proposal: define `never_mutate?` as `!never_mutate_class.nil?`. Identical truth table; pinned by test/healing_failure_contract_test.rb:64–92. A future sixth never-mutate class then changes one method, not two.
- risk: low
- within_file: yes
## C3: forbid-off-vocabulary
- where: `FailureRecord#forbid_raw_text!` (~line 365)
- principle: standard §3.1 checking vocabulary (one concept, one verb)
- evidence: siblings in the same `validate_message_ref` pipeline are `validate_reference_keys!` and `validate_non_empty_string!`; `forbid_raw_text!` performs the same act — shape check that raises `HealingPolicyError` on bad input — under a fourth verb spelling.
- proposal: rename to `validate_no_raw_text!`. Sole caller is line 425 inside this file (grep-proven).
- risk: low
- within_file: yes
## C4: message-ref-ternary-step-down
- where: `FailureRecord#initialize` `untrusted_message_ref:` kwarg (~lines 131–133)
- principle: 3 (one level of abstraction) / 5 (step-down)
- evidence: `untrusted_message_ref: untrusted_message_ref.nil? ? nil : Tamoz::Core.deep_freeze(validate_message_ref(untrusted_message_ref))` inlines nil-guard + freeze + validate inside the `super` kwargs, while the two other composite fields read `Tamoz::Core.deep_freeze(validate_retryability(retryability))` / `…(validate_trusted_context(trusted_context))` via named composers.
- proposal: add private `def normalize_message_ref(value) = value.nil? ? nil : Tamoz::Core.deep_freeze(validate_message_ref(value))` (§3.1 normalize_* returns the canonical form); kwarg becomes `untrusted_message_ref: normalize_message_ref(untrusted_message_ref),`.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `FailureRecord.message_ref` (line 268) builds a new value, so §3.1 construction vocabulary would name it `build_message_ref` — but callers exist outside this file (test/healing_failure_contract_test.rb:111,121,124), so within_file: no and rejected here; owner may want it as a deliberate rename slice. Same for `never_mutate_class` ("class" returns a symbol label), which I deliberately did NOT propose: the noun matches P12 §3 design language and tests pin it.
- Reviewed, no action: `initialize`'s size/complexity and the 19-field signature mirror `Data.define`'s design-ordered field list verbatim and are recorded per-file exclusions in `.rubocop_todo.yml` (Metrics/*, Style/MultilineTernaryOperator); `typed_signal`/`to_h`/`fingerprint` repeat literal key lists deliberately — different projections with different key order, and `digest` hashes `to_h` bytes, so deriving one from another risks wire bytes for zero gain.
