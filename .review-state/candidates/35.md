# Candidates — rank 35 gems/tamoz-agent-session/lib/tamoz/agent/session_adaptive.rb
LOC: 520  Verdict: CANDIDATES
## C1: dispatch-monolith-not-stepped-down
- where: `SessionAdaptive#dispatch` (~line 150)
- principle: 2 (≤20 normally, hard ceiling 30 — this is ~90), 3, 5; §4 one responsibility
- evidence: four outcomes built inline — unknown/wait block (169–186), failure hand-off (188),
  budget exhaustion (195–209), pending observation (210–238); `value.fetch('truncated', false)`
  and `value.fetch('provenance', 'workspace')` each computed twice (213–214 vs 232–233);
  byte-budget arithmetic (`byteslice`, `MAX_OBSERVATION_BYTES`) sits beside effect dispatch.
- proposal: reuse the file's own `*_update` builder seam (`terminal_update`, `failed_update`,
  `handoff_update` already exist): extract `blocked_dispatch_update(state, context, intent, step,
  outcome)`, `budget_exhausted_update(action, intent, step, outcome)`,
  `observation_pending_update(state, context, action, intent, step, outcome, value)`; compute
  truncated/provenance/source_id once and pass them in. Event-construction order and every key
  must stay byte-identical.
- risk: medium
- within_file: yes
## C2: decide-action-branch-inline
- where: `SessionAdaptive#decide` (~line 71)
- principle: 5 (step-down); consistency with the class's own update-builder DSL
- evidence: the `'final'` branch delegates to `final_update`, but the `'action'` branch inlines an
  11-line literal hash (71–82); `decide` totals ~56 lines against the 30 ceiling.
- proposal: extract `accepted_action_update(decision, digest, record, model_event, prepared)`
  returning the `adaptive_action`/`adaptive_decisions`/`next_node: 'adaptive_validate'` hash merged
  with lifecycle events then compaction — same merge order as today.
- risk: low
- within_file: yes
## C3: validate-tail-not-stepped-down
- where: `SessionAdaptive#validate` (~line 119)
- principle: 2, 5
- evidence: after the guard clauses, lines 119–145 build the `accepted`/`step` literals, call
  `build_intent`, and assemble the returned hash inline; `validate` is ~54 lines.
- proposal: extract `prepared_step_update(state, context, capability_id:, arguments:, iteration:)`
  spanning plan-id construction through `next_node: 'adaptive_dispatch'`; guards remain in
  `validate`. `build_intent` arguments and `sub_operation:` values must be passed through unchanged.
- risk: medium
- within_file: yes
## C4: dead-digest-parameter
- where: `SessionAdaptive#final_update` (~line 366)
- principle: §4 (keyword args over positional lists; one responsibility), principle 1
- evidence: `def final_update(state, decision, _digest, record)` — `_digest` is never referenced in
  the body; the sole caller (line 69) passes `digest`.
- proposal: drop the parameter; call site becomes `final_update(state, decision, record)`.
- risk: low
- within_file: yes
## C5: adaptive-step-id-derived-thrice
- where: `#validate` (~lines 120, 124), `#dispatch` (~line 153), `#handoff_update` (~line 412)
- principle: 1 (a protocol fact with no name), 5; §3 naming
- evidence: `"adaptive.#{iteration}"` is constructed at three sites and matched against at the
  fourth (dispatch's intent lookup); `"adaptive.handoff.#{...}"` is a fifth derivation. Drift
  between producer and lookup strings would fail silently at dispatch.
- proposal: add `private def adaptive_step_id(iteration) = "adaptive.#{iteration}"` and use it at
  all sites (handoff: `"#{adaptive_step_id(action.fetch('iteration'))}.handoff"` — byte-identical).
- risk: low
- within_file: yes
## C6: prompt-observations-projection-mixed-level
- where: `SessionAdaptive#decision_prompt` (~line 288)
- principle: 3 (one level of abstraction)
- evidence: the `filter_map` projecting observation records into prompt fields (288–299) sits
  between capability-surface assembly and JSON rendering inside a ~36-line method.
- proposal: extract `private def adaptive_observations(state)` returning the projected array;
  `decision_prompt` then reads assemble-surface → project-evidence → compact → render.
- risk: low
- within_file: yes
## C7: duplicate-disable-rationale
- where: `SessionAdaptive` class comment (~lines 10–13)
- principle: §11 (a why-comment earns its place once; not restated)
- evidence: line 10 ends `-- graph-node methods keep each durable transition visible and ordered.`,
  repeating sentence one of the paragraph at lines 11–13 verbatim in substance.
- proposal: trim the trailing `--` reason from the disable line; keep the full paragraph.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Class-wide Metrics disable (line 10) also exempts non-node helpers (`decision_prompt` ~36,
  `failed_update` ~33) although its stated reason covers only the four graph-node methods; if
  C1–C3 land, consider narrowing the disable to those four methods so extracted builders are held
  to the normal ceilings.
- `SessionAdaptive#action_signature(capability_id, arguments)` shares its name with
  `Deliberation.action_signature(plan)` (kernel gem) but digests differently (no versioned
  prefix; `SessionRecords.digest` vs `Tamoz::Core.digest("tamoz.agent.deliberation.actions.v1…")`).
  Same concept, two spellings of its identity — changing either alters stored digests, so this is
  an owner decision, not a candidate.
