# Candidates — rank 1 gems/tamoz-sqlite/lib/tamoz/sqlite/migrator.rb
LOC: 1593  Verdict: CANDIDATES
## C1: apply-migrations-step-down
- where: `Tamoz::SQLite::Migrator#apply_migrations` (~line 1506)
- principle: 2 (≤30-line hard ceiling) + 3/5 (step-down); standard §4 one-responsibility
- evidence: body is ~40 lines mixing five levels: BEGIN EXCLUSIVE, the per-ordinal
  loop, `Transaction.new(...)` construction, statement-label scheme
  `"migration.#{ordinal}.#{index + 1}"`, the `tamoz_schema_migrations` insert, both
  PRAGMA stamps, and COMMIT/ROLLBACK — policy, orchestration and persistence in one method.
- proposal: extract private `apply_pending_migration(connection:, ordinal:)` that builds
  the Transaction, executes statements + time scalar + record row, and returns the
  transaction; `apply_migrations` keeps BEGIN EXCLUSIVE → loop → stamp → COMMIT → ROLLBACK
  rescue (~12 lines). Fault-injection labels (`migration.N.M`, `.time`, `.record`,
  `migration.application_id`, `migration.user_version`) must stay byte-identical.
- risk: medium
- within_file: yes
## C2: name-the-checksum-seam
- where: `MIGRATION_1_CHECKSUM` … `MIGRATION_21_CHECKSUM` (~lines 373–1396)
- principle: 4 (call sites read as domain) + §6 DRY hygiene
- evidence: the incantation `Digest::SHA256.hexdigest(MIGRATION_N.join("\n-- tamoz migration boundary --\n")).freeze`
  is copy-pasted 21 times; the boundary-separator literal exists in 21 places, so a typo in
  MIGRATION_22's copy silently breaks verification. test/memory_store_test.rb:29 already
  re-implements the same join inline because no named seam exists.
- proposal: define before the constants: `MIGRATION_BOUNDARY = "\n-- tamoz migration boundary --\n"`
  plus `def self.migration_checksum(statements); Digest::SHA256.hexdigest(statements.join(MIGRATION_BOUNDARY)).freeze; end`
  (then `private_class_method :migration_checksum`); each constant becomes
  `MIGRATION_2_CHECKSUM = migration_checksum(MIGRATION_2)`. Digest bytes unchanged —
  verifiable by the forward-migration tests that seed real checksums.
- risk: low
- within_file: yes
## C3: private-constant-list-skips-migration-21
- where: `private_constant` list (~line 1571)
- principle: §6 narrow public APIs (consistency of intent)
- evidence: list ends `…:MIGRATION_19_CHECKSUM, :MIGRATION_20, :MIGRATION_20_CHECKSUM, :MIGRATIONS`
  — `:MIGRATION_21` and `:MIGRATION_21_CHECKSUM` are missing, so those two constants are
  publicly reachable while all 40 siblings are private. Clearly an omission, not policy.
- proposal: append the two missing names; or replace the hand-maintained list with
  `private_constant(*constants.grep(/\AMIGRATION_\d/), :APPLICATION_ID)` placed after
  `MIGRATIONS` so MIGRATION_22 cannot be forgotten. Safe: tests reach these via
  `class_eval`/`const_get` (sqlite_kernel_test.rb:65, memory_store_test.rb:27), which see
  private constants.
- risk: low
- within_file: yes
## C4: stale-test-pointer-in-header
- where: class-header comment block (~lines 8–58, pointer at ~12–13)
- principle: §11 — "a doc describing a surface the repository does not have is worse than
  no doc"; narrative duplication
- evidence: cites "the monotonic-ordering test in test/sqlite_migration_test.rb asserts it"
  — no such file exists (glob/grep over test/). The assertions live in
  test/sqlite_approval_stores_test.rb:36, test/cancellation_visibility_test.rb:26,
  test/memory_store_test.rb:126. The 50-line block also re-narrates P11/P14/COMMS/ADR-049/JCS
  history that each `MIGRATION_N`'s own adjacent comment already carries.
- proposal: shrink the header to the load-bearing why only: ordinals are consumed
  monotonically and never reused, pinned by the three monotonic-ordering tests (named
  correctly). Drop the per-migration history lines duplicated beside each constant.
  Comments don't feed the checksums.
- risk: low
- within_file: yes
## C5: unread-public-attr-reader
- where: `attr_reader :path, :limits, :fault_injector` (~line 1424)
- principle: §6 narrow public APIs — expose what collaborators need, nothing more
- evidence: sole instantiation is adapter.rb:111, whose result is used only for `.migrate!`;
  repo-wide grep finds zero `.fault_injector` reader calls and no other holder of a
  Migrator instance.
- proposal: delete the attr_reader line (constructor kwargs remain the internal channel).
- risk: low
- within_file: yes
## C6: backend-time-sql-to-frozen-constant
- where: `#backend_time_sql` (~line 1561)
- principle: 2 (does one trivial thing awkwardly) + §5 values
- evidence: rebuilds an invariant SQL string on every call, with
  `sql.lines.map(&:strip).join(" ")` existing purely to defeat heredoc layout; single
  call site inside C1's loop.
- proposal: hoist to `BACKEND_TIME_SQL = "SELECT ( CAST(strftime('%s','now') AS INTEGER) * 1000 +
  CAST(substr(strftime('%f','now'), 4, 3) AS INTEGER) )".freeze` (byte-equal output,
  add to private_constant) and delete the method.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- test/memory_store_test.rb:29 duplicates the migration checksum rule inline because
  Migrator offers no nameable seam; after C2 lands the owner may want that test to call
  `migration_checksum` (needs send or a public seal) instead of re-deriving the boundary
  string — cross-file edit, outside this analyzer's file ownership. Otherwise none.
