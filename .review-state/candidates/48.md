# Candidates — rank 48 gems/tamoz-scheduler/lib/tamoz/scheduler/schedule.rb
LOC: 424  Verdict: CANDIDATES
## C1: schedule-validate-step-down
- where: `Schedule#validate!` (~line 271), with `#validate_times!` (~line 338)
- principle: #2 short/one thing (method is ~39 lines, reek: 21 statements — over the hard ceiling 30, §4) + #5 step-down
- evidence: `validate!` is one flat run of 18 validator calls followed by a hand-spelled 22-field result hash; the same field list is spelled four times across `initialize` → `validate!` call → signature → returned hash. `validate_times!` also silently validates the expression (line 345 `validate_expression!(kind, expression)`), so its name understates its scope.
- proposal: step `validate!` down into ordered phase validators that each take and return the fields hash (so freeze/deep-freeze normalization survives): `validate_identity!` (id, revision, owner) → `validate_kind!` → `validate_times!` (start/end/ordering only) → `validate_expression!` hoisted to its own explicit call in `validate!` → `validate_policies!` (misfire enum+limit, overlap enum, max_concurrency, jitter_window) → `validate_artifacts!` (payload_ref … budgets) → `validate_lifecycle!` (created_by, created_at); finish with the frozen result hash built once. Do NOT rename `validate_times!` — it is repo-wide vocabulary (tamoz-comms uses the same name for the same role); hoisting the expression check makes the existing name accurate. The phase sequence above preserves today's exact raise order, so which ConfigurationError fires first never changes.
- risk: medium
- within_file: yes
## C2: misfire-selection-shared-arm
- where: `Schedule#misfire_selection(due_instants)` (~line 130)
- principle: #2 one thing / §6 duplication — three case arms return byte-identical literals
- evidence: lines 135 (`:skip`), 140 (`:latest`), 146 (`:fire_once`) all return exactly `{materialize: [due_instants.last], skipped: due_instants[0...-1]}`.
- proposal: merge the arms into `when :skip, :latest, :fire_once` returning the shared literal once, keeping only `:replay` separate; replace the three per-arm comments with one "why" line noting the three policies coincide today by construction (deliver the latest instant, record older ones as skipped per design §6). Simpler than extracting a helper, and any future divergence has to edit the arm visibly.
- risk: low
- within_file: yes
## C3: interval-anchor-vocabulary
- where: `Schedule#next_interval_fire` (~line 241), `Schedule#due_occurrences` (~line 113), `Schedule#due_interval_occurrences(now:, start:, limit:)` (~line 258)
- principle: #5 step-down + §3.1 one concept, one spelling — the class header names this idea "explicit anchor" (line 10) and the call site spells it `anchor`
- evidence: line 242–243 `duration = expression.to_i; anchor = start_at || created_at`; line 113 recomputes `anchor || start_at || created_at`; line 258 then renames the same concept to parameter `start:`.
- proposal: extract tiny private `interval_anchor` returning `start_at || created_at`; use it in `next_interval_fire` and in the line-113 dispatch (`anchor || interval_anchor`); rename `due_interval_occurrences`' keyword `start:` → `anchor:` to match. Private method, single call site — pure internal vocabulary alignment.
- risk: low
- within_file: yes
## C4: budgets-shape-delegates-to-validate-hash
- where: `Schedule#validate_budgets!(value)` (~line 402)
- principle: §6 duplication / #3 one level — reimplements the exact contract `validate_hash!` already owns
- evidence: lines 403–405 raise `"budgets must be a non-empty hash"` and line 413 re-runs `Tamoz::Core.deep_freeze(value)` — byte-identical message and effect to `validate_hash!(value, "budgets")` (lines 395–400).
- proposal: open `validate_budgets!` with `validate_hash!(value, "budgets")` (message bytes unchanged, deep_freeze idempotent) and keep only the per-key positive-integer loop. Reek's TooManyStatements on this method clears too.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Tied to C2: `:skip`, `:latest`, and `:fire_once` are documented as distinct durable policies yet produce indistinguishable `{materialize:, skipped:}` arrays. If they are meant to diverge later (e.g., policy-specific durable skip reasons), that belongs in this method's return contract — a design decision, not a refactor; misfire-policy semantics stay untouched either way.
