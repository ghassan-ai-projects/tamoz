# Candidates — rank 28 gems/tamoz-agent-healing/lib/tamoz/agent/healing/rule.rb
LOC: 570  Verdict: CANDIDATES

## C1: dedupe-triggers-matcher
- where: `HealingRule#triggers?` (~lines 146–164)
- principle: #2 one thing / #5 step-down; §4 one responsibility
- evidence: three copy-pasted blocks differing only in key pair — `operations`/`operation`,
  `tools`/`tool`, `effect_states`/`effect_state`: `if x.is_a?(Array) && !x.empty?
  return false unless x.include?(signal["…"])`
- proposal: add private constant
  `OPTIONAL_TRIGGER_MATCHERS = { "operations" => "operation", "tools" => "tool",
  "effect_states" => "effect_state" }.freeze`; body becomes the categories guard +
  `OPTIONAL_TRIGGER_MATCHERS.all? { |tkey, skey| subset_admits?(trigger[tkey], signal[skey]) }`
  with private `subset_admits?(allowed, actual)` returning true unless `allowed` is a
  non-empty Array. Signature and result identical; a fourth optional matcher stops being
  a third copy of the block.
- risk: low
- within_file: yes
## C2: one-checking-verb-no-stray-bangs
- where: `HealingRule` checking helpers (~lines 292–315, 341–355, 364–375, 398–412, 434–439, 480–545)
- principle: #1 name states intent; §3 (`!` only when a safe twin exists — none of these
  has one) + §3.1 (one concept, one verb)
- evidence: the same meaning — raise `HealingPolicyError` on bad field input — is spelled
  three ways: `validate_trigger` (no bang), `validate_hash!` / `validate_array!`
  (bang, no safe twin), `assert_budget_keys!` / `assert_single_step!` (assert_, yet the
  failure is bad input named by field, i.e. §3.1's `validate_*`, not a Tamoz bug)
- proposal: unify to `validate_*` without `!` across the 17 helpers:
  `validate_header!`→`validate_header`, `assert_format_version!`→`validate_format_version`,
  `assert_rule_version!`→`validate_rule_version`,
  `assert_minimum_confidence!`→`validate_minimum_confidence`,
  `validate_hash!`→`validate_hash`, `validate_array!`→`validate_array`,
  `assert_trigger_shape!`→`validate_trigger_shape`, `assert_trigger_keys!`→`validate_trigger_keys`,
  `assert_plan_review_keys!`→`validate_plan_review_keys`,
  `assert_plan_and_critic_required!`→`validate_plan_and_critic_required`,
  `assert_single_step!`→`validate_single_step`, `assert_budget_keys!`→`validate_budget_keys`,
  `validate_budget_entries!`→`validate_budget_entries`, `assert_oracle_keys!`→`validate_oracle_keys`,
  `validate_oracle_kind!`→`validate_oracle_kind`, `validate_oracle_digest!`→`validate_oracle_digest`,
  `validate_oracle_check_name!`→`validate_oracle_check_name`. Scope proven: grep over gems/
  and test/ shows every call site inside this file; the same-named `validate_hash!` /
  `validate_array!` in scheduler/schedule.rb, healing/failure_record.rb and evals/schema.rb
  are each class's own private helper with zero cross-object callers.
- risk: low
- within_file: yes
## C3: hide-header-validators
- where: `HealingRule#validate_header!` + `assert_format_version!` / `assert_rule_version!` /
  `assert_minimum_confidence!` (~lines 292–315)
- principle: §6 narrow public API — expose what collaborators need and nothing more
- evidence: four constructor-only helpers sit between `from_h` and `private`, so they ship
  as public methods; grep shows no caller outside `initialize`'s chain (and the pinned
  public-api inventory for tamoz-agent-healing lists only `Remediation` + `VERSION`)
- proposal: move the four below the existing `private` line (pairs naturally with C2's renames;
  apply together)
- risk: low
- within_file: yes
## C4: step-down-remediation-step-validator
- where: `HealingRule#validate_remediation_step` (~lines 441–462)
- principle: #2 ≤20 lines (22 today) / #5 step-down; §4 one responsibility
- evidence: one method does shape check, permitted-form check, effect-safety check, and the
  unsafe/bounded_retry ban; the last is written inverted — `return unless String(step.fetch("safety"))
  == "unsafe" && form == :bounded_retry` followed by `raise` — which reads backwards from its intent
- proposal: keep the messages byte-identical and split the tail into named checks, e.g.
  `validate_step_form(step)`, `validate_step_safety(step)`, and a positive-form
  `refuse_unsafe_bounded_retry!(step, form)` (or, post-C2, `validate_unsafe_form_pair`) so
  `validate_remediation_step` composes three one-level calls like `validate_budgets` already does
- risk: low
- within_file: yes
## C5: name-the-compensation-vocabulary
- where: `HealingRule#validate_compensation` (~line 552)
- principle: #3 one level of abstraction / consistency with the class's own convention
- evidence: `%w[restore_preimage cancel_with_receipt authorized_compensation contain_only]`
  is buried inline in the validator, while every sibling closed vocabulary
  (`LIFECYCLE_MODES`, `RISK_CLASSES`, `EFFECT_CLASSES`, `ORACLE_KINDS`) is a frozen
  top-of-class constant
- proposal: hoist to `COMPENSATION_KINDS = %w[restore_preimage cancel_with_receipt
  authorized_compensation contain_only].freeze` beside `ORACLE_KINDS` (name unused repo-wide,
  grep-verified); error bytes unchanged — the message never interpolates the set
- risk: low
- within_file: yes
## C6: step-down-from_h
- where: `HealingRule.from_h` (~lines 253–290)
- principle: #2 short functions (38 lines vs hard ceiling 30); §1 diagnostic-limit exception
  must be named, not defaulted into
- evidence: the method mixes the invariant-18 version gate, the type guard, a 23-field fetch
  table, and the KeyError→CheckpointCorruptionError rescue in one body
- proposal: extract the flat table to `def self.rule_attributes(hash)` returning the kwargs
  hash verbatim (same fetch/[] mix per field); `from_h` keeps guard + version gate +
  `new(**rule_attributes(hash))` + rescue. Pure line move; `fetch` still raises KeyError so
  the rescued message bytes are unchanged
- risk: medium
- within_file: yes

## FLAG (owner decisions only)
- `step_for(family)` (~178) would read better as `remediation_step_for(family)` at its call
  site (`preflight.rb:138`: `context.rule.step_for(...)`), but the caller lives outside this
  file — rejected here; owner call for a later cross-file pass.
- `validate_oracle_check_name!` (~540) normalizes via `SafeText.normalize` and discards the
  return: the stored oracle keeps the raw `check_name` while construction validated the
  normalized copy. If normalization ever changes bytes, validation and stored digest diverge.
  Fixing it would alter digest bytes — a contract decision, not a refactor.
- `budget(name)` (~194) raises bare `KeyError` on an unknown budget while every other bad
  input in the class raises `HealingPolicyError`. Unifying would change error identity —
  owner decision.
