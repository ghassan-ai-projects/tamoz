# Candidates — rank 11 gems/tamoz-sqlite/lib/tamoz/sqlite/memory_store.rb
LOC: 735  Verdict: CANDIDATES
## C1: dead-address-local-in-append
- where: `MemoryStore#append` (~line 124) + private `#normalize_memory_address` (~line 561)
- principle: 2 (does one thing — no dead steps); §4 one responsibility
- evidence: `address = normalize_memory_address(index)` is never read again; the
  transaction addresses the store directly with `namespace: index.store_namespace,
  key: index.key`. The helper exists only for this dead assignment.
- proposal: delete line 124 and the `normalize_memory_address` helper. Same sweep:
  drop the dead pre-assignments `version_rows = nil` (~454) and `index_rows = nil`
  (~472) in `purge` — both are unconditionally reassigned before any read.
- risk: low
- within_file: yes
## C2: dead-constants-eligible-states-search-columns
- where: `MemoryStore::ELIGIBLE_STATES` (~line 32), `MemoryStore::SEARCH_COLUMNS` (~line 35)
- principle: 1/3 (names must point at something live); §6 narrow surface
- evidence: zero references repo-wide (`gems/ apps/ test/`, Ruby + yaml/json/md). Meanwhile
  the SQL hardcodes `IN ('active', 'consolidated')` twice (lines 248, 301) and
  `match_clause` hardcodes statement_search/layer/class — so the constants are a
  false single-source-of-truth, not documentation that stays true.
- proposal: delete both constants. (Alternative — interpolating them into the SQL —
  adds machinery for no behavioral gain; deletion is the simple path.)
- risk: low
- within_file: yes
## C3: demote-single-caller-publics-to-private
- where: `#scan_matched_restricted` (~282), `#expired_tombstones` (~378), `#pending_tombstones` (~512)
- principle: 4 (public surface reads as domain DSL); §6 narrow public APIs
- evidence: grep across gems/, apps/, test/ shows zero external callers. The real
  cross-gem surface is append/search/purge/current_version (+ version, index_row,
  purge_expired in tests). scan_matched_restricted takes internal plumbing
  `(namespace, caller_values, terms, layer, klass, now_ms)`; expired/pending are
  consumed only by `purge_expired` (lines 497–498).
- proposal: move all three under `private`. Behavior identical; `search` and
  `purge_expired` remain the only retrieval/purge entries.
- risk: low
- within_file: yes
## C4: dedupe-tombstone-query
- where: `#expired_tombstones` (~381–401) vs `#pending_tombstones` (~515–534)
- principle: 5 step-down / one spelling per concept (§3.1 spirit)
- evidence: the two methods run a byte-identical SELECT (heads ⋈ versions ⟕ index,
  `h.deleted = 1 OR i.state = 'deleted'`, namespace LIKE prefix); they differ only in
  statement label ("memory.tombstones" vs "memory.pending") and predicate direction
  (`>=` at 407 vs `<` at 537).
- proposal: extract private `tombstone_clause` returning `[sql, binds]` — exactly the
  existing seam style of `situation_boundary`/`match_clause` — leaving each caller's
  own `open_transaction`, label, and retention filter_map untouched. Transaction
  boundaries stay where they are.
- risk: low
- within_file: yes
## C5: step-down-validate-caller-situation-identity
- where: `MemoryStore#validate_caller!` (~600–636)
- principle: 2 (>30 lines with comments), 5 step-down
- evidence: one method validates six flat scalar fields AND the T0.3 situation triple
  (both key conventions, all-or-nothing completeness, lines 618–630).
- proposal: extract `validate_situation_identity!(caller)` returning
  `[situation_type, entity_type, entity_id]`; `validate_caller!` drops to ~20 lines of
  flat field validation plus the merge. Name follows §3.1 validate_* (bad input named
  by field — unchanged error bytes/messages).
- risk: low
- within_file: yes
## C6: retention-ms-reader
- where: lines ~379, ~425, ~513
- principle: 1 (one concept, one verb/spelling)
- evidence: `(store.adapter.limits.deletion_retention * 1_000).ceil` computed inline in
  three methods (`expired_tombstones`, `purge`, `pending_tombstones`).
- proposal: private `def retention_ms` — name says what it returns (§3). Note
  `thread_tombstone.rb:191` repeats the same expression in its own family; leave that
  file alone this pass.
- risk: low
- within_file: yes
## C7: checking-verb-names
- where: `#text_value(value, name)` (~668), `#normalize_limit(value)` (~728)
- principle: §3.1 vocabulary — checking verbs
- evidence: neither method transforms anything it keeps secret; both return the value
  or raise `ConfigurationError` naming the field ("must not be empty", "must be between").
  That is `validate_*` ("value matches declared shape; failure bad input named by
  field"), not bare nouns or `normalize_*`.
- proposal: rename to `validate_text` and `validate_limit` (no bang — §3: bang only
  when a safe twin exists). Private, all callers in-file.
- risk: low
- within_file: yes
## C8: unshadow-now-ms-local-in-search
- where: `MemoryStore#search` (~212)
- principle: 3 (one level of abstraction / no self-shadowing reads)
- evidence: `now_ms = now_ms()` — the local shadows the method it derives from, so the
  rest of a 68-line method mixes "current time" and "query instant" under one name;
  it then flows into `scan_matched_restricted(..., now_ms)`.
- proposal: name the local `as_of_ms` (and the parameter it feeds stays `now_ms`);
  clarifies the search ran AT an instant, which the validity filter semantics depend on.
- risk: low
- within_file: yes
## C9: single-index-column-order-constant
- where: INSERT column list (~141–147), `#index_row` SELECT (~341–345),
  `#index_row_binds` (~565–575), `#index_row_from_row` (~577–598)
- principle: 5 (small concrete operations over parallel positional ladders)
- evidence: five hand-synced positional lists: the 18-column INSERT list, the 18-value
  binds ladder, the 18-column SELECT + 18 `row.fetch(N)` mapping, and the 16-column
  search SELECT + 16-fetch mapping — aligned only by position; adding one index column
  silently misaligns any of them.
- proposal: one ordered `INDEX_COLUMNS` constant driving the INSERT column list,
  `index_row_binds` (single klass→"class" exception mapped explicitly), and the
  `index_row` SELECT/mapping. Do NOT widen the search SELECT — its deliberate omission
  of statement_search is invariant-24 load-bearing.
- risk: medium
- within_file: yes
## FLAG (owner decisions only)
- `MemoryStore#fetch` (~175) has zero callers anywhere (all `.fetch` hits are Hash#fetch);
  likely reserved API symmetry with `version` — owner should confirm delete-vs-keep.
- ELIGIBLE_STATES/SEARCH_COLUMNS may be reserved for planned P11 phases (agent-memory
  defines its own symbol-valued copies); if so, prefer wiring them into the SQL over C2 deletion.
- If `expired_tombstones`/`pending_tombstones` are planned orchestration surface beyond
  `purge_expired`, keep them public despite C3.
- Cross-gem note: tamoz-agent-memory `surface.rb:105` constructs `MemoryStore::IndexRow`
  directly — IndexRow's constructor signature is load-bearing cross-gem and must not move
  under C9.
