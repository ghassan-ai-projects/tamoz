# Candidates — rank 37 gems/tamoz-evals/lib/tamoz/evals/benchmark/scoreboard.rb
LOC: 498  Verdict: CANDIDATES
## C1: step-down split of regression_check
- where: `Scoreboard.regression_check` (~line 78, body ~39 lines)
- principle: #2 (>30-line hard ceiling), #5 step-down, #3; §4 one responsibility
- evidence: method carries rubocop disables for AbcSize/CyclomaticComplexity/MethodLength/PerceivedComplexity; mixes manifest validation, interval-completeness gate, drop-row literal shaping (lines 93–104), result assembly (106–112), and failure raise. Result 5-key shape is duplicated verbatim in `passed_regression` (217–222).
- proposal: extract private singleton helpers `regression_drops(current, intervals)` (the filter_map building drop rows) and a shared `regression_result(current, prior_root:, acknowledged:, regressions:)` used by both the main path and `passed_regression`; `regression_check` becomes a ~10-line orchestrator (read → validate → complete? → drops → pass/raise). No signature or error-message bytes change.
- risk: low
- within_file: yes
## C2: rename lower_score → normalize_lower_score
- where: `Scoreboard#lower_score` (~line 445)
- principle: #1 name states intent; §3.1 vocabulary (`normalize_*` returns canonical form)
- evidence: sibling is `normalize_higher_score` (line 440); the pair is asymmetric — `lower_score` says which metrics it serves, not what it does (inverts a lower-is-better metric onto the 0..METRIC_SCALE scale). Sole call site line 434, grep-proven in-file only.
- proposal: rename to `normalize_lower_score` so the three scorers read as one family (`normalize_higher_score`, `normalize_lower_score`, `cost_score`). Pure rename, no callers outside file.
- risk: low
- within_file: yes
## C3: unwrap metric-value plumbing out of metric_score
- where: `Scoreboard#metric_score` (~line 418)
- principle: #3 one level of abstraction, #5 step-down
- evidence: one body does metric lookup (420), availability guard `return nil if value.is_a?(Hash) && value['status'] == 'unavailable'` (424), hash-unwrap `value['normalized'] || value['score'] || value['value']` (426–430), and direction dispatch (433–436); carries AbcSize/Cyclomatic/PerceivedComplexity disables.
- proposal: extract `metric_unavailable?(value)` and `unwrap_metric_value(value)` (returns raw value when not a Hash); leaves `metric_score` a short lookup→guard→dispatch read. Behavior-preserving, all callers in-file.
- risk: low
- within_file: yes
## C4: extract verdict-source selection from build_axis_verdicts
- where: `Scoreboard.build_axis_verdicts` (~line 300)
- principle: #3, #5; carries AbcSize/Cyclomatic/PerceivedComplexity disables
- evidence: lines 301–308 collect candidate hashes (`axis_verdicts`, `verdicts`, reshaped `axes`) plus scalar fallback before any per-axis mapping happens — two distinct operations at two abstraction levels.
- proposal: extract `axis_verdict_source(report)` returning `[source_hash, scalar_or_nil]`; the map over AXES stays as the readable top half. No wire/output change — same hash bytes produced.
- risk: low
- within_file: yes
## C5: interval discovery step-down + named container keys
- where: `Scoreboard.interval_documents` (~line 231) and `find_interval` (~line 259)
- principle: #5, #2
- evidence: `interval_documents` mixes three path sources in one body (fixed REPORT_FILENAMES, the `%w[report_path report intervals_path axis_intervals_path]` manifest-reference loop at 237–240, glob sweep); `find_interval` recurses through an inline magic list `%w[axis_intervals intervals axes report score]` (259) while every other key list lives as a named frozen constant.
- proposal: extract `manifest_referenced_paths(root, manifest)`; hoist the nested-container list to a constant e.g. `INTERVAL_CONTAINER_KEYS` beside `REPORT_FILENAMES`. Same paths visited, same order.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Four `self.class.send(:...)` bridges (lines 355, 368, 390, 391: `validate_entries!`, `safe_artifact_root?`, `build_axis_verdicts`, `hard_zero_names`) reach instance code into private class-method space. Clean fix is a shared private-helper module, but that touches class structure/public table — owner call.
- `intervals_for_entry` (line 120) and `acknowledged_note?` (129) sit above `private` yet have zero external callers (grep across repo proves in-file-only use). Narrowing them to private fits §6 narrow-public-APIs but changes the public method table — flagged, not proposed.
