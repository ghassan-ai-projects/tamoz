# Candidates — rank 39 gems/tamoz-agent-session/lib/tamoz/agent/session_context_controls.rb
LOC: 491  Verdict: CANDIDATES
## C1: unify compact_transcript with the apply_control fenced-commit skeleton
- where: `SessionContextControls#compact_transcript` (~160) duplicating `#apply_control` (~277)
- principle: 5 (step-down) + standard §4 one-responsibility
- evidence: compact_transcript re-implements apply_control's whole transaction line for line: `guard_state!` → `app_for_thread` → `open_writer(thread_id:, namespace: [], owner_id: control_owner(request_id), ttl: ...)` → `control_base(app, writer)` → append record to `context_controls` → `commit_control` → `ContextControlProjection.new(...)`. ~20 duplicated lines; two places own the exact-one-audit-record invariant.
- proposal: extract one private fenced-commit helper that owns guard/writer-open/base/append/commit/projection, yields `(source, writer)` inside the fence and expects the built record back; `apply_control` becomes a thin adapter passing its `SessionRecords.build` fields (keeping its candidate-mutation block), `compact_transcript` becomes an adapter that summarizes under the fence (`writer.effects`, `source`) and returns its record. Projection construction happens once. Record bytes unchanged (same `SessionRecords.build` inputs); `/new`'s explicit nil successor equals `record['successor_thread']` on a compact record.
- risk: medium
- within_file: yes
## C2: compact_record takes six positional parameters
- where: `SessionContextControls#compact_record` (~389), call site ~174
- principle: 4 (call sites read as domain) + standard §4 keyword args over positional lists
- evidence: `def compact_record(thread, request_id, before_digest, summarized, fragment_count, pinned)`; the call site passes a bare `conversation.length` mid-list — unreadable without jumping to the definition.
- proposal: keyword signature `compact_record(thread:, request_id:, before_digest:, summarized:, truncated_fragments:, pinned:)`; single call site is in this file.
- risk: low
- within_file: yes
## C3: context_report builds the transcript layer inline at the wrong abstraction level
- where: `SessionContextControls#context_report` (~231–238)
- principle: 3 (one level of abstraction) + §4 one responsibility
- evidence: inside the `layers:` literal, the `'transcript'` value embeds arithmetic (`total - (offset || 0)`) and a buried predicate `controls.any? { |record| record['control'] == 'compact' && record.key?('summary_digest') }`.
- proposal: extract `transcript_layer(total, controls)` plus predicate `summary_pinned?(controls)`, placed beside the already-extracted sibling `memory_layer` (~485).
- risk: low
- within_file: yes
## C4: compaction_configuration defines anonymous Struct classes on every call
- where: `SessionContextControls#compaction_configuration` (~427–431)
- principle: 2/3 (concrete named operations) + standard §5 values
- evidence: `Struct.new(:model_call_safety, :model, :profile, :toolbox, :mcp).new(:idempotent, @model, nil, Struct.new(:catalog_digest).new(@toolbox.catalog_digest), nil)` — two anonymous class definitions executed per call, positionally initialized so the trailing `nil, nil` are untraceable at the call site.
- proposal: hoist named constants next to the existing `ControlBase` precedent (~310): `CompactionConfiguration = Struct.new(:model_call_safety, :model, :profile, :toolbox, :mcp)` and a small named toolbox struct (or keyword init) so each field reads by name.
- risk: low
- within_file: yes
## C5: summarize_conversation misnames what it returns
- where: `SessionContextControls#summarize_conversation` (~376–387)
- principle: 1 (name states intent) + §4 keyword args
- evidence: it first runs `BoundedCompactor#compact` and `return result unless result.compacted` — i.e. it usually returns a bounds-check result, only sometimes a summary; the caller names the outcome `summarized` either way. Four positional params.
- proposal: rename to `bounded_compaction` with keywords `(writer:, source:, observations:, request_id:)` — the name then matches both return paths of the BoundedCompactor envelope.
- risk: low
- within_file: yes
## C6: verbose_input duplicates the compaction-entry literal twice
- where: `SessionContextControls#verbose_input` (~359–374)
- principle: 2 (one thing; repeated shape belongs to one builder)
- evidence: both map branches hand-build the identical `{'output' => …, 'provenance' => …, 'truncated' => false}` entry shape.
- proposal: extract `compaction_entry(output, provenance)` used by the fragment branch and the observations branch.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `set_reasoning_depth` / `set_answer_verbosity` use the banned `set_` prefix (standard §3). All callers live OUTSIDE this file (`gems/tamoz-agent-cli/lib/tamoz/agent/cli_session_commands.rb:277,286`, `gems/tamoz-comms/lib/tamoz/comms/gateway.rb:556-557`, three test files) — multi-file rename, owner call.
- `#new_generation` (~111) has no production caller: CLI and comms gateway route every other control but never this one; only `test/session_context_controls_test.rb:98,188` calls it. Public-surface suspicion worth confirming before someone treats it as load-bearing.
- The `active_preferences` body (`last_preference(controls, 'think'|'verbose', …)` pair, here ~480–481) exists verbatim in `gems/tamoz-agent-session/lib/tamoz/agent/session_planning_context.rb:540-541`. Consolidating behind a module_function'd helper is cross-file and therefore out of scope for this pass.
