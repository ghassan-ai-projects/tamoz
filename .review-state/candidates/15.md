# Candidates — rank 15 gems/tamoz-graph/lib/tamoz/graph/checkpoint_codec.rb
LOC: 719  Verdict: CANDIDATES
## C1: load-outcome step-down
- where: `CheckpointCodec#load_outcome` (~line 171)
- principle: 2 (≤30-line hard ceiling — body is ~68 lines, the driver of the file's Metrics/MethodLength todo) + 5 (step-down)
- evidence: One method does shape check, write-order sort + contiguity check, a two-kind `case` over every write with inline routes-envelope parsing, then composes the `Outcome`. E.g. lines 185–216 interleave channel decoding, routes JSON parsing, and version checks.
- proposal: Pure move, same statements/order, same raises: extract `outcome_writes_in_order!(writes)` (sort_by write_index + contiguous-index rejection), `decode_pending_channel_write(write, update)` (the `"channel"` arm incl. post-routes and repeat-channel rejections), `decode_pending_routes_write(write)` (the `"routes"` arm returning routes). `load_outcome` becomes validate → fold writes → build `Outcome`. All new methods private; no wire bytes touched.
- risk: medium
- within_file: yes
## C2: load attribute-table step-down
- where: `CheckpointCodec#load` (~line 103)
- principle: 2 (body ~35 lines > 30) + 3/5 (parse/dispatch vs field-by-field table construction)
- evidence: Lines 103–118 build the 14-field attributes hash inline between identity enforcement and the rescue chain; the method mixes envelope framing with per-field decode dispatch.
- proposal: Extract `decoded_attributes(wire, strict:)` containing the hash literal verbatim plus the `attributes[:state] =` line; `load` keeps validate_input → parse → canonical check → require_array → identity gate → decoded_attributes → freeze. Statement order preserved, so first-failure semantics and all message bytes are unchanged.
- risk: medium
- within_file: yes
## C3: name the outcome/routes/request envelope tags
- where: `CheckpointCodec#dump_outcome` (~line 133), `#dump_outcome_writes` (~160), `#load_outcome` (~207), `#dump_request_payload` (~246), `#load_request_payload` (~259)
- principle: 3 (one level of abstraction — magic literals inside wire assembly) + §3 (constants) mirroring the existing `FORMAT` convention
- evidence: `"tamoz.graph.outcome"`, `"tamoz.graph.routes"`, `"tamoz.graph.request"` each appear twice (write side + read side); only the checkpoint envelope got a named constant.
- proposal: Add `OUTCOME_FORMAT = "tamoz.graph.outcome"`, `ROUTES_FORMAT = "tamoz.graph.routes"`, `REQUEST_FORMAT = "tamoz.graph.request"` (frozen, added to `private_constant`) and use them at all five sites. Identical bytes on the wire; pairs the writer and reader spellings so they cannot drift. `script/tamoz_sqlite_oracle` restates the literals independently and stays as-is.
- risk: low
- within_file: yes
## C4: shared strictly-ascending guard for sorted wire sections
- where: `#decode_pending` (~405), `#decode_interrupts` (~449), `#decode_attempts` (~474), `#decode_resume_values` (~581 and ~595)
- principle: 5 (one repeated concrete operation should be one named function) / §3.1 checking vocabulary
- evidence: Five copies of `if previous && x <= previous … raise CheckpointCorruptionError, "…unique…sorted…"`.
- proposal: Private `enforce_strictly_ascending!(current, previous, message)` raising CheckpointCorruptionError with the passed message; five call sites collapse to one line each. Message bytes and error identity unchanged per site.
- risk: low
- within_file: yes
## C5: align input-normalization name with StateCodec spelling
- where: `CheckpointCodec#validate_input` (~line 293)
- principle: §3.1 (one concept, one verb repo-wide)
- evidence: `Tamoz::StateCodec#validate_input_bytes` (gems/tamoz-core/lib/tamoz/state_codec.rb:372) is the same contract — String check, size cap, UTF-8 force/validity, returns text — spelled `validate_input_bytes`; this file spells it `validate_input`. Two spellings for one concept.
- proposal: Rename to `validate_input_bytes`. Sole caller is line 94 in this file; no `send`-based references anywhere (grepped gems/ + test/).
- risk: low
- within_file: yes
## C6: verify_ verb for canonicality check
- where: `CheckpointCodec#canonical_value_bytes` (~line 616)
- principle: §3.1 (checking verbs) — it verifies a claim (stored bytes equal a fresh canonical dump) and rejects tampered/non-canonical rows; the bare-noun name reads like normalize_, which it does not do
- evidence: Raises `CheckpointCorruptionError, "checkpoint contains a non-canonical …"` and returns the original bytes frozen — verify_-returns-evidence shape.
- proposal: Rename to `verify_canonical_value_bytes`. Callers: lines 67 and 110, both in-file. Note: two comment-only mentions in `test/agent_non_ascii_session_test.rb` (~184, ~244) go stale — comments, not callers; implementer may refresh them in the same slice.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `CheckpointCodec#load(bytes, validate_identity: true)` violates §4 no-boolean-parameters, and the flag threads through tamoz-sqlite's public surface (`checkpoint_store.latest` → `checkpoint_queries.materialize` → `checkpoint_wire.materialized`). Fixing means a cross-gem signature change — owner decision, out of scope here.
- docs/reviews/codebase-review-2026-08/tamoz-graph.md M1 ("CheckpointCodec is 712 lines mixing four codecs") remains open by design under the recorded cohesion exception; C1–C4 shrink the Metrics/* todo entries without touching the four-codec question.
