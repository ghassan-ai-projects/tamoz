# Candidates — rank 16 gems/tamoz-evals/lib/tamoz/evals/benchmark/readiness.rb
LOC: 711  Verdict: CANDIDATES
## C1: dedup surface-record shape predicate
- where: `Readiness.validate_surface_execution!` (~line 248) vs `Readiness.valid_surface_map?` (~line 344)
- principle: 2 (one thing) + §4 one responsibility
- evidence: identical 3-line compound in both: `MISSION_SURFACES.include?(surface) && record.is_a?(Hash) && record.keys.sort == SURFACE_FIELDS.sort && SURFACE_STATUSES.include?(record['status']) && record['provenance'].is_a?(Hash)`
- proposal: extract private `surface_record_shape_valid?(surface, record)` holding that compound; `validate_surface_execution!` keeps its distinct provenance-content check and message; `valid_surface_map?` calls the predicate in its block. Behavior byte-identical (same truth conditions, same order of checks).
- risk: low
- within_file: yes
## C2: step artifact_document_reasons down to parse + reason-chain
- where: `Readiness.artifact_document_reasons` (~line 508)
- principle: 2 and 5 (≤20 lines, step-down); §4 small methods
- evidence: 23-line method whose body after the secret check is five repetitions of `reason = artifact_X_reason(...); return [reason] if reason`
- proposal: extract `first_document_reason(document, mission, manifest, mission_catalog)` = `artifact_binding_reason(...) || artifact_shape_reason(...) || artifact_mission_reason(...) || artifact_result_reason(...) || artifact_provenance_reason(...)` — Ruby `||` preserves today's lazy short-circuit exactly (each returns String or nil); wrapper keeps parse + secret check + rescues (~10 lines).
- risk: low
- within_file: yes
## C3: split real-provider provenance checks out of artifact_provenance_reason
- where: `Readiness.artifact_provenance_reason` (~line 575)
- principle: 2 and 5 (23 lines, mixed abstraction: binding equality then receipt/trace forensics); §4
- evidence: guard chain runs from `artifact_provenance_mismatch` through receipts, independent trace, span counting under `rubocop:disable Metrics/AbcSize` (line 574)
- proposal: keep the two manifest-binding/surface guards here, move the `run_kind == 'real_provider'` tail (receipts validity, trace digest, `valid_independent_trace?`) into `real_provider_trace_reason(provenance, document, mission)` returning nil-or-reason; likely drops the AbcSize disable. Same check order, same reason strings.
- risk: low
- within_file: yes
## C4: name evidence-binding predicates verify_, not valid_
- where: `valid_real_provider_mission?` (~351), `provider_trace_valid?` (~606), `valid_independent_trace?` (~629), `valid_durable_mission?` (~654)
- principle: 1 + §3.1 checking vocabulary (`validate_`/shape vs `verify_`/claim-against-evidence)
- evidence: these match `DIGEST_PATTERN`, compare recomputed digests, and bind durable-row identity — claims against evidence — while the same file already uses the verify lexicon for the same idea (`artifacts_verified?`, line 504); shape predicates correctly stay `valid_*?`
- proposal: rename to `real_provider_mission_verified?`, `provider_trace_digest_verified?`, `independent_trace_verified?`, `durable_mission_verified?` and update their four in-file call sites (lines 317, 354, 590, 594). No external references (grep across gems/ and test/: only readiness.rb).
- risk: low
- within_file: yes
## C5: one non_empty_string? predicate instead of eight inline spellings
- where: lines ~169, ~294, ~309, ~631, ~633, ~634, ~650, ~672
- principle: 5 (small concrete operation named once) + §3 predicates end in ?
- evidence: `x.is_a?(String) && !x.empty?` repeated verbatim, e.g. `return false unless evidence['trace_id'].is_a?(String) && !evidence['trace_id'].empty?` (three times in `valid_independent_trace?`)
- proposal: add private `non_empty_string?(value)` and replace the eight occurrences (manifest strings, catalog goal/list entries, trace ids, safe path head, durable-mission binding).
- risk: low
- within_file: yes
## C6: merge twin missing/unexpected mission-id reasons
- where: `Readiness.missing_mission_reasons` (~444) + `Readiness.unexpected_mission_reasons` (~453)
- principle: 2 (one comparison reported in two halves)
- evidence: sibling bodies share `return [] unless expected_ids` and the same id normalization; called adjacently at lines 119–120
- proposal: single `expected_mission_id_reasons(missions, expected_ids)` emitting `mission_missing:*` then `mission_unexpected:*`; `structural_reasons` concatenates once. Reason order and bytes unchanged (`reasons.uniq` downstream unaffected).
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `schema_error(message)` (~703) raises `Tamoz::Evals::SchemaError` but reads as a noun query; a rename toward the §3.1 enforce/assert lexicon would touch ~26 in-file call sites — owner call whether it earns its diff.
- `validate_mission!` (~313) raises 'benchmark mission has an invalid status' when the full mission shape (metrics, hard_zero, effect_outcomes, surfaces) is invalid — message misleads, but fixing it changes error-message bytes, which this pass forbids.
