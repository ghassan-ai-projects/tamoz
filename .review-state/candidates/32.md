# Candidates — rank 32 gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_comms_fixture.rb
LOC: 560  Verdict: CANDIDATES
## C1: split-initialize-into-setup-phases
- where: `OpenclawCommsFixture#initialize` (~line 213)
- principle: P2 length (40 lines > hard ceiling 30) + P3 mixed abstraction + P5 step-down
- evidence: one method provisions tmpdir+config+profile (221–228), opens runtime and deploys
  the surface (230–233), wires gateway/sink/worker (235–249), then seeds workspace files
  (250–251); the default model_factory argument (214–218) also embeds a full unnamed plan
  literal inside the signature.
- proposal: hoist the default plan to a frozen `DEFAULT_PLAN` constant beside `PLAN_STEPS`;
  extract private phases preserving exact ordering: `provision_runtime_directory`,
  `deploy_surface_and_bind_correspondent`, `wire_delivery_pipeline`. Public kwargs unchanged.
- risk: medium
- within_file: yes
## C2: extract-snapshot-sub-projections
- where: `OpenclawCommsFixture#snapshot` (~line 389)
- principle: P2 length (34 lines > ceiling 30) + P3/P5 (row shaping, projection resolution and
  milestone filtering interleaved)
- evidence: nested `filter_map { ... next nil unless ... }.first` resolution block (413–419)
  sits beside top-level fact aggregation; milestone select/map chain (406–409) is a second
  vocabulary in the same method.
- proposal: extract private `pushed_milestones` and
  `resolved_request_projections(references, conversations)`; public signature stays
  `snapshot(conversations:)` (12+ external callers verified).
- risk: low
- within_file: yes
## C3: drop-dead-worker-events-and-sent-readers
- where: `OpenclawCommsFixture.attr_reader` (~line 36)
- principle: §6 narrow public API ("expose what collaborators need"); P1 surface states intent
- evidence: `attr_reader :transport, :store, :runtime, :worker_events, :sent, :now` — no
  `@worker_events`/`@sent` ivar is ever assigned and grep over gems/ + test/ finds zero callers;
  both always return nil.
- proposal: delete `:worker_events, :sent` from the attr_reader list.
- risk: low
- within_file: yes
## C4: share-worker-construction
- where: `OpenclawCommsFixture#initialize` (~line 245) and `#fresh_worker` (~line 371)
- principle: P5 step-down / DRY (§6 composition)
- evidence: five identical lines building `Tamoz::Agent::Worker.new(runtime:, session_builder:,
  emitter:, once: true)` duplicated verbatim; these are the first worker and the recovery worker
  whose parity crash scenarios depend on.
- proposal: private `new_worker`; `initialize` assigns `@worker = new_worker` and
  `fresh_worker` becomes `def fresh_worker = new_worker`.
- risk: low
- within_file: yes
## C5: collapse-cli-submit-duplication
- where: `OpenclawCommsFixture#submit_cli_task` / `#submit_cli_cancel` (~lines 158–176)
- principle: P4 public methods read as DSL + P5 step-down
- evidence: both repeat bind_cli_thread → cli_request_id → durable_runner.submit → return
  request_id, differing only in payload/operation/delivery.
- proposal: private `submit_cli_request(payload, thread_id:, purpose:, operation:, delivery:)`;
  each public method reduces to its distinguishing arguments. Signatures unchanged.
- risk: low
- within_file: yes
## C6: name-descriptor-admission-branch
- where: `OpenclawCommsFixture#descriptor` (~line 266)
- principle: §4 (mode argument = two behaviors in one body) + P3
- evidence: `admission: mode == :pairing ? { direct: 'pairing', correspondents: [] }
  : { direct: 'allowlist', correspondents: ["telegram:user:#{USER_BOUND}"] }` buries policy
  shape selection inside an otherwise declarative SurfaceDescriptor.build.
- proposal: extract private `admission_spec(mode)` returning that hash; descriptor then reads as
  pure declarative data. All callers (initialize, drain) are inside this file.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `#submit(updates, now:)` (~309) does not submit — it batches updates and serves one gateway
  pass (`serve_once(drain: false)`); ~20 cross-file callers in openclaw_comms_runner.rb and
  canonical_cross_surface_composition_test.rb make the rename multi-file, so owner's call
  (e.g. `serve_updates`).
- Fixture reaches past public seams across gem boundaries:
  `@runtime.instance_variable_set(:@delivery_sink, @recording_sink)` (~244) into WorkerRuntime
  and `adapter.__send__(:read, ...)` (~455) into the sqlite adapter; a sanctioned injection/
  read seam would remove both.
- `#drain` (~319) builds `descriptor(:allowlist)` while the deployed surface may be
  `admission_mode: :pairing` — benign only while DeliveryDrainer ignores the admission section;
  worth an explicit owner confirmation.
