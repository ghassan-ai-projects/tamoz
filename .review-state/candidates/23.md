# Candidates — rank 23 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_selector_control.rb
LOC: 598  Verdict: CANDIDATES
## C1: split-normalize-selector
- where: `SQLiteSelectorControl.normalize_selector` (~line 253)
- principle: #2 short/one thing (76 lines vs hard ceiling 30) + #5 step-down; §4 one responsibility
- evidence: one method does shape check, point allow-list, Phase-2 operation policy (`phase == 2 && kill_required == true`), optional-statement normalization, scenario binding, attempt_class policy, occurrence bound, iteration_class allow-list, selector-digest verification, and body assembly.
- proposal: extract in-order private helpers, preserving validation order and exact message bytes: `selector_point(object)`, `enforce_phase2_operation!(object)`, `optional_statement(object)`, `enforce_selector_binding!(object, scenario:)` (scenario match + attempt_class), `iteration_class_value(object)`, `verify_selector_digest!(body, claimed)`; normalize_selector becomes a ≤20-line assembly step. New helpers are called only here.
- risk: low
- within_file: yes
## C2: prepare-step-down
- where: `SQLiteSelectorControl.prepare!` (~line 104)
- principle: #2 (36 lines incl. rescue vs ceiling) + #5; §4
- evidence: mixes same-filesystem policy check, directory-name validation, creation mechanics (lstat-absent guard → Dir.mkdir 0700 → chmod → parent fsync), layout verification, and compensating rmdir rollback.
- proposal: extract `create_control_directory!(root_path:, directory_name:)` for the absent-guard/mkdir/chmod/fsync sequence; keep device-match check, naming, layout build, and the rescue-rollback in prepare!. Call order preserved exactly.
- risk: low
- within_file: yes
## C3: build-expectation-record-assembly
- where: `SQLiteSelectorControl.build_expectation` (~line 182)
- principle: #2 (48 lines) / #3 / #5
- evidence: normalizes three inputs, derives hook, validates registry hook, assembles the record hash, stamps content_digest, freezes, dumps canonical bytes, size-guards, and wraps errors.
- proposal: extract `control_record(scenario:, selector:, registry_reference:, hook:)` covering assembly through the MAX_CONTROL_BYTES guard so build_expectation reads as orchestration. Keep the name `build_expectation` — it is send-invoked by both sibling classes and test/sqlite_selector_control_test.rb:735, so renaming is out of scope.
- risk: low
- within_file: yes
## C4: dedupe-trusted-path-prologue
- where: `SQLiteSelectorControl#private_directory` (~line 463) and `#filesystem_anchor_stat` (~line 488)
- principle: §4 one responsibility (duplicated security prologue); §3.1 resolve_
- evidence: both repeat NUL-byte check → File.expand_path → lstat symlink rejection → File.realpath; message templates are byte-identical under the existing `name:` parameterization.
- proposal: extract `resolved_real_path(value, name:)` used by both; private_directory keeps ownership/0700 enforcement, filesystem_anchor_stat returns File.stat of the resolved path. No message bytes change.
- risk: medium
- within_file: yes
## C5: checking-vocabulary-names
- where: validators at lines 537–578 plus `expected_hook` (~line 330)
- principle: §3.1 (`validate_*` = shape check raising named-field error; `build_*` = returns a new value) + #1 name states intent
- evidence: `exact_hash`, `identifier`, `digest_value`, `positive_integer`, `nonnegative_integer` each raise ExecutionError naming the field but read as constructors/accessors at call sites; `expected_hook(selector, hook_version:)` constructs a value.
- proposal: rename in this file only: `validate_exact_hash`, `validate_identifier`, `validate_digest`, `validate_positive_integer`, `validate_nonnegative_integer`, `build_expected_hook`. Grep-proven across gems/ and test/: no caller or send-reference outside this file (identically named helpers elsewhere are other classes' own private copies).
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Hidden public surface: seven "private" singleton methods are invoked cross-file via `.send` by the Stopper/Intervention reopenings (and `:build_expectation` also from test/sqlite_selector_control_test.rb): validate_layout!, build_expectation, ensure_control_absent!, deeply_frozen?, validate_registry_unchanged!, sync_directory, preflight_record!. Renames there (e.g. preflight_record! → validate_record!, sync_directory → fsync_directory) are coordinated multi-file decisions; making the back-channel an explicit internal contract is structural and out of scope this pass.
- The validator quintet spelling (`exact_hash`/`digest_value`/`positive_integer`) is copy-pasted across sqlite_scenario_registry.rb, sqlite_trace_recorder.rb (+ `exact_hash!` variants in sqlite_trace_manifest_verifier.rb, tamoz-core, otel, mcp); C5 aligns one file — a repo-wide single spelling/seam is the owner-level fix.
