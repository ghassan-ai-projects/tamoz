# Candidates — rank 33 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_trace_recorder.rb
LOC: 543  Verdict: CANDIDATES

## C1: record! step-down
- where: `SQLiteTraceRecorder#record!` (~line 207)
- principle: 2 (short/one thing), 5 (step-down), §4 one responsibility; body is 52 lines vs ≤20 normal / 30 hard ceiling
- evidence: one method interleaves four levels — policy guards (`"second operation"`, `MAX_EVENTS`), input acceptance (`deeply_frozen?`, `validate_hook!`, operation match, `attempt == 1`), three accountings (`track_unique!` ×2, occurrence counter, `enforce_semantic_bound!`), and the raw event-hash append
- proposal: extract named steps, keeping every statement in its current order so identical inputs still raise the identical first error: `enforce_event_capacity!` (second-operation + event-cap guards), `validated_hook_fields(point, metadata)` returning the frozen `[point_name, operation_name, statement, attempt]` tuple, `next_occurrence!(occurrence_key)` returning the incremented count with the MAX_OCCURRENCES refusal. `record!` then reads as guard → accept → resolve → enforce_protocol! → account → append
- risk: medium — guard order is observable through pinned error identity/message bytes; extraction must not reorder any check
- within_file: yes

## C2: finish step-down
- where: `SQLiteTraceRecorder#finish` (~line 157)
- principle: 2, 5, §4 one responsibility; body is 45 lines vs ≤20 normal / 30 hard ceiling
- evidence: completeness check (`protocol_state == :complete && events.any? && statements.any?`), registry-stability comparison against `@registry_ref`, selector derivation plus MAX_SELECTORS cap, `build_manifest`, seven freezes, state transition — all inline in one body under one mutex
- proposal: extract `ensure_trace_complete!`, `ensure_registry_stable!`, and `derive_selectors(events)` (keeping the MAX_SELECTORS refusal beside derivation); optionally `freeze_results!` for the freeze sweep. Preserve exact sequencing: dup.freeze events before `SelectorDeriver`, manifest built before collections freeze, `@state = :finished` last
- risk: low — pure ordering-preserving extraction; digest/event ordering untouched
- within_file: yes

## C3: normalize_subject over ceiling
- where: `SQLiteTraceRecorder#normalize_subject` (~line 374)
- principle: 2 (body 35 lines > 30 hard ceiling), 3 (field-validation detail below the shape→digest→freeze level it sits at)
- evidence: shape check, then five sibling validator calls, then an inline `unless dirty == true || dirty == false raise ExecutionError, "subject dirty state must be boolean"` that has no counterpart in the file's validator family (`identifier`, `version_value`, `git_object`, `digest_value`, `positive_integer`)
- proposal: extract `validated_subject_body(normalized)` performing the five field validations and the dirty check, returning the pre-digest body; `normalize_subject` becomes shape → body → content_digest → DeepFreeze, mirroring `normalize_scenario`. Optionally admit a `boolean(value, name:)` twin so the dirty check joins its siblings
- risk: low
- within_file: yes

## C4: initialize phase-2 policy gate
- where: `SQLiteTraceRecorder#initialize` (~line 106)
- principle: 3 (mixed abstraction levels), §3.1 enforce_* = policy applies and may refuse; body sits at 29 lines, at the reviewed ceiling, mixing construction with a registry-policy verdict
- evidence: `unless operation_entry && operation_entry.fetch("phase") == 2 && operation_entry.fetch("kill_required") == true raise ExecutionError, "trace operation is not a Phase 2 kill-required boundary"` sits inline between ivar assignments
- proposal: extract `enforce_phase2_operation!` (fetches `@registry.operation(@operation)` and refuses non-phase-2/non-kill-required operations); `initialize` then reads as normalize inputs → validate registry → pin reference → enforce policy → init state
- risk: low
- within_file: yes

## C5: shared failure envelope for call/finish
- where: `SQLiteTraceRecorder#call` (~lines 145–153) and `#finish` (~lines 193–201)
- principle: 4/one-responsibility (§4): the invariant "any failure marks the recorder failed" is stated twice as two identical rescue pairs differing only in wrap-message prefix
- evidence: both sites do `rescue ExecutionError → @state = :failed; raise` and `rescue StandardError → @state = :failed; raise ExecutionError.new("<prefix>#{error.class}: #{error.message}"), cause: error`
- proposal: extract `with_failure_tracking(wrap_prefix) { ... }` wrapping only the guarded region (the fresh-state early return in `call` stays outside it), passing each prefix verbatim so wrapped message bytes are unchanged
- risk: medium — exception control flow, `cause:` preservation, and divergent return values (`nil` vs `@manifest`) must survive verbatim
- within_file: yes

## C6: exact_hash misnomer
- where: `SQLiteTraceRecorder#exact_hash` (~line 449)
- principle: 1 (name states intent), §3.1 validate_* = "this value matches its declared shape"; a bare-noun name performs enforcement
- evidence: `def exact_hash(value, keys, name:)` raises `"#{name} shape is invalid"` unless the hash has exactly those keys; called only at lines 353 and 375 inside this file
- proposal: rename to `validate_exact_hash(value, keys, name:)`, aligning with the class's own `validate_registry`/`registry.validate_hook!` verb usage and the spelling already used in `sqlite_selector_control.rb`
- risk: low
- within_file: yes

## FLAG (owner decisions only)
- Validator vocabulary is fragmented across tamoz-evals harness files: the same exact-shape check is spelled `exact_hash` (this file), `exact_hash!` (sqlite_scenario_registry, sqlite_trace_manifest_verifier), `validate_exact_hash` (sqlite_selector_control); `positive_integer` has ~4 signatures repo-wide. One spelling per §3.1 concept is an owner-level, multi-file decision.
- `deeply_frozen?` is hand-rolled identically in three harness classes (recorder:514, sqlite_scenario_registry:448, sqlite_selector_control:626) while `Tamoz::Immutable` exposes no deep-frozen predicate — one shared helper would remove the triplication; cross-file, so out of scope here.
- `build_manifest` hardcodes `"manifest_version" => 1` (line 332) next to `VERSION = 1`; if manifest schema version is intentionally decoupled from recorder version this is correct as-is — otherwise it should reference `VERSION`. Wire-format adjacent, so confirming intent beats editing.
