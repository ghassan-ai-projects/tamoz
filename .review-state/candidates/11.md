# Candidates — rank 9 gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_comms_oracles.rb
LOC: 748  Verdict: CANDIDATES
## C1: c5-dead-targets-local
- where: `OpenclawCommsOracles#c5` (~line 213)
- principle: §4 one responsibility / dead computation; principle 2
- evidence: `targets = facts['outbox'].all? do |row| ... end` (lines 213–218) is assigned and never referenced again in the returned metrics/hard_zero document.
- proposal: Delete the `targets` block (6 lines). Pure reads only, no side effects; also drops c5 from 30 to ~24 rubocop lines, under the reviewed ceiling. Behavior identical.
- risk: low
- within_file: yes
## C2: remove-dead-safe_facts
- where: `OpenclawCommsOracles#safe_facts` (~line 293)
- principle: dead code; §3.1 (a JSON reader would be `parse_*`, this name states nothing)
- evidence: `def safe_facts(row); JSON.parse(row['markup']); rescue StandardError then {}` — repo-wide grep (gems/, apps/, bin/, test/, docs/) shows the definition and zero callers.
- proposal: Delete the method. Fixture snapshots arrive pre-parsed; nothing calls it.
- risk: low
- within_file: yes
## C3: remove-dead-MILESTONE_KINDS
- where: `OpenclawCommsOracles::MILESTONE_KINDS` (~line 15)
- principle: dead code; §3.1 second spelling of an existing idea
- evidence: `%w[request.claimed request.running ...]` defined once, never referenced in this file or anywhere else in the repo — while `tamoz-comms/outbox_delivery_sink.rb` already owns a different `MILESTONE_KINDS` (`running waiting progress`).
- proposal: Delete the constant. Two same-named constants with different members is exactly the unsearchable duplicate spelling §3.1 bans.
- risk: low
- within_file: yes
## C4: authority_stable-predicate-lies
- where: `OpenclawCommsOracles#authority_stable?` (~line 252)
- principle: §3 "predicates end in ?" + return booleans; principle 1
- evidence: returns `FAIL`/`PASS` (0/1) scores, not booleans; call sites treat it as a metric (`authority == PASS ? 'passed' : 'failed'`, lines 230, 285).
- proposal: Rename to `authority_stability_score(facts)`. All three callers (lines 212, 275, 539) are inside this file (verified repo-wide). Metric key strings `'authority_stability'` are protocol data and stay untouched.
- risk: low
- within_file: yes
## C5: token_stream-polarity-misnomer
- where: `OpenclawCommsOracles#token_stream?` (~line 177)
- principle: principle 1 (name states intent); §3 predicates read as questions with honest polarity
- evidence: true means a milestone row's text is NOT `"#{reference}: #{phase}"`, i.e. the hard zero FAILS — yet the name asks positively "is a token stream".
- proposal: Rename to `milestone_text_off_format?(facts)`. Sole call site line 172; the `'token_stream'` hard-zero KEY pinned in SCENARIO_INDEX.json is a string literal, unaffected.
- risk: low
- within_file: yes
## C6: c8_history_clean-param-misnomer
- where: `OpenclawCommsOracles#c8_history_clean?` (~line 710)
- principle: principle 3/§3 — parameter names must not lie about scope
- evidence: signature is `(facts, conversation)` but the internal caller passes the per-run subset: `c8_history_clean?(run, conversation)` (line 635), where `run = facts.fetch(name)` from c8.
- proposal: Rename the parameter `facts` → `run`. Positional signature unchanged, so the external test caller (`canonical_cross_surface_composition_test.rb:451`) is unaffected.
- risk: low
- within_file: yes
## C7: conflict_merged-reuses-conflict-check
- where: `OpenclawCommsOracles#conflict_merged?` (~line 247)
- principle: principle 5 step-down; DRY on the existing seam
- evidence: body re-derives `conflicting = facts['inbound'].any? { |row| row['reason'] == 'integrity_conflict' }`, duplicating `identity_conflict?` (line 96) verbatim.
- proposal: Reduce to `identity_conflict?(facts) && facts['conflict_reply_absent'] == true`. Only caller is c5 line 231 (in-file, verified).
- risk: low
- within_file: yes
## C8: c2-step-down-extraction
- where: `OpenclawCommsOracles#c2` (~line 141)
- principle: principles 2 & 5 (rubocop MethodLength 33/20, AbcSize 47/20, cyclomatic 18/8)
- evidence: lines 142–155 compute pushed-event filtering, sequence matching, back-covering, and pending count inline before the document literal.
- proposal: Extract three named helpers following the file's existing per-scenario prefix seam: `pushed_milestones(facts)`, `milestones_push_backed?(projected, pushed)`, `pushes_cover_projected?(projected, pushed)`; c2 keeps metric assembly only (~18 lines). Mechanical pure-local extraction; oracle tests pin scores.
- risk: low
- within_file: yes
## C9: c9-step-down-isolation-helpers
- where: `OpenclawCommsOracles#c9` (~line 301)
- principle: principles 2 & 5 (worst in file: rubocop MethodLength 41/20, cyclomatic 26/8, AbcSize 59/20)
- evidence: four isolation computations (`cross_resolved`, `sends_isolated` incl. nested history block, `own_references`+`milestone_isolated`) live inline across lines 302–324 before the document.
- proposal: Extract `c9_cross_resolved?(facts, refs)`, `c9_sends_isolated?(facts, conversations)`, `c9_history_isolated?(facts, conversations)`, `c9_milestone_isolated?(facts, own_references)` — mirroring the established `c8_*` helper naming in this file; c9 becomes ~20 lines of assembly. Larger surgery than C8, hence medium.
- risk: medium
- within_file: yes
## FLAG (owner decisions only)
- Scenario oracles are named `c1`..`c9` (principle 1 violation by design): intent lives only in openclaw_comms_runner.rb's dispatch ladder (`when 'C1' then .c1(...)`). A cross-file rename to domain names plus a dispatch hash keyed by scenario id would fix it — owner decision, touches the runner.
- `unavailable(reason)` (line 19) vs `unavailable_edge(fact, reason)` (line 427) build near-identical unavailable markers; consolidating on one constructor is surface cleanup worth considering if any other file ever grows callers.
