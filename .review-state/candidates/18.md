# Candidates — rank 18 gems/tamoz-agent-kernel/lib/tamoz/agent/episode_nodes.rb
LOC: 695  Verdict: CANDIDATES

## C1: recall-stepdown
- where: `EpisodeNodes#recall` (~line 54)
- principle: 2 (function short/one thing — 72 lines, far past the 30 hard ceiling) + 5 step-down
- evidence: the node mixes guard clauses, tenant matching, identity + `LogicalCallKey`
  construction, and a ~30-line `EffectDispatcher.run` block that shape-validates the
  recaller result, tenant-scopes every projection, and cross-checks digests (ll. 93–115),
  then closes with an outcome `case`.
- proposal: extract private `recall_identity(snapshot, tenant)`, `recall_logical_key(episode,
  identity)`, and move ll. 90–118 into `validated_recall_payload(result, tenant:)`; `recall`
  becomes guards → tenant check → key → dispatch → outcome case (~25 lines). The helpers are
  called from INSIDE the run block: first-run behavior identical, replay still returns the
  recorded receipt without executing the block. Error strings stay literal
  ("episode_recall/result_invalid", …); the request-side logical key (JCS digest of the
  situation identity) is byte-identical — no bypass, reorder, or re-keying of effect routing.
- risk: low
- within_file: yes

## C2: reason-stepdown-shared-outcome-triage
- where: `EpisodeNodes#reason` (~line 362) and `EpisodeNodes#execute_tool` (~line 433)
- principle: 2 (reason is 45 lines, past the reviewed 30; execute_tool is 32) + 5 step-down
- evidence: reason inlines `ModelCall::InvocationIdentity` construction (ll. 374–380) between
  the budget pre-check and the call; both nodes repeat the same outcome triage —
  `result.unknown?` → ProtocolError "…is unknown (no blind retry)", `result.failed?` →
  "…failed" (ll. 390–395 vs 451–456, differing only in "model"/"tool").
- proposal: extract private `invocation_identity(episode, ordinal)` and a shared
  `enforce_successful_outcome!(result, subject:)` whose messages interpolate `subject`
  ("episode #{subject} call is unknown (no blind retry)" / "episode #{subject} call failed")
  — byte-identical for subject="model"/"tool". reason drops to ~25 lines; execute_tool to ~24.
  Ordinal derivation (`model_receipts.length`) and slot derivation (`tool_results.length`)
  stay at the call sites; keys unchanged.
- risk: low
- within_file: yes

## C3: dedupe-frame-input-assembly
- where: `EpisodeNodes#build_frame` (~line 331) and `EpisodeNodes#rebuild_frame` (~line 470)
- principle: 2 (one thing) — the two nodes duplicate catalog/skills/builder assembly
- evidence: both do `DiagnosisCatalog.verify_wire(wire.fetch("diagnosis_catalog_json"),
  wire.fetch("diagnosis_catalog_sha256"))` + `SkillSet.verify_wire(wire.fetch("skill_refs_json",
  ""), source: @skills_source)` + `@frame_builder_factory.call(catalog, wire.fetch("objective",
  "")).build(snapshot:, prompt:, prompt_version:, prompt_sha256:, skills:, memory:)`;
  rebuild_frame adds only `tool_results:` and `repair_directive:`.
- proposal: extract private `verified_frame_inputs(wire)` returning `[catalog, skills]` (and
  optionally `new_frame_builder(wire)` wrapping the factory call). CAVEAT for the implementer:
  the `IntentCatalog.verify_wire` pair exists ONLY in build_frame (ll. 338–341) — do not
  propagate it into rebuild_frame or remove it; that asymmetry is today's behavior.
- risk: low (with the intent-catalog caveat above)
- within_file: yes

## C4: judge-stepdown-to-decision-map
- where: `EpisodeNodes#judge` (~line 204)
- principle: 5 step-down + 4 (node reads as domain language) + 2
- evidence: 22 lines with a three-way branch inlining per-command `byteslice(0, 256)`
  truncation and three judgement-hash literals (ll. 208–223) — policy and shape assembly at
  one level.
- proposal: extract private `judgement_for(command, invalidated)` holding the
  withdraw/downgrade/let_stand branch; `judge` becomes fetch → cap at MAX_COMMANDS →
  invalidated ids → map → `{"judgements" => …}` (~7 lines). Output hashes unchanged.
- risk: low
- within_file: yes

## C5: watch-intent-keywords
- where: `EpisodeNodes#watch_intent` (~line 261)
- principle: standard §4 (keyword args beyond one-or-two positional parameters)
- evidence: `def watch_intent(catalog, episode, snapshot, now)` — four positional params;
  sibling `build_compensating_intent` already takes the same data as keywords.
- proposal: `watch_intent(catalog:, episode:, snapshot:, now:)`; sole caller is line 249
  (grep-verified: no other hits in gems/, apps/, test/).
- risk: low
- within_file: yes

## C6: narrow-validate-tool-request-input
- where: `EpisodeNodes#validate_tool_request!` (~line 591)
- principle: 3 (one level of abstraction) + §4 (pass the needed value, not the world)
- evidence: signature takes whole `state` but reads exactly one leaf:
  `state.fetch(:wire).fetch("tool_catalog_json", "").to_s`; the caller (l. 438) already holds
  document/request at the same level.
- proposal: change the private signature to `validate_tool_request!(tool_catalog_json,
  tool_name, arguments)`; the node digs the wire value at ITS level (consistent with how
  other nodes unpack `wire` first). Sole caller l. 438, in-file.
- risk: low
- within_file: yes

## C7: rename-frame-from
- where: `EpisodeNodes#frame_from` (~line 626)
- principle: 1 (name states intent); §3.1 (`from`/`build_` read as conversion — this asserts presence)
- evidence: `def frame_from(projection); projection || raise(EpisodeFrameError,
  "episode_frame/missing"); end` — no conversion happens; it is a presence assertion raising
  a typed failure.
- proposal: rename to `required_frame(projection)` (callers ll. 364 and 534, both in-file;
  grep shows no external use; method sits below `private`). Error class/message bytes untouched.
- risk: low
- within_file: yes

## C8: compensation-summary-explicit-condition
- where: `EpisodeNodes#compensation_summary` (~line 577)
- principle: 3/1 (object equality used as a hidden boolean; name should carry the condition)
- evidence: `return "reconsideration: #{judgements.length} compensation(s) proposed" if
  intents == compensations` — array-equality as a proxy for "at least one compensation
  survived the ceiling"; breaks silently if intent post-processing ever diverges.
- proposal: `def compensation_summary(compensations, judgements)` branching on
  `compensations.empty?` — provably equivalent (when empty, intents is `[watch_intent…]`,
  never equal to `[]`). Caller l. 253 in-file; drop the now-unneeded `intents` argument there.
- risk: low
- within_file: yes

## FLAG (owner decisions only)
- Public-surface suspicion (§6 narrow APIs): six methods sit ABOVE `private` yet have zero
  external callers and are not graph-wired — `memory_entries`, `parse_reconsideration`,
  `watch_intent`, `build_compensating_intent`, `risk_above_ceiling?`,
  `invalidated_command_ids` (graph wiring is exactly the 11 `nodes.*` calls in
  gems/tamoz-agent/lib/tamoz/agent/episode_graph.rb). Tightening visibility is an
  API-surface change, so it is a decision, not a candidate.
- test/benchmark_controls_test.rb:276 reaches into the class with
  `EpisodeNodes.allocate.send(:ground_evidence!, …)` — pins a private method name from
  outside (§9: do not test private implementation), which is why `ground_evidence!` is
  effectively frozen against even within-file renames.
