# Candidates — rank 6 gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_durable_cli_adapter.rb
LOC: 1027  Verdict: CANDIDATES
## C1: result_for-step-down
- where: `OpenclawDurableCliAdapter#result_for` (~line 157)
- principle: P2/P5 (≤30-line ceiling; one level of abstraction) + §4 one responsibility
- evidence: 45-line body under `rubocop:disable Metrics/AbcSize, Metrics/MethodLength`; interleaces hard-zero override merge (167–171), effect-outcome reasons (172–173), provenance assembly (180–188), and metric join (189–196) in one flat sequence.
- proposal: extract two verbatim private steps — `evaluated_hard_zero(mission:, evidence:, durable_receipts:, overrides:, extra_reasons:)` returning `[hard_zero, reasons]` (moves 165–171) and `provenance(mission:, provider:, model:, thread:, receipts:, independent_trace:)` (moves 180–188); `result_for` drops to ~20 one-level steps. Signature and result bytes unchanged.
- risk: low
- within_file: yes
## C2: hard-zero-single-pass
- where: `OpenclawDurableCliAdapter#hard_zero_evidence` (~line 455)
- principle: P2 (one thing) / DRY
- evidence: `hard_zero_status(name, evidence:, receipts:)` is invoked twice per name — once for `.first` (statuses, 458) and again for `.last` (reasons, 461) — recomputing every verdict.
- proposal: build one `verdicts = names.to_h { |n| [n, hard_zero_status(n, evidence:, receipts:)] }`; return `[verdicts.transform_values(&:first), verdicts.values.filter_map(&:last)]`. Identical keys, order, and values (test contract at openclaw_durable_cli_adapter_test.rb:207 unchanged).
- risk: low
- within_file: yes
## C3: enforce_credential_availability
- where: `OpenclawDurableCliAdapter#credential_binding!` (~line 238)
- principle: P1 / §3.1 checking verbs (`enforce_*`: policy applies and may refuse)
- evidence: sole caller `prepare!` (line 100) discards the returned binding hash; the method exists to raise `CredentialUnavailable` when no provider credential is present — its builder-shaped name hides that.
- proposal: rename to `enforce_credential_availability!` (keep returning the binding). Only caller is in-file (grep: lines 100/238).
- risk: low
- within_file: yes
## C4: extract-cli-failure-label
- where: `OpenclawDurableCliAdapter#invoke` (~line 307)
- principle: P3 (levels of abstraction)
- evidence: the raise message interpolates a multi-line `argv.drop_while do |arg| arg.start_with?('--') end.first` block inside the string (312–314), burying the control flow of an otherwise linear CLI call.
- proposal: extract `cli_failure_label(argv)` returning the same expression; message bytes identical.
- risk: low
- within_file: yes
## C5: fold-thread-id-alias
- where: `#thread_id_for` (~116) delegating to private `#thread_id` (~405)
- principle: P1 (one concept, one name) / P2
- evidence: two names, one behavior — `thread_id_for(mission_id)` body is a single forwarding line to `thread_id(mission_id)`.
- proposal: move the digest body into public `thread_id_for`, delete private `thread_id`. External callers (scenario_driver.rb:306, driver seam list :285) require the public name, which is preserved.
- risk: low
- within_file: yes
## C6: model_generate_receipts-name
- where: `#model_receipts` (~line 410)
- principle: P1 / §3 "method names say what they return"
- evidence: it filters durable receipts to `model.generate.*` and slices fields — the name reads as construction, not projection of the generate subset.
- proposal: rename `model_generate_receipts`; sole caller line 161 (grep: other `model_receipts` hits are unrelated state keys / SQL labels).
- risk: low
- within_file: yes
## C7: shared-verification-refs-reader
- where: `#fabricated_evidence_status` (~505) vs `#evidence_quality` (~806)
- principle: P2/DRY
- evidence: both re-derive `verification['evidence'] if verification.is_a?(Hash)` plus the observation-ref lookup — the exact spot where the two guards have already diverged (strict `is_a?(Array)` vs lenient `Array(...)`).
- proposal: extract `verification_refs(evidence)` (returns raw `refs` and raw `observation_refs`); each caller keeps its own strictness guard so behavior is byte-identical.
- risk: low
- within_file: yes
## C8: seam-above-private
- where: line 855 `public :enqueue!, :worker!, :worker_until_effect!, :worker_subprocess!`
- principle: P4 (public surface reads as the domain) / §4
- evidence: the scenario-driver seam is declared public after `private`, so the class bottom contradicts the layout above it and the real API is invisible in definition order.
- proposal: define those four next to the other public seam methods (`prepare!`, `result_for`, `evidence_for`) above `private`; delete the trailing `public :` line. Names and visibility unchanged for scenario_driver.rb:286–402.
- risk: low
- within_file: yes
## C9: unshadow-metrics-local
- where: `#metrics` (~line 613)
- principle: P1/P3
- evidence: `metrics = { ... }` local shadows the enclosing `def metrics` — a recursion footgun and a reader trap inside the most complex join.
- proposal: rename local to `base_metrics` (three occurrences, 613–636).
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- The `Metrics/ClassLength` disable lets this class hold CLI driving, polling, receipt validation, AND the whole metric catalog (~649–805). Extracting the catalog family to a sibling would shrink it sharply — structural split is out of scope this pass, owner's call.
- `Tamoz::SQLite.const_get(:Wire, false)` (~1005) probes the constant at runtime instead of a direct reference — looks like load-order avoidance; worth confirming intent.
- `EffectPoller#poll(**)` (~49) silently discards every kwarg the SubprocessRunner interface passes — cross-file contract, owner may want an explicit signature.
- 20-line narrative comment block above `catalog_metrics` (649–668) documents each metric's oracle mapping — useful domain info, but §11 tension; protocol docs may be its home.
