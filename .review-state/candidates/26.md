# Candidates — rank 26 gems/tamoz-agent-session/lib/tamoz/agent/session_planning_context.rb
LOC: 583  Verdict: CANDIDATES

## C1: decompose add_control_context (live gate offenses)
- where: `SessionPlanningContext#add_control_context` (~line 523)
- principle: 2 short/one thing; 3 one level of abstraction; §4 one responsibility per method
- evidence: rubocop reports live offenses — Metrics/AbcSize 23.28/20, CyclomaticComplexity 9/8,
  PerceivedComplexity 9/8, all on this one method; reek adds TooManyStatements (~12) and
  FeatureEnvy (`directives`). It mixes two concerns: preference→directive extraction
  (`think`→`reasoning_depth`, `verbose`→`answer_verbosity`) and latest-/compact-summary lookup.
- proposal: extract `directives_from(controls)` (the two `SessionContextControls.last_preference`
  lookups returning a hash) and `latest_compaction_summary(controls)` (the reversed `find` +
  digest/text pair); the method body becomes guard + two merge steps in the existing `add_*`
  mutator family. Clears all three metric offenses without weakening any gate.
- risk: low
- within_file: yes
## C2: unify the strip-'output' observation shape
- where: `BoundedCompactor#bound_observations` (~line 198), `#observation_metadata` (~line 303),
  `#unavailable_observation` (~line 241)
- principle: 3 one level of abstraction; 2 does one thing (reek: NestedIterators +
  TooManyStatements on bound_observations)
- evidence: the "drop the `'output'` key" shape is written out three times:
  `observation.reject { |key, _value| key.to_s == 'output' }` verbatim at lines 198 and 303;
  line 241 re-derives the same shape plus markers inside `unavailable_observation`. Inside
  `bound_observations` this shape-building is nested inline in the byte-budget filter_map.
- proposal: extract `without_output(observation)` (the shared reject) and a small
  `truncated_observation(observation)` that merges the `output_truncated`/`output_unavailable`
  markers; reuse in all three sites so `bound_observations` reads as pure budget accounting.
- risk: low
- within_file: yes
## C3: drop unused `_state` param from memory_caller; narrow both memory helpers
- where: `SessionPlanningContext#memory_caller` (~line 394), `#memory_owner` (~line 406)
- principle: 1 name states intent (a parameter that is ignored misstates the contract); §4; §6 narrow API
- evidence: `def memory_caller(_state)` never references state; sole call site is line 562
  (`caller: memory_caller(state)`). Grep across gems/ and test/: no caller of
  `.memory_caller` or planning-context `.memory_owner` outside this file (worker_runtime.rb and
  session_memory.rb define their own unrelated `memory_owner`); test/public_api_test.rb pins
  constants only, not methods.
- proposal: rename to zero-arg `memory_caller`, update line 562, and move `memory_caller` /
  `memory_owner` below `private` (they are internal seams; `authoritative_frame` stays public —
  session_context_controls.rb:448 calls it).
- risk: low
- within_file: yes
## C4: compaction_frame is public but has no external caller
- where: `BoundedCompactor#compaction_frame` (~line 68)
- principle: 4 public surface reads as DSL; §6 narrow public APIs
- evidence: defined above `private` alongside `compact`/`summarize`; its only caller is line 61
  inside `compact`. Grep across gems/ and test/: no other reference, though BoundedCompactor is
  instantiated directly in session_context_controls.rb:377 and several tests — all use only
  `compact`/`summarize`.
- proposal: move `compaction_frame` below `private`; the tested surface stays `compact`/`summarize`.
- risk: low
- within_file: yes
## C5: rename deterministic_result to fallback_result
- where: `BoundedCompactor#deterministic_result` (~line 131)
- principle: 1 name states intent
- evidence: the method builds only the model-unavailable fallback path (fixed summary text
  'Deterministic bounded context retained; model summary unavailable.', record
  `status: 'fallback'`), reached from both `summarize` and `summarize_with_model`'s rescue. Its
  name names the material ('deterministic') not the intent, so next to `summarize_with_model` it
  reads as a sibling producer rather than the failure arm.
- proposal: rename to `fallback_result` (private, both call sites lines 97 and 128 are in-file);
  the durable `mode: 'deterministic'` string is untouched.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `SessionPlanningContext#planning_context_for` (~line 358) has no callers anywhere in gems/ or
  test/ (no `send(:...)` dispatch either); the only references are a stale comment in
  tamoz-agent-improvement/promotion.rb:192 citing a nonexistent `SessionNodes#planning_context_for`,
  and an old plan doc. Dead public seam — removal is an API decision, not proposed here.
- The `:reek:DataClump :reek:DuplicateMethodCall :reek:UtilityFunction` suppressions at class top
  (lines 9) silence those three smells for every method of both classes, including future ones —
  §1 wants exceptions at the site; narrowing them per-method would let the ratchet see new smells.
- `compact_for(compaction:)` (~line 362) takes a loosely named `{effects:, durable_context:}`
  bundle; flattening to two keywords touches session_lifecycle.rb:127 (sole caller passing it),
  so multi-file → rejected as candidate, owner's glance only.
