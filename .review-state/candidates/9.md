# Candidates — rank 9 gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb
LOC: 909  Verdict: CANDIDATES
## C1: drain_to_terminal interleaves four concerns in one loop
- where: `CLI#drain_to_terminal` (~line 248)
- principle: #2 (24 statements / 54 lines, ceiling 30), #3, #5; §4 one-responsibility
- evidence: `loop do … render_pending_stale_failures … tracked_request_queued? … run_next … case view.status when :paused … :running` — stale-failure render bookkeeping, tracked-request short-circuit, queued-request advance, and the pause/resume machine all share one body; reek TooManyStatements(24), NilCheck, `session.view` called 6×.
- proposal: step down inside the same method (name is pinned by `test/sqlite_stale_request_test.rb:872/915/928` via `send`, signature untouched): extract the run_next advance (~264–274) into `advance_queued_request(session, thread_id:, owner_id:, options:)` returning the advanced request or nil, and the paused-branch resume (~279–287) into `resume_paused_view(session, view, thread_id:, owner_id:, options:, resume_options:)`. Loop then reads render-stale → check-tracked → advance → case.
- risk: low
- within_file: yes
## C2: build_model mixes precedence policy, credential enforcement, provider resolution, construction
- where: `CLI#build_model` (~line 779)
- principle: #2 (22 statements / 63 lines), #3, #5; §4 "one responsibility per method"
- evidence: one method does flag/env precedence (785–789), credential-ref refusal (790–807, raises typed `ProfileRoleUnavailableError`), provider default + `assume_model_exists` refusal (808–816), env-key lookup (817–819), constructor with rescue→typed-error translation (820–840).
- proposal: keep `build_model` as the orchestrator; extract `enforce_role_credential!(profile)` (returns api_key or nil, keeps both raise sites byte-identical) and `build_provider_model(options, model_name:, provider:, api_key:, api_base:)` holding the constructor + rescue translation. Signature of `build_model` unchanged — it is called cross-file (`cli_worker_commands.rb:641`, `cli_comms_shared.rb:200`) and by tests via `send`.
- risk: medium (error-translation path is pinned; move raise sites verbatim)
- within_file: yes
## C3: run_durable mixes provisioning, wiring, assembly, cleanup
- where: `CLI#run_durable` (~line 551)
- principle: #2 (18 statements / 55 lines), #3; §4 policy+persistence+orchestration in one body
- evidence: mkdir+mode enforcement (556–561), adapter/MCP/approval-engine wiring (554–579), a ~30-line `Session.new` kwargs assembly including the inline routing ternary (580–597), signal install + ensure close (598–604).
- proposal: extract `provision_private_session_dir!(options)` (556–561, keeps the ArgumentError bytes) and `build_durable_session(model:, toolbox:, adapter:, mcp:, profile:, options:, thread_id:)` for the Session assembly. Cross-file callers (`cli_session_commands.rb` ×13, `cli_worker_commands.rb`) keep the `run_durable` signature.
- risk: low
- within_file: yes
## C4: run_one_shot renders its result inline
- where: `CLI#run_one_shot` (~line 156)
- principle: #2 (21 statements / 42 lines), #3, #5
- evidence: validation → session delegation → model/runtime build → stream render → the 10-line human summary block (186–195: answer, responded?/satisfied label) → exit status, all in one method.
- proposal: extract `print_one_shot_result(result)` for 186–195 (prompt-text bytes unchanged); `run_one_shot` then reads validate → delegate → build → run → print → `result.exit_status`. Sole caller is `run` (line 103).
- risk: low
- within_file: yes
## C5: run_with_stream builds context, spawns worker, pumps, and defers prompts in one body
- where: `CLI#run_with_stream` (~line 366)
- principle: #2 (16 statements / 40 lines), #3; reek FeatureEnvy(sink)
- evidence: sink/emitter/mode selection + `Tamoz::Context.new` (367–378), worker Thread spawn/join (384–398), stream pump (394), deferred interrupt rendering (400–402).
- proposal: extract `build_stream_context(thread_id:, request_id:, options:)` returning the `Tamoz::Context` (pure construction, no behavior). All callers are inside cli.rb (199, 215, 219, 268, 285, 290). Leave the concurrency/pump core intact — that part is cohesive.
- risk: low
- within_file: yes
## C6: drive_resume embeds blocked-thread reporting inside state dispatch
- where: `CLI#drive_resume` (~line 207)
- principle: #2 (16 statements / 32 lines, at ceiling), #3
- evidence: the `:blocked` branch (222–230) inlines JSON-vs-text rendering (`emit_cli_event("cli.paused", …)` / two `@err.puts` hint lines) inside the status case.
- proposal: extract `report_blocked_thread(thread_id, blocked, json:)`; the case branch becomes one call + `return EXIT_PAUSED`. Message bytes identical. Called from `cli_session_commands.rb:51` — name/signature unchanged.
- risk: low
- within_file: yes
## C7: "flags → routing mode" spelled twice with different precedence
- where: `CLI#run_one_shot` (~177–183) and `CLI#run_durable` (~592–596)
- principle: #1, §3.1 one concept one spelling
- evidence: one-shot: `experimental_routing ? :experimental : shadow_routing ? :shadow : :legacy`; durable: `adaptive_routing ? :adaptive : experimental_routing ? :experimental : :legacy`.
- proposal: do NOT unify into one resolver — durable silently ignores `--shadow-routing` today, so unification changes behavior. Name each site: `one_shot_routing(options)` and `durable_routing(options)` (verified collision-free in this gem). Flag unification to the owner instead.
- risk: low
- within_file: yes
## C8: five-parameter clump threads through the whole drain family
- where: `CLI#drain_to_terminal`/`#run_with_stream`/`#collect_answers` (~248/366/459)
- principle: #4 (call sites read as plumbing, not domain); reek DataClump on 9 methods
- evidence: reek: `(session, thread_id, owner_id, request_id, options[, resume_options])` repeated across drive_turn/drive_resume/drive_continue/drain_to_terminal/run_with_stream/collect_answers/render_pending_stale_failures.
- proposal: first land C1/C5 (they shrink the clump's hottest members); then introduce an immutable turn-scope value (`Data.define(:session, :thread_id, :owner_id, :options)`) starting at the seam whose callers are all in-file (`run_with_stream`, `collect_answers`). Do not change `drive_*`/`drain_to_terminal` signatures — they are called from cli_session_commands.rb and pinned by tests.
- risk: medium
- within_file: yes
## FLAG (owner decisions only)
- Boolean parameters reek flags here are cross-file by their callers: `run_durable(read_only:)` (cli_session_commands ×4) and `resolve_interrupt_decision(interactive:)`. §4 bans them; any fix reshapes signatures other files own — owner decision.
- Three spellings of effect-resolution answers: `map_answer`'s synonym set ("fixed"/"approve"/"ok"/… → :succeeded) vs `RESOLUTIONS = %w[succeeded failed abandoned]` (cli_session_commands.rb:23) vs comms ops `%w[succeeded failed]` (cli_comms_ops.rb:204). Unifying changes accepted input bytes — behavior, not rename.
- `render_runtime_event` (15 statements, FeatureEnvy event ×12) left alone deliberately: flat per-event rendering dispatch reads like a table; splitting it is churn, not step-down.
