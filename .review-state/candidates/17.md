# Candidates — rank 17 gems/tamoz-agent-cli/lib/tamoz/agent/cli_worker_commands.rb
LOC: 711  Verdict: CANDIDATES
## C1: step-down-observe-doctor
- where: `CLIWorkerCommands#observe_doctor` (~line 147)
- principle: P2 (41-line body vs hard ceiling 30) + P3/P5; duplicated render branch
- evidence: one method parses args, builds recorder/producer, emits two probes, reads back the journal, computes the verdict, renders, AND rescues; the json-vs-text branch appears twice (175–179 happy path, 182–187 rescue) differing only in the document; carries `# rubocop:disable Metrics/CyclomaticComplexity, Metrics/PerceivedComplexity` with no reason nearby.
- proposal: extract `doctor_redaction_report(directory)` (probe emission + flush + readback + verdict document, lines 152–174) and `render_doctor_outcome(document)` (shared json/text branch); rescue becomes `render_doctor_outcome("ok" => false, "redaction" => false); 1`. Output bytes and exit codes 0/1 unchanged; retire the cop disables if complexity drops below the gate.
- risk: medium
- within_file: yes
## C2: split-queue-add
- where: `CLIWorkerCommands#queue_add` (~line 197)
- principle: P2 (36-line body) + P5 step-down
- evidence: option parsing (201–209) plus the durable-submission core (211–233: profile existence check, thread generation, profile binding, runner submit, render) in one body.
- proposal: extract `submit_queued_request(runtime, task:, profile_id:, thread_id:)` (the two load-bearing "why" comments about binding authority before visibility move with the code); optionally also `parse_queue_add_options(options, argv)`. Each half lands ≤ ~18 lines.
- risk: low
- within_file: yes
## C3: extract-worker-option-parser
- where: `CLIWorkerCommands#cmd_worker` (~line 265)
- principle: P2 (35-line body) + P3
- evidence: lines 266–279 are pure OptionParser plumbing for --once/--concurrency/--poll-interval; 281–300 are worker construction + signal trap — two abstraction levels interleaved.
- proposal: extract `parse_worker_options(argv, options)` returning `{once:, concurrency:, poll_interval:}`; cmd_worker keeps construction and the Trap.install block visible.
- risk: low
- within_file: yes
## C4: extract-tail-loop-body
- where: `CLIWorkerCommands#observe_tail` (~line 92)
- principle: P2 (34-line body) + P5
- evidence: the poll loop nests parser setup, journal read, per-entry dedup (`seen[identity]`), json/text emit, flush, follow/sleep (93–126).
- proposal: extract `render_new_observations(entries, seen)` (the `entries.each` dedup+emit block, 111–120); observe_tail keeps parse + read/poll/sleep cadence untouched.
- risk: low
- within_file: yes
## C5: extract-approval-parser
- where: `CLIWorkerCommands#cmd_approve` (~line 339)
- principle: P2 (34-line body) + P3
- evidence: the subtle `parser.order!(argv); request_id = argv.shift; parser.parse!(argv)` dance (344–355) sits above mode dispatch (357–358) and the decision-recording block (362–373).
- proposal: extract `parse_approval_target(argv, options)` returning `[request_id, reload_path, mode, thread_id]`; cmd_approve reduces to dispatch + record/report.
- risk: medium
- within_file: yes
## C6: narrow-public-surface
- where: `CLIWorkerCommands` (`private` boundary at line 416)
- principle: P4 / standard §6 (narrow public APIs)
- evidence: `format_observation` (192), `queue_add` (197), `queue_list` (236), `observe_tail/metrics/doctor` (92–147), `approve_reload` (379), `approve_mode_switch` (401) are all defined ABOVE `private` and therefore public on the CLI, yet grep across gems/, apps/, bin/, script/, test/ shows every caller is inside this file; dispatch goes through `__send__`.
- proposal: move the single `private` keyword up before `format_observation` (one-line diff); only the `cmd_*` entry points stay public.
- risk: low
- within_file: yes
## C7: delete-stale-not-built-comment
- where: `CLIWorkerCommands#cmd_approve` (~lines 324–326)
- principle: standard §11 ("a doc describing a surface the repository does not have is worse than no doc")
- evidence: "Declared in SUBCOMMANDS so the surface is honest about what is coming, and implemented in the slices that follow. A clear 'not built yet' beats a NoMethodError…" describes an unbuilt placeholder — but the fully implemented `cmd_approve` (approve/deny/--reload/--mode) follows immediately.
- proposal: delete those three lines; keep the accurate docstring starting "`tamoz approve REQUEST_ID [--deny]` — the human half of unattended work."
- risk: low
- within_file: yes
## C8: rename-format-to-render
- where: `#format_observation` (~line 192), `#format_worker_event` (~line 699)
- principle: standard §3.1 vocabulary (render_* produces the outward form a human reads; `format_*` is not in the lexicon)
- evidence: both build display strings from documents/events (`"#{observed_at_ms} #{name} #{correlation}"`, worker-event key=value line) — exactly the `render_*` meaning used by `render_status` (590) and `CLIRendering` in the same class.
- proposal: `format_observation` → `render_observation` (caller line 117), `format_worker_event` → `render_worker_event` (caller in `worker_emitter`, line 667).
- risk: low
- within_file: yes
## C9: shared-guarded-session-view
- where: `#paused_approvals` (~488) and `#session_status` (~514)
- principle: P5 step-down / one spelling for one operation
- evidence: both open with `runtime.open_occurrences(limit: 500)` then repeat `runtime.session_for(thread_id).view(thread: …)` inside `begin/rescue StandardError` (491–496 vs 516–520), differing only in the fallback (nil vs unavailable hash).
- proposal: extract `session_view(runtime, thread_id)` returning the view or nil on StandardError; `paused_approvals` uses it directly, `session_status` maps nil → `unavailable_session_status(record)`. Preserve the existing `view.is_a?(Hash)` branch verbatim.
- risk: medium
- within_file: yes
## FLAG (owner decisions only)
- §3.1 repository-wide: `stringify` names three different projections — symbol-flattening entry normalization here (706) vs recursive value stringifiers in `tamoz-sqlite`/`tamoz-stream` verification stores. A unifying rename crosses gem boundaries, so it is an owner decision, not a within-file edit.
