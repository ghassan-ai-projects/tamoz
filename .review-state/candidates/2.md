# Candidates — rank 2 gems/tamoz-agent/lib/tamoz/agent/worker.rb
LOC: 1310  Verdict: CANDIDATES

## C1: dedupe the approval-settlement plumbing shared by apply_decision and apply_timeout_denial
- where: `Worker#apply_decision` (~line 431) and `Worker#apply_timeout_denial` (~lines 1081–1090)
- principle: 2 (one thing), §4 one responsibility; one domain operation, two spellings
- evidence: both walk `view.interrupts`, pick `descriptor['kind'] == 'approve_tool' && (asked = descriptor['decision'])`,
  and call `@runtime.approval_engine.resolve(decision_id: asked.fetch('id'), answer: …, scope: …)`;
  both then build `(answers[interrupt.task_id] ||= {})[interrupt.call_index] = answer_for(interrupt, granted)`
- proposal: extract two private helpers used by both call sites —
  `resolve_approval_asks(interrupts, answer:, scope:)` (engine resolve per approve_tool ask) and
  `answers_for(interrupts, granted)` (the task_id→call_index map via the existing `answer_for`).
  Same calls in same order; `granted ? :approve : :deny` / `granted ? :once : nil` stay at the caller.
- risk: low
- within_file: yes

## C2: apply_timeout_denial is 37 lines — over the reviewed ceiling and four abstraction levels
- where: `Worker#apply_timeout_denial` (lines 1061–1097)
- principle: 2 (≤20 normal / 30 hard ceiling), 3 (one level of abstraction), 5 (step-down)
- evidence: one method does occurrence-id lookup (`meta&.fetch(:occurrence_id) || @runtime.open_occurrences(limit: 500).find {…}`),
  session/view rebuild + deniability guard, engine resolution, answer assembly, and `conclude_resolution`
- proposal: extract `timeout_occurrence_id(thread_id)` for the meta-or-scan fallback (lines 1063–1067); with C1's
  helpers applied first, the remainder reads claim-guard → deny → conclude at ~20 lines
- risk: low
- within_file: yes

## C3: triplicated terminal settlement prefix in settle_completed_view / settle_failed_view / settle_blocked_view
- where: `Worker#settle_completed_view` (666), `#settle_failed_view` (681), `#settle_blocked_view` (699)
- principle: 2, 5; three spellings of one settlement contract
- evidence: each repeats `notify_sink(thread_id, <kind>, <text>, request_id:) → close_occurrence → unpark`
  before its emit (668–670, 684–687, 700–707); only kind/text/event fields genuinely differ
- proposal: extract `settle_terminal_delivery(thread_id:, occurrence_id:, kind:, text:)` = notify_sink → close_occurrence → unpark,
  preserving the design-§11 ordering (outbox row BEFORE close). Leave each `emit(...)` payload untouched at its
  call site so event document bytes/order are unchanged. The `@processed` increment stays in settle_completed_view.
- risk: low
- within_file: yes

## C4: enforce_ask_deadlines mixes three levels: parked ages, store-scan ages, policy application
- where: `Worker#enforce_ask_deadlines` (lines 1027–1052)
- principle: 3 (one level of abstraction), 5 (step-down)
- evidence: monitor-synchronized park scan, then `candidates[thread_id] = now - Time.parse(opened) unless candidates.key?(thread_id)`
  inline in an open_occurrences scan, then the `lane_ask`/`apply_timeout_denial` loop — all in one body
- proposal: extract `deadline_candidate_ages(now)` built from two private collectors
  (`parked_deadline_ages(now)` under @monitor, `open_occurrence_deadline_ages(candidates, now)` preserving the
  parked-wins precedence); main method reduces to collect → judge → deny
- risk: low
- within_file: yes

## C5: handle_thread_failure interleaves queued-failure settlement with generic failure reporting
- where: `Worker#handle_thread_failure` (lines 318–341)
- principle: 2, 4; the `terminal_request` branch (settle_child_error + close_failed_occurrence +
  durably_fail_request + notify_sink + park) is a distinct responsibility from the emit/unpark frame
- evidence: `settle_child_error(...) if terminal_request; close_failed_occurrence(...) if terminal_request` then a
  second `if terminal_request … else unpark` block re-deciding the same fact twice
- proposal: extract `fail_queued_request(entry, error, thread_id:, occurrence_id:)` containing the terminal branch;
  handle_thread_failure becomes report → branch once → PARKED
- risk: low
- within_file: yes

## C6: name the five-times-repeated pending status projection
- where: lines 674, 692, 713, 725, 769 (`SessionStatusProjection.document(view, request_id: occurrence_id, delivery_state: 'pending')`)
- principle: 1 (name states intent), §3.1 one concept one spelling
- evidence: identical three-line construction repeated at five emit sites across settle_*_view and emit_approval_request
- proposal: extract `pending_status_projection(view, occurrence_id)`; call sites shrink to one line each, and the
  "delivery_state is always pending here" invariant gets a single home
- risk: low
- within_file: yes

## C7: dead keyword parameter `now:` on lane_ask
- where: `Worker#lane_ask` (lines 1054–1059)
- principle: §4 (signatures say what they need); principle 1 — the parameter implies time-dependence the rev-keyed cache does not have
- evidence: body never references `now`; callers are lines 1046 and 1076, both in this file
- proposal: drop the kwarg — `def lane_ask(thread_id)` and update both callers
- risk: low
- within_file: yes

## C8: emit_observability refetches the catalog entry and sits at the ceiling
- where: `Worker#emit_observability` (lines 1177–1205, 29 lines)
- principle: 2, 3
- evidence: `Tamoz::Observability::Catalog.fetch(name)` called at line 1191 AND again inside the filter_map at line 1196;
  correlation assembly (1187–1190) is a separate level from attribute mapping
- proposal: fetch `.optional` once into a local; extract `correlation_for(document, observability:)` and
  `observable_attributes(name, document)`; emit_observability drops to ~8 lines. Mapping order unchanged.
- risk: low
- within_file: yes

## FLAG (owner decisions only)
- Law-of-Demeter chain `session.app.durable_runner.run_next/recover` appears twice (lines 486, 505). A session-level verb
  (e.g. resume/recover execution) would hide the app internals — but Session is shared surface beyond this gem, so it is the owner's call.
- Two spellings of the runtime seam: most access goes through `@runtime.*` wrappers, but mode-switch/drain reach
  `@runtime.checkpoints.*` directly (244, 921–926, 948–952) and schedule settlement reaches `@runtime.schedule_store.*` (552, 562).
  Consolidating is a WorkerRuntime API decision, out of scope for a within-file pass.
