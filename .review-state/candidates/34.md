# Candidates — rank 34 gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_mission_runner.rb
LOC: 541  Verdict: CANDIDATES
## C1: initialize-unpack-lockstep
- where: `OpenclawMissionRunner#initialize` (~line 40)
- principle: P3 (one level of abstraction); §4 one responsibility
- evidence: `values = arguments.fetch_values(:protocol, :catalog, … :executor)` then `@protocol, @catalog, raw_run_kind, … @executor = values` — two 15-slot ordered lists must stay in lockstep; a swapped key silently misbinds, and per-field `String()` coercion is interleaved with bulk destructuring via throwaway `raw_*` locals.
- proposal: assign each attribute explicitly from `arguments.fetch(...)` with its coercion on the same line (`@run_kind = String(arguments.fetch(:run_kind))`, …). One list instead of two; drop the `raw_*` temporaries and likely the AbcSize disable (15 plain assignments).
- risk: low
- within_file: yes
## C2: run-inline-manifest-literal
- where: `OpenclawMissionRunner#run` (~lines 62–88)
- principle: P2/P5 (step-down); §4
- evidence: `run` is MethodLength-disabled while it embeds a 19-line manifest literal whose `'surface_executions'` value is itself a nested `artifacts.to_h { [artifact.fetch('mission').fetch('id'), …] }`.
- proposal: extract `build_manifest(artifacts)` (returns the literal) and `surface_execution_index(artifacts)` for the nested `to_h`; `run` then reads make-dir → run missions → build → persist → `Result`, and both disables can go. No byte changes.
- risk: low
- within_file: yes
## C3: run-mission-duplicate-record-builders
- where: `OpenclawMissionRunner#run_mission` (~lines 180–205) vs `OpenclawMissionRunner#mission_record` (~line 394)
- principle: P2/P5; §4 one responsibility
- evidence: success path builds an 12-key mission-record literal inline (with `artifact_path`/`artifact_digest`/conditional `reason`/`.compact`) while the rescue path calls the separate `mission_record(failure, mission)` builder with a different key set — two spellings of "the manifest row for one mission".
- proposal: unify into one builder `mission_record(result, mission, artifact: nil)` covering both paths: `artifact_path`/`artifact_digest` from `artifact&.fetch(...)`, `durable_mission` via `result['durable_mission']`, all under `.compact`; `reason` included only when present. Output is key-for-key identical for both paths today, and `CanonicalJSON.dump` sorts object keys (canonical_json.rb `normalize_object`), so artifact/manifest bytes are unchanged. Then extract the builder call out of `run_mission` so it reads execute → write-if-ready → build records → rescue to normalized failure (~10 lines).
- risk: medium
- within_file: yes
## C4: normalize-result-flat-sequence
- where: `OpenclawMissionRunner#normalize_result` (~lines 246–273)
- principle: P5 (step-down); §3.1 normalize_*
- evidence: AbcSize+MethodLength-disabled; three copies of the idiom `X = default unless status == 'ready'` (metrics/hard_zero/surfaces) sit at the same level as the final `result.merge(...)` shape decision.
- proposal: extract `normalize_evidence(result, mission, status)` returning `{ metrics:, hard_zero:, effect_outcomes:, surface_executions: }` by calling the existing helpers in the current order (validation/raise order preserved); `normalize_result` keeps status → evidence → `outcome_status` → merge (~12 lines) and drops its disables.
- risk: medium
- within_file: yes
## C5: independent-trace-giant-boolean
- where: `OpenclawMissionRunner#validate_independent_trace!` (~lines 449–464)
- principle: P2/P5
- evidence: complexity-disabled; one 11-condition `valid = …` mixing envelope checks (source, trace_id/run_id/thread_id, mission_id), digest binding, and span counting (`model_spans >= receipts.length`).
- proposal: split into named predicates, e.g. `trace_envelope_valid?(evidence, mission)` and `trace_digest_bound?(evidence, receipts)`; keep the single `raise SchemaError, 'real-provider mission lacks independent trace evidence'` unchanged.
- risk: low
- within_file: yes
## C6: ready-provenance-two-jobs
- where: `OpenclawMissionRunner#validate_ready_provenance!` (~lines 406–424)
- principle: P5
- evidence: first validates generic run-kind/provider/model binding, then — only for `real_provider` — does receipt + trace-digest + independent-trace verification inline (12 lines).
- proposal: extract `validate_real_provider_trace!(provenance, mission)` for the `real_provider` branch; outer method becomes two reads. Error messages untouched.
- risk: low
- within_file: yes
## C7: outcome-status-positional-list
- where: `OpenclawMissionRunner#outcome_status` (~line 343), sole caller `normalize_result` (~260)
- principle: §4 keyword args over positional lists
- evidence: `def outcome_status(status, hard_zero, effect_outcomes, surface_executions)` — four same-typed hash/array positionals; the call site reads as noise.
- proposal: keyword arguments `status:, hard_zero:, effect_outcomes:, surface_executions:`; sole caller updated in-file.
- risk: low
- within_file: yes
## C8: unavailable-surfaces-hidden-special-case
- where: `OpenclawMissionRunner#unavailable_surfaces` (~line 373)
- principle: P3 (levels of abstraction)
- evidence: inside a generic per-surface builder, `'status' => reason == 'executor_error' ? 'blocked' : reason` silently encodes a reason→status remap known only to callers.
- proposal: name the mapping next to the sibling status constants, e.g. `SURFACE_STATUS_BY_REASON = { 'executor_error' => 'blocked' }.freeze`, used as `SURFACE_STATUS_BY_REASON.fetch(reason, reason)`.
- risk: low
- within_file: yes
## C9: execute-name-collides-with-executor
- where: `OpenclawMissionRunner#execute` (~line 208), sole caller `run_mission` (~181)
- principle: P1; §3 method names say what they return
- evidence: private `execute(mission)` actually returns the validated, normalized executor result — and sits beside the injected `@executor.call`, so reader must disambiguate two "execute" verbs.
- proposal: rename to `validated_execution(mission)` (or `normalized_executor_result`).
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- §5 asymmetry in the public result: `Result.new(manifest:, artifacts: artifacts.freeze)` freezes the array but not the manifest hash it hands to Readiness/tests — freeze-the-value fix is one word, owner call whether the manifest is treated as frozen public surface.
- Manifest `missions[]` rows intentionally differ between failure and blocked paths (no `artifact_path`/`artifact_digest`/`durable_mission` keys on failure). If C3 unifies the builders, confirm this asymmetric wire shape is deliberate before merging; bytes stay identical either way.
