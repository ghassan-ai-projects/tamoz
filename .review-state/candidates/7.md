# Candidates — rank 7 gems/tamoz-stream/lib/tamoz/stream/situation_request.rb
LOC: 958  Verdict: CANDIDATES
## C1: split-finalize-stream
- where: `EpisodeRunner#finalize_stream` (~line 504)
- principle: #2/#3/#5 one thing, one level; §4 "one responsibility per method … policy, persistence, orchestration **and** rendering is the canonical violation"
- evidence: one method derives the terminal status, loads checkpoint state, builds the artifact manifest, retains artifacts, emits model events, updates budget, translates the decision, opens verifications, then emits terminal — orchestration plus four concrete concerns.
- proposal: step down, preserving exact ordering: extract `emit_produced_artifacts(envelope, stream, adapter, terminal_state)` (manifest build + `retain_manifest_artifacts` + `emit_model_events` + conditional `emit_budget_update`) and `record_produced_decision(envelope, stream, snapshot, terminal_state)` (`translate_decision` + `open_verifications`); `finalize_stream` keeps status / `load_terminal_state` / fallback manifest / `stream.terminal`.
- risk: medium
- within_file: yes
## C2: execution-id-single-owner
- where: `EpisodeRunner#build_context` (~line 476); owner is `EpisodeRequestEnvelope#request_id` (~line 136)
- principle: #1/#3 — the fence-embedded id join (the fencing invariant's byte format) has two owners
- evidence: `execution_id: "episode.#{envelope.episode_id}.#{envelope.attempt_id}.#{envelope.fence}"` re-implements `"episode.#{episode_id}.#{attempt_id}.#{fence}"` two lines below kwargs that already use `envelope.request_id` twice.
- proposal: pass `execution_id: envelope.request_id` (byte-identical today); the format stays asserted independently by tests (`test/stream_episode_crash_matrix_test.rb:170` et al.), so drift remains caught.
- risk: low
- within_file: yes
## C3: parsed-reconsideration-redundant-guard
- where: `EpisodeRequestEnvelope#parsed_reconsideration` (~line 96)
- principle: #2 does one thing; dead condition
- evidence: `return nil unless KIND_NAMES.key?(@wire.kind)` is subsumed by the next line — an unknown wire kind maps `kind` to nil, so `kind != :reconsider` already returns nil.
- proposal: replace both guards with `return nil unless reconsider?` (predicate already defined ~line 358); sole caller `wire_payload` (~line 346) runs post-`validate!`.
- risk: low
- within_file: yes
## C4: kwargs-over-positional-lists
- where: `EpisodeRunner#episode_content_for` (~line 596), `#open_verification_for_intent` (~line 619), `#finalize_stream` (~line 504)
- principle: §4 "keyword arguments beyond one or two positional parameters"
- evidence: `def episode_content_for(envelope, snapshot, entity, decision, digest)` — five positional params of mixed kinds; call site (line 590) reads as a bare list.
- proposal: convert these privates to keyword args (`episode_content_for(envelope:, snapshot:, entity:, decision:, digest:)`; likewise `open_verification_for_intent`); `finalize_stream`'s list dissolves under C1's split. All callers are in this class (grep-proven).
- risk: low
- within_file: yes
## C5: enforce-genuine-provider
- where: `EpisodeRunner#ensure_genuine_provider!` (~line 672)
- principle: §3.1 checking verbs — a refusal with authority here is `enforce_`; `ensure_` is outside the lexicon
- evidence: the method raises `wire_refused_model_event/forged_provider_marker` and `…/missing_provider`; the same gem already spells this role `enforce_result_bounds!` (`gems/tamoz-stream/lib/tamoz/stream/capability_host.rb:180`).
- proposal: rename to `enforce_genuine_provider!`; sole callsite line 654, grep-proven in-file only.
- risk: low
- within_file: yes
## C6: unify-wire-code-derivation
- where: `EpisodeRunner#run` (~lines 404–408)
- principle: #3/#5 — the failure-code derivation is written twice at two confidence levels
- evidence: `rescue StreamError … code: error.class::CATEGORY` beside `rescue StandardError … error.class.const_defined?(:CATEGORY) ? error.class::CATEGORY : "internal_error"`.
- proposal: extract `def wire_code_for(error)` and call it from both rescues; every StreamError subclass defines CATEGORY (`errors.rb:9–45`), so real-path behavior is identical.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Filename lies: `situation_request.rb` holds `EpisodeRequestEnvelope` + `EpisodeRunner`; nothing named SituationRequest lives here. Rename touches require paths across gems/tests — multi-file, out of scope for this pass.
- `validate_identity_fields!` (~193) checks emptiness twice with divergent messages ("episode identity is incomplete…" vs "#{field} is required" at ~211); collapsing them changes error-message bytes, so only the owner can pick the survivor.
- `wall_time` is enforced (`validate_budget!`, ~293) but never propagated by `budget_hash` — the dead claim already logged as G6 in docs/new-design pattern audits; carrying it forward is a behavior change, not a refactor.
- `identity_for` (~420) mints an ad hoc Struct per failure; `docs/repo-quality-audit-2026-08-20/missing-abstractions.md:103` already requests a named immutable episode identity value (cross-file: `EpisodeStream` consumes it duck-typed).
