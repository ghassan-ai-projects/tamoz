# Candidates — rank 31 gems/tamoz-evals/lib/tamoz/evals/verifier.rb
LOC: 561  Verdict: CANDIDATES
## C1: split verify_provenance!
- where: `Verifier#verify_provenance!` (~line 388)
- principle: 2 (60 lines, hard ceiling 30) + 3 + 5 — mixes three unrelated jobs: component/authority identity, run-attempt structure, and citation checks
- evidence: lines 389–409 check component role/id uniqueness and gate↔decision_authority equality; lines 411–426 check attempt/snapshot/fixture uniqueness and repetition bounds; lines 428–446 check that attempts and infrastructure_errors cite known evidence
- proposal: extract `verify_provenance_components!(document)` (components + authority match) and `verify_provenance_attempts!(document, reference_ids)` (attempts, repetition, snapshots/fixtures, citation loops); bodies moved verbatim so error message bytes are unchanged
- risk: low
- within_file: yes
## C2: split verify_evidence_process!
- where: `Verifier#verify_evidence_process!` (~line 264)
- principle: 2 (60 lines) + 5 — termination policy and stdout/stderr byte accounting are two different subjects at two levels
- evidence: lines 267–308 are exit/signal exclusivity plus the `termination_reason` case; lines 310–322 are an unrelated loop over `%w[stdout stderr]` comparing `bytes`/`captured_bytes`/`truncated`
- proposal: extract `verify_process_termination!(process)` (exclusivity, timeout agreement, reason case, KILL consistency) and `verify_process_streams!(process)` (the stream loop); parent becomes a 3-line step-down; messages untouched
- risk: low
- within_file: yes
## C3: resolve_reference_path! out of verify_references!
- where: `Verifier#verify_references!` (~line 102)
- principle: 2 (50 lines) + 3 + §3.1 (`resolve_` "turns a reference into the thing it names")
- evidence: lines 107–120 are a self-contained escape/containment unit (NUL, backslash, absolute, `..`, realpath prefix check) nested inside a size/digest loop
- proposal: extract `resolve_reference_path!(relative_path, root)` returning `real_path`; the loop body then reads fetch-size → read → compare size → compare digest → build result hash
- risk: low
- within_file: yes
## C4: one helper for the triplicated "cites missing evidence" loop
- where: `Verifier#verify_semantics!` (~line 176), `Verifier#verify_evidence_semantics!` (~line 228), `Verifier#verify_provenance!` (~line 428)
- principle: 4 (call sites read as the domain) + §4 one responsibility
- evidence: three identical shapes — `missing = x.fetch("evidence_ids"/"reference_ids") - reference_ids; unless missing.empty? raise InvalidArtifactError, "<kind> <id.inspect> cites missing evidence <missing.inspect>"` for hard gates, claims, and attempts
- proposal: extract `validate_citations!(kind, record, reference_ids)`; the three messages interpolate kind/id/missing, so bytes are preserved exactly ("hard gate …", "claim …", "attempt …")
- risk: low
- within_file: yes
## C5: rename assert_unique_ids! → validate_unique_ids!
- where: `Verifier#assert_unique_ids!` (~line 449)
- principle: §3.1 checking-verb table — failure here is bad artifact input ("references contain duplicate ids"), not a bug in Tamoz-owned state, so it is `validate_*`, not `assert_*`
- evidence: raised class is InvalidArtifactError; all 10 occurrences (def + 9 calls) are in this file per repo-wide grep; `Schema.load(...).validate!` already uses the same verb in this gem
- proposal: pure rename to `validate_unique_ids!`; same reasoning applies to `assert_no_diagnostic_errors!` (~line 527) → `validate_no_diagnostic_errors!` (its failures are also artifact-content refusals) — take both or just the first
- risk: low
- within_file: yes
## C6: case dispatch in verify_semantics!
- where: `Verifier#verify_semantics!` (~line 153)
- principle: 2 (36 lines) + 3/4 — sequential `if artifact_type == "case"` / `if artifact_type == "evidence"` followed by ~25 lines of inline result rules
- evidence: lines 154–160 two guarded returns; lines 162–187 the entire result-artifact rule set inline
- proposal: extract `verify_result_semantics!(document)` (returns the mapped decision verbatim) and reduce the parent to a three-arm `case artifact_type` returning `"verified"` / evidence decision / result decision; `else` is provably `"result"` since `DIGEST_DOMAINS.fetch` gated entry
- risk: low
- within_file: yes
## C7: expand_artifact_path out of verify
- where: `Verifier#verify` (~line 39)
- principle: 2 (34 lines, over ceiling) + 3 — input-path normalization sits at a different level than the verify pipeline
- evidence: lines 40–45 coerce/rescue `File.path`, reject NUL bytes, then `File.expand_path`; the rest of the method is the linear verify pipeline ending in `Verification.new`
- proposal: extract `expand_artifact_path(path)` containing lines 40–47 verbatim (TypeError/ArgumentError rescue travels with it; the Errno/JSON rescues stay on `verify`)
- risk: low
- within_file: yes
## C8: group process checks in verify_evidence_semantics!
- where: `Verifier#verify_evidence_semantics!` (~line 214)
- principle: 2 (49 lines) + 5 — claims/content-policy level interleaved with per-process level
- evidence: lines 237–259 fetch processes, require records for process-kind evidence, require an intentional intervention for selector evidence, then compare each process duration against the timing envelope
- proposal: extract `verify_evidence_processes!(document, timing)` holding lines 237–259 verbatim; parent keeps uniqueness, content policy, citations, status
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `verify_evidence_status!` (~325) and `verify_decision_evidence!` (~473) are deliberate mirrors: five status arms × evidence-vs-result with per-arm distinct message bytes. Not proposed as a merge (message identity + verdict semantics untouchable), but if a third artifact surface ever needs status rules, owner should decide whether the mirror becomes declarative data rather than a third copied case.
- Public-surface suspicion: `test/release_evaluation_manifest_test.rb:150` reaches into `Verifier::DIGEST_DOMAINS`, and `RESULT_DECISIONS`/`Verification` sit on the gem's constant surface while only `verify(path)` is used cross-file — candidates for narrowing if nothing outside tamoz-evals needs them.
