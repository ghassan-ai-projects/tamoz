# Candidates — rank 3 gems/tamoz-agent/lib/tamoz/agent/worker_runtime.rb
LOC: 1225  Verdict: CANDIDATES
## C1: inert frozen_string_literal and its recorded lint debt
- where: `WorkerRuntime` file top (lines 1–2) + duplicate `private` (~line 1033)
- principle: §2 (magic comment "is the first line of every file"); §1 gate policy (.rubocop_todo.yml "only shrinks")
- evidence: line 1 `require 'set'` precedes `# frozen_string_literal: true` on line 2, so the comment never applies — every string literal in this 1225-line durability file is mutable. `.rubocop_todo.yml` records exactly this fallout against this file: Style/FrozenStringLiteralComment, Lint/RedundantRequireStatement (`require 'set'` is redundant on the pinned 3.3.11), and Lint/UselessAccessModifier (the bare `private` at 1033 follows the one at 909 with nothing public between).
- proposal: delete line 1 and the redundant `private` at 1033, then drop those three per-file exclusions from .rubocop_todo.yml in the same change. Verified safe: the only mutation site is `Set#<<` (line 1151); no string literal is mutated or identity-compared.
- risk: low
- within_file: yes
## C2: repeated schedule-projection literals deserve pinned constants
- where: `#scheduled_effect_state` (~946), `#scheduled_delivery_state` (~955), `#scheduled_phase` (~967); schema string at ~441 and ~916; `%i[completed failed blocked]` at ~233 and ~1214
- principle: §3.1 (one concept, one spelling) + §7 (durable contract strings stay byte-exact)
- evidence: the terminal-state list `%w[succeeded failed cancelled unknown skipped coalesced]` is spelled out three times inside the projection case statements; `"tamoz.scheduled_work.v1"` twice (healthy vs degraded document); the child terminal-view tuple twice. A one-word drift in any copy silently mislabels a status.
- proposal: `SCHEDULE_TERMINAL_STATES`, `SCHEDULED_WORK_SCHEMA = "tamoz.scheduled_work.v1".freeze`, and a name for the `%i[completed failed blocked]` view statuses; substitute at all sites. The SQL IN-lists in tamoz-sqlite's migrator are a different representation and stay untouched. No key added or removed — the emitted document is unchanged.
- risk: low
- within_file: yes
## C3: occurrence-age pair duplicates its durable read
- where: `WorkerRuntime#occurrence_age_seconds` (~393) / `#occurrence_age_milliseconds` (~402)
- principle: 2 (does one thing) / 5 (step-down to a shared concrete reader)
- evidence: both wrap the identical `durable("occurrence age for #{thread_id.inspect}") { record(OPEN_OCCURRENCES, thread_id)&.fetch("opened_at", nil)` + `Time.parse` block; only the unit math differs.
- proposal: extract private `occurrence_opened_at(thread_id)` returning the parsed Time or nil; each public method keeps its own `durable` label and unit conversion. Public signatures untouched — worker.rb calls both names externally.
- risk: low
- within_file: yes
## C4: refusers should carry the enforce_ verb
- where: `#narrowed_tools(resolved, allowed_tools)` (~1187); `#ensure_child_capacity!` (~632)
- principle: §3.1 checking table — "policy applies and may refuse the operation" is `enforce_*`
- evidence: `narrowed_tools` raises `ToolPolicyError, "child capabilities exceed profile tools"` — it does not compute a narrowing, it refuses one; `ensure_child_capacity!` raises `'child delegation concurrency budget is exhausted'`. Both names read as builders/guards, hiding that authority lives here.
- proposal: rename `narrowed_tools` → `enforce_narrowed_tools` and `ensure_child_capacity!` → `enforce_child_capacity!`. All callers verified in-file (`build_session`, `reserve_child_slot!`). (`validate_child_transition!` stays: it is a genuine shape check.)
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `durable`'s contract comment (~541: "Every durable read and write in this file goes through here") is false for four writers: `bind_thread_profile` (~119), `store_schedule_payload` (~139), `open_occurrence` (~319), `record_budget_exhaustion` (~416) call `record`/`upsert` unwrapped, so raw storage failures escape instead of StoreUnavailableError — the exact guess-vs-refuse invariant the method exists for. Fixing means changing error identity on those paths → compatibility decision, not refactor.
- `close_approval_session` (~36) crashes on nil `@bound_approval_sessions` when teardown runs before any session was bound (@approval_engine is always set by initialize); today masked only by the `rescue StandardError` in worker.rb:109.
- Metrics/ClassLength exclusion keeps absorbing growth of a class mixing four responsibilities (durable record access, schedule projection, child-task lifecycle, session/profile building); structural splits are out of scope this pass.
