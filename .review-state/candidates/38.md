# Candidates — rank 38 gems/tamoz-observability/lib/tamoz/observability/recorders.rb
LOC: 493  Verdict: CANDIDATES
## C1: rename-format-line-to-render-line
- where: `Journal#format_line` (~line 298)
- principle: §3.1 vocabulary (render_/parse_ pair) + principle 1 (name states intent)
- evidence: `def format_line(signal); JSON.generate(signal.to_h.merge('policy_digest' => ...))` produces the outward durable form that `Files.parse_entry` (~line 439, `JSON.parse`) reads back — the exact render_/parse_ pair §3.1 names. Repo has 39 `def render_*` sites and zero other `format_*` producers.
- proposal: rename `format_line` → `render_line`; sole caller is `append_line` (~line 295, same file). Generated line bytes untouched — wire contract preserved.
- risk: low
- within_file: yes
## C2: step-down-journal-initialize-paths
- where: `Journal#initialize` (~lines 166–189)
- principle: principles 2, 3, 5 — method spans 24 lines (over the normal ≤20 ceiling) and mixes three abstraction levels: path-string plumbing, limit coercion, drain wiring
- evidence: lines 170–173 derive `@directory`/`@path`/`@health_path` via `File.expand_path`/`File.join`/`sanitize_role` interleaved with policy and queue-config assignments before `super(...)`.
- proposal: add private `resolve_journal_paths(directory:, role:, pid:)` returning `[directory, path, health_path]`; initialize starts `@directory, @path, @health_path = resolve_journal_paths(...)` and keeps the existing `prepare_directory(@directory)` position. Filename templates `"#{@role}-#{Integer(pid)}.ndjson"` and `".health.json"` stay byte-identical (Files globbing depends on them).
- risk: low
- within_file: yes
## C3: rename-sanitize-role-to-normalize-role
- where: `Journal#sanitize_role` (~line 378)
- principle: §3.1 vocabulary (normalize_ returns the canonical form of its input)
- evidence: `String(role).gsub(/[^a-zA-Z0-9_.-]/, '_')` canonicalizes input — gem-mates spell this identically: `Signal#normalize_key` (signal.rb:148), `ContentPolicy#normalize_classification` (content_policy.rb:97), `Metrics#normalize_labels`. Repo-wide: ~95 `normalize_*` vs 4 `sanitize_*`.
- proposal: rename `sanitize_role` → `normalize_role`; sole caller is `initialize` (~line 171, same file). Regex unchanged so journal filenames are unchanged.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Fanout#guarded (~line 135) rescues any StandardError into `:dropped` — correct for #record, wrong for its other two callers: in `#flush` (line 125) a child failure yields `Integer(:dropped || 0)` → TypeError escapes the guard entirely; in `#health` (line 120) a failing child injects the Symbol `:dropped` as a health document. Fixing means error-path behavior change → owner decision, not a refactor candidate.
- Memory and Journal duplicate the drop-ledger/validation helpers verbatim (`validate!`, `drop_key`, `drops_hash`, `count_invalid_drop`) and share the record/rescue shape; deduplicating needs a shared module — structural, outside this pass's method-level scope.
- All four public classes carry no §11 class docstrings; Style/Documentation debt is absorbed by per-file `.rubocop_todo.yml` excludes (17 cops list this file). Whether doc debt earns a slice is an owner call.
