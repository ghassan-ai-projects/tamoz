# Candidates — rank 24 gems/tamoz-evals/lib/tamoz/evals/harness/subprocess_runner.rb
LOC: 593  Verdict: CANDIDATES

## C1: step-down inside wait_for_child
- where: `SubprocessRunner#wait_for_child` (~lines 258–353)
- principle: 2 (96 lines vs ≤20 norm / 30 ceiling) + 5 (step-down); aligns with docs/reviews/codebase-review-2026-08-20/missing-abstraction.md §9
- evidence: one method interleaves (a) the `Process.waitpid2` polling loop, (b) twin kill-on-decision blocks at ~283–292 and ~308–318 (identical shape: `signal_group("KILL", pid)` → `wait_for_exit` → assert SIGKILL status → `return [status, false, "kill", source]`), and (c) the post-deadline TERM→KILL escalation ladder at ~324–352.
- proposal: extract three private helpers, statement-for-statement identical, zero reordering of kill/wait/deadline logic: `escalate_termination(pid)` (owns the TERM→KILL ladder, returns today's 4-tuple), `kill_on_decision(pid, source)` (shared KILL block; error-message bytes preserved via `"#{source} did not produce SIGKILL process status"`), and `poll_stopped_intervention(pid, stop_signal, intervention, remaining_ms)` (returns continue-signal or the kill tuple). `wait_for_child` keeps the loop and all deadline arithmetic byte-for-byte.
- risk: medium
- within_file: yes

## C2: drain_readers! does not drain
- where: `SubprocessRunner#drain_readers!` (~line 392)
- principle: 1 (name states wrong intent) + §3.1 checking verbs (`assert_`) + §3 (`!` needs a safe twin)
- evidence: after the child is reaped it never reads anything — it escalates TERM→KILL on the process group, closes readers, and raises "child descendants retained output streams after process exit".
- proposal: rename to `assert_readers_finished!`, the enforcing twin of the `readers_finished?` predicate it calls at lines 393/396/399. Sole call site is line 176.
- risk: low
- within_file: yes

## C3: signal_group boolean parameter
- where: `SubprocessRunner#signal_group(signal, pid, allow_missing: false)` (~line 405)
- principle: §4 "no boolean parameters (Reek BooleanParameter)"
- evidence: strict call sites 284 and 309 rely on ESRCH raising; five sites (324, 335, 395, 397, 419) pass `allow_missing: true` for best-effort signaling — one flag, two behaviors.
- proposal: split into `signal_group(signal, pid)` (raises when the group is gone) and `signal_group_if_present(signal, pid)` (returns true/false); drop the flag. Call sites then read as the domain: `signal_group_if_present("TERM", pid)`. EPERM handling stays identical in both.
- risk: low
- within_file: yes

## C4: capture_thread hides the capture algorithm
- where: `SubprocessRunner#capture_thread` (~lines 224–256)
- principle: 2 + 5 (thread construction vs stream-capture algorithm at two abstraction levels)
- evidence: named for building a Thread, yet the chunk-read loop with SHA256 digesting and output-limit accounting plus `Stream` construction all sit inline in the block (33 lines).
- proposal: extract the block body to `capture_stream(reader)` returning the frozen Stream, so `capture_thread` reduces to `Thread.new { capture_stream(reader) }`; optionally `append_bounded_chunk(captured, chunk, limit)` for the limit accounting. Read loop order untouched.
- risk: low
- within_file: yes

## C5: resolve_executable mixes validation with search
- where: `SubprocessRunner.resolve_executable` (~lines 73–105)
- principle: 2 (33 lines, above reviewed ceiling) + 3 (input validation vs PATH search)
- evidence: validates/normalizes the basename (74–80), validates/normalizes the search path (81–88), then scans absolute PATH entries (90–97) in one body.
- proposal: extract `normalize_executable_name(name)` and `normalize_search_path(path)` using the file's existing `normalize_*` vocabulary; they own today's exact error strings verbatim. Public name, signature, search loop, and raised messages unchanged.
- risk: low
- within_file: yes

## C6: poll_running_child polls the poller, not the child
- where: `SubprocessRunner#poll_running_child` (~line 355)
- principle: 1 (subject of the name is wrong)
- evidence: it invokes `poller.poll(pid:, remaining_ms:)` and shape-checks the decision; the child's actual liveness is polled by `waitpid2` back in `wait_for_child`.
- proposal: rename to `poller_decision(poller, pid, remaining_ms)` (methods say what they return), symmetric with `poll_intervention`; sole call site line 278.
- risk: low
- within_file: yes

## C7: execute inlines pipe teardown and Result assembly
- where: `SubprocessRunner#execute` (~lines 153–208)
- principle: 5 (step-down); 2 (56-line orchestrator)
- evidence: the ensure block hand-guards four IO closes (202–207) and `Result.new` is populated field-by-field mid-method (183–193).
- proposal: extract `close_pipes!(*ios)` for the guarded closes and `build_result(...)` for the Result construction, leaving execute reading spawn→watch→drain→result; residual length remains orchestration (§4: limits are diagnostic).
- risk: low
- within_file: yes

## FLAG (owner decisions only)
- `wait_for_child` returns a positional 4-tuple destructured at line 169; folding it into a small `Data.define` termination value during C1 would beat passing tuples between the extracted helpers, but adds a class to this file — owner call before implementing C1.
- Cross-gem/public surface confirmed consumed elsewhere and excluded from all renames above: `INTERVENTION_KILL` (openclaw_durable_cli_adapter.rb:56, sqlite_selector_control_intervention.rb:50), `Stream`/`Result` (test/sqlite_selector_control_test.rb:783–822), `capture` and `resolve_executable` (adapter + tests).
- C1/C3 edit code inside the timing-sensitive timeout/deadline path (known flaky under parallel load): validate with `ruby -Itest test/subprocess_runner_test.rb` alone, never batched.
