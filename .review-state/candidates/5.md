# Candidates — rank 5 gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_comms_runner.rb
LOC: 1088  Verdict: CANDIDATES

## C1: remove-shadowed-duplicate-failed-q
- where: `OpenclawCommsRunner#failed?` (~line 69, shadowed by second def ~line 83)
- principle: 2 (one thing — one definition per intent); cop-confirmed `Lint/DuplicateMethods`
- evidence: `def failed?; @failed == true; end` at 69–71 and `def failed?; @failed; end` at 83–85; Ruby keeps the last, so the first is dead and the pair reads as two intents for one name.
- proposal: delete lines 69–71. Sole external caller (`script/benchmark_comms_run:67`, `exit 2 if runner.failed?`) resolves to the surviving definition either way — behavior identical.
- risk: low
- within_file: yes
## C2: dedupe-scripted-parity-factory
- where: `#drive_c1` (~279–290) vs `#drive_c6` (~363–374)
- principle: 2 / §4 one responsibility; §3.1 `build_*` construction named once
- evidence: byte-identical 12-line `OpenclawCommsFixture.model_factory(plan: [goal 'answer the task', step s1 read_file], review: [ACCEPTED_REVIEW], verify: [VERIFY_OK.first + 'operator answer…'])` literal in both drivers (drive_c7/c8_waiting_factory re-repeat the read/edit step hashes a third/fourth time).
- proposal: private `def parity_answer_factory` returning that exact factory; drive_c1 and drive_c6 call it. Optional same-pattern follow-up: shared frozen step constants for c7/c8.
- risk: low
- within_file: yes
## C3: extract-fresh-outbox-since-baseline
- where: `#leg_delivery_state` (~342), `#status_probe_words` (~700–703), `#run_command_sweep` (~756–758), `#control_reply_text` (~997–999)
- principle: 3 + 5 (one level of abstraction; name the recurring concept); §4 one responsibility
- evidence: four inline copies of "capture `outbox.map delivery_id` before submit, then `outbox.reject { |row| baseline.include?(row.fetch('delivery_id')) }`".
- proposal: private `fresh_outbox_rows(fixture, baseline_ids)` = outbox rows whose delivery_id is not in baseline; each site collapses to one call with identical expressions and ordering.
- risk: low
- within_file: yes
## C4: rename-stale-owner-refused-predicate
- where: `#stale_owner_refused` (~531), sole caller ~490
- principle: 1 / §3 predicates end in `?`; cop-confirmed `Naming/PredicateMethod`
- evidence: returns a boolean (`result != :marked`) but the name reads as a command.
- proposal: rename to `stale_owner_refused?`; caller becomes `stale_mark_refused = stale_owner_refused?(fixture)`. No callers outside this file (grep across gems/ and test/).
- risk: low
- within_file: yes
## C5: share-latest-pending-answer-row
- where: `#pending_answer` (~515) vs `#stale_owner_refused?` body (~532)
- principle: 2 / DRY on one concept, one spelling
- evidence: identical query duplicated verbatim: `fixture.outbox(statuses: %w[pending]).reverse.find { |candidate| candidate['kind'] == 'answer' }`.
- proposal: extract `latest_pending_answer_row(fixture)` returning the row; `pending_answer` becomes `latest_pending_answer_row(fixture)&.fetch('delivery_id')`; the stale-owner probe uses the row helper directly.
- risk: low
- within_file: yes
## C6: status-probe-reuses-control-reply-capture
- where: `#status_probe_words` (~700–707) vs `#control_reply_text` (~996–1002)
- principle: 5 step-down (probe should read as domain: submit /status → compare reply word to view word)
- evidence: status_probe_words repeats control_reply_text's submit→reject→find-control sequence only to regex `reply['text'][/\btask=([a-z]+)/, 1]`.
- proposal: `'reply_word' => control_reply_text(fixture, update_id, '/status')[/\btask=([a-z]+)/, 1]` — equivalent results ('' and missing-reply both yield nil through the regex).
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Fact-key suspicion: private `message_ids` builds `{message_id => true}` but feeds artifact key `message_id_by_update_id`, which oracle c5 probes via `facts[...][row['reply_to']]` (openclaw_comms_oracles.rb:216) while `raw_update` sets `message_id = update_id + 10_000` — key content does not match the key's stated shape. Renaming the fact key changes artifact bytes/digests, so owner decision only; I proposed no rename there.
- `test/canonical_cross_surface_composition_test.rb:510–514` carries its own copy of the runner's private `leg_delivery_state` — cross-file duplication/drift risk; owner may want it sourced from one place.
- File-wide gate state: RuboCop reports 79 live offenses here (ClassLength 884/250; MethodLength offenders incl. drive_c7 50, drive_c8_raced_restart 46, drive_c6 43, drive_c1/drive_c5/drive_c9 36) with no `.rubocop_todo.yml` entry. Splitting those linear scenario scripts is sequencing-sensitive (medium risk) and outside this pass's low-risk mandate — owner decides recorded-debt vs an authorized split slice. A mechanical hygiene bundle also exists if wanted: `Lint/UselessConstantScoping` ×4 (:150, :711, :843, :844), redundant cop-disable :100, correctable Style/SafeNavigation :516/:1018.
