# Candidates — rank 22 gems/tamoz-sqlite/lib/tamoz/sqlite/boundary_source_audit.rb
LOC: 614  Verdict: CANDIDATES
## C1: parse-call-dispatch-unwrap
- where: `Auditor#parse_call` (~line 383)
- principle: 2 (51 lines vs hard ceiling 30) + 5 step-down
- evidence: one case over Ripper node shapes (386–408) is followed by a 17-line
  inline `__send__`/`public_send`/`send` unwrapping block (410–426) before the
  final `Call.new`; two concerns in one body.
- proposal: extract lines 410–426 into `resolve_dispatched_target(name,
  positional)` returning `[name, positional.drop(1)]` and raising the existing
  "dynamic dispatch is forbidden…" ConfigurationError verbatim; `parse_call`
  then reads classify → unwrap dispatch → build Call (~30 lines).
- risk: low
- within_file: yes
## C2: forbidden-call-guards-out-of-walker
- where: `Auditor#inspect_forbidden!` (~line 531)
- principle: 2 (33 lines > ceiling) + 3
- evidence: the tree walk mixes its recursion skeleton with four sequential
  call-shape prohibition guards (`execute` outside tx, filesystem mutation,
  dynamic execution) at 544–561.
- proposal: extract 544–561 into `assert_call_allowed!(call)` keeping every
  raise/message byte-identical; the walker reduces to constant check → guard →
  recurse (~12 lines). `assert_` per §3.1 (invariant this code owns).
- risk: low
- within_file: yes
## C3: name-index-placeholder-rule
- where: `Auditor#resolve_string` (~line 317)
- principle: 2 (32 lines > ceiling) + 3
- evidence: the five-condition conjunction at 329–335 (`allow_index &&`
  `!value.is_a?(String) && reviewed_index_expression?(expression) && index ==
  content.length - 1 && DYNAMIC_INDEX_PREFIXES.include?(fragments.join)`) is
  embedded mid-loop inside string assembly — the subtlest rule in the file,
  currently unnamed.
- proposal: hoist that conjunction verbatim into predicate
  `reviewed_trailing_index_slot?(value:, expression:, fragments:, last:)`;
  control flow and error bytes unchanged, `resolve_string` drops to ~25 lines.
- risk: medium
- within_file: yes
## C4: audit-step-down
- where: `Auditor#audit!` (~line 62)
- principle: 3 + 5; §3.1 assert_*
- evidence: six named steps (`parse_source`, `reject_duplicate_operations!`,
  `inspect_root`, `compare_registry!`) are followed by an inline 5-line
  unreachable-helper check (68–72) and an 11-line nested sort/to_h/deep_freeze
  expression (74–84) — concrete shaping mixed into orchestration.
- proposal: extract those two blocks into `assert_no_unreachable_helpers!` and
  `frozen_source`; `audit!` becomes ~8 lines reading as a pure step list,
  symmetric with the existing `reject_…!/compare_…!` naming.
- risk: low
- within_file: yes
## C5: name-keyword-binding
- where: `Auditor#bind_arguments` (~line 238)
- principle: 5 step-down
- evidence: three-way ternary chain encodes nil-vs-absent keyword semantics
  (explicit argument resolves; explicitly-passed-nil inherits
  `caller_environment`; absent means UNKNOWN) with no name for the distinction.
- proposal: extract the loop body of 238–247 into
  `bound_keyword_value(name, call_keywords, environment)` returning one of the
  three outcomes; identical values, one readable name.
- risk: low
- within_file: yes
## C6: call-nodes-constant
- where: `Auditor#collect_roots!` (~line 144) + `Auditor#inspect_node` (~line 169)
- principle: §3.1 one concept one spelling / 3
- evidence: literal `%i[command command_call method_add_arg]` duplicated in both
  methods; both mean "Ripper nodes that may carry a call".
- proposal: hoist to `CALL_NODES = %i[command command_call method_add_arg].freeze`
  beside `TRANSACTION_METHODS` (already `private_constant`-tracked at 605–609).
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `resolve_value(..., label:)` and `resolve_string(..., allow_index:)` are
  keyword booleans acting as mode switches; §4 bans boolean parameters (Reek's
  detector may not cover keyword forms), but splitting the resolver would
  duplicate its dispatch — owner call whether this is worth machinery.
