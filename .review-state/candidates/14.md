# Candidates — rank 12 gems/tamoz-sqlite/lib/tamoz/sqlite/schedule_store.rb
LOC: 721  Verdict: CANDIDATES
## C1: claim-scan step-down
- where: `ScheduleStore#claim_one_schedule` (~line 271) and `#materialize_due` (~line 224)
- principle: 2/3/5 (§4 one responsibility, ≤30 ceiling); claim_one_schedule is ~99 lines mixing grant enforcement, template resolution/validation, misfire recording, overlap policy, and enqueue; materialize_due is ~43 lines
- evidence: `claim_one_schedule` spans lines 271–369 doing five distinct jobs sequentially in one body; `materialize_due` (224–266) adds the per-schedule begin/rescue on top
- proposal: step-down INSIDE the class only — transaction block and tx threading stay exactly where they are. Extract named private steps that receive `tx:`: `resolve_request_template(request_template, schedule)` (§3.1 resolve_, incl. the Hash validation raise), `record_misfire_skips(schedule, selection:, now:, tx:)` (lines 321–326), `deliver_or_record(schedule, occurrence, ...)` for the per-instant case at 346–366, and move the 253–262 begin/rescue into `claim_with_conflict_record(schedule, ...)`. Both public signatures unchanged; boundaries stay visible via the passed tx
- risk: medium
- within_file: yes
## C2: put_schedule step-down
- where: `ScheduleStore#put_schedule` (~line 39)
- principle: 2/5 (64 lines vs ≤30 ceiling; one-level violation — CAS, enabled-lifecycle policy, insert, and rematerialize in one body)
- evidence: lines 59–75 embed the enabled-preservation decision (with its load-bearing why-comment) mid-insert; lines 42–43 inline the definition-digest prefix
- proposal: extract `preserved_enabled(schedule, current_revision, tx)` carrying the lifecycle comment to the named seam, plus `schedule_definition_digest(payload_bytes)` for the ENQUEUE_CONTEXT_DOMAIN+SHA256 pair. Transaction core untouched
- risk: medium
- within_file: yes
## C3: keyword-args and boolean parameter on P13-B internals
- where: `#build_request_payload` (~line 659), `#enqueue_occurrence` (~line 431), `#record_terminal` (~line 476)
- principle: §4 (no boolean parameters — Reek BooleanParameter; keyword args over positional lists)
- evidence: `def build_request_payload(schedule, occurrence, template, include_provenance = true)` is a positional boolean default; `record_terminal(schedule, occurrence, :skipped, "misfire", now, tx)` has six positionals whose meaning only the def reveals; `enqueue_occurrence` takes six positionals + one kw
- proposal: make `include_provenance:` a keyword on both methods; convert `record_terminal` to `record_terminal(schedule, occurrence, state:, reason:, now:, tx:)` and `enqueue_occurrence` to keywords for now/owner/request_template/tx. All call sites are in this file (grep across gems/, apps/, bin/, test/ shows zero external callers of these three names)
- risk: low
- within_file: yes
## C4: narrow the public surface
- where: `ScheduleStore` (whole file; no `private` section exists)
- principle: §6 narrow public APIs; principle 4 (public surface should read as the domain DSL)
- evidence: grep proves `occurrence_state`, `occurrence_exists?`, `record_grant_denied`, `record_scan_conflict`, `record_terminal`, `enqueue_occurrence`, `build_occurrence`, `normalize_request_address`, `build_request_payload`, `materialize_schedule`, `materialize_occurrence`, `symbolize_keys`, `now_ms`, `claim_one_schedule` have zero callers outside this file, yet all are public because the class never marks anything private
- proposal: mark the helper clusters private (before `occurrence_state` ~line 371 and before the `--- internals ---` section ~line 636), keeping the cross-gem surface public: contract methods plus `acknowledge_occurrence`, `occurrence_for_request`, `fetch_schedule`, `list_schedules` (used by tamoz-agent/tamoz-agent-cli)
- risk: low
- within_file: yes
## C5: enable/disable twins share one write path
- where: `#enable_schedule` (~line 142) / `#disable_schedule` (~line 171)
- principle: 2/5 (one responsibility, step-down); the two bodies are byte-identical except the operation labels ("schedule.enable"/"schedule.disable") and `enabled = 1` vs `0`
- evidence: lines 145–166 vs 174–195: same latest-revision SELECT, same conflict raise message bytes, same UPDATE shape
- proposal: shared private `set_schedule_enabled(id, expected_revision:, enabled_value:, operation_prefix:, tx)` (named value, not a boolean flag); the two public contract methods become two-line delegations preserving every label string and error byte
- risk: medium
- within_file: yes
## C6: occurrence_state returns an unlabeled pair
- where: `#occurrence_state` (~line 376), consumed at ~line 311
- principle: §5 values/state (internal stable shapes as immutable Data values, not bare arrays)
- evidence: `[row.fetch(0).to_i, row.fetch(1).to_i]` returned positionally; `non_terminal, pending = occurrence_state(schedule.id, tx)` then separately incremented at 364–365
- proposal: return `OccurrenceState = Data.define(:non_terminal, :pending)` (frozen, file-local constant); call sites read `state.non_terminal` and rebind via `.with(...)`
- risk: low
- within_file: yes
## C7: enqueue_occurrence step-down
- where: `#enqueue_occurrence` (~line 431)
- principle: 2/5 (~39 lines vs ceiling; digest derivation and occurrence-row insert at one level)
- evidence: lines 433–445 derive thread/namespace, payload bytes, and two Wire.digests before the enqueue and the INSERT at 452–467
- proposal: extract `request_payload_digests(operation, payload)` returning the payload_digest/input_digest pair (or fold into C3's keyword reshaping); insert stays inline so the atomic sequence remains readable
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `acknowledge_occurrence` is called cross-gem (gems/tamoz-agent/lib/tamoz/agent/worker.rb:552, duck-typed on schedule_store) but is absent from `Tamoz::Scheduler::ScheduleStore` CONTRACT_VERSION=2 — versioned-contract/public-surface suspicion worth an owner look.
- `disable_schedule(id, expected_revision:, reason:)` accepts `reason:` and silently drops it (nothing persists it); dead parameter on the versioned contract — removing it changes the cross-gem signature, persisting it changes stored bytes, so both are out of scope here.
