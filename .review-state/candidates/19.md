# Candidates — rank 19 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_scenario_runtime.rb
LOC: 678  Verdict: CANDIDATES

## C1: extract frozen failure payload from commit-failed preparer
- where: `SQLiteScenarioRuntime#prepare_checkpoint_commit_failed` (~line 438)
- principle: 2 (short/one thing) + §4 hard ceiling — body is 35 lines vs reviewed ceiling 30; only method in the file over it
- evidence: L450–459 embeds a 10-line frozen literal inside setup orchestration: `{ "graph" => GRAPH_NAME, "node" => "work", "task_id" => "task.phase2", ... "error_class" => "ScenarioFailure", "safe_message" => "fixed failure" }.freeze`
- proposal: private method `failure_payload` returning that frozen array; preparer drops to ~25 lines and reads lease→start→claim→transition→attributes→action at one level
- risk: low
- within_file: yes

## C2: takeover-after-claim pair → one named helper
- where: `#prepare_request_recover_stale` (~199), `#prepare_request_recover_claimed` (~211), `#prepare_request_recover_running` (~222/236)
- principle: 4 + 5 — call sites should read as domain; identical two-step idiom repeated 3×
- evidence: each preparer repeats `lease, request = claimed_request(:turn)` … `current = takeover_lease(lease)` (L199–200, L211–212, L222+236)
- proposal: private `takeover_claimed_request(operation)` returning `[current, request]`; sites become `current, request = takeover_claimed_request(:turn)`
- risk: low
- within_file: yes

## C3: step down inside the two longest commit preparers
- where: `#prepare_checkpoint_commit_advance` (~327), `#prepare_checkpoint_commit_turn` (~357)
- principle: 3 (one level of abstraction) — both are 27-line bodies mixing store writes, state-manager math, transition building, attribute assembly, action wiring
- evidence: advance computes state inline (`@compiled.state_manager.apply_outcomes(checkpoint.state, [outcome], remaining_steps: ...)`, L332–336); turn builds a 6-line request_transition hash inline (L362–367)
- proposal: extract `advanced_state(checkpoint, outcome)` and `completed_transition(request)`; each preparer then reads as a linear scenario recipe
- risk: low
- within_file: yes

## C4: drop write-only @wire ivar in setup!
- where: `#setup!` (~line 33)
- principle: 5 (values/state) — instance state nothing else ever reads
- evidence: `@wire = Tamoz::SQLite.const_get(:Wire, false)` then only use is next line `@namespace = @wire.namespace([])`; no other occurrence in file or repo
- proposal: inline to `@namespace = Tamoz::SQLite.const_get(:Wire, false).namespace([])`; keeps M7-relevant load semantics untouched, removes dead state
- risk: low
- within_file: yes

## C5: name the column allowlist in update_lease_fixture!
- where: `#update_lease_fixture!` (~line 513)
- principle: 2/3 — 29-line body mixes input validation with the transaction+assert core
- evidence: L514–518 `unless %w[greatest_backend_time_ms lease_expires_at_ms].include?(column) ... raise ExecutionError`
- proposal: predicate `fixture_column?(column)` (§3 predicates end in ?); remaining body is one level: guard, transact, assert changes == 1
- risk: low
- within_file: yes

Deliberately not proposed: `checkpoint_attributes` (30 lines) is a single cohesive defaults-from-checkpoint mapping — §4's "never split cohesive logic merely to satisfy a number" applies; the 21-line preparers are within normal tolerance. No file split proposed (out of scope this pass).

## FLAG (owner decisions only)
- Cross-gem surface (already logged as M7 in docs/reviews/codebase-review-2026-08/tamoz-evals.md, still open): this runtime reaches into tamoz-sqlite internals via `Tamoz::SQLite.const_get(:Wire, false)`, `defined?` capability probes, and `__send__ :acquire_lease/:validate_lease/:renew_lease/:release_lease/:transaction` on Adapter privates. Owner call: promote a minimal public test seam on the adapter or accept eval-only reach-through.
- `setup!`/`action!` lifecycle names are thin ("action!" states little); rename would touch sqlite_scenario_driver.rb too (L95/L98), so it fails the within-file rule — owner decision if wanted.
- File carries `.rubocop_todo.yml` exclusions incl. Metrics/ClassLength (678 > 250) and Metrics/MethodLength — recorded debt; candidates above shrink MethodLength exposure without structural splits.
