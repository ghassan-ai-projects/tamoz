# Candidates — rank 45 gems/tamoz-mcp/lib/tamoz/mcp/supervisor.rb
LOC: 439  Verdict: CANDIDATES

## C1: step-down-supervisor-initialize
- where: `Supervisor#initialize` (~line 142)
- principle: 2 (≤20 normally, hard ceiling 30 — method is 43 lines) + 5 (step-down); §4 one responsibility
- evidence: constructor mixes three jobs: config/store validation (`unless config.is_a?(ServerConfig)…`, `store = circuit_store || MemoryCircuitStore.new…; validate_circuit_store!(store)`, lines 152–158), ~14 direct ivar assignments, and the SDK `super` call. Sibling `HttpSupervisor#initialize` (http_supervisor.rb:21–46) already shows the target shape with extracted `validate_config!` and `build_circuit_store`.
- proposal: extract two private helpers mirroring the sibling's vocabulary (§3.1: `validate_*` shape-check, `build_*` returns the value): `validate_config!(config)` raising the byte-identical `"config must be a Tamoz::Mcp::ServerConfig"`, and `build_circuit_store(circuit_threshold:, circuit_store:)` containing the existing two lines plus `validate_circuit_store!`. Call them at the exact current positions so validation order and error identity are unchanged. Residual length is the pinned 8-keyword signature shared verbatim with HttpSupervisor; do NOT group ivar assignments into a settings-blob helper — §4 forbids splitting cohesive logic just to hit a number.
- risk: low
- within_file: yes
## C2: split-child-environment-sources
- where: `Supervisor#child_environment` (~line 295)
- principle: 5 (step-down) + 2 (two responsibilities: allowlist inheritance AND fail-closed credential resolution); §3.1 `resolve_*`
- evidence: one 16-line method runs two different loops — `@config.env_allowlist.each { … }` copying inherited values, then `@config.credential_refs.each { … }` raising `ValidationError` on a missing ref. `HttpSupervisor#resolve_credential(header, ref)` (http_supervisor.rb:120) already names the credential half repository-wide.
- proposal: step down to `inherited_environment` (allowlist copy, nil-skipping), `resolved_credential_environment` (credential refs), sharing `resolve_credential(name)` that raises the byte-identical missing-ref message; `child_environment` becomes `inherited_environment.merge(resolved_credential_environment)` and reads like the domain. Values and overwrite precedence (credential wins on duplicate name) are preserved; `Process.spawn` key order is semantically irrelevant. Test hook `supervisor.send(:child_environment)` (test/mcp_supervisor_test.rb:94) keeps working.
- risk: low
- within_file: yes
## C3: extract-stderr-retention-policy
- where: `Supervisor#start_stderr_capture` (~line 395)
- principle: 5 (step-down) + 3 (thread lifecycle vs bounded-buffer policy are two abstraction levels)
- evidence: the `Thread.new` block inlines setup flag, `loop { readpartial … }`, and the retention policy (`@stderr_buffer << chunk.b; if @stderr_buffer.bytesize > limit … byteslice(-limit, limit)`) in one 12-line block.
- proposal: extract `append_stderr_chunk(chunk, limit:)` owning the identical `@stderr_mutex.synchronize` critical section and trim logic; the thread block reduces to flag + read loop. Mutex discipline, chunk encoding (`.b`), and the rescue scope (`IOError, Errno::EBADF, EOFError` around the read loop) stay byte-equivalent — String-only buffer ops cannot raise those, so moving the append out of the rescue changes nothing observable. Method-level only; no reorder of capture/teardown.
- risk: low
- within_file: yes
## C4: rename-cleanup-stderr
- where: `Supervisor#cleanup_stderr` (~line 379)
- principle: 1 (name states intent); §3/§3.1 one verb per concept — teardown steps otherwise form a clean `close_transport_pipes` / `terminate_process_group` / `mark_closed` family
- evidence: `cleanup_stderr` actually closes the stderr pipe and joins `@stderr_thread` — i.e., closes the stderr *capture*; "cleanup" is a second spelling for `close_*` already used two lines earlier in the same caller (`close`, line 247).
- proposal: rename `cleanup_stderr` → `close_stderr_capture`. Sole caller is `close` in this same file (grep across gems/ and test/: only supervisor.rb:249 and :379; other hits are prose docs). No signature or behavior change.
- risk: low
- within_file: yes

Not proposed, checked and rejected: `MemoryCircuitStore` is on the documented public API (`docs/public_api.json`, `test/public_api_test.rb`) — its method names are pinned and its methods are already short and single-level; `spawn_child_process` (23 lines) is under the reviewed ceiling and cohesive; `terminate_process_group`'s escalation ladder is pinned lifecycle semantics — structure left untouched; redundant trailing `@state` reads in `record_failure`/`record_success`/`reset` are noise but map to no cited principle strongly enough to be worth the diff.

## FLAG (owner decisions only)
- `read_line(method, params)` (~line 278): BOTH parameters are unused — the signature only mirrors the MCP SDK's private API (gem pinned `mcp ~> 1.1`; docs/reviews/codebase-review-2026-08/tamoz-mcp.md already lists this override family as the gem's largest fragility). Do not let a later "dead param" cleanup break the SDK call convention; a guard comment or upstream check belongs to the owner.
- `HttpSupervisor` builds an injected circuit store WITHOUT `validate_circuit_store!` (http_supervisor.rb:102–104) while stdio `Supervisor` enforces the full duck-typed `CircuitStore` contract (DR-2). The durable-swap guarantee is asymmetric across the two supervisors; fixing it adds behavior to another file, so it needs an owner decision. If taken, C1's `build_circuit_store` is the natural home to unify in `CircuitSupervision`.
- Circuit-state machinery is duplicated between `MemoryCircuitStore` (supervisor.rb:26–107) and `EgressCircuit` (websearch/egress_circuit.rb:121+ has its own `conditions_digest` and `CONDITIONS_DOMAIN`) — prior review M1 flagged ~120 lines. Dedup is a multi-file structural edit, out of scope for this pass.
