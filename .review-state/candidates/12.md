# Candidates — rank 10 gems/tamoz-mcp/lib/tamoz/mcp/invocation.rb
LOC: 743  Verdict: CANDIDATES

## C1: dedupe-call-reissue-pinned-preamble
- where: `Tamoz::Mcp::Invocation.call` (~line 84) and `.reissue` (~line 107)
- principle: #2 one thing / #5 step-down (six identical preamble steps duplicated)
- evidence: both bodies repeat `verify_pinned_digest!` → `ensure_available!` → `build_client` → `ensure_connected!` before computing `effect_key` and entering `round_trip`; they differ only in `validate_arguments!` (call-only), `Elicitation.answer` (reissue-only) and the `input:` value
- proposal: extract private `open_pinned_channel(descriptor, snapshot:, supervisor:, client_factory:)` holding the four shared calls **in current order**; `call` keeps `validate_arguments!` ahead of it, `reissue` keeps `Elicitation.answer` after it — effect ordering provably identical (retry path's own `ensure_connected!` at line 365 keeps calling the existing method directly)
- risk: medium
- within_file: yes
## C2: round-trip-step-down
- where: `#round_trip` (~line 352)
- principle: #2 (35 lines incl. comments vs ≤20 norm) / #5 step-down — one method owns retry policy, transport classification, remote-error row, protocol row, and interrupt row (five rescue arms)
- evidence: lines 353–368 are the read-only retry loop (`max_attempts`/`attempts`/`transport_failure`/`retry_eligible?`/restart+reconnect); lines 369–383 are three independent taxonomy rows
- proposal: move the `Timeout::Error, MCP::Client::RequestHandlerError` arm into private `transport_round_trip(descriptor:, arguments:, client:, supervisor:, input:)` with its `begin/rescue/retry` intact; `round_trip` keeps the ServerError/ValidationError/InputRequired arms wrapped around it. Also fold the inline ValidationError message (lines 374–376) into a named builder `malformed_response_error(descriptor)` beside its siblings `corruption_error`/`read_only_unavailable_error` (exact message bytes preserved). Escape semantics unchanged: exceptions raised inside a rescue arm already bypass sibling rescues today, and `restart`/`ensure_connected!` surface only Tamoz-typed errors, which the MCP-client rescues never matched
- risk: medium
- within_file: yes
## C3: rename-transport-failure-to-record-transport-failure
- where: `#transport_failure` (~line 421)
- principle: #1 name states intent / §3 vocabulary — side-effecting name reads as a query
- evidence: docstring says "Records the failure and returns whether the request was fully written"; the body calls `supervisor.record_failure` (advances circuit counters, DR-2 context) while the name promises a failure value, not a recording
- proposal: rename to `record_transport_failure`. Sole caller is line 359 in this file (grep-proved; `gems/tamoz-telegram/lib/tamoz/telegram/client.rb#transport_failure` is an unrelated class)
- risk: low
- within_file: yes
## C4: rename-classify-response-to-succeeded-outcome
- where: `#classify_response` (~line 542)
- principle: #1 / #4 — Outcome builders should read as the domain
- evidence: it validates result shape, rejects `isError` results, validates structured content, records success, builds the Observation, and returns `Outcome(status: :succeeded)`; "classify" names none of that, while its sibling `interrupt_or_deny` names exactly the Outcome it returns
- proposal: rename `classify_response` → `succeeded_outcome(response, descriptor:, supervisor:, effect_key:)`. Sole caller is line 385 in this file (grep-proved)
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `strict_schema`/`deep_strictify`/`strictable_object?` are duplicated verbatim between this file (237–268) and `gems/tamoz-mcp/lib/tamoz/mcp/elicitation.rb` (218–245, comment: "Same default-deny rule as Invocation"). Cross-file dedupe = structural extraction, out of scope for this pass; also why NO rename candidate is proposed for either copy (renaming one would diverge the pair).
- `headless:` boolean keyword on public `call`/`reissue` violates §4 (no boolean parameters); public/cross-gem surface, untouchable here — owner call if ever revisited.
- Minor: `CLIENT_INFO` and `CONTROL_CHARACTER_PATTERN` are consumed here but defined in `Tamoz::Mcp::SharedConstants` (shared_constants.rb) via nesting lookup — conventional for this gem, noted for visibility only.
