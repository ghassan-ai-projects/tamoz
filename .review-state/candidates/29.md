# Candidates — rank 29 gems/tamoz-graph/lib/tamoz/graph/executor.rb
LOC: 568  Verdict: CANDIDATES
## C1: step-down the super-step loop in `run`
- where: `Tamoz::Graph::Executor#run` (~line 13, body 13–185)
- principle: 2 (≤20 normally, 30 hard ceiling — `run` is ~170 lines) + 5 (step-down); carries recorded `.rubocop_todo.yml` debt for Metrics/MethodLength, AbcSize, CyclomaticComplexity, PerceivedComplexity on this file
- evidence: one loop interleaves limit enforcement, execution, and three terminal sequences, each shaped append-checkpoint → emit → `RunResult`: failed branch (~60–88), paused branch (~106–131), and `non_interactive_interrupt` (~433–488) repeats the failed sequence almost line-for-line; `terminal_request_transition(...)` blocks are nested inside kwargs hashes (~71–78, ~116–122, ~170–178)
- proposal: extract move-only private terminals — `record_failed_run(...)` shared by the errors branch and `non_interactive_interrupt` (dedupes the duplicated failed sequence), `record_paused_run(...)`; compute request transitions above the append calls. Preserve DR-2 order exactly: writer append before emits, emits before `RunResult`; `context.check!`/`writer.check!` stay at loop top and immediately before the advance append (~150–151); the advance branch (~134–181) keeps its exact call order. No reordering of dispatch, commit, or recovery.
- risk: medium
- within_file: yes
## C2: local `cancelled` shadows private method `cancelled`
- where: `#run` (~line 49 destructure, ~line 54 use) vs `#cancelled` (~line 499)
- principle: 1 (name states intent) / 3 (one level of abstraction — one identifier, two bindings)
- evidence: `successes, interruptions, errors, cancelled = classify(...)` then `return cancelled(current) if cancelled || context.cancelled?` — same word is a boolean local and a result-builder method in one line
- proposal: rename the local to `tasks_cancelled` (or `pool_cancelled`); no signature changes
- risk: low
- within_file: yes
## C3: name what `classify` classifies; rename `expected`
- where: `#classify` (~line 284) and local `expected` (~line 34)
- principle: 1 (a function name states its intent)
- evidence: `classify(to_execute, results, current)` names neither input nor output — it buckets pool results into successes/interrupts/errors/cancelled and silently index-couples to `tasks.fetch(index)`; `expected = tasks.to_h { |task| [task.id, task] }` reads as an adjective, but is a scheduled-task lookup filtered against `pending`
- proposal: rename `classify` → `classify_pool_results`; rename local `expected` → `scheduled_by_id`. Call sites are lines 34 and 49 only (grep over gems/ and test/ shows no other reference)
- risk: low
- within_file: yes
## C4: step down `execute_task` (context, events, invocation, journaling in one body)
- where: `#execute_task` (~lines 212–264, ~50 lines) — also carries Metrics/ParameterLists debt (6 params)
- principle: 2 + 3 + 5
- evidence: derives `InterruptCursor`/`SubgraphRuntime` task context, emits `:task_start` (~234), resolves input, calls the node (~243), normalizes, builds `Outcome` (~245–253), emits `:task_end` (~254), then `writer.append_writes` (~262)
- proposal: extract `invoke_node(task, checkpoint, task_context)` covering input-resolution → Outcome construction, plus a small event-payload builder for the two `emit` pairs. Keep statement order verbatim — especially `append_writes` after the `task_end` emit (journal receipt ordering, DR-2)
- risk: medium
- within_file: yes
## FLAG (owner decisions only)
- `test/graph_interrupt_test.rb:117–138` white-boxes this class: `const_get(:Executor, false)` + `__send__(:validate_outcome!, ...)` pins a private name outside the file, so `validate_outcome!` cannot be renamed under the within-file rule (within_file: no). Worth an owner pass anyway: §3.1 argues the verb is wrong (it asserts Tamoz-owned identity invariants and raises CheckpointConflictError — assert_/verify_, not validate_), and its `pending:` keyword is a boolean behavior flag (§4) splitting two validation behaviors that could be named separately — but any change must update that test contract in the same commit.
