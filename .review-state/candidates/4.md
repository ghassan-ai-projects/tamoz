# Candidates — rank 4 gems/tamoz-sqlite/lib/tamoz/sqlite/comms_store.rb
LOC: 1113  Verdict: CANDIDATES
## C1: admit-and-encode step-down
- where: `CommsStore#admit_and_enqueue` (~line 118) and `CommsStore#request_cancellation` (~line 308)
- principle: P2 (≤20 lines; body is ~46, over the hard 30 ceiling), P5 step-down, §4 one responsibility
- evidence: lines 149–157 and 308–313 are the identical trio `payload_bytes = @checkpoints.checkpoint_codec.dump_request_payload(...)` / `payload_digest = Wire.digest(...)` / `input_digest = Wire.digest(JSON.generate([OP, DELIVERY, ...]))`; lines 138–148 inline the request-row binds + INSERT OR IGNORE inside the admission flow.
- proposal: extract private `encoded_request(operation_text, delivery_text, payload)` returning the bytes/digest/input_digest trio (§3.1 encode_* is the durable-codec verb pair) used by both call sites, and private `insert_admitted_request!(txn, envelope_wire, surface_id:, bot_id:, thread:, profile_id:, reservation:, now:)` for lines 137–148. Both stay inside their existing single transaction block — boundaries untouched, digest bytes identical.
- risk: low
- within_file: yes
## C2: narrow the accidental public surface
- where: `CommsStore` — helpers defined above `private` (line 1022): `capacity_saturated?`, `turn_payload`, `resolve_request_ref`, `status_projection`, `cancellation_document`, `cancellation_facts`, `cancellation_outcome`, `queue_facts`, `queue_position`, `oldest_open_created_at_ms`, `backend_now_ms`, `conversation_runtime_status`, `generation_row!`, `open_requests_for`, `active_request_row`, `task_state_for`, `effect_state_for`, `effect_statuses`, `summarize_effect_statuses`, `capability_state_for`, `terminal_reason_for`, `lifecycle_status_for`, `lifecycle_next_action`, `lifecycle_events_for`, `capability_invoked?`, `capability_bound?`, `delivery_state_for` (~lines 196–732)
- principle: §6 narrow public APIs ("expose what collaborators need and nothing more"); P4 public reads-as-domain
- evidence: every one takes a raw `txn` or checkpoint row — implementation vocabulary, not domain. Repo-wide grep (gems/, apps/, test/, bin/, docs excluded as non-callers): zero callers outside this file; gateway/drainer/sink/fixture call only contract+facade methods. The §13 contract (`gems/tamoz-comms/lib/tamoz/comms/comms_store.rb`) requires none of them.
- proposal: move the 28 listed methods below `private`. No signature or caller changes anywhere.
- risk: low
- within_file: yes
## C3: name the session-checkpoint incantation once
- where: `CommsStore` (~lines 641, 665, 675, 680)
- principle: P5 step-down / duplicated operation, §11 why-comment earns its place at one site instead of four
- evidence: `@checkpoints.latest(thread_id:, namespace: [], validate_identity: false)` repeated verbatim in `effect_statuses`, `capability_state_for`, `terminal_reason_for`, `lifecycle_status_for` (one `&.` variant). The flag's reason is documented only in docs/approval-policy-redesign-2026-08-22/08-implementation-bars.md:310 — the latest row belongs to the session graph while this checkpointer is compiled against the channel-gateway graph.
- proposal: private `def session_checkpoint(thread_id); @checkpoints&.latest(thread_id:, namespace: [], validate_identity: false); end` carrying a one-line why-comment; four call sites become one word.
- risk: low
- within_file: yes
## C4: pairing filter assembly step-down
- where: `CommsStore#pairing_challenges` (~line 950)
- principle: P2 (~24-line body), P3 one level of abstraction (filter policy interleaved with SQL string building)
- evidence: lines 954–971 build `clauses`/`binds` with three nested conditionals, then line 970–972 assembles `sql << " WHERE #{clauses.join(' AND ')}"`.
- proposal: extract private `pairing_filters(status:, surface_id:, correspondent_id:)` returning `[clauses, binds]`; `pairing_challenges` drops to read → assemble → map.
- risk: low
- within_file: yes
## C5: compose the decision store like the other seams
- where: `CommsStore#consume_prompt` line 846 vs `initialize` (lines 65–70)
- principle: §5 explicit collaborators over hidden construction; consistency with the composed-store seam the class already establishes
- evidence: `CommsDecisionStore.new(adapter: @adapter).insert_decision_in_transaction!(txn, decision_wire)` constructs a collaborator mid-transaction while `@outbox`/`@routes` are composed in `initialize`.
- proposal: add `@decisions = CommsDecisionStore.new(adapter:)` in `initialize` (constructor is `initialize(adapter:)` only); consume_prompt calls `@decisions.insert_decision_in_transaction!`.
- risk: low
- within_file: yes
## C6: stop round-tripping facts through the hash just built
- where: `CommsStore#status_projection` (~line 437) → `conversation_runtime_status` (~line 539), and `conversation_status` (~lines 375–380)
- principle: P3 one level of abstraction; §4 parameters carry what the caller already knows
- evidence: `status_projection` receives `thread_id, request_id` as locals, embeds them in `base`, then `conversation_runtime_status(base, ...)` re-derives them via `route_status.fetch('thread_id')` / `.fetch('request_id')`. In `conversation_status`, `(active && active.fetch(2)) || route.fetch(0)` reads positional indices two SELECTs away.
- proposal: pass `(thread_id, request_id, surface_id, conversation_id)` to `conversation_runtime_status` directly; name the tuple reads at the SELECT sites (`active_request_id`, `active_thread_id`). Return shapes unchanged.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Cross-gem vocabulary: the turn-payload builder has three spellings — `Tamoz::Core::TurnContext.task` (canonical), `SessionPlanningContext.turn_payload` (one-line wrapper, agent-session), `CommsStore#turn_payload` (one-line wrapper, here). §3.1 one-concept-one-verb says pick one home repo-wide.
- Contract naming: `disposition_only` (§13 contract method, callers across tamoz-comms gateway) doesn't say what it returns the way its siblings do (`@return [:recorded, :conflict_recorded, :duplicate]`); any rename is a cross-gem contract change, so owner-only.
