# Candidates — rank 40 gems/tamoz-mcp/lib/tamoz/mcp/server_config.rb
LOC: 476  Verdict: CANDIDATES

## C1: endpoint-security-triple-negative-to-guard-clauses
- where: `ServerConfig#validate_endpoint_security!` (~line 241)
- principle: #2/#5 step-down + readability; §4; shrinks the file's `Style/GuardClause` todo entry
- evidence: `return unless uri.scheme == "http" && !loopback_host?(uri.host) &&
  !(allow_insecure_http && private_ip_host?(uri.host))` — a three-term compound with two
  nested negations decides whether credentials-bearing config is admitted over plain HTTP.
- proposal: rewrite as sequential guards with identical truth table and identical message
  bytes: `return unless uri.scheme == "http"`; `return if loopback_host?(uri.host)`;
  `return if allow_insecure_http && private_ip_host?(uri.host)`; then the existing
  `raise ValidationError, "endpoint must use https unless it targets loopback"`.
- risk: low
- within_file: yes
## C2: process-surface-positional-list-to-keywords
- where: `ServerConfig#validate_process_surface!` (~line 309; call site line 139)
- principle: §4 "keyword arguments beyond one or two positional parameters"; Q6
  parameters ≤ 5 ceiling — this takes 6 positional params (file is in the
  `Metrics/ParameterLists` todo exclude)
- evidence: `def validate_process_surface!(transport, command, arguments,
  env_allowlist, working_directory, workspace)`; call site destructures the returned
  `[command, working_directory]` pair, so which argument is which is invisible at line 139.
- proposal: convert to keyword parameters — `validate_process_surface!(transport:,
  command:, arguments:, env_allowlist:, working_directory:, workspace:)`; the sole caller
  already holds identically named locals, so the call site becomes self-documenting with a
  one-line diff. Body, branch order, and all message bytes unchanged.
- risk: low
- within_file: yes (grep over gems/ apps/ test/: only reference is line 139 of this file)
## C3: inline-budgets-check-breaks-validator-pipeline
- where: `ServerConfig#initialize` (~lines 144–146)
- principle: #4 public constructor reads as a small DSL; #5 step-down consistency
- evidence: every field flows through a named `validate_*!` seam
  (`server_id = validate_server_id!(...)` …), then `budgets` alone is checked inline:
  `unless budgets.is_a?(Budgets) … raise ValidationError, "budgets must be a
  Tamoz::Mcp::ServerConfig::Budgets"`.
- proposal: extract `validate_budgets!(value)` (returns `value` if `is_a?(Budgets)`,
  else raises the byte-identical message) beside the other validators, reducing
  `initialize` to a uniform 14-line validator pipeline. Residual method length is the
  pinned kwarg signature plus `super(...)` — no further split proposed (§4: limits are
  diagnostic, logic is cohesive).
- risk: low
- within_file: yes
## C4: describe-budgets-literal-duplicates-budgets-members
- where: `ServerConfig#describe` (~lines 174–184)
- principle: #2 single source of truth; §5 — the 10-line literal must be hand-synced
  whenever `Budgets` gains or renames a field, or the operator preview silently lies
- evidence: `"budgets" => { "max_catalog_entries" => budgets.max_catalog_entries, … }`
  restates all nine members of `Budgets` in definition order.
- proposal: `"budgets" => Budgets.members.to_h { |name| [name.to_s, budgets.public_send(name)] }.freeze`
  — key order and values are byte-identical today (members order == literal order) and
  stay correct when Budgets changes; the surrounding `describe` hash keeps `.freeze`.
- risk: low
- within_file: yes
## C5: unnamed-header-value-byte-cap
- where: `ServerConfig#validate_header_value!` (~line 300)
- principle: #1 name states intent; §3 constants — the file names every other security
  bound (`MAX_ARGUMENT_BYTES`, `MAX_DESCRIPTION_BYTES`)
- evidence: `value.bytesize <= 4096` — a bare literal guarding HTTP header size.
- proposal: add `MAX_HEADER_VALUE_BYTES = 4096` next to `MAX_ARGUMENT_BYTES` and compare
  against it. Same number, same behavior, bound becomes searchable and greppable.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `Tamoz::Mcp::ServerConfig.credential_env_name?` is public surface consumed across gems
  (`tamoz-mcp/elicitation.rb`, `tamoz-evals/…/agent_smoke_corpus.rb`) and pinned by tests —
  correctly out of rename scope; noted so nobody "privatizes" it.
- Pinned error text `"endpoint must use https unless it targets loopback"` understates the
  rule it enforces (explicit `allow_insecure_http` private-IP opt-in also admits HTTP);
  message bytes are pinned, so rewording is an owner compatibility decision, not a refactor.
