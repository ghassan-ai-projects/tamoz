# Candidates — rank 36 gems/tamoz-core/lib/tamoz/core/jcs.rb
LOC: 515  Verdict: CANDIDATES
## C1: step-down-emit-object-array
- where: `Tamoz::Core::JCS#emit` (~line 112)
- principle: 2 (44 lines vs hard reviewed ceiling 30, CODING_STANDARD §4) + 3 + 5
- evidence: `when Hash` branch (lines 114–132) inlines duplicate-key detection after
  stringification, the UTF-16BE sort policy (`key.encode("UTF-16BE").b`), and brace/comma
  emission inside what is otherwise a pure type dispatcher; `when Array` (lines 133–139)
  repeats the sequence mechanics. The scalar branches already delegate (`emit_string`,
  `integer_to_s`, `float_to_s`) — only containers don't.
- proposal: move both branch bodies VERBATIM into sibling helpers `emit_object(value, out)`
  and `emit_array(value, out)` placed next to `emit_string` (~line 157), leaving `emit` a
  ~15-line dispatch. Byte-proof: identical statements in identical execution order; no
  literal, sort key, or escape-table byte moves. Grep across gems/ and test/ shows zero
  callers of `emit*` outside jcs.rb (test hits are stream/observability `sink.emit`), so no
  signature or surface changes; mark new helpers `private_class_method` if desired.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- JCS internals (`emit`, `emit_string`, `shortest_digits`, `format_number`, `round_trips?`)
  are public module functions today though only used in-file; collapsing them to
  private_class_method is a public-surface decision, not proposed as a candidate.
- The double-exactness invariant is duplicated between `integer_to_s` (line 186,
  `value.abs <= MAX_SAFE_INTEGER || value == value.to_f`) and `Scanner#number_from`
  (line 473, same predicate on text-derived integer). They must stay in sync; sharing one
  predicate is safe only because error-message bytes differ per site — owner call, since
  error identity/message bytes are frozen by this review's constraints.
