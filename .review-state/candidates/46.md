# Candidates — rank 46 gems/tamoz-evals/lib/tamoz/evals/benchmark/scenario_driver.rb
LOC: 437  Verdict: CANDIDATES
## C1: step-down the executor, drop the Metrics-disable crutch
- where: `ScenarioDriver#call` (~line 295)
- principle: 2 (38 lines vs ≤20 norm / hard 30) & 5 step-down; §4 one responsibility; §1 exception-at-site
- evidence: lives under `# rubocop:disable Metrics/AbcSize, Metrics/MethodLength, Metrics/PerceivedComplexity`; one body mixes guard/passthrough, provider/model coercion, prepare!/prepare_changes!/fixture setup, drive dispatch, evidence join, oracle-argument selection, result assembly.
- proposal: extract `oracle_arguments(evidence:, pre_kill_effect_key:)` (the restart if/else hash literal at ~313–321) and `scored_result(mission:, provider:, model:, thread:, pre_kill_effect_key:)` (`evidence_for` + `oracle` + `result_for`, ~312–328). `call` keeps the exact sequence (raise message bytes `'scenario driver only supports real_provider runs'` and rescue unchanged) and shrinks to ~17 lines; delete all three disables. File has no `.rubocop_todo.yml` entry, so nothing else holds it up.
- risk: low
- within_file: yes
## C2: initialize repeats the same scenario condition twice
- where: `ScenarioDriver#initialize` (~line 285)
- principle: 2 does-one-thing (accidental duplication); §4
- evidence: `required.push(:worker_until_effect!, :worker_subprocess!) if @scenario.fetch('id') == RESTART_SCENARIO_ID` immediately followed by `required.push(:prepare_changes!) if @scenario.fetch('id') == RESTART_SCENARIO_ID`.
- proposal: one conditional push — `required.push(:worker_until_effect!, :worker_subprocess!, :prepare_changes!) if restart_scenario?` (`@scenario` is already assigned two lines above).
- risk: low
- within_file: yes
## C3: materialize_fixture un-materializes in restart mode
- where: `ScenarioDriver#materialize_fixture` (~line 345)
- principle: 1 name states intent; 3 one level of abstraction
- evidence: on the restart path it calls `remove_owned_restart_fixture` then raises `'restart_fixture_already_exists'` if anything survives — it clears, not materializes; the `return write_fixture(...) unless restart_scenario?` guard-return buries the fork.
- proposal: rename to `prepare_fixture`; split into `write_status_fixture` vs `prepare_restart_fixture` (remove-owned + absence enforcement). Keep error message bytes exactly.
- risk: low
- within_file: yes (sole caller is `#call`, ~line 305)
## C4: restart_task re-lists create_file keys by hand
- where: `ScenarioDriver#restart_task` (~line 406)
- principle: 5/simple-solution owner directive; single source of truth
- evidence: `JSON.generate('path' => fixture.fetch('path'), 'content' => ..., 'expected_sha256' => ..., 'mode' => ...)` restates the four keys of the frozen `RESTART_FIXTURE` (~212–217) that `setup.fixture` already carries verbatim.
- proposal: `arguments = JSON.generate(definition.fetch('setup').fetch('fixture'))` — byte-identical output since insertion order of RESTART_FIXTURE matches the manual list; args can no longer drift from the constant.
- risk: low
- within_file: yes
## C5: restart_fixture_owned? digest check can never decide
- where: `ScenarioDriver#restart_fixture_owned?` (~line 365)
- principle: 2 one thing / simple solution
- evidence: `content == RESTART_FIXTURE_CONTENT && Digest::SHA256.hexdigest(content) == RESTART_FIXTURE_DIGEST` — content equality already implies the constant's digest; second conjunct never changes the outcome.
- proposal: keep the content comparison only; trim the ownership-marker comment (~355–356) to match. Truth table unchanged, rescue clause untouched.
- risk: low
- within_file: yes
## C6: receipt_digest mixes key lookup with output scraping
- where: `T3M1M2Oracle.receipt_digest` (~line 63)
- principle: 5 step-down; 3 levels of abstraction
- evidence: explicit-key fallback chain (`'content_digest' || 'observation_digest' || 'digest'`) then regex-scrapes `sha256:` from receipt output in the same body.
- proposal: extract `output_digest(receipt)` holding result/output unwrap + match; `receipt_digest` becomes one fallback chain. Sole caller is `recovery_metric` in the same class.
- risk: low
- within_file: yes
## C7: oracle result shape and hard-zero reason strings duplicated
- where: `T3M1M2Oracle#call` + `#hard_zero_reasons` / `T3M3M4Oracle#call` + `#hard_zero_reasons`
- principle: §3.1 one spelling per concept; 4 (oracle call reads as DSL); §6.2 two real consumers
- evidence: both classes build the identical `{'metrics'=>…, 'hard_zero'=>…, 'hard_zero_reasons'=>…}` literal, and `hard_zero_failed:X` / `hard_zero_unverifiable:X` strings are spelled in both.
- proposal: tiny shared private helper module inside this file (e.g. `result(metrics:, hard_zero:, reasons:)` plus `hard_zero_reason(name, status)` mapping failed/unknown/other), included in both singleton classes; emitted hashes byte-identical.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Public surface suspicion: `ScenarioDriver.implemented?` (~269) and `.definitions` (~265) have zero callers anywhere in gems/, apps/, test/ — delete under §6 narrow-API, or pin them as benchmark protocol contract.
- Structural (out of scope this pass): file is 437 lines, past the §2 ~250-line signal; natural seam is giving T3M1M2Oracle/T3M3M4Oracle their own Zeitwerk files — their constants are referenced nowhere else.
- Naming across files: module function `oracle(scenario_id:, **)` scores evidence rather than returning an oracle (`score` would state intent); tests call it, so multi-file — owner decision.
