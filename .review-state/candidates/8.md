# Candidates — rank 8 gems/tamoz-comms/lib/tamoz/comms/gateway.rb
LOC: 910  Verdict: CANDIDATES
## C1: wrap-the-eight-disposition-clumps
- where: `Gateway#admit`, `#resolve_callback`, `#refuse_admission` (lines 255–342, 423)
- principle: 5 (step-down) + §4 one responsibility; Reek DataClump currently suppressed at class level
- evidence: `@store.disposition_only(envelope, surface_id:, bot_id:, disposition: …, reason: …, now:)` appears verbatim at 8 call sites; only disposition/reason vary
- proposal: private `record_disposition(envelope, disposition:, reason:, now:)` forwarding to the store with `surface_id:`/`bot_id:` supplied once; rewrite the 8 sites as one-liners (`record_disposition(envelope, disposition: 'rejected', reason: 'binding_mismatch', now:)`). This alone drops `resolve_callback` from ~34 to ~26 lines and makes refusal blocks read as domain
- risk: low
- within_file: yes
## C2: admit-steps-down-through-the-control-branch
- where: `Gateway#admit` (~line 240)
- principle: 2 (42 lines > 30 ceiling) + 3 (decision dispatch mixed with size-check/persistence/duplicate logic)
- evidence: `when :control` branch (~lines 259–275) nests an if/else containing `disposition_only`, a duplicate replay guard with its own comment, and command-vs-reply routing — two abstraction levels below the surrounding case dispatch
- proposal: extract the whole branch body into private `admit_control(envelope, decision, now:)`; the in-branch `return if outcome == :duplicate` becomes the helper's natural return, so control flow is unchanged. `admit` then reads as pure disposition dispatch at ~25 lines
- risk: medium
- within_file: yes
## C3: bind-admission-out-of-admit-request
- where: `Gateway#admit_request` (~line 382)
- principle: 2 (body ~34 lines) + 5; comment itself says "Authority binding precedes work" — that phase deserves its own name
- evidence: lines 385–398 interleave `bind_thread_profile`, `bind_conversation(Comms::Conversation.new…)`, and `bind_allowlisted_correspondent` inside an if/elsif, between thread derivation and `admit_and_enqueue`
- proposal: extract the conditional into private `bind_admission(envelope, thread, conversation, now:)`; `admit_request` then reads resolve-thread → bind → enqueue → disposition. Pure move preserves the write-once ordering invariant
- risk: medium
- within_file: yes
## C4: one-renderer-for-shared-status-axes
- where: `Gateway#status_text` / `#request_status_text` (lines 639–648 vs 658–665)
- principle: §4 one responsibility / DRY of the projection-rendering DSL
- evidence: the chunk `"phase=…; event=#{…}##{…}; effect=#{…}; capability=#{…}; "` is duplicated byte-for-byte across both renderers (only receiver name differs); the renderers differ legitimately only in task/delivery ordering and leading sentence
- proposal: private `state_axes(projection)` returning that exact four-axis fragment; each renderer interpolates it. Output bytes identical, and the real difference between conversation vs per-request status becomes visible
- risk: low
- within_file: yes
## C5: name-the-command-request-id-derivation
- where: `#run_context_control` (536), `#redirect_request` (696), `#cancel_request` (809)
- principle: 5 (step-down) + §3.1 one spelling per idea
- evidence: `Comms::Canonical.hexdigest('tamoz.comms.command.v1', [surface_id, envelope.fetch('update_id'), …])` hand-rolled three times with varying tag tails ('context_control'+intent.name, 'redirect', 'cancel')
- proposal: private `command_request_id(envelope, tags)` = hexdigest over `[surface_id, update_id, *tags]`; call sites pass `%w[redirect]`, `%w[cancel]`, `['context_control', intent.name]`. Digest inputs unchanged — wire-safe
- risk: low
- within_file: yes
## C6: split_callback-denies-in-one-spelling
- where: `Gateway#split_callback` (lines 367–374)
- principle: §5/§3 consistent value types ("remaining operations small and concrete")
- evidence: prefix path returns String action (`'approve'`/`'deny'`), bare-reference path returns Symbol `[:deny, text]`; caller compares `action == 'approve'`. Harmless today only because `DecisionRecord.build` calls `.to_s`
- proposal: return `'deny'` (String) in the else branch too; verified behavior-preserving — the comparison outcome and the built decision wire (digest inputs included) are byte-identical
- risk: low
- within_file: yes
## C7: say-why-the-conflict-reraise-exists
- where: `Gateway#serve_once` (lines 188–189)
- principle: §11 load-bearing why (the code reads as dead machinery)
- evidence: `rescue Comms::PollerConflictError\n raise` looks like a no-op, but `PollerConflictError < CommsError` (errors.rb:62) — without this clause the fallthrough `rescue … Comms::CommsError` would classify a lost lease as `:transient` and serve_loop would keep polling unowned, the exact 409 storm the class comment warns about
- proposal: keep the clause; add one why-line, e.g. "must escape before the CommsError catch-all: a lost lease is never transient". No structural change
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- `start_text` (line 553): on an already-bound conversation, `/start <any code>` answers START_PAIRED_REPLY without ever checking the code against pending challenges, while the unbound path (`pairing_start_reply`) does verify. Reads as intentional feedback-only design (comment at 550), but the ignored argument is silent — owner confirm.
- Public surface checked: external callers (cli_comms_commands.rb, openclaw_comms_fixture.rb, tests) use only `new/start/stop/serve_once/serve_loop`; `admit` is internal — all candidates above keep signatures untouched.
