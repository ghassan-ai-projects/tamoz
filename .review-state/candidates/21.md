# Candidates — rank 21 gems/tamoz-agent/lib/tamoz/agent/runtime.rb
LOC: 623  Verdict: CANDIDATES
## C1: routed-observation-producers-name-their-return
- where: `Runtime#run_routed_discovery` (~line 283), `Runtime#execute_routed_read_only_plan` (~line 292)
- principle: 1 (name states intent) + §3.1 (one concept, one verb)
- evidence: both are `observations, = execute(...); observations` wrappers that return only the observation array, yet neither name says "observations"; sibling `discover_action_observations` (~373) already owns the right spelling for this exact concept
- proposal: rename `run_routed_discovery` → `routed_discovery_observations(discovery_plan)`; rename `execute_routed_read_only_plan` → `routed_read_only_observations(plan, discovery_observations)`; update the two call sites (~266, ~271)
- risk: low
- within_file: yes
## C2: record-action-plan-is-not-a-predicate
- where: `Runtime#action_plan_recorded?` (~line 464)
- principle: §3 (predicates end in ? and read as questions) + §4 (one responsibility per method)
- evidence: the name queries, but the body records — it mutates five `state` keys (`seen_actions`, `plan`, `review`, `prior_plans`, `prior_reviews`) when novel, or sets `terminal_reason` and emits `:repair_stopped` when repeated; caller assigns a non-question local (`recorded = action_plan_recorded?(...)`, ~413)
- proposal: rename to imperative `record_action_plan(state, plan, review, context)` returning the loop-continuation boolean (or, with C5 in place, reduce it to novelty-record + `stop_repair(...)` on repeat); rename local `recorded` accordingly
- risk: low
- within_file: yes
## C3: positive-form-route-guard
- where: `Runtime#routing_decision` (~line 211)
- principle: 3 (one level of abstraction / reads as intent) + §3 clarity
- evidence: `unless !request.direct_response? || RequestRoute.self_contained_task?(task)` — a double negative over two predicates; the reader must De Morgan it to learn the fallback fires for "direct response on a non-self-contained task"
- proposal: rewrite as `if request.direct_response? && !RequestRoute.self_contained_task?(task)` with identical body (same truth table, emit + return nil unchanged)
- risk: low
- within_file: yes
## C4: verify-takes-keywords-not-a-positional-list
- where: `Runtime#verify` (~line 556)
- principle: §4 ("keyword arguments beyond one or two positional parameters; no long positional lists")
- evidence: `def verify(task, plan, review, observations, verification_context:)` — four anonymous positionals plus a kwarg; at call sites (~115, ~253, ~274) the argument order is pure convention
- proposal: `def verify(task:, plan:, review:, observations:, verification_context:)` and keyword-ize the three in-file call sites
- risk: low
- within_file: yes
## C5: stop-repair-one-helper-for-four-copies
- where: `Runtime#draft_action_plan` (~457), `#action_plan_recorded?` (~475), `#continue_repair?` (~520 and ~533)
- principle: 5 (step-down) + §4 (one responsibility)
- evidence: four copies of the same terminal sequence — `state[:terminal_reason] = reason; emit(:repair_stopped, context.fetch(:metadata).merge("reason" => reason [, detail]))`; only the reason string and an optional signature key differ
- proposal: extract private `stop_repair(state, context:, reason:, detail: nil)` that sets `terminal_reason` then emits exactly today's payload (`metadata.merge("reason" => …).merge(detail)` preserves key order/bytes); replace the four sites
- risk: medium
- within_file: yes
## C6: routed-discovery-plan-step-down
- where: `Runtime#routed_discovery_plan` (~lines 302–327)
- principle: 2/3/5 (26 lines mixing acceptance policy with emission mechanics)
- evidence: one method does structural-issue gate, two `emit(:plan_reviewed…)` payloads, semantic-review gate, fallback returns, and accept-emission; the policy (`issues → review → accept`) is buried inside event plumbing
- proposal: step down only — extract `emit_discovery_structural_review(plan, issues)` (the drafted+structural pair) and `semantic_review_accepted?(review)` (`review.nil? || review.fetch("decision") == "accept"`); every `emit` keeps its exact type, payload bytes, and order
- risk: medium
- within_file: yes
## C7: finish-says-when-not-what
- where: `Runtime#finish` (~line 157)
- principle: 1 (name states intent) + 4 (call sites read as the domain)
- evidence: `finish(result, verification_context:)` enforces the configured-check policy, emits `:completed`, and returns the final result — the name carries none of that; three call sites (~75, ~79, ~167) read as bookkeeping, not domain
- proposal: rename to `final_result(result, verification_context:)` so sites read `return final_result(routed) …` / `final_result(result, verification_context:)`
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Event-block plumbing: `{ |event| yield event if block_given? }` is forwarded through ~40 private hops in this file alone. A single sink captured once in `run` (and consumed by `emit`) would delete most of it, but the same pattern lives in the sibling files `runtime/plan_review.rb` and `runtime/step_execution.rb` — cross-file by construction, so not proposed here.
- `action_state` (~390) threads one mutable symbol-keyed hash through 8 repair-loop methods against §5's immutable-shape preference; fixing it needs a named state class in its own file (structural extraction), out of scope this pass.
- No public-surface suspicion: all proposals touch privates of `Tamoz::Agent::Runtime` only; `Runtime.new` keywords, `Runtime#run`, and `Result`'s public shape are untouched.
