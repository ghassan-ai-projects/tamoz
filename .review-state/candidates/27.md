# Candidates — rank 27 gems/tamoz-agent-session/lib/tamoz/agent/session_records.rb
LOC: 576  Verdict: CANDIDATES
## C1: step-down the `load!` gate into header and version validators
- where: `SessionRecords#load!` (~line 382)
- principle: 2 (short, does one thing) + 5 (step-down); §4 one responsibility
- evidence: ~28 code lines fuse four phases: payload/identity checks (383–395), version gate (397–406), migration + legacy defaults (408–409), and the validate/reject pipeline (411–414) — two levels of abstraction in one body, sitting directly above its own "Rejection order is load-bearing" comment
- proposal: extract `validate_record_header!(value, expected_kind)` (Hash check + allowlist + expected-kind match; returns `stored_kind`) and `validate_record_version!(value, stored_kind)` (usable positive Integer + supported ceiling; §3.1: both are shape/schema conformance), leaving `load!` an ~8-line ordered pipeline whose steps mirror that comment. Plain step-down only — docs/reviews/codebase-review-2026-08-20 explicitly rejects table-izing this ladder. Error classes and message bytes untouched; no existing signature changes.
- risk: low
- within_file: yes
## C2: pull the `memory_epoch` special case out of `validate_fields!`
- where: `SessionRecords#validate_fields!` (~line 547)
- principle: 3 (one level of abstraction); §4 one responsibility
- evidence: after uniform schema-driven loops for unknown keys (526–531), required fields (532–539), and optional fields (540–544), lines 547–554 hand-roll a semantic rule for one hardcoded optional key ("memory_epoch must be \"none\" or an object")
- proposal: extract `validate_memory_epoch!(value)` beside the other single-concern validators; `validate_fields!` drops to ~20 uniformly schema-driven lines. Grep across gems/ and test/: `SessionRecords.validate_fields!` has no caller outside this file.
- risk: low
- within_file: yes
## C3: one nested-value walk beneath both rejectors
- where: `#reject_sensitive!` (~485) and `#reject_credential_values!` (~502)
- principle: 2 (each does one thing); §6 composition over duplicated structure
- evidence: identical recursion skeletons — `value.each_value { |entry| reject_sensitive!(entry) }` (491) vs `reject_credential_values!(entry)` (511), same Array branch (493 / 513) — differing only in the leaf test and error message
- proposal: extract `each_nested_value(value) { |leaf| ... }` (Hash → each_value, Array → each, else yield leaf) and reduce both rejectors to leaf-test + return `value`. Traversal order, error classes, and message bytes preserved; both public signatures unchanged (external callers exist: session_adaptive.rb:341, session_planning_context.rb:118, test/agent_session_test.rb:303).
- risk: low
- within_file: yes
## C4: `check_type!` takes keyword context
- where: `SessionRecords#check_type!` (~line 558)
- principle: §4 keyword arguments over positional lists
- evidence: `check_type!(value.fetch(name), type, kind, name)` — a 4-positional list where `kind, name` are pure error-message context (call sites 538, 543 read as a tuple dump, not the domain)
- proposal: `check_type!(value, type:, kind:, name:)`; both call sites live in this file — grep across gems/ and test/ finds no other reference
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- Deliberate public-surface pass: `check_type!`, `validate_fields!`, `migrate_to_current!`, `apply_legacy_session_defaults!` have zero callers outside this file today (grepped); a §6 exported-surface audit could make them private, but that touches asserted package surface — owner call, not this refactor. Otherwise none: durable shapes pinned, no extraction proposed anywhere (cohesion audit respected).
