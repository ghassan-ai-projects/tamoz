# Candidates — rank 20 gems/tamoz-core/lib/tamoz/circuit/record.rb
LOC: 639  Verdict: CANDIDATES
## C1: with_failure is a 62-line transition mixing five jobs
- where: `Record#with_failure` (~line 321, case body 330–367)
- principle: 2 (hard ceiling 30) + 5 (step-down)
- evidence: one method validates inputs (`Circuit.owner_id!`, `admit!`, `identity!`), mutates sub-state for five condition kinds in a 37-line `case`, then builds the candidate and evaluates/opens. Contrast `with_success`, which does the same shape in 24 lines.
- proposal: keep the atomic read-modify-write body intact inside this method; step the `case` down into private per-kind appliers on the copied owner entry: `apply_failure_condition(entry, condition, kind:, context_digest:, run_id:, fingerprint:, now_ms:)` dispatching to `count_consecutive_failure(entry, condition)`, `count_immediate_failure(entry, condition, now_ms:)`, `count_window_failure(entry, condition, context_digest:, now_ms:)`, `count_run_failure(entry, condition, run_id:, fingerprint:)`, `count_rate_failure(entry, condition, now_ms:)`. Pure code motion — every predicate still evaluated inside the transition before the candidate is returned; stored byte shapes untouched.
- risk: medium
- within_file: yes
## C2: validate! runs three abstraction levels in one body
- where: `Record.validate!` (~line 83–122)
- principle: 3 (+2; 40 lines over ceiling)
- evidence: key-set algebra (`payload.keys - KEYS`), scalar field guards (threshold/probe_window/reset_authority), and the cross-field open/opened_at invariant sit inline next to delegation to `validate_owners!`/`validate_conditions_met!`.
- proposal: extract `validate_key_set!(payload)` (lines 84–87) and `validate_fields!(payload, scope, scope_id)` (lines 89–118); `validate!` becomes a ~6-line ordered checklist calling it plus the three existing validators. Error message bytes unchanged.
- risk: low
- within_file: yes
## C3: local variable `print` shadows Kernel#print
- where: `Record#with_failure` run branch (~line 357)
- principle: 1 / §3 naming
- evidence: `print = Circuit.identity!(fingerprint, name: "failure fingerprint")` — shadows `Kernel#print`; siblings are named for what they hold (`failure_kind`, `run`).
- proposal: rename local to `normalized_fingerprint` (and optionally `run` → `normalized_run_id` for symmetry). Local variable, zero surface.
- risk: low
- within_file: yes
## C4: admit! neither admits nor has a safe twin
- where: `Record#admit!` (~line 577; callers at ~324 and ~390)
- principle: §3 ("`!` only when a safe twin exists") + §3.1 checking verbs (policy refusal = `enforce_*`)
- evidence: the method mutates nothing; it raises `CircuitPolicyError` when the owner map is full — an enforcement, not an admission or mutation.
- proposal: rename to `enforce_admission!(owner)` (or `enforce_owner_capacity!`). Grep across gems/ and test/: both callers are in this file; no external references.
- risk: low
- within_file: yes
## C5: Record.repaired reaches into private state via __send__
- where: `Record.repaired` (~line 460, `__send__(:replace, ...)` at 464)
- principle: 5 / §4 encapsulation clarity
- evidence: `base.__send__(:replace, ...)` bypasses `private` when the public surface already supports the same construction: `initial(...).to_payload.merge(changes)` handed to `new(scope:, payload:, now_ms:)` (the exact path `replace` itself takes).
- proposal: build `payload = base.to_payload.merge(reset_fields)` and return `new(scope: resolved, payload: payload, now_ms: now_ms)`; drop the `__send__`. Same deep-freeze discipline, identical resulting record.
- risk: low
- within_file: yes
## C6: bound_counts opens with a no-op transform
- where: `Record#bound_counts` (~line 610)
- principle: simplicity / §2 (choose the simple solution)
- evidence: `next_counts = counts.to_h { |key, value| [key, value] }` — `counts` is always a Hash (validated in `validate_sub_state!`, built as `{}` on write), so this rebuilds the same hash; `counts.dup` is the whole intent.
- proposal: replace with `counts.dup`. One-line diff.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- §3.1 reserves `with_*` for block-scoped context ("a with_* method that returns a configured object instead of yielding is misnamed") — `with_failure`/`with_success`/`with_reset` return new records. They read well as a transition DSL, but by the letter of the standard they are misnamed; any rename is public + cross-gem (tamoz-sqlite circuit_store calls all three) → owner call.
- Near-collision: stored accessor `conditions_met` (persisted evidence ring) vs derived `met_conditions(now_ms:)` (live predicate evaluation) — opposite lifecycles, near-identical names. Both public surface; rename would be API change.
- Uncalled public surface, zero references in gems/, apps/, test/: `in_flight_disposition`, `healed`, `probe_allowed?`, `effective_state`, `open?`'s sibling predicates `owner_admitted?`/`owners_full?`, and the `in_flight_rule` delegator. Deliberate DR-2 contract surface, or removable?
- `deep_copy` here is the sixth private deep-freeze/deep-dup variant in the monorepo (core.rb, mcp/canonical_json, comms/surface_descriptor, stream/decision_builder, agent-capabilities/mcp_capability_source, sqlite/boundary_source_audit); a shared `Tamoz::Core` copy-twin of `deep_freeze` would consolidate them — cross-gem interface change, ask first.
