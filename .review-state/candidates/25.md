# Candidates — rank 25 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_convergence_probe.rb
LOC: 593  Verdict: CANDIDATES

Scope note: probe verdict semantics, error message bytes, fact-key bytes, and the
CanonicalJSON digests are pinned by `test/sqlite_convergence_probe_test.rb`; every
candidate below preserves them exactly. The test exercises only `.definition`,
`.digest`, `.new`, `.run` (no `send` into privates), and the class is
`private_constant` under `Tamoz::Evals::Harness` — all internal names are referenced
only inside this file (verified by grep across gems/, test/, apps/, bin/), so every
rename/extract is within_file: yes.

## C1: step-down-lease-fencing-probe
- where: `SQLiteConvergenceProbe#probe_lease_fencing` (~line 286)
- principle: 2 (50 lines — hard ceiling 30) + 5 step-down; one method does read,
  construct-stale, acquire, assert-fence, assert-staleness, release, render facts
- evidence: L288–309 raw row read with positional indices (`prior.fetch(0/1/2)`) plus
  inline `LeaseRecord.new(...)` construction; L311–328 acquire + two separate unless-
  assertions; L325–334 release + fact hash — seven distinct moves in one body.
- proposal: extract private helpers, each ≤20 lines, calls to
  `adapter.__send__(:read/:acquire_lease/:release_lease/:validate_lease)` kept verbatim:
  `read_prior_lease(adapter, encoded)` returning the raw row;
  `stale_lease_record(prior, encoded)` building the `LeaseRecord` (name its locals
  `owner_id`/`fence`/`expires_at_ms` instead of `prior.fetch(0..2)`);
  `assert_fence_advanced!(current, prior_fence)` and keep `stale_lease_fenced?` as is.
  `probe_lease_fencing` becomes a ~12-line orchestration reading as the domain.
- risk: low
- within_file: yes
## C2: step-down-run-orchestration
- where: `SQLiteConvergenceProbe#run` (~line 152)
- principle: 2 (47 lines — hard ceiling) + 3/4 one level of abstraction; §4 "one
  responsibility per method" — policy validation, path validation, ledger policy,
  adapter/app wiring, dynamic dispatch, report rendering, error translation, cleanup
- evidence: L154–166 five validations inlined; L159–166 the ledger if/elsif/else policy;
  L170–177 `send("probe_#{probe.tr("-", "_")}", ...)` metaprogrammed dispatch inline;
  L184–197 rescue/ensure translation and adapter close.
- proposal: extract `resolve_ledger(probe, ledger_path)` (the if/elsif/else → nil or
  validated path, per §3.1 `resolve_*` turns a reference into the thing it names) and
  `execute_probe(probe:, app:, adapter:, scenario:, classification:, ledger:)`
  wrapping the `send` dispatch at a single seam. `run` keeps validations, rescue/ensure,
  and reads as: validate → wire → execute → report. No change to dispatch strings or
  raised errors.
- risk: low
- within_file: yes
## C3: extract-replay-failure-detail
- where: `SQLiteConvergenceProbe#probe_pending_write_replay` (~line 452)
- principle: 2 (36 lines) + 5; the failure branch builds diagnostic text at a lower
  abstraction than the assertion around it
- evidence: L473–480 multi-clause unless whose else-path joins a 5-element string array
  (`"request=#{...}"`, `"checkpoint=#{...}"`, …) inside the probe body.
- proposal: extract `replay_failure_message(recovered, snapshot, after_count)` returning
  the byte-identical joined string; the unless then reads
  `raise ExecutionError, replay_failure_message(...)`. Message bytes unchanged.
- risk: low
- within_file: yes
## C4: hoist-recovery-history-table
- where: `SQLiteConvergenceProbe#probe_request_recovery` (~line 387); literal at L400–404
- principle: 2 (35 lines) + consistency with the file's own seam; §6 "a long declarative
  registry may be acceptable; a long method is not"
- evidence: `expected_history = {"request.recover-claimed" => 2, ...}.fetch(scenario)`
  is a per-scenario table rebuilt on every call, while sibling tables live as frozen
  class constants (`REQUEST_STATES`, `CHECKPOINT_STATES`, L48–103).
- proposal: hoist to `RECOVERY_HISTORY_COUNTS = DeepFreeze.call({...})` beside the other
  state tables and add it to the `private_constant` list (L583). Method drops to ~28
  lines and the table becomes greppable next to its siblings.
- risk: low
- within_file: yes
## C5: split-validate-path-shape-from-safety
- where: `SQLiteConvergenceProbe#validate_path!` (~line 221)
- principle: 1/2 (27 lines, two jobs) + §3.1 checking verbs: shape checks are
  `validate_*`, refusing an unsafe path is `enforce_*`
- evidence: L222–228 pure-string shape checks (encoding, bytesize, absolute), then
  L229–241 a second job: `File.lstat` uid/nlink/mode/size parent-directory policy with a
  different failure message ("is invalid" vs "is unsafe").
- proposal: keep `validate_path!` as the frozen-returning entry that calls
  `validate_path_shape!(value, name)` and `enforce_path_safety!(value, name)`; move the
  `rescue Errno::ENOENT, Errno::ELOOP` mapping with whichever half performs lstat so
  both message byte sets stay exact.
- risk: low
- within_file: yes
## C6: name-the-classification-consistently
- where: `#run` local `state` (L155–183); `state` params of five probes (L344, 426,
  452, 515, 533)
- principle: 1 + §3.1 one concept, one name
- evidence: `run` validates `classification:` then binds it to a local named `state`
  (`state = validate_classification!(...)`, L156) and maps it back at
  `report(classification: state, ...)` (L180) — while `snapshot.state.fetch(:value)`
  elsewhere means graph state, a different concept sharing the spelling.
- proposal: rename the local and the probe params to `classification` (positional
  `send` dispatch unaffected; comparisons like `state == "new"` at L434 read correctly
  afterwards). All sites in this file.
- risk: low
- within_file: yes
## C7: name-request-checkpoint-relation-guard
- where: `SQLiteConvergenceProbe#probe_request_checkpoint_reopen` (~line 533)
- principle: 2 (31 lines, over ceiling) + 3; four-clause compound guard buries the
  invariant it states
- evidence: L548–555 single `unless history.length == expected_count &&
  snapshot.status.to_s == checkpoint_status && request&.status&.to_s == request_status &&
  (request.checkpoint_id.nil? || request.execution_id == snapshot.execution_id)`.
- proposal: extract predicate
  `request_checkpoint_relation_consistent?(history_length:, snapshot:, request:,
  expected:)` (§4 keyword args) holding the identical condition; probe keeps the same
  raise and fact hash. Also brings the method back under 30.
- risk: low
- within_file: yes
## C8: rename-report-to-build-report
- where: `SQLiteConvergenceProbe#report` (~line 565), sole call site L178
- principle: 1 + §3 "method names say what they return" / §3.1 `build_*` returns a new
  value
- evidence: `def report(scenario:, classification:, probe:, facts:)` assembles and
  returns a digest-stamped frozen hash; bare `report(` at a call site reads as an
  action ("report this"), not construction.
- proposal: rename to `build_report`; call site becomes
  `build_report(scenario: ..., ...)`. Single caller, no external references.
- risk: low
- within_file: yes
## FLAG (owner decisions only)
- This evals gem reaches into tamoz-sqlite non-public surface:
  `adapter.__send__(:read/:acquire_lease/:release_lease/:validate_lease)` (L288, 311,
  325, 338) and `Tamoz::SQLite.const_get(:Wire/:LeaseRecord, false)` (L287, 301).
  Deliberate here (cross-process oracle must bypass convenience layers?), but if
  tamoz-sqlite ever grew a public convergence-read seam, these calls are the ones to
  migrate — out of scope for this pass, public/cross-gem signatures untouchable.
