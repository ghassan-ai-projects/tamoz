# Combined pass — rank 131 gems/tamoz-agent-profile/lib/tamoz/agent/profile/egress_validator.rb

## Phase 1 — ANALYZE (written before any edit)

Baseline: `ruby -c` OK; `rubocop --cache false` BEFORE = 0 offenses.
Callers: only `gems/tamoz-agent-profile/lib/tamoz/agent/profile.rb:566` (`EgressValidator.call(hash, path)`)
via `validate_egress!`. Focused test: `test/websearch_egress_test.rb` (drives every refusal branch).
Constraint honored: refusal message bytes pinned — no string touched.

Candidates:

- C1 (APPLY): rename private `integer!(value, field, bounds)` → `validate_integer!`.
  §3.1: this helper is exactly the `validate_*` verb ("value matches its declared shape,
  bad input named by field"); the repo already spells the concept `validate_integer!`
  (tamoz-evals schema.rb), `validate_positive_integer!` / `validate_non_negative_integer!`
  (observability, healing), and sibling `profile.rb` uses `validate_*!` uniformly —
  `integer!` is a second spelling of an existing idea. Private method; all callers proven
  in-file by repo-wide grep incl. send/__send__/public_send (zero hits); pure rename,
  body and messages untouched. Aligns `validate_limits!` to one abstraction level next to
  `validate_boolean!`.
- R1 (REJECTED): drop dead trailing `host` return in `validate_dns_name!` and dead
  `return value if` in `integer!` — returns are unconsumed but removal is churn with no
  readability gain; guard-style return reads naturally.
- R2 (REJECTED): rename `delimited?` for truthfulness about whitespace — every honest
  replacement is longer or negative-form; the two-line comment above it already states
  the whitespace inclusion, and the :reek:UtilityFunction suppression is deliberate.
- R3 (REJECTED): inline `delimited?` into its single caller to shed a reek note — loses
  the named vocabulary; an inline `.any?` chain reads worse than `delimited?(host)`.
- R4 (REJECTED): unify unknown-keys style between `validate_shape!` and `validate_circuit!`
  — both idioms read fine; consistency churn.
- R5 (REJECTED): extract shared distinct-strings predicate for hosts/refs — two uses with
  different refusal messages; machinery outweighs dedup.
- R6 (REJECTED): rename `refuse_ip_literal!` toward §3.1 checking verbs — `refuse_*` is
  already established repo vocabulary (`profile.rb:419 refuse_evidence_only_activation!`);
  not a new spelling.
- R7 (REJECTED): cosmetic items (dangling `#` separators at lines 100/111, placement of
  `HOST_DELIMITERS`) — style churn.

Step-down, lengths (all ≤20 lines), DSL surface (`call`, `validate!`), and the recorded
in-file cohesion notes (:reek:DataClump/TooManyMethods/MissingSafeMethod/TooManyStatements)
audit clean. No extraction anywhere — method-level only.

## Phase 2 — APPLY

Applied C1 only. Refusal bytes, ordering, and public API untouched; pure private-method
rename proven by repo-wide grep (incl. send/__send__/public_send: zero hits).

Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename private `integer!` → `validate_integer!`, 4 call sites + definition — §3.1 one-verb rule, matches repo's existing `validate_*_integer!` spelling); Rejected: R1 (dead returns — churn, no readability gain); R2 (`delimited?` rename — every honest name is longer/negative-form, comment already states whitespace inclusion); R3 (inline `delimited?` — loses named vocabulary); R4 (unknown-keys style unification — churn); R5 (shared distinct-strings predicate — machinery for 2 uses); R6 (`refuse_ip_literal!` rename — `refuse_*` is already repo vocabulary, `profile.rb:419`); R7 (cosmetic comment/placement items — churn)
Diff stat: 1 file changed, 5 insertions(+), 5 deletions(-) (net 0)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase) · ruby -Itest test/websearch_egress_test.rb → 10 runs, 74 assertions, 0 failures, 0 errors, 0 skips
Deviations: none (~25 min budget held; no extraction — method-level only per binding note; refusal message bytes untouched)
FLAG (owner decisions): none
