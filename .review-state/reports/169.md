# Combined pass — rank 169 gems/tamoz-agent-memory/lib/tamoz/agent/memory/retrieval.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (named constants for pinned ranking weights: MAX_QUERY_TERMS, FULL_QUALITY_REFS, FRESHNESS_DECAY_HOURS, MS_PER_HOUR, STALE_WINDOW_HOURS, STALE_PENALTY, CONTRADICTION_PENALTY, CORRECTION_PENALTY — sibling situation_recaller precedent; literal hoists only, values byte-identical); C2 (step-down: extract private `materialize(rows)` from `recall` — the store.get + MemoryRecord type-check loop sat two abstraction levels below the surrounding policy steps); C3 (DROPPED_EVENT constant for :memory_dropped, symmetry with existing RECALL_EVENT)
Rejected: R1 (`automatic:` boolean kwarg on `recall` and `apply_budget` — §4 no-boolean-params violation, but `recall` is cross-gem public API (tamoz-agent-session, tamoz-evals, episode_nodes) and untouchable; splitting apply_budget would duplicate the pinned budget-drop loop); R2 (`query[:layer]&.to_s` instead of `&&` form — churn, zero intent gain); R3 (ranking-formula comment above `rank` duplicates the class-level comment — locally useful at point of use; removal is churn, not clarity); R4 (`next nil unless` → `next unless` in filter_map — RuboCop-clean already); R5 (`selected.include?` O(n²) in budget loop — lists bounded by max_lexical_hits, not worth touching pinned semantics); R6 (sort_by tie-order instability in `rank` — any tie-break change alters recall order = pinned behavior)
Diff stat: 1 file changed, 29 insertions(+), 15 deletions(+)
Commands: ruby -c retrieval.rb → Syntax OK · rubocop --cache false retrieval.rb → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/memory_engine_test.rb → 25 runs, 162 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/memory_session_integration_test.rb → 4 runs, 24 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
