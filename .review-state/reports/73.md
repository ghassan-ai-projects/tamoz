# Combined pass — rank 73 gems/tamoz-agent-kernel/lib/tamoz/agent/effect_dispatcher.rb
Verdict: CANDIDATES-ONLY
Applied: none (0 candidates cleared the high-confidence/low-risk bar on the durable heart)
Rejected: C1 (rename `resolve_decision` → `decision_outcome`-style name — §3.1 `resolve_*`
means turning a reference into what it names, and this maps a prepared decision to its
Outcome; mechanically safe — repo-wide grep proves zero external references and no
send/__send__/public_send reaches it — but pure naming churn on the dispatch path with
marginal reader gain; rails: when in doubt, candidates-only);
C2 (merge the two `effects.reconcile(...)` call shapes in `run_reconciliation` behind one
computed disposition/evidence pair — behavior-identical on paper, but it rewrites the
journal reconciliation write path the brief fences off ("nothing may change dispatch,
key derivation, receipt handling, or journal sequencing") and the crash-matrix tests pin;
zero functional gain);
C3 (privatize `resolve_key`, `run_reconciliation`, `recorded_outcome`, `execute_outcome`,
`build_logical_key`, `terminal_attempt`, `current_attempt_identity` — only three helpers
are in the `private_class_method` list — §6 narrow-surface instinct is right, but
public/cross-gem API is untouchable and the module surface is pinned by
`test/public_api_test.rb` against `docs/public-api.json`);
C4 (hoist the duplicated `attempt = terminal_attempt(decision.record)` out of
`resolve_decision`'s `:return`/`:failed` branches — receipt-handling-adjacent churn; a
variable whose lifetime silently spans two branches is not clearer than the duplication);
C5 (rename `recorded_outcome` → `receipt_outcome`/`build_outcome` — every call site already
passes a journal record, so the name reads true today; churn without a reader-level win).
Diff stat: (no change — owned file clean before and after)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE = AFTER)
Commands: ruby -Itest test/agent_session_effect_test.rb → 18 runs, 70 assertions, 0 failures, 0 errors, 0 skips
Commands: ruby -Itest test/p16_tools_gem_test.rb → 15 runs, 129 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none — for completeness: `execute_outcome` counts ~31 code lines but
sits under recorded Metrics/MethodLength todo exclusions (pre-existing debt that only shrinks;
not touched); splitting it would be extraction inside the recorded durable-transaction-core
cohesion exception, which forbids extraction.
