# Combined pass — rank 123 gems/tamoz-mcp/lib/tamoz/mcp/catalog.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (split `validate_and_canonicalize_schema` → `canonicalize_schema` + `validated_schema_candidate`, §3 one-verb rule); C2 (extract dup-guard → `record_unique_entry_name!(name, seen)`); C3 (rename `handshake` → `negotiate_protocol_version`, name says what it returns); C4 (rename `assert_protocol_version_shape!`/`assert_protocol_version_in_range!` → `validate_*`, §3.1 table: server-supplied value vs declared shape/constraint); C5 (extract `canonicalize(object)` for the 4× `deep_freeze(normalize(...))` idiom)  |  Rejected: C6 (split `compile` at 23 lines — orchestrator, steps already named seams, §4 forbids splitting for the number); C7 (rename `bounded_description` — already states its return, churn); C8 (dedupe `"sha256:…"` suffix across the two digest methods — two sites, helper adds indirection); C9 (table-driven `collect_entries` dispatch — explicit conditionals pin digest-relevant entry ordering)
Diff stat: gems/tamoz-mcp/lib/tamoz/mcp/catalog.rb | 30 insertions(+), 19 deletions(-) (net +11)
Commands: `ruby -c` → Syntax OK · `rubocop --cache false <file>` → 0 offenses (BEFORE recorded: 0; no increase, nothing suppressed) · `ruby -Itest test/mcp_catalog_test.rb` → 9 runs, 80 assertions, 0 failures, 0 errors · `ruby -Itest test/mcp_elicitation_test.rb` → 19 runs, 71 assertions, 0 failures, 0 errors · `ruby -Itest test/websearch_circuit_test.rb` → 8 runs, 33 assertions, 0 failures, 0 errors
Deviations: none. All renames are private class-side methods proven in-file-caller-only by repo-wide grep including send/__send__/public_send; error message bytes, raise ordering, digest inputs, Entry/Catalog shapes untouched. Known-red lanes touching this file (mcp_supervisor, mcp_invocation, agent_mcp_adversarial) not run per brief.
FLAG (owner decisions): C4's range check could alternatively read as `enforce_protocol_range!` if the configured range is viewed as policy-refusal (§3.1 enforce_) rather than value-vs-declared-constraint (validate_); chose validate_ for in-file symmetry with `validate_entry_name!` — trivially renameable later.

---

## Appendix — Phase 1 audit record (candidates written before any edit)

BEFORE gates: `ruby -c` OK · `rubocop --cache false` 0 offenses. File was 240 lines,
not declarative data → not EXEMPT. Binding note honored: method-level naming/step-down only;
emitted shapes pinned (sibling capability_source passed earlier with vocabulary renames).

Candidate list as filed pre-edit:
- C1 SPLIT `validate_and_canonicalize_schema` (§3 "one verb per method"; two real steps:
  admission gate vs canonical transform).
- C2 EXTRACT dup-guard in `tool_entries` → `record_unique_entry_name!(name, seen)`; mirrors
  the passed sibling's `record_unique_descriptor!`; lifts `tool_entries` to one level.
- C3 RENAME `handshake` → `negotiate_protocol_version` (caller in-file only, line 40).
- C4 RENAME both `assert_protocol_version_*` helpers → `validate_*` (server-supplied input,
  not a Tamoz-owned invariant; in-file precedent `validate_entry_name!`).
- C5 EXTRACT `canonicalize(object)` = `deep_freeze(normalize(object))` — repeated 4× with
  order that matters.
- C6 REJECT compile split. C7 REJECT bounded_description rename. C8 REJECT sha256 suffix
  dedupe. C9 REJECT collect_entries table-driven rewrite.

Behavior-preservation argument: C1 keeps the identical nil→{} coercion, Hash check, SDK
admission (`MCP::Tool::InputSchema.new`), fail-closed rescue and both error messages, then
canonicalizes the same candidate object; C2 moves the duplicate-name raise verbatim behind a
guard with identical mark-after-check order (seen is method-local, discarded on abort);
C3/C4 are pure renames; C5 evaluates the same expression at each former site. No digest
payload field, wire shape, or public API changes.
