# Combined pass — rank 158 gems/tamoz-graph/lib/tamoz/graph/compiler.rb

Verdict: PENDING (candidates listed before any edit)

## Phase 1 — ANALYZE candidate list (written BEFORE editing)

- C1 `validate!` is ~38 body lines (over the 30 hard ceiling) and mixes six concerns at one
  level: presence checks, reference checks, routing-mode rules, entry edge, reachability,
  path-to-END. → Step down into named private validators called in the same order
  (`validate_required_declarations!`, `validate_references!`, `validate_routing_modes!`,
  `validate_topology!`). Behavior-preserving: identical statements, identical sequence, each
  raises immediately so first-error semantics and message bytes are unchanged.
- C2 `declared_targets(node)` is dead code: repo-wide grep (incl. send/__send__/public_send)
  finds zero callers; class is `private_constant :Compiler`. → Remove.
- C3 `validate_dependencies!` local `protocol` names only half of what it asserts (pinned
  protocol version AND durable?). → Rename local to `durable_protocol`. Pure local rename.
- C4 `reachability` / `reverse_reachable_from_end` share the manual BFS loop shape. → REJECT:
  outside this pass's pinned scope (method-level naming/step-down only); the two loops differ
  in neighbor lookup (`fetch` vs `fetch(node, [])`, END skip) so unification is machinery, not
  simplification.
- C5 `target_map` nested begin/freeze memo. → REJECT: idiomatic as written; churn.
- C6 `descriptor` inline `"format_version" => 1`. → REJECT: live digest input (wire bytes
  untouchable); hoisting to a constant adds a name for one use site.
- C7 hash-as-set idiom (`seen`, `terminal`, `static_sources`). → REJECT: consistent in-file
  idiom, no Set require needed for a private hot path.

## Phase 2 — APPLY

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down of `validate!` into `validate_required_declarations!` / `validate_references!` / `validate_routing_modes!` / `validate_topology!`, same check order); C2 (removed dead private `declared_targets`, zero repo-wide callers incl. send patterns, class is `private_constant`); C3 (renamed local `protocol` → `durable_protocol` — it asserts pinned protocol version AND durable?, the old name covered half of it)
Rejected: C4 (BFS dedup out of pinned scope for this file — method-level naming/step-down only — and loops differ in neighbor lookup semantics; machinery, not simplification); C5 (`target_map` memo idiom as written; churn); C6 (`"format_version" => 1` is live digest input, wire bytes untouchable; one use site doesn't need a constant); C7 (hash-as-set is the file's consistent idiom)
Diff stat: gems/tamoz-graph/lib/tamoz/graph/compiler.rb | 29 ++++++++++++++++++---------- (19 insertions, 10 deletions; net +9)
Commands:
- `ruby -c gems/tamoz-graph/lib/tamoz/graph/compiler.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses, AFTER 0 offenses (no increase)
- `ruby -Itest test/graph_definition_test.rb` → 11 runs, 42 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/graph_execution_test.rb` → 11 runs, 43 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/graph_checkpoint_codec_test.rb` → 4 runs, 22 assertions, 0 failures, 0 errors, 0 skips
Deviations: none. Behavior-preserving proof for C1: identical statements executed in identical sequence, each sub-validator raises immediately so first-error ordering and message bytes are unchanged; memoized helpers called in the same order. C2 proven dead by repo-wide grep (definition + an unrelated test-name string only). C3 is a pure local rename.
FLAG (owner decisions): none
