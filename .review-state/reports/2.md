# Fix report — rank 2 gems/tamoz-agent/lib/tamoz/agent/worker.rb
Applied: C1 (dedupe approval settlement — `resolve_approval_asks` + `answers_for`, one spelling for `apply_decision` and `apply_timeout_denial`; engine-authority comment moved with the code); C2 (`timeout_occurrence_id` extracted from `apply_timeout_denial`, now ~14 code lines); C3 (`settle_terminal_delivery(thread_id, kind, occurrence_id, text)` = notify_sink → close_occurrence → unpark with the §11 outbox-before-close ordering named once; emit payloads untouched at call sites; `@processed` increment stays in `settle_completed_view`); C4 (`enforce_ask_deadlines` reads collect → judge → deny via pure collectors `parked_deadline_ages(now)` / `open_occurrence_deadline_ages(now)`; parked-wins precedence is the visible `||=` merge instead of a hidden `unless candidates.key?`; value semantics identical since ages are never falsy); C6 (`pending_status_projection(view, occurrence_id)` replaces the five identical `SessionStatusProjection.document(..., delivery_state: 'pending')` constructions at 674/692/713/725/769); C7 (dead kwarg `now:` dropped from private `lane_ask`; both in-file callers updated); C8 (`emit_observability` split into `observability_correlation` + `observable_attributes`; `Catalog.fetch(name)` taken once instead of per-key; mapping order unchanged) | Rejected: C5 (its "report → branch once" shape moves `settle_child_error`/`close_failed_occurrence` across the emitted `request.failed` event, reordering durable settlement against the event stream — crash-window observable, forbidden by review rule (a)); the order-preserving variant leaves the method essentially as written. FLAGs untouched (cross-file owner decisions).
Diff stat: gems/tamoz-agent/lib/tamoz/agent/worker.rb | 174 +++++++++++++++-------------- → 93 insertions(+), 81 deletions(+), net +12 LOC (1310 → 1322)
Commands:
- `ruby -c gems/tamoz-agent/lib/tamoz/agent/worker.rb` (before and after) — PASS ("Syntax OK")
- `rubocop --cache false <owned file>` BEFORE — PASS: 1 file inspected, **0 offenses**
- `rubocop --cache false <owned file>` AFTER — PASS: 1 file inspected, **0 offenses** → 0 new vs baseline (corrected gate rule satisfied with explicit before/after counts)
- `ruby -Itest test/agent_worker_test.rb` — **BLOCKED, 0 tests ran**: load-time crash in `gems/tamoz-sqlite/lib/tamoz/sqlite/migrator.rb:373` (`undefined method 'migration_checksum'`) raised from `test_helper.rb` before any test loads
- `ruby -Igems/tamoz-sqlite/lib -e 'require "tamoz/sqlite"'` — FAILS on its own, zero involvement of worker.rb → proves the blockage is external: `migrator.rb` is concurrently modified in this shared tree by another agent (git status grew from clean-at-start to 8 modified files across 4 gems during this run; re-probed three times over ~8 minutes, never loadable)
- `ruby /tmp/tamoz-agents/fixer2_smoke.rb` — PASS (SMOKE_OK): standalone require of worker.rb plus real assertions over every extracted helper — answers_for grant/deny projections, observability_correlation assembly, parked_deadline_ages signature filter + exact ages, open_occurrence_deadline_ages nil-opened_at skip, parked-wins merge precedence, timeout_occurrence_id parked→scan→nil fallback
Deviations: focused-test gate could not execute for ANY repo file because sibling fixers' in-flight edits leave `tamoz-sqlite` unloadable; independence proven as above, so gates rest on syntax + RuboCop (0 new) + helper-level behavioral smoke. C4 applied with pure collectors merged at the call site rather than the candidate's mutating `open_occurrence_deadline_ages(candidates, now)` — same result, no argument mutation. No commits made; working tree carries ONLY worker.rb changes from this agent.
