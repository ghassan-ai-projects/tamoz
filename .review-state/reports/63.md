# Combined pass — rank 63 gems/tamoz-agent-cli/lib/tamoz/agent/cli_schedule_commands.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (delete dead `SCHEDULE_PAYLOADS` constant + its comment — unreferenced duplicate of WorkerRuntime's constant at worker_runtime.rb:135, zero readers repo-wide); C2 (remove dead duck-type tolerance in `schedule_occurrences` — `page.respond_to?(:occurrences)` has no producer: the only `bind_schedule_store` is tamoz-sqlite's adapter and its `list_occurrences` returns a plain Array)
Rejected: C3 (split `schedule_set_enabled(options, argv, enabled:)` over §4 boolean-param / §3 set_ rules — both remedies either duplicate pinned emit/exit logic ~2× or add a policy object for two behaviors; tests pin both branches' bytes; FLAGGED for owner); C4 (extract schedule_add's OptionParser block, 74 lines vs Q6 hard 30 — sibling convention returns positional arrays from parse helpers (`parse_queue_add_options`), so a 10-value array violates §4 long-positional-list itself and a hash return adds a second spelling per option; debt already recorded in .rubocop_todo.yml Metrics/MethodLength exclude; §4 limits are diagnostic); C5 (dedupe the id-shift/accept_json/validate triplet across show/pause/resume/remove/run-now — inline form is the universal convention in every sibling CLI module and the documented DataClump exception; a helper adds a new spelling for an established idiom, §3.1); C6 (rename `value` OptionParser block param — it IS the repo-wide spelling, cli_argument_parser grammar included); C7 (drop `!` from `parse_instant!`/`validate_schedule_id!`, no safe twins — consistent raising-convention within the file, pure churn); C8 (extract trigger-resolution if/else at lines 83–88 into a helper — single call site, locally readable, negligible gain); C9 (schedule_list per-entry payload fetch N+1 shape — performance concern not a naming/structure defect, changing fetch strategy risks behavior, out of clean-code scope)
Diff stat: 1 file changed, 1 insertion(+), 8 deletions(-) (gems/tamoz-agent-cli/lib/tamoz/agent/cli_schedule_commands.rb)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase) · grep -l AgentScheduleTest test/*.rb → test/agent_schedule_test.rb · ruby -Itest test/agent_schedule_test.rb → 9 runs, 65 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): C3 — `schedule_set_enabled(options, argv, enabled:)` carries a boolean kwarg (`enabled:`) and a `set_` name (CODING_STANDARD §3/§4). The clean fix is a real design decision (full pause/resume split duplicates pinned output logic; a named-policy param adds machinery), so it was rejected here as risky. If you want it split, say which shape.

## Phase 1 candidates (written before any edit)

Evidence gathered first: module included only by gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb:21;
no dynamic dispatch (`send/__send__/public_send`) reaches these methods; BEFORE gates:
`ruby -c` OK, `rubocop --cache false` = 0 offenses (file carries pre-existing .rubocop_todo.yml
Exclude debt for 14 cops incl. Metrics/MethodLength); dedicated test file test/agent_schedule_test.rb
pins exit codes (0/1/USAGE_ERROR), message fragments (/--task/, /--interval or --at/,
/mutually exclusive/, /must be letters/, /no schedule "nope"/) and JSON keys.
