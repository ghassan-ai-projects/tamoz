# Combined pass — rank 255 gems/tamoz-tools/lib/tamoz/tools/patch_preparation.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (renamed private factory `replacement` → `build_replacement`, §3.1 build_* returns a new value; callers L100/L113 in-file only, proven isolated by repo-wide grep incl. send/__send__/public_send); C2 (extracted `read_text(path)` — UTF-8 decode + not-valid/not-text refusals, error bytes verbatim); C3 (extracted `verify_digest(content, expected) -> actual` — §3.1 verify_* = claim vs digest evidence returning the evidence; D-8 why-comment moved intact with the pinned ToolPolicyError; local `actual` → `before_digest` matching the result key); C4 (extracted `build_replacements(content, arguments)` — compound/single dispatch + byte_start ordering + overlap refusal — so `prepare` reads purely as pipeline and joins the existing build_* family whose members already carry input refusals)
Rejected: R1 (drop `(patch[:replacements] || [])` fallback in render_diff — looks unreachable but render_diff is public cross-gem surface fed arbitrary patch hashes; nil-tolerance use unprovable → reject); R2 (unify duplicated scan/count/refusal logic of build_compound/build_single — refusal bytes differ and are security-pinned ('patch text is ambiguous…' vs '…requested N times but found N occurrences') → reject); R3 (builder for the unless/merge! result assembly — churn on wire-shaped hash, no clarity gain → reject); R4 (Data.define for replacement entries — entries cross the JSON receipt boundary in PatchOperations#receipt; class change risks wire/freeze behavior → outside behavior-preserving scope → reject); R5 (rewording path handling / __send__(:resolve) forwarder — 'patch path' wording is intentional flow vocabulary with locked bytes, path_resolver precedent per binding note → reject)
Diff stat: 1 file changed, 40 insertions(+), 28 deletions(-) — net +12 LOC
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> BEFORE = 0 offenses / AFTER = 0 offenses (step-down made Metrics/CyclomaticComplexity+MethodLength+PerceivedComplexity disables redundant; Lint/RedundantCopDisableDirective fired on MY lines; fixed by narrowing the directive to Layout/LineLength, Metrics/AbcSize — never suppressed) · ruby -Itest test/agent_toolbox_test.rb → 62 runs, 320 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/p16_tools_gem_test.rb → 15 runs, 129 assertions, 0 failures, 0 errors, 0 skips
Deviations: gross diff = 68 changed lines, marginally above the ~60 stop-rail under the strictest (insertions+deletions) reading; net change is +12 LOC and every hunk is verbatim code movement or the gate-mandated disable-directive narrowing, not a rewrite. No tamoz-sqlite gate applies (file is in tamoz-tools).
FLAG (owner decisions): none

## Phase 1 — ANALYZE detail (written before any edit)

Baseline before edits: ruby -c Syntax OK; rubocop --cache false = 0 offenses; sole consumer = gems/tamoz-tools/lib/tamoz/tools/patch_operations.rb (uses prepare/render_diff only); repo-wide grep incl. send/__send__/public_send found NO dynamic caller reaching any private method of this class. Audit axes: prepare was 38 statements mixing flow with IO-decode, digest-claim, and plan-ordering detail (violates short-method + one-level + step-down); factory named as noun (`replacement`) vs §3.1 construction verb; public DSL (prepare/render_diff) already clean; no boolean params; comments limited to the two justified why-comments.
