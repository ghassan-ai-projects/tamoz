# Combined pass — rank 196 gems/tamoz-evals/lib/tamoz/evals/harness/agent_smoke_scorecard.rb

## Phase 1 — ANALYZE (written before any edit)

EXEMPT check first (per binding note): this file is NOT mostly declarative data — it is report
assembly logic (`run`/`build_case_report`/`build_report`/`aggregate` + helpers) around one
constructed document literal. Precedent: sibling harness files were treated as code under a
byte-identical-output constraint (rank 70 memory_treatment_profile.rb, rank 174 agent_run_audit.rb),
not EXEMPT. Family rule therefore applies: recorded scorecard output bytes must stay identical;
method-level naming/step-down only, no extraction.

BEFORE baselines: `ruby -c` → Syntax OK · `rubocop --cache false <file>` → 0 offenses.
Callers: class used by gems/tamoz-evals/lib/tamoz/evals/cli.rb (constructor only),
test/agent_scorecard_test.rb (SLOW_TESTS lane per Rakefile:48, run as A/B baseline anyway),
test/memory_treatment_profile_test.rb:370 (`.run` only).

### Candidates

C1 — APPLY: merge the two identical pass-through rescue clauses in `run`
(`rescue InvalidArtifactError; raise` + `rescue ExecutionError; raise`) into one
`rescue InvalidArtifactError, ExecutionError; raise`. Proof: both clauses perform the identical
action (bare re-raise), so clause order and subclass specificity are irrelevant; the merged form
re-raises identically and the StandardError catch-all is untouched. Error classes/messages bytes
unchanged. Net −2 lines.

C2 — APPLY: rename private `aggregate(cases)` → `build_aggregate(cases)` (§3.1 build_ vocabulary,
matching siblings `build_case_report`/`build_report`) and to stop shadowing the local `aggregate`
bound at its own call site (`aggregate = aggregate(cases)`). Proof of safety: private method,
exactly ONE in-file caller (line 70); repo-wide grep including send/__send__/public_send finds zero
external dispatch; the `"aggregate" =>` JSON key (line 115) is a string literal bound to the local
variable, not the method — wire bytes unchanged.

### Rejected (with reasons)

R1 — `gate(id, passed)` boolean param (§4): same rejection as recorded rank 70 for the IDENTICAL
helper in memory_treatment_profile.rb — every call site passes a self-describing predicate
expression, never an opaque literal; signature rewrite is churn with zero behavioral gain.

R2 — split `build_report` (~51 lines incl. why-comments) or step-down inside `aggregate`
(30 lines, at hard ceiling): family rule forbids extraction on scorecards; the document literal IS
the wire shape whose bytes feed CanonicalJSON.content_digest and pinned digests.

R3 — delete the pass-through rescue clauses as "no-ops": NOT behavior-preserving — both classes are
StandardError subclasses (errors.rb:13/33 via Tamoz::Error < Tamoz::Core error root), so without the
explicit re-raise they would be swallowed by the catch-all and mis-wrapped as generic ExecutionError.
(C1 merges them instead of removing them.)

R4 — condense/remove the T8.3 / P17-honesty / DR-3 comments: §11 "why" records; the P17 block
documents the not_claimed network-enforcement posture that downstream assertions rely on.

R5 — endless method defs on `Report` (`passed?`/`to_h`/`to_json`) → regular defs: pure style churn;
endless defs are idiomatic here (the boundary_source_audit endless-def rail is tamoz-sqlite ONLY).

R6 — `Report#initialize` override calling `super(document: DeepFreeze.call(document))`: load-bearing
immutability contract; `Data.define` default would leave the document mutable.

## Phase 2 — APPLY

Verdict: COMMITTED-CANDIDATES
Applied: C1 (merge the two bare-raise pass-through rescue clauses in `run` into one
`rescue InvalidArtifactError, ExecutionError`); C2 (rename private `aggregate(cases)` →
`build_aggregate(cases)` at both its definition and its single call site)
Rejected: R1 (`gate(id, passed)` boolean — §4 letter only, call sites self-describing, same recorded rejection as rank 70's identical helper); R2 (extract from `build_report`/`aggregate` — scorecard family rule forbids extraction, document literal is digest-pinned wire shape); R3 (delete pass-through rescues as no-ops — NOT behavior-preserving, both classes would be mis-wrapped by the StandardError catch-all); R4 (condense T8.3/P17/DR-3 why-comments); R5 (endless defs on Report → regular defs — style churn); R6 (Report#initialize override — load-bearing DeepFreeze contract)
Diff stat: 1 file changed, 3 insertions(+), 5 deletions(-) — net −2 LOC (rail ~60)
Commands:
- BEFORE: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses
- BEFORE: `ruby -Itest test/agent_scorecard_test.rb` → 6 runs, 215 assertions, 0 failures, 0 errors, 0 skips (35.3s; SLOW_TESTS lane per Rakefile:48, run anyway as A/B baseline)
- AFTER: `ruby -c <file>` → Syntax OK
- AFTER: `rubocop --cache false <file>` → no offenses detected (no increase vs BEFORE)
- AFTER: `ruby -Itest test/agent_scorecard_test.rb` → 6 runs, 215 assertions, 0 failures, 0 errors, 0 skips (byte-for-byte identical counts to BEFORE; suite pins two-run content-digest stability :94–102 and exact `.to_json` :358, so this evidences unchanged scorecard output bytes)
- AFTER: `ruby -Itest test/memory_treatment_profile_test.rb` → 14 runs, 218 assertions, 0 failures, 0 errors, 0 skips (second grep hit referencing AgentSmokeScorecard#run; matches rank 70's recorded post-edit counts)
Focused-test note: `grep -l AgentSmokeScorecard test/*.rb` → exactly these two files; no other
test touches the file.
Deviations: none
FLAG (owner decisions): none
