# Combined pass — rank 105 gems/tamoz-core/lib/tamoz/core/capability/descriptor.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (drop dead `reject { |k,_| k == :definition_digest }` in `compute_digest` — the
input is always the literal hash built at the end of `validate!`, which never carries that key;
single in-file caller; JCS canonicalization is a pure serializer, so digest bytes are identical)
Rejected: C2 (hoist egress_policy_ref fetch — mechanical churn, no gain);
C3 (decompose ~120-line `validate!` into predicates — binding pins this seam to method-level
naming/step-down only, the in-file ModuleLength disable records the closed-world contract as
intentionally kept together, and extraction risks error-message/ordering drift on a cross-gem
value contract); C4 (rename `validate!` → normalize_/build_ — name defensible, churn on a pinned
contract for marginal gain); C5 (`1 * 1024 * 1024` cosmetic); C6 (requested_scopes double-fetch
local)
Diff stat: `1 file changed, 1 insertion(+), 2 deletions(-)` (net −1)
Commands: BEFORE `ruby -c` OK / `rubocop --cache false` 0 offenses · AFTER `ruby -c` OK /
`rubocop --cache false` 0 offenses (no increase) · `ruby -Itest test/capability_descriptor_contract_test.rb`
→ 4 runs, 18 assertions, 0 failures, 0 errors · `ruby -Itest test/capability_closed_world_test.rb`
→ 3 runs, 14 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): `validate!` (~120 lines) exceeds the ≤20/≤30 step-down ceilings; left as-is
per the pinned-shape/no-extraction constraint on this cross-gem contract. If the owner ever wants it
decomposed, that should be a sanctioned two-agent pipeline change, not a combined-pass edit.
