# Combined pass — rank 159 gems/tamoz-comms/lib/tamoz/comms/approval_prompt.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (delete duplicated class-rationale block — lines restating "fields ARE the value / splitting would fragment the row"); | Rejected: C2 (`validate_required_evidence!` names the INV-C rule at the call site and homes its non-coercion comment — DecisionRecord's named-private-bang pattern, e.g. validate_id! → validate_hex!); C3 (`self.wire_time(wire, key)` is public API mirroring DecisionRecord:214 / InboundEnvelope:116; three-copy extraction already tracked in docs/repo-quality-audit-2026-08-20/duplication.md item 2 — visibility change = cross-gem API change); C4 (initialize kwarg reorder would be behavior-preserving but pure churn on the pinned 15-field constructor signature); C5 (`expired?(now) = now >= @expires_at` ivar form is character-for-character DecisionRecord:159's mirror — switching to `expires_at` diverges from the pinned idiom); C6 (normalizing validator guard style touches methods whose raise-message bytes and check order are pinned approval semantics); C7 (Shapes.member? vs raw include? — Shapes is tamoz-comms' shared field-shape vocabulary, not a defect);
Diff stat:  gems/tamoz-comms/lib/tamoz/comms/approval_prompt.rb | 4 ---- ; 1 file changed, 4 deletions(-)
Commands:
- `ruby -c gems/tamoz-comms/lib/tamoz/comms/approval_prompt.rb` → Syntax OK (before AND after)
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (not increased)
- `ruby -Itest test/comms_values_test.rb` → 20 runs, 78 assertions, 0 failures, 0 errors
- `ruby -Itest test/comms_deny_callback_test.rb` → 3 runs, 8 assertions, 0 failures, 0 errors
- `ruby -Itest test/comms_evidence_gated_approval_test.rb` → 15 runs, 27 assertions, 0 failures, 0 errors
Deviations: none — change is comment-only (4 deleted lines); wire hash bytes/order, ValidationError message bytes, public/cross-gem API, and approval semantics untouched; rubocop:disable line and both :reek: annotation lines preserved intact.
FLAG (owner decisions): surface_descriptor.rb:25 carries a third, cross-file copy of the same class-rationale sentence ("per-field rule set … splitting …") — out of this pass's single-file ownership, so left untouched; a repo-wide rationale-dedup would be an owner call.

## Phase 1 — ANALYZE detail (written before editing)

Baseline: branch verification-again, owned file clean; `ruby -c` OK; RuboCop BEFORE count = 0.
Idiom reference pinned by brief: decision_record.rb. Authority-surface rails honored.
Candidates C1–C7 as summarized above, with apply/reject reasoning recorded here first:

- C1 APPLY: lines 24–27 duplicated lines 20–22 nearly verbatim; DecisionRecord states the
  monolith rationale once before its annotations. Deleted only the duplicate narrative;
  kept `# rubocop:disable Metrics/...` (real suppressed offenses) and both `:reek:` lines.
- C2 REJECT: looks like a no-op wrapper (`AuthorityEvidence.from(return discarded)`) but it
  IS the step-down seam naming the per-field rule; inlining saves 3 lines, loses the name.
- C3 REJECT: public visibility is the established Comms codec idiom; dedup is multi-file.
- C4/C5/C6 REJECT: churn or idiom divergence with zero reader benefit on authority-surface code.
- C7 OBSERVED: no change warranted.

## Phase 2 — GATES

All five commands above executed after the edit; all green. Net LOC delta: −4.
