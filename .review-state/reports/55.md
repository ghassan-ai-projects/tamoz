# Combined pass — rank 55 gems/tamoz-approval/lib/tamoz/approval/engine.rb

Verdict: COMMITTED-CANDIDATES

## Phase 1 — ANALYZE

Baseline: `ruby -c` OK; `rubocop --cache false` = **0 offenses** (BEFORE). File is 369 lines;
`engine.rb` sits on several `.rubocop_todo.yml` exclusion lists, so the live-config baseline is clean.

Audit against the five criteria + CODING_STANDARD §3/§4/§5/§6/§11:

- Public surface reads as a lifecycle DSL: `build_request → decide / decide_or_reuse /
  simulate → resolve → bind_session / rebind_session / release_session / close_session → reload`,
  with `policy_for` deliberately re-opened via `public def` inside the private section
  (comment-anchored: run-path consumers must ask here, not at `@policy`). Good.
- Naming follows §3 (`validate_answer`, `validate_actor_evidence`, `build_request`,
  `mint_grant`, `find_live_session_grant`, `replay?` predicate). No boolean params. No `get_/set_`.
- All error sites are pinned contracts (message bytes) — untouched.
- Comments audited under §11: all are why-comments (exactly-once critical section,
  record-before-insert ordering, time contract, §2.6 rebind exception, dangling-symlink failure
  model). None restate code; none added or removed.

Candidates:

- C1 (apply): L92–98 in `resolve` — five explicit kwargs collapse to Ruby shorthand
  `record_resolution(decision_id:, answer:, scope:, actor_evidence:, grant:)`. All five names are
  in-scope locals; receiver, keys, values and evaluation order byte-identical. Matches the file's
  existing shorthand idiom (L62–67, L52) and drops `resolve` from ~20 counted code lines toward the
  §4 ≤20 normal ceiling without extraction.
- C2 (reject): split `resolve` (33 physical lines > 30 hard) into validate/apply helpers.
  This is the exactly-once resolution critical section (lookup→record→insert under one mutex);
  extraction adds indirection around pinned approval semantics and §4 forbids splitting cohesive
  logic merely to satisfy a number. Recorded cohesion exceptions (durable transaction cores)
  allow method-level improvements only.
- C3 (reject): collapse the dangling-symlink branch in `canonicalize_target` (L355–357).
  Provably redundant — its condition implies `!File.exist?(target)`, so control reaches the
  identical final `File.expand_path` either way. Rejected anyway: branch+comment carry a real gate
  failure model ("glob denies still match it") inside pinned gate-input canonicalization;
  4 saved lines do not pay for the lost intent and review risk.
- C4 (reject): hoist duplicate `tool.to_sym` in `build_request` (L34–35) to a local.
  Adjacent lines, micro-churn, no measurable clarity gain.
- C5 (reject): sweep remaining explicit kwargs (`log_decision`'s `step_scope: step_scope`,
  `record_switch`'s mixed style) to shorthand for uniformity. Pure style sweep across pinned
  logging/mode-switch paths; the brief bans mass reformatting and no length diagnostic improves.
- C6 (reject): rename/relocate public entry points (`simulate`, `public def policy_for`
  placement, release/close pairing). Public/cross-gem API is untouchable; placement is deliberate.
- C7 (reject): drop the `recorded &&` guard in `replay?`. The guard is live logic: when no prior
  resolution exists `recorded` is nil and must flow to the mint path, not raise.

## Phase 2 — APPLY

(applied after this block was written; see verdict/diff stat below)

Applied: C1 (record_resolution kwargs → shorthand; −5/+1, net −4 lines in `resolve`).
Rejected: C2 (pinned exactly-once critical section; §4 forbids splitting for a number);
C3 (redundant-but-intent-carrying branch in gate-input canonicalization); C4/C5 (style churn
on pinned paths); C6 (public/cross-gem API untouchable); C7 (guard is live nil-path logic).

Diff stat: gems/tamoz-approval/lib/tamoz/approval/engine.rb | 6 +----- (1 insertion, 5 deletions)

Commands:
- BEFORE: ruby -c → Syntax OK; rubocop --cache false <file> → 0 offenses
- ruby -c → Syntax OK
- rubocop --cache false gems/tamoz-approval/lib/tamoz/approval/engine.rb → no offenses detected
  (0 AFTER vs 0 BEFORE)
- ruby -Itest test/agent_approval_boot_test.rb → 5 runs, 18 assertions, 0 failures, 0 errors
- ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors

Deviations: none. No renames (no public API touched); all error messages byte-identical;
policy loading semantics untouched; net change well under the ~60-line stop rail.

FLAG (owner decisions): C3 noted for the record — the dangling-symlink branch in
`canonicalize_target` is provably redundant with the fall-through `File.expand_path`; kept as
executable documentation of the glob-deny failure model. Owner may prefer collapsing it.
