# Fix report — rank 9 gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb

Applied: C1 (drain_to_terminal step-down: `advance_queued_request` + `deliver_resume`/`deliver_continue`; loop now reads render-stale → tracked-check → advance → status case; drain signature untouched, send-pins honored) · C2 (build_model step-down: `primary_credential_ref` + `enforce_role_credential!` + `build_provider_model`; both ProfileRoleUnavailableError raise sites byte-identical, enforcement still precedes the --model check; build_model(options, profile:) signature unchanged for cross-file callers and send-pinned tests) · C3 (run_durable step-down: `provision_private_session_dir!` keeping the ArgumentError bytes, `build_durable_session` for the Session assembly; run_durable signature unchanged for cli_session_commands.rb ×13 / cli_worker_commands.rb) · C4 (`print_one_shot_result(result)` — prompt-text bytes unchanged) · C6 (`report_blocked_thread(thread_id, blocked, json:)` — cli.paused event + both hint lines byte-identical) · C7 (`one_shot_routing` / `durable_routing` named separately, NOT unified — durable keeps ignoring --shadow-routing)

Rejected: C5 (extraction slices the sink/emitter/mode/context wiring group in half — helper stays coupled through the sink argument while the pump keeps the rest; adds indirection without removing complexity, review rule (c); reek's FeatureEnvy(sink) lives in the pump section C5 leaves intact) · C8 (turn-scope Data value is new machinery whose seam crosses the included CLIWorkerCommands/CLISessionCommands modules and test pins; medium risk, depends on C5 landing; per brief, prefer few high-confidence changes — owner-level redesign if wanted)

Diff stat: gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb | 249 ++++++++++++++++------------ — 1 file changed, 143 insertions(+), 106 deletions(+)

Commands:
- ruby -c gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb → Syntax OK (before and after)
- rubocop --cache false <owned file> → BEFORE: "1 file inspected, no offenses detected" (0); AFTER: 0 offenses — count did not increase; no suppressions added
- ruby -Itest test/sqlite_stale_request_test.rb → 26 runs, 176 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_profile_machinery_test.rb → 24 runs, 230 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_cli_test.rb → 33 runs, 753 assertions, 0 failures, 0 errors, 0 skips
- New helper names verified collision-free across gems/, apps/, bin/, test/ (grep)
- No test was edited or skipped; none of the run suites are on the known-red list

Deviations:
- C1 shape: instead of candidate's `resume_paused_view` returning a truthiness the caller branches on, the two early-return guards stay visible in the state case and only the delivery mechanics moved into `deliver_resume`/`deliver_continue`. Reason: session.resume's return value must not become an accidental control-flow signal. Candidate's `advance_queued_request` name kept as proposed.
- C2 shape: helpers take the resolved `credential_ref` rather than `profile`, so enforcement and the rescue translation share one lookup; both pinned messages byte-identical.
- Working tree note: git status showed pre-existing modifications to gems/tamoz-mcp/lib/tamoz/mcp/invocation.rb, gems/tamoz-sqlite/lib/tamoz/sqlite/memory_store.rb, gems/tamoz-sqlite/lib/tamoz/sqlite/schedule_store.rb before this work started (other owners). This agent touched ONLY gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb.

FLAGs reiterated for owner (owner decisions only, unchanged): routing-mode unification (durable ignores --shadow-routing), boolean params run_durable(read_only:)/resolve_interrupt_decision(interactive:), three spellings of effect-resolution answers.
