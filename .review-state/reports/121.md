# Combined pass — rank 121 gems/tamoz-agent-improvement/lib/tamoz/agent/improvement/provenance.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down: extract the REQUIRED_AXES/sub-key completeness loop from `assert_complete!` into private `assert_axes!`; pure code motion, error order and message bytes identical)
Rejected: C2 (`blank?`'s `when false then false` duplicates `else false` — the explicit branch carries the §11 why: a recorded `false` is an answer; removal is churn with real loss); C3 (use `blank?(candidate_id)` instead of `String(candidate_id).empty?` — NOT behavior-preserving: blank? strips, String().empty? does not, so a whitespace-only id flips pass→raise); C4 (reuse a shared blank helper from tamoz-core — none exists repo-wide, grep-proven; creating one is a cross-gem interface change needing owner sign-off); C5 (restructure `assert_lineage!`'s inverted guard — rubocop-clean already, style churn on correct code); C6 (trim the longer invariant comments — they are §11 "why" comments documenting safety invariants in a validation domain)
Diff stat: gems/tamoz-agent-improvement/lib/tamoz/agent/improvement/provenance.rb | 31 ++++++++++++---------- (17 insertions(+), 14 deletions(-); net +3 LOC)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE also 0) · ruby -Itest test/improvement_candidate_test.rb → 14 runs, 284 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE (written before any edit)

BEFORE state: `ruby -c` OK · rubocop --cache false = 0 offenses. Users: `promotion.rb`
(same gem: `assert_complete!`, field accessors) and `test/improvement_candidate_test.rb`
(`complete?`, `assert_complete!`, `digest`, `REQUIRED_AXES`, `AXIS_KEYS`, error messages).
No dynamic-dispatch callers found beyond the test's `public_send(axis)` over Data accessors.

Pinned per binding note: `DIGEST_DOMAIN`, `#digest` bytes, `#to_h` string-keyed shape
(wire), all error message bytes, `Data.define` member names (public/cross-gem API).

- C1 APPLY — step-down / one level of abstraction: `assert_complete!` mixes three levels
  (scalar candidate_id guard; inline axis+sub-key iteration with nested guards; delegation
  to four named asserts). Extracting the axis loop verbatim into private `assert_axes!`
  leaves the entry point reading as the DSL of checks it performs (~24 lines → ~12),
  matches the existing assert_* vocabulary family, and provably preserves evaluation
  order (candidate_id → axes in REQUIRED_AXES order with AXIS_KEYS sub-keys →
  trajectories → boundary → scope → lineage) and every raised message byte.
- C2 REJECT — see header.
- C3 REJECT — see header (semantic delta, not provable).
- C4 REJECT — see header (boundary).
- C5 REJECT — see header.
- C6 REJECT — see header.
- Method lengths: all ≤26 lines (within 30 hard cap); no boolean params; kwargs used;
  predicates end in ?; no get_/set_. Public entry points read as a small DSL:
  `assert_complete!` / `complete?` / `to_h` / `digest`.
