# Fix report — rank 21 gems/tamoz-agent/lib/tamoz/agent/runtime.rb

Applied: C1 (renamed `run_routed_discovery` → `routed_discovery_observations`, `execute_routed_read_only_plan` → `routed_read_only_observations` — names now say what they return per §3, matching sibling `discover_action_observations`; both call sites updated); C2 (split non-predicate `action_plan_recorded?` into pure predicate `repeated_action?(state, signature)` + pure mutator `record_action_plan(state, plan, review, signature)`, terminal branch explicit in the repair loop; see Deviations for why not the literal proposal); C3 (route guard rewritten to positive form `if request.direct_response? && !RequestRoute.self_contained_task?(task)` — truth-table identical: body fires iff A && !B, same as `unless !A || B`); C4 (`verify` keyword-ized to `(task:, plan:, review:, observations:, verification_context:)`, three in-file call sites updated — §4 no long positional lists); C5 (extracted private `stop_repair(state, context:, reason:, detail: nil)` replacing four copies of the `terminal_reason` + `:repair_stopped` sequence; payload key order metadata→reason→detail preserved exactly = byte-identical events); C6 (step-down in `routed_discovery_plan`: extracted `emit_discovery_structural_review(plan, issues)` verbatim + `semantic_review_accepted?(review)`; all emit types/payloads/order unchanged)
Rejected: C7 (`finish` → `final_result` is a lateral rename that removes no complexity; `finish` already names the domain concept the method owns — the turn finishing with the `:completed` emission — while `final_result` reads as a query and hides the side effect; churn in 4 places for no clarity gain)

Diff stat: gems/tamoz-agent/lib/tamoz/agent/runtime.rb | 122 ++++++++++++++-------------- → 63 insertions(+), 59 deletions(-), net +4 LOC (623 → 627)

Commands:
- `ruby -c gems/.../runtime.rb` — Syntax OK (before AND after)
- `rubocop --cache false gems/.../runtime.rb` — before: 0 offenses; after: 0 offenses (no increase; nothing suppressed). Two transient offenses introduced mid-pass were fixed in code: Naming/PredicateMethod on the boolean-returning interim name, Layout/HashAlignment on wrapped-arg calls
- `ruby -Itest test/agent_runtime_test.rb` — 11 runs, 54 assertions, 0 failures, 0 errors
- `ruby -Itest test/agent_mode_switch_test.rb` — 4 runs, 45 assertions, 0 failures, 0 errors (covers legacy/shadow/experimental routing incl. routed discovery + repair loop)
- `ruby -Itest test/public_api_test.rb` — 3 runs, 1015 assertions, 0 failures, 0 errors (public surface untouched proof)
- Rename-scope greps: `run_routed_discovery|execute_routed_read_only_plan|action_plan_recorded` appear ONLY in runtime.rb repo-wide; included modules runtime/plan_review.rb + runtime/step_execution.rb reference none of the touched names; no `send(:verify/:finish/:action_plan_recorded?...)` or `.verify(`/`.finish(` reach-ins on Runtime anywhere (all hits are unrelated classes: Evals, Stream, WitnessVerifier, SessionNodes lifecycle, Oracle); zero test references to any renamed identifier

Deviations:
1. C2 applied as a refined split rather than the proposed single imperative returning boolean: RuboCop Naming/PredicateMethod (active in this repo) rejects methods returning bare true/false without a `?` name, so "imperative name returning the continuation boolean" cannot pass the gate unsuppressed. The applied split is strictly more honest: pure predicate + pure mutator, identical state writes (same five keys, same order), identical event sequence (signature computed before novelty check as before; repeat path emits the same `:repair_stopped` payload then breaks).
2. `semantic_review_accepted?` omits the candidate's defensive `review.nil? ||` clause — its only call site is guarded by `if review`, so the nil branch was dead code (owner directive: don't cover cases that cannot happen).
3. Multiline `stop_repair` calls formatted one-keyword-per-line to satisfy Layout/HashAlignment under the committed config.
4. FLAGs recorded only, no action taken (owner decisions): (a) `{ |event| yield event if block_given? }` plumbing spans ~40 hops here and the same pattern lives in runtime/plan_review.rb and runtime/step_execution.rb — cross-file by construction; (b) symbol-keyed mutable `action_state` hash threaded through 8 repair-loop methods needs a named state class in its own file — structural extraction out of scope.
5. Working tree also contains OTHER workers' in-flight edits (gems/tamoz-evals/.../sqlite_selector_control.rb, subprocess_runner.rb at report time; earlier gems/tamoz-agent-cli/.../cli_worker_commands.rb). Not mine, untouched; my changes are confined to the owned file.
