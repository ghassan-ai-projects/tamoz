# Combined pass — rank 227 gems/tamoz-graph/lib/tamoz/graph/builder.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract repeated capacity guard into private `enforce_capacity!` — state/node/edge/branch all opened with the same two-line "graph exceeds" refusal); C2 (split boolean-kwarg `normalize_endpoint(value, source:)` into `normalize_source`/`normalize_target` — §4 names the remedy "split the method") | Rejected: C3 (rename `actual_default` local — churn, name already reads plainly); C4 (`branch` raising ArgumentError vs GraphDefinitionError elsewhere — refusal semantics pinned; class change is a behavior change); C5 (`node(... routes: [])` mutable default arg per §5 — no defect possible here: literal re-created per call and NodeSpec copies+freezes at node_spec.rb:32-34 without mutating the argument; pure churn on a pinned DSL signature); C6 (extract duplicate-key check shared by state/node — saves ~1 line, adds indirection; methods already read at one level after C1)
Diff stat: gems/tamoz-graph/lib/tamoz/graph/builder.rb | 42 +++++++++++++++-------------- · 1 file changed, 22 insertions(+), 20 deletions(-)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE also 0) · ruby -Itest test/graph_definition_test.rb → 11 runs, 42 assertions, 0 failures, 0 errors · ruby -Itest test/graph_determinism_property_test.rb → 2 runs, 480 assertions, 0 failures, 0 errors · inline spot-check via test_helper env: END-in-source-slot and START-in-target-slot refusals byte-identical ("invalid edge endpoint Tamoz::{...}"), START/END edges still accepted as sentinels
Deviations: none
FLAG (owner decisions): none

## Analyzer notes (Phase 1, written before any edit)

Public surface is the DSL itself (`Builder.build`, `state`, `node`, `edge`, `branch`,
`definition`) — untouched. `Builder` is `private_constant`; sole external entry is
`Tamoz.graph` → graph.rb:27. `normalize_endpoint` was private with exactly one caller
in-repo (builder.rb:95; repo-wide grep incl. send/__send__/public_send found no other).
BEFORE: ruby -c OK · rubocop --cache false = 0 offenses.

- C1 — old lines 39-41, 69-71, 92-94, 104-106 repeated the same guard: capacity check +
  `"graph exceeds #{MAX_*} …"` GraphDefinitionError. Extracted private
  `enforce_capacity!(current, maximum, what)` (§3 enforce_ vocabulary: "policy applies and
  may refuse"; mirrors existing in-gem shapes executor.rb#enforce_pending_limit!,
  checkpoint_codec.rb#enforce_size!). Interpolated bytes identical
  (`MAX_CHANNELS.to_s == "10000"`), per-method check order preserved.
- C2 — `normalize_endpoint(value, source:)` was a Reek BooleanParameter; §4 remedy is
  "split the method". Split to `normalize_source` / `normalize_target`; keeps
  `"invalid edge endpoint #{value.inspect}"`, `"edge source"`, `"edge target"` byte-identical;
  START allowed only in source slot, END only in target slot; symbolization unchanged.
- Method lengths all within ceilings (state body 19, node body 20 — both shrink after C1);
  validation ordering (capacity → identity → duplicates) left exactly as-is (pinned refusals);
  branch's ArgumentError-before-capacity order preserved.
