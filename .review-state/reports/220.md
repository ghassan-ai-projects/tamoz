# Combined pass — rank 220 gems/tamoz-cancellation/lib/tamoz/cancellation_token.rb
Verdict: CLEAN
Applied: (none) | Rejected: C1 (blocked by Layout/LineLength ceiling — would add an offense); C2 (extraction crosses pinned mutex scope / callback ordering); C3 (pre-declared locals required by Ruby block scoping; moving remove_callback inside the lock risks lock-order inversion); C4 (zero intent gain, style churn); C5 (one call site, indirection against simple-over-complicated); C6 (two sites only; swallow is part of pinned dispatch semantics)
Diff stat: (no edit — working tree unchanged for the owned file)

## Phase 1 — audit

File is a cross-gem concurrency primitive (consumers: tamoz-agent{,-cli,-session},
tamoz-concurrency, tamoz-graph ×5 files, tamoz-stream). Thread-safety and callback
ordering are pinned by binding; public API untouchable. Audited against the five
criteria + CODING_STANDARD §3/§3.1/§4/§5/§6/§11:

- Names state intent throughout: `validate_timeout!` (§3.1 validate_*), `normalize_reason`
  (normalize_*), predicates end `?` (`cancelled?`), no get_/set_, no boolean params,
  kwargs per §4. Registration/removal is a complete pair (`on_cancel` → `Subscription#unsubscribe`,
  §4 "state that can be added can be removed").
- Surface is narrow: both nested classes and both constants under `private_constant`;
  opaque callback ids are frozen `Object.new.freeze` values (hash-key protocol at an
  internal boundary — correct per §5).
- Length pressure exists exactly where recorded: `cancel!` (~20 body lines) and
  `on_cancel` (~24 body lines) exceed Metrics/MethodLength Max 20 and are listed in
  `.rubocop_todo.yml` for this file. Recorded exception stands; splitting either would
  move code across a `@mutex.synchronize` boundary on a pinned primitive → no extraction.
- The two `rescue StandardError` swallow sites are the recorded §7 pattern (one failing
  callback must not starve the rest nor crash `cancel!`/late delivery) — left alone.

### Candidates

- **C1** — `on_cancel`: flatten inner capacity check
  `if @callbacks.length >= max_callbacks … raise StateLimitError … end` to modifier form
  (step-down: removes one nesting level inside else-inside-synchronize). REJECT: the
  single-line spelling measures ~124 chars vs `Layout/LineLength` max 120 (not overridden
  in `.rubocop.yml`, so default applies) → introduces a new offense, failing gate 2;
  current form is already the shortest rubocop-clean spelling.
- **C2** — extract the late-delivery branch of `on_cancel` (reason captured → `callback.call`
  under begin/rescue → `ClosedSubscription::INSTANCE`) into a private helper. REJECT:
  extraction relocates code around the mutex scope of a primitive whose thread-safety and
  once-only delivery ordering are pinned; a helper hides the invariant it must keep.
- **C3** — simplify `Subscription#unsubscribe` capture (drop pre-declared locals, or call
  `remove_callback` inside the synchronize block). REJECT: pre-declaring `token`/`callback_id`
  before the block is what makes them visible after it (Ruby block scoping); calling
  `remove_callback` while holding the subscription mutex takes the token lock inside the
  subscription lock — reorder/deadlock risk on pinned code.
- **C4** — rename local `safe_reason` in `cancel!` to `normalized_reason`. REJECT: no intent
  gain over the current name; pure churn.
- **C5** — extract `validate_max_callbacks!` out of `initialize`. REJECT: single call site,
  adds indirection; owner directive prefers the simple path; message bytes are pinned.
- **C6** — dedupe the two `rescue StandardError … nil` blocks into one helper. REJECT:
  two sites; each sits inside different lock/dispatch contexts with pinned semantics.

No candidate survived the apply bar → nothing edited; gates run on the untouched file.

Commands: ruby -c OK · rubocop --cache false = 0 offenses (BEFORE) / 0 (AFTER, no edit) ·
focused tests: see below.
Deviations: none
FLAG (owner decisions): none
