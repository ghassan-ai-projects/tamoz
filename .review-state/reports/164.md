# Combined pass — rank 164 gems/tamoz-evals/lib/tamoz/evals/benchmark/metrics.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down: extracted `self.calibration_rows(cells, codes)` from `calibration` — 22 → 17 lines, pure code motion of the (confidence, correct) row build into a `def self.` helper matching the file's existing helper seam, so it stays off the module_function include surface exactly like `mean`/`confusion`; identical expressions, iteration order, and float op order)
Rejected: C2 (rename `f1`/`recall` param `confusion` → `counts`: domain-accurate term for confusion-matrix entries, zero ambiguity in body — churn); C3 (rename `normalized` → `normalize_probabilities` per §3 verb vocabulary: REJECTED because `cell_extractor.rb:310` already defines `normalize_probabilities(vector)` in the same `Benchmark::` namespace with DIFFERENT semantics (integer METRIC_SCALE rounding vs unit-sum floats) — the collision would trade one naming quibble for a real confusion hazard; adjective form is the distinguisher); C4 (add protocol comment to `cost_per_cell`, the only undocumented entry point: §11 comments default none and the name already carries intent — churn); C5 (unify `module_function` entry points vs `def self.` helpers: the split deliberately keeps helpers off the include surface; touching it alters public API — untouchable); C6 (single-pass rewrite of `confusion`'s three `count`s: perf irrelevant at benchmark scale, current form mirrors the definition — churn/risk); C7 (extract last-bin-closed predicate from `calibration`'s select: needs a boolean arg, §4 forbids boolean params — reject).
Diff stat: 1 file changed, 10 insertions(+), 6 deletions(-) (net +4)
Commands: ruby -c gems/tamoz-evals/lib/tamoz/evals/benchmark/metrics.rb → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE recorded 0; no increase) · ruby -Itest test/benchmark_harness_test.rb → 25 runs, 67 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/benchmark_report_test.rb → 9 runs, 65 assertions, 0 failures, 0 errors, 0 skips (report digests bind these metric values end to end)
Deviations: none
FLAG (owner decisions): C3 note only — if §3 vocabulary compliance is ever forced here, `Metrics.normalized` cannot become `normalize_probabilities` while `CellExtractor#normalize_probabilities` (integer-scale) exists; one of the two names must change elsewhere first. No action requested.
