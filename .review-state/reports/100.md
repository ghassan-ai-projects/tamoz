# Combined pass — rank 100 gems/tamoz-agent-healing/lib/tamoz/agent/healing/classification.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract shared predicate `typed_evidence_complete?(signal, category)` used by both `confidence_for` and `evidence_for`; removes the duplicated `category == :unknown` special-case + duplicated `REQUIRED_EVIDENCE.fetch(...).call(signal)`, making "serialized `typed_evidence_complete` flag agrees with confidence grade" structural; new helper kept off the public surface via `private_class_method`) | Rejected: C2 (privatize `build`/`confidence_for`/`evidence_for` — they are today reachable public module-function surface via `module_function`; public/cross-gem API untouchable despite zero external callers on repo-wide grep); C3 (kwargs re-signature of the five `*_decision` helpers — replums every pinned-outcome branch for marginal gain); C4 (defaults on `build`'s `never_mutate:`/`never_mutate_class:` — hides outcome-relevant fields behind defaults to save ~4 lines); C5 (merge the five `*_decision` constructors into one parameterized builder — the five names are the in-file DSL naming each pinned outcome); C6 (comment trims — headers carry §11 design-section safety invariants); C7 (renames — vocabulary already conforms: predicates end ?, `build_` present, classify reads as single entry point)
Diff stat:
 gems/tamoz-agent-healing/lib/tamoz/agent/healing/classification.rb | 12 +++++++++---
 1 file changed, 9 insertions(+), 3 deletions(-)
Commands:
- ruby -c gems/tamoz-agent-healing/lib/tamoz/agent/healing/classification.rb → Syntax OK (before and after)
- rubocop --cache false (json) → offenses=0 BEFORE, offenses=0 AFTER (no increase)
- ruby -Itest test/healing_failure_contract_test.rb → 35 runs, 264 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/healing_matrix_test.rb → 4 runs, 44 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

---

## Appendix — Phase 1 candidate audit detail (written before any edit)

BEFORE gates: `ruby -c` OK · `rubocop --cache false` offenses=0.
Not EXEMPT: real logic (classify step-down + Decision value), not mostly declarative data.
Outcomes pinned per binding note: no change to any classification result, reason string,
confidence grade, or Decision shape. Method-level naming/step-down only.

- C1 (APPLIED): see Applied above. Behavior-preserving proof: identical evaluation order and
  results — unknown returns early in both consumers exactly as before (predicate never
  evaluated for unknown); non-unknown evaluates the same pure evidence lambda over the frozen
  signal; `REQUIRED_EVIDENCE[:unknown] -> false` made the old duplicated form equal even in
  the unreachable branch.
- C2–C7: reasons in the Rejected list above.
- Context checks that informed the calls: repo-wide grep found no external callers of
  `confidence_for`/`evidence_for`/`Classification.build`; `:observe` in `NON_FORM_FAMILIES`
  is live (`classification/legacy_text_adapter.rb:88`) and untouched; sibling files
  `classification/matrix.rb`, `remediation/session.rb`, tests are read-only here.
