# Fix report — rank 17 gems/tamoz-agent-cli/lib/tamoz/agent/cli_worker_commands.rb

Applied: C1 (observe_doctor split into doctor_redaction_report + render_doctor_outcome; rescue renders `{"ok"=>false,"redaction"=>false}` through the same path — bytes and exit codes 0/1 unchanged; both Metrics cop disables retired, complexity now well under the Max 8 gate); C2 (queue_add → parse_queue_add_options + submit_queued_request; both load-bearing authority-binding comments moved verbatim with the code; json branch passed as `json:` keyword mirroring the file's existing report_decision precedent incl. its :reek:ControlParameter pragma); C3 (cmd_worker → parse_worker_options returning `{once:, concurrency:, poll_interval:}`, double-splatted into Worker.new; option parsing stays BEFORE with_worker_runtime so OptionParser errors still fire before any runtime is opened); C4 (observe_tail loop body → render_new_observations(entries, seen, options)); C5 (cmd_approve parser dance order!/shift/parse! → parse_approval_target, moved verbatim; returns [request_id, reload_path, mode, thread_id, deny]); C7 (deleted the stale 3-line "declared in SUBCOMMANDS … not built yet" comment — approve/--deny/--reload/--mode are fully implemented immediately below); C8 (format_observation → render_observation, caller line 117; format_worker_event → render_worker_event, caller line ~681; repo-wide grep proves both callers are in-file only); C9 (shared session_view(runtime, thread_id) rescuing StandardError→nil; paused_approvals uses it directly, session_status maps nil→unavailable_session_status — exact equivalence because Session#view always returns a SessionView or raises, never nil).

Rejected: C6 (special acceptance proof FAILS for the candidate as written: moving `private` above format_observation/line 192 would privatize cmd_worker/cmd_status/cmd_approve — they sit between line 192 and old private at 416 — whose ONLY callers are `__send__(handler)` dispatch at gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb:151–153, i.e. a __send__ caller exists for names whose visibility would change; the same placement also leaves observe_tail/metrics/doctor (92–147) public, inverting the candidate's own goal "only the cmd_* entry points stay public"); FLAG stringify (owner-only cross-gem naming decision — untouched).

C6 evidence recorded for a compliant re-issue: the eight helper names (observe_tail, observe_metrics, observe_doctor, format_observation, queue_add, queue_list, approve_reload, approve_mode_switch) have ZERO direct or send/__send__/public_send callers anywhere in gems/, apps/, bin/, script/, test/ outside this file (only substring hits inside Minitest method names test_approve_reload_*, test_queue_add_*); test/public_api_test.rb pins constants only, none of these method names; docs/public-api.json pins none of them. A passing version must keep all seven cmd_* entry points public (relocate the helpers below an access marker or add explicit public/private sections) so no __send__-dispatched name changes visibility.

Diff stat: gems/tamoz-agent-cli/lib/tamoz/agent/cli_worker_commands.rb | 214 ++++— 1 file changed, 114 insertions(+), 100 deletions(+)

Commands:
1. `ruby -c gems/tamoz-agent-cli/lib/tamoz/agent/cli_worker_commands.rb` — Syntax OK (before and after edit)
2. `rubocop --cache false <owned file>` — BEFORE: 1 file inspected, no offenses detected; AFTER: 1 file inspected, no offenses detected (count 0→0, not increased; two transient Layout/HashAlignment offenses introduced by emit-line compaction were safe-autocorrected on the owned file only — never suppressed)
3. `ruby -Itest test/agent_worker_test.rb` — 24 runs, 114 assertions, 0 failures, 0 errors, 0 skips (covers queue add incl. unknown-profile refusal, worker run)
4. `ruby -Itest test/agent_approval_boot_test.rb` — 5 runs, 18 assertions, 0 failures, 0 errors, 0 skips (covers approve reload/publish paths)
5. `ruby -Itest test/observability_cli_test.rb` — 2 runs, 11 assertions, 0 failures, 0 errors, 0 skips (covers observe doctor --json exit 0 + redaction=true through the extracted path)
6. `ruby -Itest test/agent_mode_switch_test.rb` — 8 consecutive green runs (4 runs, 45 assertions, 0 failures each)

Deviations:
- C5 signature corrected: the candidate omitted `deny`; parse_approval_target returns five values so the :approve/:deny direction logic is preserved exactly.
- One early agent_mode_switch_test run showed 2 failures ("paused approvals expected empty"); attribution run at HEAD version of the owned file passed, after restoring my version the suite passed 8/8 consecutively — attributed to sandbox timing contention (multiple fixers running suites concurrently), not to the edit; no revert made, orchestrator may wish to re-run it once before committing.
- Working tree note: gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb, gems/tamoz-evals/.../readiness.rb and gems/tamoz-graph/.../checkpoint_codec.rb carry OTHER agents' pre-existing modifications; I touched nothing outside my owned file.
