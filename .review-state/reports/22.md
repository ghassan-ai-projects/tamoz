# Fix report — rank 22 gems/tamoz-sqlite/lib/tamoz/sqlite/boundary_source_audit.rb

Applied: C1 (extracted send-dispatch unwrap from `parse_call` into `resolve_dispatched_target(name, positional)`; "dynamic dispatch is forbidden…" message byte-identical; single `Call.new` tail replaces the duplicated early-return construction); C2 (extracted the three call-shape prohibition guards 544–561 into `assert_call_allowed!(call)`, walker reduced to constant check → guard → recurse; every message and the execute→filesystem→dynamic refusal order unchanged); C3 (hoisted the five-condition conjunction in `resolve_string` verbatim into predicate `reviewed_trailing_index_slot?(value:, expression:, fragments:, last:)`; short-circuit evaluation order preserved — allow_index stays at the call site, so no extra evaluations); C4 (`audit!` step-down: extracted `assert_no_unreachable_helpers!` and `frozen_source`; audit! now reads as the pure step list parse → reject duplicates → inspect roots → assert unreachable → compare registry → frozen source, same raise order); C5 (extracted nil-vs-absent keyword semantics from `bind_arguments` ternary chain into `bound_keyword_value(name, call_keywords, caller_environment)`, three outcomes verbatim); C6 (hoisted the duplicated `%i[command command_call method_add_arg]` literal to `CALL_NODES` beside `TRANSACTION_METHODS`, added to the `private_constant` list) | Rejected: FLAG (`resolve_value(..., label:)` / `resolve_string(..., allow_index:)` boolean keyword modes) — owner decision only; splitting the resolver would duplicate its dispatch (rejection criterion c: indirection without complexity removed), left untouched.

Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/boundary_source_audit.rb | 130 ++++++++++++--------- (1 file changed, 72 insertions(+), 58 deletions(-)); net +14 LOC (614 → 628)

Commands:
- `rubocop --cache false <owned file>` BEFORE edit: 1 file inspected, **0 offenses** (baseline recorded)
- `ruby -c <owned file>`: Syntax OK — pass
- `rubocop --cache false <owned file>` AFTER edit: 1 file inspected, **0 offenses** — count did not increase (= baseline); no suppressions added
- `ruby -Itest test/sqlite_boundary_source_audit_test.rb`: 7 runs, 55 assertions, **0 failures, 0 errors, 0 skips** — pass
- Test coverage note: grep confirms this is the ONLY test file requiring/exercising `BoundarySourceAudit`; all renamed/extracted methods are private scope with in-file callers verified during review.

Deviations:
- none in code. Working-tree notes for the orchestrator: (1) `gems/tamoz-agent-cli/lib/tamoz/agent/cli_worker_commands.rb` was ALREADY modified when I started — not my change, untouched; (2) `.review-state/` is untracked scratch. My only change is the owned file.
- Cohesion exception respected: everything stayed inside boundary_source_audit.rb — method-level extraction/naming only, no constant or helper moved to another file; all constants remain module-private (`CALL_NODES` added to the existing `private_constant` list).
