# Combined pass — rank 224 gems/tamoz-agent-kernel/lib/tamoz/agent/episode_tool_call.rb
Verdict: CANDIDATES-ONLY
Applied: (none) | Rejected: C1 (unpack `call: {tool_name:, arguments:}` pack/unpack round-trip in `dispatch_tool_call` → applied, but the resulting 6-kwarg signature trips `Metrics/ParameterLists` [6/5]; BEFORE count was 0 and suppression is forbidden; the ≤5-param alternatives — deriving `slot` from `logical.slot` for `call_index`/request bytes, or inlining `EffectDispatcher.run` into `#call` — touch pinned journal-key identity or merge orchestration into `#call`, so reverted to HEAD); C2 (rename `digest_request` → `build_request_digest`: single verb, says what it does, no §3.1 violation — churn only); C3 (`(context && context.episode_tools)` → `context&.` : equivalent for nil/object context, pure style churn); C4 (inline one-line `build_logical_call_key`: keeping it preserves step-down in `#call`; twin asymmetry trivial); C5 (positional `execute_tool(tool_port, tool_name, arguments)`: mirrors twin's `perform_call(request_bytes, …)` payload-positional shape, no boolean params)
Diff stat: no diff — owned file is byte-identical to HEAD after revert (git diff empty, net LOC delta 0)
Commands: `ruby -c` → Syntax OK (before & after) · `rubocop --cache false <file>` → BEFORE 0 offenses / after-C1 1 offense (Metrics/ParameterLists) / final 0 offenses · baseline `ruby -Itest test/stream_episode_replay_test.rb` → 2 runs, 11 assertions, 0 failures, 0 errors, 0 skips · rerun on final state → same counts
Deviations: none of substance. Note on test coverage: every exerciser of this class runs through `test/support/episode_composition.rb`, used ONLY by `stream_*_test` files — a known-red/do-not-chase lane. The most relevant one (`stream_episode_replay_test.rb`) was run before AND after as an A/B baseline instead of trusting a single post-run; it is fast and fully green (2 runs / 11 assertions), so the "no non-do-not-chase test touches this file" caveat is recorded explicitly. Pinned seams untouched: EffectDispatcher dispatch, logical-key/request-digest identity, receipt/projection handling, and the twin `map_outcome` seam (already aligned with episode_model_call's rename). No tamoz-sqlite rail applies.
FLAG (owner decisions): C1 is a real, small simplification (removes a hash pack-then-fetch that carries no information; the journaled request comes from `dispatch_request` and is unaffected). It is blocked only by `Metrics/ParameterLists` Max:5 on the widened private signature. If the owner wants it, the options are: raise AllowCount for private dispatch seams, or accept `slot:` being dropped in favor of `logical.slot` after proving `Integer(slot) == Integer(logical.slot)` at the LogicalCallKey boundary — both are owner calls, not mine to make under this brief.
