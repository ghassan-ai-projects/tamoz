# Combined pass — rank 222 gems/tamoz-telegram/lib/tamoz/telegram/normalizer.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (`normalize` uses the already-extracted `message` local for the observed-time dig instead of re-digging `update['message']`); C2 (`callback_envelope` digest_fields reads `message['message_id']` from the local fetched at line 70 instead of re-digging `callback['message', 'message_id']`)
Rejected: C3 (rename `text_kind` — name already states intent, single in-file caller, pure churn); C4 (shrink/split `envelope` kwarg list — recorded exception documented in-file, design §6.2 binds every fact; extraction out of scope per file note); C5 (table-drive the four `*_envelope` builders — obscures step-down, risks pinned wire bytes for no gain); C6 (restyle `my_chat_member`/`chat_member` fallback — current form is clearest)
Diff stat: gems/tamoz-telegram/lib/tamoz/telegram/normalizer.rb | 4 ++-- (2 insertions(+), 2 deletions(-))
Commands:
- ruby -c <file> → Syntax OK
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/telegram_normalizer_test.rb → 8 runs, 23 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/tamoz_telegram_transport_test.rb → 21 runs, 45 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

Behavior-preservation proofs: C1 substitutes an identical unmutated value read two lines earlier.
C2: for any Hash message both expressions are equal; if `callback['message']` were present-but-nil,
both variants compute nil digest fields and raise NoMethodError at `chat_id(message.fetch('chat'))`
before any output — identical observable behavior. Sibling test pins digest stability and content
sensitivity (not literal bytes); values are unchanged either way.

Phase-1 candidate list was written to this report before any edit (history above preserved).
