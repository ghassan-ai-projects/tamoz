# Fix report — rank 6 gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_durable_cli_adapter.rb
Applied: C1-partial (extracted private `evaluated_hard_zero`, 5 kwargs, step-down for result_for); C2 (hard_zero_evidence single-pass verdicts hash — pinned name kept); C3 (rename credential_binding! → enforce_credential_availability!, sole in-file caller updated); C4 (extract cli_failure_label(argv), message bytes identical); C5 (folded digest body into public thread_id_for, deleted private thread_id; driver seam :285/:306 preserved); C6 (rename model_receipts → model_generate_receipts, sole caller line updated; SQL label strings untouched); C7 (extracted verification_refs reader; strict/lenient guards stay per-caller, behavior byte-identical); C8 (moved enqueue!/worker!/worker_until_effect!/worker_subprocess! definitions above `private`, deleted trailing `public :` line — visibility and respond_to? seam unchanged) | Rejected: C1-provenance sub-extraction (proposed signature needs 6 keywords > Q6 ParameterLists Max 5; the only outs are a suppress comment or hiding args behind **splat/derived reads — indirection/suppression, both forbidden by brief)
Diff stat: gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_durable_cli_adapter.rb | 156 +++++++-------- (79 insertions, 77 deletions; 1027 → 1029 LOC, net +2)
Commands:
- ruby -c <owned> → Syntax OK (pre- and post-edit)
- rubocop --cache false <owned> BEFORE edit → "1 file inspected, no offenses detected" (baseline = 0)
- rubocop --cache false <owned> AFTER edit → "1 file inspected, no offenses detected" (0, not increased; nothing suppressed)
- caller greps: credential_binding/thread_id/model_receipts/thread_id_for sends → all hits are in-file or .worktrees copies; scenario_driver uses only public names
- baseline: ruby -Ilib -Itest test/openclaw_durable_cli_adapter_test.rb on pristine HEAD snapshot (/tmp/tamoz-fix6-head) → 12 runs, 59 assertions, 0 failures, 0 errors
- with changes: same file → 12 runs, 59 assertions, 0 failures, 0 errors
- with changes: ruby -Ilib -Itest test/durable_session_evidence_reader_test.rb → 1 run, 7 assertions, 0 failures, 0 errors
Deviations:
1. Main-tree focused tests could not load: a CONCURRENT lane is editing this shared root right now and its in-progress gems/tamoz-sqlite/lib/tamoz/sqlite/migrator.rb raises NoMethodError (migration_checksum) at require time, breaking every tamoz/sqlite load. Tests were therefore run in an isolated `git archive HEAD` snapshot at /tmp/tamoz-fix6-head with ONLY my owned file copied in; snapshot was proven green before my file was applied.
2. "Working tree contains ONLY my file's changes" is not achievable from my side: git status shows 8 modified files; 7 belong to other live lanes (tamoz-agent worker*.rb ×2, tamoz-comms gateway.rb, tamoz-evals openclaw_comms_runner.rb, tamoz-sqlite comms_store+migrator, tamoz-stream situation_request.rb). My edits touch exactly one file.
3. C2 nuance accepted per owner convention "do not cover rare cases": duplicate names inside one mission['hard_zero'] would now yield deduped reasons (statuses were already deduped by to_h); fixture missions list distinct names and blocked_result already dedupes the same way.
4. Pinned API respected: def metrics / def hard_zero_evidence / def effect_outcomes untouched (now at lines 614/460/537); tests reach them via send unchanged.
FLAG items (owner decisions only, noted not acted on): Metrics/ClassLength disable lets the class hold CLI driving + whole metric catalog (structural split out of scope); Tamoz::SQLite.const_get(:Wire, false) runtime probe (~load-order avoidance?) ; EffectPoller#poll(**) silently discards SubprocessRunner kwargs (cross-file contract); 20-line narrative comment above catalog_metrics documents metric↔oracle mapping (§11 tension; protocol docs may be its home).
