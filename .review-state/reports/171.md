# Combined pass — rank 171 gems/tamoz-sqlite/lib/tamoz/sqlite/backup.rb

Verdict: COMMITTED-CANDIDATES
Applied: C3 (<what>: rename private `database_file_path` → `resolve_destination_path` — disambiguates the destination's normalized path from source `database_file.path` in the same guard, §3 naming intent; single in-file caller proven by repo-wide grep incl. send/__send__/public_send)
Rejected: C1 (drop `temporary &&` guard in `call` ensure — REVERTED after gates: simplification trips Lint/NonAtomicFileOperation; the cop's remedy `FileUtils.rm_f` + no exist? check changes ensure-path exception behavior, rm_f swallows ENOENT where File.delete raises, and needs a new require — not provably behavior-preserving under pinned error bytes); C2 (drop `pages = 0` in `step_backup` — REVERTED after gates: NOT a dead store, it performs block-variable capture for the `loop do` block; without it the post-loop return `pages` is a different undefined name — Lint/UselessAssignment fired on the inner assignment once removed; my initial scoping proof was wrong); C4 (inline `close_destination` — class-head reek rationale pins the helper set as mirroring the backup protocol's ordered resource lifecycle; recorded cohesion exception stands); C5 (simplify `&.close unless &.closed?` — error-path cleanup ordering, #close idempotence unproven here, semantics pinned); C6 (extract assert-helpers from `normalize_destination` — adds machinery to a ≤20-line linear method; method-level scope only); C7 (rename `retry_backup_step?` despite its internal sleep — boolean retry-policy contract reads correctly at the call site, churn without semantic gain)
Diff stat:  gems/tamoz-sqlite/lib/tamoz/sqlite/backup.rb | 4 ++-- (2 insertions(+), 2 deletions(-), net 0)
Commands: ruby -c backup.rb → Syntax OK (before+after) · rubocop --cache false → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/sqlite_backup_test.rb → 3 runs, 16 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/sqlite_boundary_source_audit_test.rb (tamoz-sqlite extra gate) → 7 runs, 55 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE (candidate list, written before any edit)

Baseline: `ruby -c` OK · rubocop --cache false → 0 offenses · file 191 lines.
Audit vs (1) naming intent (2) ≤20/30 lines (3) one abstraction level (4) DSL-like entry
points (5) step-down; CODING_STANDARD §3/§3.1/§4/§5/§6/§11.

- C1 · initially apply, reverted at Phase 2 · `call` ensure line 34:
  `File.delete(temporary) if temporary && File.exist?(temporary)` — `temporary` is assigned
  from `temporary_path` before begin/ensure starts, so inside ensure it cannot be nil.
  Simplification introduced Lint/NonAtomicFileOperation; rejected per "fix what your lines
  introduce, never suppress" since every cop-clean form alters pinned error-path semantics.
- C2 · initially apply, reverted at Phase 2 · `step_backup` line 115 `pages = 0` looked like a
  dead store; it is load-bearing block-variable capture for `loop do`. Gate caught it.
- C3 · apply · §3 naming-intent: private `database_file_path(destination)` returns the
  DESTINATION's normalized path while `database_file.path` is the SOURCE — near-identical
  names for different values in one guard (`destination_path == database_file.path`).
  Renamed to `resolve_destination_path` (resolve_ vocabulary). Caller set: exactly line 43,
  repo-wide grep incl. send/__send__/public_send clean (only stale self-consistent .worktrees copies).
- C4..C7 · reject · reasons in header.
- Clean findings (no action): public entry point `call` reads as a small DSL with proper
  step-down (normalize → temp path → secure temp → copy → publish); all methods ≤20 lines;
  kwargs per §4; predicates end ? (`retry_backup_step?`); no get_/set_; §11 comment on
  `finish_backup` is a justified "why" (preserve primary failure) — keep.

TAMOZ-SQLITE rail check: no delegation forwarders touched, no endless methods introduced,
explicit parameter lists preserved everywhere; extra gate
`ruby -Itest test/sqlite_boundary_source_audit_test.rb` run after the edit — green.
