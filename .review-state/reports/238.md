# Combined pass — rank 238 gems/tamoz-mcp/lib/tamoz/mcp/errors.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (Error#initialize: `stderr_tail.nil? ? nil : stderr_tail.dup.freeze` → `stderr_tail&.dup&.freeze`, matching the `&.dup&.freeze` vocabulary of Tamoz::Error's own attribute initializers in core error.rb); Rejected: C2 (drop redundant `RETRYABLE = false` on AmbiguousOutcomeError — repo convention restates terminality explicitly on policy/terminal classes, cf. StaleRequestError and core CircuitPolicyError; removal is churn that weakens the taxonomy's self-description); C3 (hoist OutputLimitError's inline message into a constant — single use site on an SDK-subclass without Error::Metadata; adds machinery for nothing and message bytes are pinned anyway); C4 (default `repairable?` into Mcp::Error base — would add a public method to every subclass and erase the per-class true/false statements that read as the taxonomy DSL); C5 (trim per-class why-comments — they are §11 safety-invariant rationale: fail-closed disclosure, epoch stop, terminal-vs-retryable).
Diff stat: 1 file changed, 1 insertion(+), 1 deletion(-)
Commands: `ruby -c` → Syntax OK (before+after) · `rubocop --cache false errors.rb` → 0 offenses before, 0 after · `ruby -Itest test/mcp_invocation_test.rb` → 31 runs, 208 assertions, 0 failures, 0 errors · `ruby -Itest test/mcp_supervisor_test.rb` → 16 runs, 87 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

## Phase 1 candidate list (written before any edit)
- C1 (apply): line 17 ternary → safe-navigation dup/freeze. Provably behavior-preserving for nil and non-nil inputs; test contract (`refute_nil error.stderr_tail`, bytesize/content assertions in mcp_invocation_test) unaffected.
- C2–C5 (reject): reasons above.
- Contract check: all class names, CATEGORY/SAFE_MESSAGE/USER_VISIBLE/RETRYABLE constants, message bytes, and the `MCP::Client::RequestHandlerError` superclass are pinned — untouched. No renames attempted.
