# Combined pass — rank 239 gems/tamoz-evals/lib/tamoz/evals/cli.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract private `run_gate(label)` deduplicating treatment/scorecard report emission + InvalidArtifactError/ExecutionError→exit-code mapping)
Rejected: C2 (De Morgan rewrite of l.55 guard — taste-only churn); C3 (`map`+`EXIT_PRECEDENCE.find` alternatives less obvious — churn); C4 (`%w[--help -h].include?` for l.49 — no clarity gain); C6 (param-name asymmetry `arguments` vs `paths` — each matches its role, rename is churn)
Diff stat: gems/tamoz-evals/lib/tamoz/evals/cli.rb | 20 ++++++++------------ → 8 insertions(+), 12 deletions(-), net −4 lines
Commands:
- `ruby -c gems/tamoz-evals/lib/tamoz/evals/cli.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- `ruby -Itest test/evals_verifier_test.rb` → baseline 23 runs/294 assertions/0 fail → post-edit identical (23/294/0)
- `ruby -Itest test/memory_treatment_profile_test.rb` → baseline 14/218/0 → post-edit identical (14/218/0)
- `ruby -Itest test/agent_scorecard_test.rb` → baseline 6/215/0 → post-edit run had 1 transient error in `test_seeded_safety_violation_and_incomplete_evidence_fail_separate_hard_gates`; that test passes in isolation (1 runs/6 assertions/0 errors) and an immediate full-suite rerun is green (6/215/0 errors) → timing flake in a ~35s serial suite, not a regression from this diff
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE (written before any edit)

File: 138 lines, single CLI dispatcher class. BEFORE rubocop --cache false: **0 offenses**.
Focused-test baselines (all green): test/evals_verifier_test.rb 23/294, test/agent_scorecard_test.rb
6/215, test/memory_treatment_profile_test.rb 14/218. Public surface = `CLI.run(argv, out:, err:,
scorecard_factory:, treatment_factory:)` + exit-code constants; pinned, untouched. No dynamic
callers (repo-wide grep for `Evals::CLI` shows only `.run(` calls and constant reads).

### Candidates

- **C1 (APPLIED)** — `treatment` (l.66–84) and `scorecard` (l.86–98) duplicated the identical gate
  shape: run report → print `to_json` → `passed? ? SUCCESS : GATE_FAILURE`, plus the identical
  rescue pair InvalidArtifactError→INVALID_EVIDENCE / ExecutionError→INFRASTRUCTURE_FAILURE,
  differing only in the message label ("treatment memory:" vs "agent-smoke:"). Extracted one private
  `run_gate(label)` taking the report-producing block; both commands are now 1-line dispatches.
  Behavior-preservation proof: labels interpolate to byte-identical strings ("treatment memory: "
  / "agent-smoke: " prefixes preserved); return codes unchanged; factory invocation stays inside
  the rescued region (`yield` is covered by the method-level rescue, same as before); usage-guards
  still return before entering the gate region. In-file only; no cross-gem API change; no renames.
- **C2 (rejected)** — l.55 `unless command == "verify" && !paths.empty?` double negative could be
  rewritten via De Morgan. Reason: pure taste; both forms are idiomatic; churn without payoff.
- **C3 (rejected)** — l.57–58 `map { |p| verify_path(p) }` + `EXIT_PRECEDENCE.find { include? }`.
  Reason: clear as written; alternatives (`min_by` + index lookup) are less obvious. Churn.
- **C4 (rejected)** — l.49 `argv == ["--help"] || argv == ["-h"]` → `%w[--help -h].include?(argv)`.
  Reason: no clarity gain at this size; style churn.
- **C5 (kept)** — l.63–65 DR-3 comment. It is a §11 why-comment (why live mode must never be
  faked by the CLI); retained unchanged.
- **C6 (rejected)** — param name asymmetry `arguments` vs `paths`. Reason: each matches its role
  (subcommand args vs artifact paths); renaming is churn.
- **C7 (not a candidate)** — `DECISION_CODES.fetch` can raise KeyError for an unknown decision.
  Reason: decision set is closed by construction (Verifier); handling it would cover a case that
  cannot happen — against the standing owner directive.

Not exempt: the file is logic, not declarative data. tamoz-evals is not one of the recorded
cohesion-exception files; the C1 extraction is method-level within one class, no cross-method
re-routing beyond replacing two copies of one shape with one shared private helper.

## Post-edit state

`run_gate` is 10 body lines (≤20 ceiling); all methods remain ≤20 lines. Step-down order intact:
`run` → `treatment`/`scorecard` → `run_gate`. No comments added. Net −4 LOC.
