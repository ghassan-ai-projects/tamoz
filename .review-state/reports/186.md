# Combined pass — rank 186 gems/tamoz-core/lib/tamoz/context.rb

Verdict: COMMITTED-CANDIDATES

## Phase 1 — ANALYZE (before-state)

BEFORE rubocop (`--cache false`): 1 file inspected, 0 offenses. File is 185 lines.
Binding rails honored: public shape pinned (kwargs signature, ATTRIBUTES readers,
with/child/emit/expired?/cancelled?/check!/inspect, UNCHANGED) — no renames, no
signature changes; audit limited to internal step-down. No subclasses or reopens of
Tamoz::Context exist repo-wide; only `.new` call sites
(tamoz-agent-session ×2, tamoz-agent-cli, tamoz-stream, tamoz-graph).

Candidates:

- C1 `initialize` mixes abstraction levels and runs ~69 lines: ivar assignments are
  interleaved with eight raw predicate/raise blocks (interrupt_mode, cancellation,
  clock, notifier, emitter, interrupts, graph_runtime, episode_tools). Identity/list/
  deadline normalization already stepped down via `identity!`/`normalize_list`/
  `normalize_deadline`; collaborator validation did not. Candidate: extract three
  private validators — `validate_interrupt_mode!`, `validate_cancellation!`,
  `validate_capability!(collaborator, method, name)` — keeping call order, error
  class, and message bytes identical. Verb per §3.1: value-vs-declared-shape checks on
  bad input named by field = `validate_*`. Provable behavior-preserving: pure checks on
  locals, same sequence, nil semantics unchanged (nil fails respond_to? exactly as the
  inline versions did). APPLY if diff stays under the ~60-line stop rail.

- C2 `check!` styles one raise as an `if/end` block and the next as a modifier.
  Candidate would unify to modifiers. REJECT: zero behavioral or abstraction gain,
  pure style churn (brief forbids it).

- C3 `expired?` raises ConfigurationError lazily from inside a query method when
  clock.now is non-finite. Arguable design smell (validation in a query), but moving
  it changes when/if the error surfaces → not behavior-preserving. REJECT.

- C4 metadata Hash guard sits inline between assignments (line 56). Extracting a
  single-use one-line guard into a helper adds indirection without removing a level
  mix of the kind C1 fixes (it is already a one-liner). REJECT: simple solution wins.

- C5 `child`'s UNCHANGED sentinel default and `component` local: sentinel is named by
  its constant and has no external users; reads fine. REJECT: no change needed.

- C6 `with` / `attributes` / `normalize_list` / `inspect`: each does one thing at one
  level; public DSL reads clean. REJECT: nothing to do.

## Phase 2 — APPLY

Applied: C1 (extracted `validate_interrupt_mode!`, `validate_cancellation!`,
`validate_capability!` privates; initialize body is now flat one-step orchestration;
call order, ConfigurationError class, and every message byte unchanged; nil semantics
identical — nil fails respond_to? exactly as the inline forms did; public kwargs
signature, ATTRIBUTES, and all entry points untouched).
Rejected: C2 (style churn only); C3 (moving lazy clock validation changes error
timing — not behavior-preserving); C4 (single-use one-line guard; helper adds
indirection, simple solution wins); C5 (UNCHANGED sentinel reads fine, no external
users); C6 (nothing to do).

initialize: 69 → 55 lines. Residual length is the contract-pinned 20-kwarg signature
plus flat assignment/validation steps at one level of abstraction — §4's named
diagnostic exception ("never split cohesive logic merely to satisfy a number"); the
signature itself may not change under this file's binding rails.

Diff stat: gems/tamoz-core/lib/tamoz/context.rb | 47 +++++++++++++++---------------
1 file changed, 25 insertions(+), 22 deletions(-) — under the ~60-line stop rail.

Commands:
- BEFORE rubocop --cache false <file>: 1 file inspected, 0 offenses
- ruby -c gems/tamoz-core/lib/tamoz/context.rb → Syntax OK
- rubocop --cache false gems/tamoz-core/lib/tamoz/context.rb → 1 file inspected,
  0 offenses (= BEFORE count; nothing suppressed)
- ruby -Itest test/core_context_test.rb → 10 runs, 247 assertions, 0 failures,
  0 errors, 0 skips
- ruby -Itest test/agent_phase3_context_lifecycle_test.rb → 13 runs, 62 assertions,
  0 failures, 0 errors, 0 skips
- ruby -Itest test/capability_host_test.rb → 21 runs, 91 assertions, 0 failures,
  0 errors, 0 skips (asserts exact "must respond to" message bytes)

Deviations: none. mcp_supervisor_test also greps those message strings but sits in the
known-red supervisor lane — not chased, per brief.

FLAG (owner decisions): none
