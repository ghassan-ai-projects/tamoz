# Combined pass — rank 199 gems/tamoz-sqlite/lib/tamoz/sqlite/comms_decision_store.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename `decision_binds` -> `build_decision_binds`: §3.1 construction verb for a
  derived value; bare noun reads like an accessor. Proof: single in-file caller at the
  `comms.decision.insert` execute site; repo-wide grep incl. send/__send__/public_send finds
  no executable caller — the only other textual match is a test *name string*
  (`test_submit_decision_binds_all_eleven_fields_and_signs_under_the_domain`); not pinned by
  the tamoz-comms structural contract, test/public_api_test.rb (pins only
  Tamoz::SQLite::VERSION), or any reek context entry.)
Rejected: C2 (rename mapping pair to encode_/decode_: current names are outside the banned
  set of §3.1 and state return-and-source plainly; renaming two methods for taste is churn);
  C3 (make now_ms/wire_time/build_decision_binds/wire_from_row/select_columns private: a
  visibility change IS a public-surface change — untouchable per brief, zero gain); C4
  (rename `each_decision`, which returns rather than yields: signature is pinned verbatim by
  Tamoz::Comms::DecisionStore — contract names untouchable); C5 (dedupe the two
  `SELECT 1 … WHERE decision_id = ?` probes: labels `insert.existing`/`claim.exists` are
  distinct registry operation identities and this is a durable transaction core — recorded
  cohesion exception forbids extraction); C6 (normalize heredoc indentation in
  pending_decision_for: `<<~SQL` makes it functionally identical — pure style churn); C7
  (touch `def select_columns = …` endless reader: rail says never convert forwarders to
  endless methods and existing trivial readers stay as-is); C8 (rewrite wire_from_row's
  explicit column table into computed code: docstring pins the fixed column table; the
  fetch-vs-[] split encodes required vs nullable and pins wire bytes); C9 (split the
  ArgumentError guard out of now_ms: one line in a three-line converter — machinery for a
  non-problem).
Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/comms_decision_store.rb | 2 insertions(+), 2 deletions(-)
Commands: ruby -c <file> -> Syntax OK · rubocop --cache false <file> -> BEFORE 0 / AFTER 0 offenses
  · ruby -Itest test/comms_decision_store_test.rb -> 11 runs, 26 assertions, 0 failures, 0 errors
  · ruby -Itest test/sqlite_boundary_source_audit_test.rb -> 7 runs, 55 assertions, 0 failures,
  0 errors (tamoz-sqlite rail gate)
Deviations: none
FLAG (owner decisions): none
