# Fix report — rank 25 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_convergence_probe.rb

Applied: C1 (lease-fencing step-down: `read_prior_lease`, `stale_lease_record`,
`acquire_convergence_lease`, `assert_fence_advanced!`; probe steps kept in exact
order; all `adapter.__send__` / `const_get` calls moved verbatim, seam untouched);
C2-partial (`resolve_ledger` + `execute_probe` dispatch seam; signature reworked,
see Deviations); C3 (`replay_failure_message`, byte-identical joined message);
C4 (`RECOVERY_HISTORY_COUNTS` frozen constant beside sibling tables, added to
`private_constant`); C5 (`validate_path!` split into `validate_path_shape!` +
`enforce_path_safety!`, Errno rescue travels with the lstat half — shape half is
pure string ops and cannot raise Errno on POSIX); C6 (`state` → `classification`
in `run` local and all six probe signatures/usages; remaining `state` spellings
are the graph-state concept only); C7 (`request_checkpoint_relation_consistent?`
predicate holding the identical condition with identical short-circuit order;
table fetch stays first so KeyError translation is unchanged); C8
(`report` → `build_report`, sole call site updated) | Rejected: none of C1–C8
wholesale. FLAG (cross-gem `__send__`/`const_get`) left untouched — owner-only.

Diff stat: gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_convergence_probe.rb |
233 +++++++++++++-------- → 1 file changed, 142 insertions(+), 91 deletions(-)
(net +51)

Commands:
- ruby -c <owned file> → Syntax OK (before AND after edit)
- rubocop --cache false <owned file>: BEFORE = "1 file inspected, no offenses
  detected" (0; this file carries Metrics exclusions in .rubocop_todo.yml, hence
  the clean baseline despite legacy method sizes). AFTER = same, 0 offenses.
  One intermediate build tripped Metrics/ParameterLists [6/5] on `execute_probe`;
  fixed by rework, never suppressed; `.rubocop_todo.yml` untouched.
- Focused tests: grep shows exactly one test touching the class —
  test/sqlite_convergence_probe_test.rb — which the brief lists in the
  do-not-run known-red/slow lane ("convergence probe"). Not run, per gate 3.
  Gate 4 note: no runnable test touches this file within my constraints.
- Equivalence evidence instead (throwaway harness, not a test-file change):
  (a) DEFINITION digest HEAD vs edited identical:
      sha256:7d016bab925eb8ad88ad2af5cd6d1c8bb519fd435e4761c54f2f00dd57277594,
      26 probes, definition frozen; (b) same private-method scenarios executed
      against HEAD copy and edited file: 21/21 result rows identical except a
      random tmpdir path — path validation messages/causes incl. ENOENT→"invalid"
      mapping, frozen-dup return, resolve_ledger branches, replay failure bytes,
      relation-predicate truth table incl. nil-request short-circuit, stale
      LeaseRecord construction, execute_probe positional arg order, fence assert,
      report keys/result/frozen.
- New/changed methods measured ≤ 20 non-blank lines each (helpers 5–18);
  orchestrators improved: probe_lease_fencing 50→21, run ~40→27,
  probe_pending_write_replay 36→30, probe_request_checkpoint_reopen 31→30
  (incl. multi-line signature).

Deviations:
1. C2 applied partially as designed-within-gates: the candidate's 6-keyword
   `execute_probe(probe:, app:, adapter:, scenario:, classification:, ledger:)`
   violates Metrics/ParameterLists (Max 5) and suppression is forbidden.
   Reworked to 5 keywords — `run` binds `id = scenario.fetch("id")` once and
   passes `scenario_id:`; the seam derives the probe name via `PROBES.fetch`.
   Dispatch strings, argument order, and all error behavior unchanged.
2. The pinned convergence-probe test suite was not executed (brief's
   do-not-run lane). Proof of behavior rests on syntax gate, clean rubocop,
   pure-rename discipline verified by repo-wide grep (class referenced only via
   const_get + .definition/.digest/.new/.run), digest equality, and the
   equivalence harness above. Orchestrator should run the suite before commit.
3. Working tree also contains modifications from concurrent fixers (e.g.
   tamoz-agent-profile/profile.rb, graph/executor.rb, evals benchmark fixture).
   My diff touches ONLY the owned file.
