# Combined pass — rank 149 gems/tamoz-comms/lib/tamoz/comms/inbound_envelope.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (name the 4× repeated max-id literal `9_999_999_999_999_999` as `MAX_ID_VALUE`, sibling of `MAX_ID_BYTES`); C2 (delete the 3-line comment block restating the class comment's "fields ARE the value / per-field rule set / fragment the row" rationale verbatim)
Rejected: R1 (split `validate_identity!` into per-field assert helpers — recorded cohesion exception: this is the pinned per-field rule set; extraction is out of scope for a wire codec, method-level only, and error-message bytes are pinned); R2 (privatize/rename `InboundEnvelope.wire_time` — public singleton API surface, untouchable; repo-wide grep incl. send/__send__/public_send shows zero external callers but visibility change is not provable API-safe, and the idiom intentionally mirrors `ApprovalPrompt.wire_time` / `DecisionRecord.wire_time`); R3 (unify nil-guard idioms in `validate_identity!` to one style — style churn, no intent gain); R4 (drive `wire`/`from_wire` field lists off a shared key map — wire shapes are pinned; explicit lists are the point, meta-machinery is not simpler)
Diff stat: 1 file changed, 5 insertions(+), 7 deletions(-)  (net -2 LOC)
Commands: `ruby -c <file>` -> Syntax OK; `rubocop --cache false <file>` -> 0 offenses (BEFORE 0, no increase); `ruby -Itest test/comms_values_test.rb` -> 20 runs, 78 assertions, 0 failures, 0 errors; `ruby -Itest test/public_api_test.rb` -> 3 runs, 1015 assertions, 0 failures, 0 errors; `ruby -Itest test/telegram_normalizer_test.rb` -> 8 runs, 23 assertions, 0 failures, 0 errors. Known-red lanes not touched.
Deviations: none
FLAG (owner decisions): none

## Phase 1 audit (written before any edit)

Criteria walk: (1) names state intent (`validate_identity!`/`validate_command_fields!`/`validate_times!`,
`wire`, `from_wire`, `command?`/`text?`) — only gap is the unnamed id-bound literal, C1;
(2) length — `validate_identity!` ~44 lines exceeds the hard ceiling but is the documented,
in-file-suppressed cohesion exception (per-field rule set), no extraction allowed → stands;
(3) one level of abstraction — clean: initialize → validate! → three validators;
(4) public DSL reads as a typed value: `.new`, `.from_wire`, `#wire`, `#command?`, `#text?`;
(5) step-down already present. §3 vocabulary correct (validate_! bang family, ?-predicates,
no get_/set_). §4 kwargs-only, no boolean params. §5 frozen value object. §11: one duplicated
rationale block, C2.
