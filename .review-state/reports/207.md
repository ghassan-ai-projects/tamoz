# Combined pass — rank 207 gems/tamoz-agent-profile/lib/tamoz/agent/profile/check_spec_validator.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename misleading local `named` → `prefix`, both call sites); Rejected: C2 (dead-by-construction guard in validate_program! — single caller, validate_argv! already guarantees a non-empty String argv.first, but it is defense-in-depth in the strictest validator and its removal is out of the method-level naming/step-down scope); C3 (NUL-byte check looks subsumed by CONTROL_CHARACTER_PATTERN since \x00 ∈ [\x00-\x1f\x7f], but removing it changes the observable refusal bytes from "contains a NUL byte" to "contains a control character" — not behavior-preserving); C4 (extract the dash/directory leaf checks from validate_program! into a third refuse_* helper — adds machinery for no gain, method is 12 lines); C5 (rename refuse_workspace_relative!/refuse_wrapper! to the validate_/enforce_ lexicon — refuse_* is established repo vocabulary: content_scanner/yaml_scanner/authority_validator/secure_file in this gem alone, plus tamoz-agent-healing/scope.rb and tamoz-observability/content_policy.rb; renaming would be cross-file churn against precedent); C6 (unify "#{@path}: unknown check fields …" onto the problem prefix like every sibling refusal — changes pinned message bytes); C7 (normalize modifier-if vs block-if shapes in validate_argv_element! — rubocop-clean, pure style churn)

Diff stat: 1 file changed, 5 insertions(+), 5 deletions(-) (net LOC delta 0)

Commands:
- `ruby -c <file>` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- `ruby -Itest test/agent_profile_test.rb` → 44 runs, 130 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/agent_profile_schema_seams_test.rb` → 11 runs, 33 assertions, 0 failures, 0 errors, 0 skips

Deviations: none

FLAG (owner decisions): the "unknown check fields" refusal names only the profile path, unlike every other refusal in this file which prefixes `problem` ("path: check \"name\""); unifying it would change pinned error bytes, so left as-is.
