# Combined pass — rank 258 gems/tamoz-agent-cli/lib/tamoz/agent/cli_argument_parser.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract `default_options` private reader out of `parse` — step-down; hash literal
moved verbatim, key order and fresh-per-call semantics preserved); C3 (extract the
`--check NAME=COMMAND` handler body into private `register_check(options, entry)` so the option
registry stays one-level-of-abstraction; exception class/message bytes unchanged — the rescue in
`cli.rb#run` is class-based `rescue OptionParser::ParseError`, frame-independent; registration
order untouched so help text stays byte-identical). One mechanical follow-up during apply:
the moved empty-name guard became a modifier `if` (Style/IfUnlessModifier) once it lost a
nesting level — fixed, not suppressed.
Rejected: C2 (rename `grammar`'s block param `value`: the in-file comment records why the param
is named `value`; overriding a recorded rationale for zero functional gain is churn); C4
(extract the `subcommand = @subcommands.include?(argv.first) ? argv.shift : nil` line into a
helper: after C1 `parse` is 5 readable lines; a one-line helper around a guarded shift adds
indirection, not clarity).
Diff stat: 1 file changed, 23 insertions(+), 17 deletions(-) (40 changed lines, under the ~60 cap)
Commands:
- BEFORE baseline: `ruby -c <file>` ok · `rubocop --cache false <file>` = 0 offenses
- AFTER: `ruby -c gems/tamoz-agent-cli/lib/tamoz/agent/cli_argument_parser.rb` → Syntax OK
- AFTER: `rubocop --cache false gems/tamoz-agent-cli/lib/tamoz/agent/cli_argument_parser.rb`
  → 1 file inspected, no offenses detected (0, equal to BEFORE)
- Focused: `ruby -Itest test/agent_cli_test.rb -n "/check_with_empty_name_is_usage_error|
  check_with_empty_command_is_usage_error|duplicate_check_is_usage_error|
  check_configuration_requires_explicit_change_mode|help_output_is_stable|
  version_needs_no_provider_configuration/"` → 6 runs, 27 assertions, 0 failures, 0 errors,
  0 skips (pins the refusal bytes and stable help/version output)
- Full focused file: `ruby -Itest test/agent_cli_test.rb` → 33 runs, 753 assertions,
  0 failures, 0 errors, 0 skips (file is not in any known-red lane)
Deviations: none. No renames of public API (`initialize(out:, subcommands:)` + `parse` untouched);
sole caller remains `gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb:92/97` (repo-wide grep incl.
send/__send__/public_send found no other `.parse(` dispatch site).
FLAG (owner decisions): none
