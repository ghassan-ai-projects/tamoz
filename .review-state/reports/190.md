# Combined pass — rank 190 gems/tamoz-agent-kernel/lib/tamoz/agent/episode_model_call.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (perform_call: inline 4-line usage ternary extracted to private `usage_projection(response)` — the journaled projection hash now reads one key per level; byte-identical value by construction); C2 (`result_for_effect_outcome` → `map_outcome`, aligning the outcome→Result mapping verb with the twin `EpisodeToolCall#map_outcome` per §3.1 one-concept-one-verb)
Rejected: C3 (drop non-Hash fallback in `codec_projection` — rare-case removal, but it IS replay semantics for journals holding a bare value: pinned receipt handling, cannot prove no such journal row exists); C4 (de-duck-type `latest_attempt` respond_to? guards — outcome shapes vary across journal states, cannot personally prove every variant answers `record`/`attempts`; pinned); C5 (`invocation: invocation` / `frame_digest: frame_digest` kwarg shorthand in `build_receipt` — rubocop-clean already, pure style churn); C6 (renames `iso_time` / local `logical` — churn, names clear; `logical` threads through key-identity-adjacent signatures for zero gain); C7 (trim class header comment — every paragraph is a §11 why-comment pinning P0B/B4 invariants; keep); C8 (extract usage decoding out of `build_receipt` — the method is one flat declarative ModelReceipt kwarg list; §4 forbids adding indirection merely for ceiling headroom; pinned path)
Diff stat: gems/tamoz-agent-kernel/lib/tamoz/agent/episode_model_call.rb | 20 +++++++++------- (13 insertions(+), 7 deletions(-)) — net +6 lines, 183 → 189
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> BEFORE = 0 offenses / AFTER = 0 offenses (no increase) · repo-wide grep old/new method names incl. send/__send__/public_send surfaces → zero external/dynamic references to renamed private · ruby -Itest test/agent_model_receipt_test.rb → 15 runs, 49 assertions, 0 failures, 0 errors (pins ModelReceipt/LogicalCallKey contracts this class consumes)
Focused-test lane note: every repo test constructing EpisodeModelCall is a stream_*_test.rb or drives operation "episode.model.reason" (stream_episode_replay/crash_matrix) — ALL declared known-red/do-not-chase by the brief, so none were run; no non-lane test touches this file (stated explicitly per gate 3).
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE candidates (recorded before any edit)

BEFORE gates: ruby -c OK · rubocop --cache false = 0 offenses · branch verification-again, owned file unmodified.

Scope pins honored (brief + orchestrator rails): EffectDispatcher dispatch shape, logical-key
identity (`LogicalCallKey` construction + `to_key`), receipt/projection handling, and what counts
as a recorded model call are PINNED — candidates below are method-level naming/step-down only;
no extraction across methods' responsibilities, no semantic change to journalled values.

- C1 `perform_call` (lines 112–126): the `"usage"` value is a 4-line ternary embedding a nested
  hash literal inside the projection hash — the one spot mixing abstraction levels in an otherwise
  uniform key list. Extract `usage_projection(response)`. Journaled bytes identical (same
  expressions, same keys/values). APPLY.
- C2 `result_for_effect_outcome` (line 85): same concept, second spelling — the twin class
  `gems/tamoz-agent-kernel/lib/tamoz/agent/episode_tool_call.rb:118` names the identical
  outcome→Result step `map_outcome`. §3.1: one concept, one verb, repository-wide. Private method,
  single in-file caller (line 57); repo-wide grep (incl. worktrees excluded) shows zero external or
  dynamic references to the old name — only the sibling's own independent definition. APPLY.
- C3–C8: see Rejected above, each with reason.

Test coverage note: every repo test constructing `EpisodeModelCall` is a `stream_*_test.rb`
(fixed_graph, real_model, crash_matrix, via test/support/episode_composition.rb) and
stream_episode_replay/crash_matrix reference operation "episode.model.reason" — ALL of these are
declared known-red/do-not-chase lanes in the brief. No non-lane test touches this file; focused-test
gate will be satisfied by stating this explicitly plus the load-level checks (ruby -c, rubocop).
