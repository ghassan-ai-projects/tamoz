# Combined pass — rank 248 gems/tamoz-agent/lib/tamoz/agent/episode_graph.rb
Verdict: EXEMPT
Applied: (none) | Rejected: C1 (extract step-down helpers out of `self.build`'s ~93-line body — the definition block is evaluated via `builder.instance_eval(&block)` (gems/tamoz-graph/lib/tamoz/graph/builder.rb:14), so inside it `self` is the Builder, not EpisodeGraph; any helper must take the builder as an explicit parameter, adding parameter-threading indirection that hides the PINNED node registration order behind private methods for zero behavioral gain — §6 machinery-for-ceiling-headroom, same reasoning as rank 190's C8 rejection); C2 (collapse the 11 uniform `node(...)` registrations into a data-driven loop over a name→version table dispatching via `nodes.public_send` — replaces statically greppable wiring with reflection, makes registration order implicit in a hash literal against a rail-pinned order, needs a version table because reason/validate are "2" while the rest are "1", and rewrites identity-bearing implementation_name literals as interpolation); C3 (`"#{GRAPH_NAME}."` interpolation for the eleven implementation_name literals — churn on strings that feed NodeSpec identity; obscures exact registered names from repo-wide grep); C4 (trim P5/P6 comment labels and the header ASCII diagram — the diagram mirrors the declared topology and the channel comments pin real §11 why-invariants: situation_memory written ONCE by recall, route_kind determinism, loop bounded by Limits#max_steps; stripping labels is style churn); C5 (`state.fetch(:route)&.to_sym` nil-safety in both branch routers — behavior change on the router return value, and nil cannot occur by construction: intake writes :route and validate writes :next_node before their branches fire; rare-case code is forbidden by owner directive); C6 (drop per-node `version: "1"` in favor of the builder's nil default — cannot prove NodeSpec/compiler treat nil identically to "1" in canonical digests/checkpoint compatibility; identity seam)
Diff stat: no diff — owned file byte-identical to HEAD (git diff empty, net LOC delta 0)
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no edits made) · `ruby -Itest test/stream_episode_fixed_graph_test.rb` → 6 runs, 25 assertions, 0 failures, 0 errors, 0 skips (exercises EpisodeGraph.build end-to-end through test/support/episode_composition.rb)
Deviations: none of substance. Coverage note per gate 3: the only repo references to EpisodeGraph.build are episode_composition.rb plus stream_episode_fixed_graph/real_model/crash_matrix tests — ALL stream_* lanes are declared known-red/do-not-chase by the brief, so no non-lane test touches this file; stated explicitly, with one lane member run once anyway as build evidence (no A/B needed since the file was not edited).
FLAG (owner decisions): none

## Phase 1 — ANALYZE candidates (recorded before any edit)

BEFORE gates: ruby -c OK · rubocop --cache false = 0 offenses · branch verification-again,
owned file unmodified.

EXEMPT classification: the file is mostly declarative data. Of its 133 lines, ~92 are builder
declarations inside the single public entry point — 20 `state` channel declarations, 11 node
registrations, 12 edges, 2 branch blocks. The only imperative content is eleven one-line
delegations (`nodes.intake(state, context)` …) and two two-line branch routers. There is no
algorithm to audit for length or abstraction mixing beyond the flat registry itself; names
comply with §3 (intake/recall/build_frame/reason/validate/execute_tool/rebuild_frame/repair/
decide/judge/compensate; branches read as route_kind / validate_route), the entry point reads
as a small DSL (`EpisodeGraph.build(checkpointer:, nodes:)`), and every line sits at exactly one
abstraction level: graph-declaration statements.

Scope pins honored (brief + orchestrator rails): node registration order and the twin
map_outcome seam are pinned; episode_tool_call CANDIDATES-ONLY precedent stands — method-level
naming/step-down only, no restructuring of the wiring.

- C1 `self.build` length (~93 physical lines vs ≤20/≤30): the sole surface finding. REJECTED —
  see header. The body is the graph, literally; the owner's rails pin its shape.
- C2 data-driven node loop: REJECTED — reflection over pinned explicit registrations trades
  static clarity for line count; a transcription slip silently reorders or re-versions nodes
  whose implementation_name/version feed NodeSpec and the compiled definition.
- C3 GRAPH_NAME interpolation: REJECTED — churn on identity-bearing literals.
- C4 comment trims: REJECTED — §11 why-content, not history narrative.
- C5 router nil-safety: REJECTED — behavior change + impossible-by-construction case.
- C6 defaulting node versions: REJECTED — unprovable equivalence on the identity seam.

Test coverage note: unchanged from Commands above — no non-do-not-chase test constructs this
class; the fixed-graph lane run confirms compile+build health on the untouched HEAD state.
