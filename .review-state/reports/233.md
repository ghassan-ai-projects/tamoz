# Combined pass — rank 233 gems/tamoz-agent-kernel/lib/tamoz/agent/receipt_budget_controller.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename four private `check_*!` ceiling checks to §3.1 `enforce_*_budget!` vocabulary)  |  Rejected: C2 (extraction beyond method-level scope; indirection over pinned message path), C3 (table-driven enforcer hides wire-key↔state-key mapping; drift risk), C4 (two-line extraction = churn), C5 (`Integer()` vs `.to_i` is NOT behavior-preserving), C6 (error-class placement move = churn)
Diff stat: `1 file changed, 8 insertions(+), 8 deletions(-)` — pure in-place renames, net 0 LOC
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses before, 0 after · `ruby -Itest test/agent_budget_test.rb` → 7 runs, 20 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE (written before any edit)

Ground truth recorded BEFORE any edit:
- Branch `verification-again`; owned file has no uncommitted diff; last touch `1f70145 "Refactor receipt budget reconciliation"`.
- `ruby -c` → Syntax OK. `rubocop --cache false` BEFORE → **0 offenses**.
- Public surface pinned by callers in `gems/tamoz-agent-kernel/lib/tamoz/agent/episode_nodes.rb`
  (`check_model_call!` L333, `check_tool_call!` L396, `reconcile_model` L349, `reconcile_tool` L409,
  `.new(wire budget)` L580) — untouchable per brief.
- Repo-wide grep for the four private helper names + `add_available_usage`: matches ONLY inside the
  owned file. No dynamic dispatch risk: any `send`/`__send__`/`public_send` would still carry the
  literal name as an argument/symbol and the repo-wide grep over all file types found none.

Candidates:

- **C1 — APPLY.** Rename private `check_input_tokens!`, `check_output_tokens!`, `check_cost!`,
  `check_tool_bytes!` → `enforce_input_token_budget!`, `enforce_output_token_budget!`,
  `enforce_cost_budget!`, `enforce_tool_result_byte_budget!`. §3.1's checking table gives
  `enforce_*` = "policy applies and may refuse the operation" — exactly these four (budget policy,
  refusal = typed raise), while `check_` is not in the lexicon at all: a second spelling for an
  existing concept. Names now mirror their own error messages ("input token budget exhausted" …).
  The house verb already exists in this gem (`episode_nodes.rb#enforce_successful_outcome!`). Private,
  in-file callers only → behavior-preserving by construction. 9 lines touched.
- **C2 — REJECT.** Dedup `check_model_call!`/`check_tool_call!` into one parameterized counter-check
  helper. Extraction beyond this pass's scope (binding note: method-level naming/step-down only);
  the two public checks are the readable DSL face of the class, and parameterizing key+label adds
  indirection over the pinned exhaustion-message path to save ~8 lines.
- **C3 — REJECT.** Unify the four ceiling checks into one table-driven enforcer keyed by
  {budget_key, state_key, message}. Same scope reason as C2; hides which wire budget key guards
  which state counter behind data indirection and risks message drift against pinned semantics.
- **C4 — REJECT.** Extract the two-line result-bytes accumulation in `reconcile_tool` into
  `add_result_bytes(base, projection)` for symmetry with `add_available_usage`. Two-line extraction
  is style churn; the inline form reads directly.
- **C5 — REJECT.** Normalize `Integer(projection.fetch("result_bytes", 0))` (L80) to the `.to_i`
  idiom used elsewhere. NOT behavior-preserving: `Integer()` raises on malformed input, `.to_i`
  coerces — that error surface is pinned budget semantics.
- **C6 — REJECT (no-action).** Reading order: `StreamBudgetExceededError` is defined after the
  controller that raises it. Legal Ruby, constant resolves at call time; moving it is churn with
  zero gain.

Step-down / abstraction check: `reconcile_model`/`reconcile_tool` read public-intent → accumulate →
enforce, each private step one level down; private order matches call order. No ≤20/30-line
violations (longest method is 10 lines). No boolean params, no kwargs issues, predicates end `?`.
The long class doc comment is a pinned semantic contract (P2/§8.3 receipt-projection invariant),
not narrative — left alone per §11.

## Phase 2 — APPLY

Applied C1 only. 8 lines renamed in place (4 definitions + 4 call sites); no signature, message,
ordering, or semantics change. Post-edit repo-wide grep for the four old names: zero matches.

Test coverage note: no test names `ReceiptBudgetController`/`StreamBudgetExceededError` directly;
the focused gate `test/agent_budget_test.rb` reaches the controller through the real worker runtime
(`episode_nodes.rb#reason`/`execute_tool` → `check_*!`/`reconcile_*`). The other budget-exercising
suites (`stream_episode_loop_test.rb`, `stream_episode_stream_test.rb`) are in the brief's known-red
do-not-chase lane and were not used as gates.
