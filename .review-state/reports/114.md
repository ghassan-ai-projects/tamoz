# Combined pass — rank 114 gems/tamoz-agent-cli/lib/tamoz/agent/cli_rendering.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (deduped the four projection fields shared by the `cli.session` event payload and the show document into one private `projection_fields(view)`, splatted at identical key positions — JSON key order and rendered bytes unchanged); C2 (step-down of `render_verification`: satisfied / not-satisfied tails extracted to `render_satisfied_verification` / `render_unsatisfied_verification`, print order byte-identical; all three Metrics disables — AbcSize/CyclomaticComplexity/PerceivedComplexity — removed and rubocop stays clean); C3 (renamed private `show_document` → `build_show_document`, §3 build_ vocabulary: it constructs a value, prints nothing; sole caller L139 in this file, repo-wide grep incl. send/__send__/public_send found no other reference; two comment mentions updated)
Rejected: renaming `list_entry`/`read_list_entry`/`render_final_view` etc. (callers live in non-owned files cli.rb / cli_session_commands.rb or reached via send in tests); extracting the failure-message conditional from `render_final_view_human` (churn, no gain over the inline if); rewriting `list_entry`'s begin/ensure/rescue into method-level rescue+ensure (shifts ensure semantics onto the early return; the narrowed-rescue comment documents a real failure model); splitting `render_show_header` (reads exactly like its printed output; covered by the module's recorded TooManyStatements renderer exception)
Diff stat: 1 file changed, 32 insertions(+), 23 deletions(-)
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` BEFORE 0 offenses → AFTER 0 offenses (disables removed, none re-added) · `ruby -Itest test/agent_cli_test.rb` → 33 runs, 753 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/agent_terminal_progress_test.rb` → 4 runs, 19 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/agent_session_operations_test.rb` → 6 runs, 36 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 candidates (written before editing)

- C1 DEDUPE: the four projection fields (`progress`, `terminal`, `status_projection`,
  `lifecycle_events`) are built identically in two places — the `cli.session` event payload in
  `render_final_view` (was L66–69) and `show_document` (was L158–161). Extract one private builder and
  splat it into both hashes at the same key position; JSON key order is preserved, so rendered
  bytes are unchanged. Directly serves this module's stated "two audiences, one source of truth".
- C2 STEP-DOWN: `render_verification` (19-line body) carries AbcSize/CyclomaticComplexity/
  PerceivedComplexity disables. Split the satisfied / not-satisfied tails into named helpers;
  output order byte-identical. Keep only if all three disables can then be removed (verified by
  rubocop run); otherwise revert C2 entirely. → Held: rubocop clean without them.
- C3 NAMING: rename private `show_document` → `build_show_document` (§3: it constructs a value,
  it prints nothing; contrast sibling `render_show`). Sole caller is L139 in this same file —
  repo-wide grep incl. send/__send__/public_send found no other reference. Update the two
  comment mentions inside this file.
- Rejected: renaming `list_entry`/`read_list_entry`/`render_final_view` etc. — their callers live
  in non-owned files (cli.rb, cli_session_commands.rb) or tests (`send(:render_verification)`).
- Rejected: extracting the failure-message conditional out of `render_final_view_human` — churn,
  no readability gain over the inline if.
- Rejected: rewriting `list_entry`'s begin/ensure/rescue to method-level rescue+ensure — shifts
  ensure semantics (ensure would run on the early return too), and the narrowed-rescue comment
  documents a real failure model. Leave as is.
- Rejected: splitting `render_show_header` — it reads exactly like its printed output; covered by
  the module's recorded TooManyStatements renderer exception.
