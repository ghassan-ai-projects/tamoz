# Combined pass — rank 241 gems/tamoz-telegram/lib/tamoz/telegram/client.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (drop dead `_method` param from private `transport_failure`, 3 in-file call sites updated — grep proves zero refs outside this file); C2 (step-down: extract `build_request(method, params)` so `call` reads build http → build request → stream → dispatch, statements inside moved verbatim)
Rejected: C3 (dedup `JSON.parse(body, create_additions: false)` behind a parse_body helper — three sites have distinct surrounding semantics: ok-validation vs rescued fallback vs dig-with-default; a helper hides parsing points inside the pinned boundary and removes no real duplication); C4 (rename public `call` — public/cross-gem API, untouchable); C5 (split `call`'s status dispatch into helpers — class doc records the deliberate "one bounded HTTP call" design, transport/retry ordering pinned by binding note; splitting also would not clear the pre-existing complexity offenses without reshuffling branches); C6 (`|| 1` fallback in retry_after_from → named constant — single site, method name already states intent; churn)
Diff stat: 1 file changed, 12 insertions(+), 7 deletions(-)
Commands: `ruby -c gems/tamoz-telegram/lib/tamoz/telegram/client.rb` → Syntax OK; `rubocop --cache false gems/tamoz-telegram/lib/tamoz/telegram/client.rb` → 2 offenses, identical to the recorded BEFORE count (pre-existing CyclomaticComplexity 11/8 + PerceivedComplexity 10/8 on `call`), nothing new introduced; `ruby -Itest test/tamoz_telegram_transport_test.rb` → 21 runs, 45 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 audit notes (written before any edit)

BEFORE state: branch verification-again, file clean; `ruby -c` OK; rubocop --cache false = 2 offenses,
both pre-existing on `call` (Metrics/CyclomaticComplexity 11/8, Metrics/PerceivedComplexity 10/8).
Audit criteria walked: (1) names — `build_http`, `path_for`, `conflict_message`, `retry_after_from`
read as intent; public `call` is generic but is the cross-gem entry point (untouchable);
(2) length — `call` exceeds ceilings by design, documented inline (:reek: + rubocop-disable comments
name it as one HTTP boundary); (3) abstraction level — wire-format construction (Post.new, body,
Content-Type) sits one level below `call`'s stream/dispatch narrative; (4) public DSL — `new` +
`call(method, params, idempotent:)` plus attr_readers is a small coherent surface;
(5) step-down — only violation is (3). §4 boolean-kwargs: private `transport_failure` takes a
positional boolean but converting to kwargs is signature-style churn beyond the binding's
"naming/step-down only"; noted, not applied.
Proof for C1: repo-wide grep `transport_failure|build_http|path_for` — client.rb internal only
(mcp `record_transport_failure` and sse `build_http` are unrelated methods on other classes);
no send/__send__/public_send hits.
