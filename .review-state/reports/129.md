# Combined pass — rank 129 gems/tamoz-observability/lib/tamoz/observability/content_policy.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename shadowing locals/param `max_bytes` → `limit` in `normalize_option` / `validate_max_bytes!`, matching `attach_capture!`'s existing `limit`; removes shadowing of the private reader `max_bytes(content_class)`; error-message literal untouched)
Rejected: C2 (dedup `"sha256:"+hexdigest` idiom shared by `compute_policy_digest`/`content_digest` into a helper — two one-line sites on a byte-pinned digest path; extraction adds indirection for zero behavior gain); C3 (strip `!` from raisers `refuse_restricted_capture!`/`validate_max_bytes!`/`guard_value!` — repo-wide dialect uses `!` on guard methods, e.g. child_task.rb `enforce_parent_bounds!`, mcp_capability_source.rb `assert_depth!`; renaming makes this file the outlier); C4 (rename `serialize_canonical` per §3.1 "serialize_* not used here" — sibling metrics.rb pins `serialize_series`/`serialize_histograms`, so a single-file rename widens the split; gem-wide → FLAG); C5 (`to_h` block → `transform_values(&:dup)` — pure style churn); C6 (drop defensive `.to_s` in `truncate_to_limit` — byte-pinned truncation path, benefit nil); C7 (rewrite dense `repair_utf8_tail` modifier-while — correct, single-purpose, multi-byte tail semantics pinned); C8 (interpolate MAX_CONTENT_ENTRIES into hash-limit error message like the array branch does — error/message bytes pinned); C9 (rename `parse_enabled`/`parse_max_bytes` — they interpret constructor-option shapes, not a render_/parse_ wire pair; churn)
Diff stat: 1 file changed, 5 insertions(+), 5 deletions(-) (content_policy.rb only; error-message literal and pinned `:max_bytes` output key untouched)
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses BEFORE, 0 AFTER (no increase) · `ruby -Itest test/observability_runtime_test.rb` → 12 runs, 49 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): §3.1 names `serialize_*` as a banned third spelling, yet tamoz-observability uses it in two files (metrics.rb `serialize_series`/`serialize_histograms`, content_policy.rb `serialize_canonical`) for outward-form generation. Unifying is a gem-wide naming decision spanning files outside this pass's ownership.
