# Combined pass — rank 148 gems/tamoz-evals/lib/tamoz/evals/benchmark/report.rb

## Phase 1 — ANALYZE (candidates, written before any edit)

BEFORE gates: `ruby -c` OK; `rubocop --cache false` = **0 offenses** (file carries TODO debt
exclusions for Metrics/{AbcSize,CyclomaticComplexity,MethodLength,ParameterLists,
PerceivedComplexity}, Performance/MapCompact, Style/MultipleComparison, Style/StringLiterals{,InInterpolation},
Layout/SpaceInsideHashLiteralBraces in .rubocop_todo.yml — untouchable per brief).

- C1 (APPLY): step-down — `build` is ~31 code lines (over both ceilings) because it mixes
  orchestration with the 9-line `Comparison.new.paired(...)` setup. Extract that call verbatim
  into private `compute_comparison(strongest)` (extends the file's own `compute_metrics` verb;
  no new vocabulary). Report hash literal, its insertion order, and `content_digest` inputs are
  untouched → shape/digest pinned by construction. Evaluation order preserved; nil-`strongest`
  still raises the same NoMethodError at the same point.
- C2 (REJECT): loop the four repeated `kwargs[:k] = family[k] if accepted.include?(k)` lines in
  `run_baseline`. Marginal gain; replaces four named protocol fields with dynamic `key.to_s`
  indirection directly under a comment that explains the explicit convention. Churn.
- C3 (REJECT): inline single-use temp `names = @protocol.fetch("baselines")` in `run_baselines`.
  Zero readability gain; pure churn.
- C4 (REJECT): normalize stray single-quoted `'readiness'` literals (build tail, content_digest).
  Covered by recorded Style/StringLiterals debt for this whole file; fixing two spots changes no
  gate count and no bytes of behavior. Style churn on a pinned-shape file.
- C5 (REJECT): `.map { }.compact` → `filter_map` in `codes_from`. Behavior-identical micro-idiom;
  MapCompact debt entry for this file cannot be removed from the forbidden TODO, so the change is
  gate-invisible. Neither naming nor step-down.
- C6 (REJECT): `Report.build` class-method kwarg list duplicates `initialize`'s. Standard
  value-object factory; public/cross-gem API is untouchable; ParameterLists debt is recorded.

Verdict plan: COMMITTED-CANDIDATES (C1 only) unless a gate disagrees → revert to CLEAN/CANDIDATES-ONLY.

## Phase 2 — APPLY

Applied C1 only. The `Comparison.new.paired(...)` argument list moved verbatim into private
`compute_comparison(strongest)`; `build` now reads as orchestration + the pinned report shape.
Report hash literal, insertion order, `content_digest` inputs untouched → report shape/digest
bytes identical by construction. Evaluation order preserved (`strongest` before comparison,
violations after); nil-`strongest` raises the same NoMethodError at the same point.

Diff stat:
```
 .../lib/tamoz/evals/benchmark/report.rb | 22 +++++++++++++---------
 1 file changed, 13 insertions(+), 9 deletions(-)
```

Commands:
- `ruby -c gems/tamoz-evals/lib/tamoz/evals/benchmark/report.rb` → Syntax OK (before + after)
- `rubocop --cache false <file>` → 0 offenses BEFORE / 0 AFTER (no increase; nothing to fix)
- `ruby -Itest test/benchmark_harness_test.rb` → 25 runs, 67 assertions, 0 failures, 0 errors
- `ruby -Itest test/benchmark_controls_test.rb` → 14 runs, 42 assertions, 0 failures, 0 errors
- `ruby -Itest test/openclaw_benchmark_readiness_test.rb` → 10 runs, 41 assertions, 0 failures, 0 errors

Deviations: none. Net +4 lines (22 changed, under the ~60 cap). No renames, no public-API change,
no comment changes; `.rubocop_todo.yml` untouched (forbidden).

FLAG (owner decisions): none for this file. Noted for the quality program, not actionable here:
the file carries whole-file TODO exclusions (MethodLength, AbcSize, PerceivedComplexity,
StringLiterals, MapCompact, …) that a future slice could retire once `build` is this small —
removing those entries is outside this brief's ownership.

# Combined pass — rank 148 gems/tamoz-evals/lib/tamoz/evals/benchmark/report.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract verbatim paired-comparison setup into private compute_comparison(strongest) — step-down; build now orchestration + pinned shape); Rejected: C2 (kwargs loop = churn under explicit convention), C3 (inline single-use temp = churn), C4 (quote normalization = recorded StringLiterals debt, gate-invisible churn), C5 (filter_map micro-idiom = gate-invisible, MapCompact debt untouchable), C6 (factory kwarg duplication = public API, untouchable)
Diff stat: 1 file changed, 13 insertions(+), 9 deletions(-)
Commands: ruby -c OK · rubocop --cache false 0→0 offenses · benchmark_harness_test 25/67 ✓ · benchmark_controls_test 14/42 ✓ · openclaw_benchmark_readiness_test 10/41 ✓
Deviations: none
FLAG (owner decisions): none
