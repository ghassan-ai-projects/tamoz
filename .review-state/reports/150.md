# Combined pass — rank 150 gems/tamoz-concurrency/lib/tamoz/stream_sink.rb

Verdict: COMMITTED-CANDIDATES

## Candidates (written before editing)

- C1 [APPLY] Step-down on `initialize`: extract the four constructor validations verbatim into
  private `validate_capacity!`, `validate_cancellation_token!`, `validate_clock!`,
  `validate_max_namespaces!` — the established seam one file over (`pool.rb` lines 171–195,
  same gem, same `return if … / raise ConfigurationError` shape). Validation order and error
  message bytes preserved exactly; runs single-threaded before any shared state exists, so no
  ordering/threading impact. Fixes the one hard-cap violation in the file (initialize body ≈31
  lines) without touching lock structure.
- C2 [APPLY] Drop redundant `.freeze` on `"cancelled"` in `close_from_cancellation` — file has
  `# frozen_string_literal: true`, so the literal is already frozen; the suffix falsely implies
  other literals are unfrozen. No rescue-semantics change.
- C3 [REJECT] Unify `emit`'s fallback asymmetry (`run_id || default` vs `task_id.nil? ? …`)
  — the two differ observably for `false`; unifying changes behavior and I cannot prove the
  asymmetry is unintended. Not mine to decide.
- C4 [REJECT] Reorder `emit` to reserve the sequence before building the part (removing the
  `candidate`/`with(sequence:)` two-step) — moves `@clock.now` relative to the
  `@state_mutex` acquisition inside `reserve_sequence!`; emitted_at ordering is pinned.
- C5 [REJECT] Merge the outer `finished?` early-return in `close` with the inner `@finished`
  check under the mutex — outer check avoids normalizing the reason after finish; it is a
  deliberate fast-path over pinned locking.
- C6 [REJECT] Shared integer-bound validator parameterized over value/max/message for
  capacity + max_namespaces — two call sites, adds indirection machinery, risks message drift;
  rule of three not met (owner directive: simple over complicated).
- C7 [REJECT] Rename `changed` locals in `finish`/`close`, or restructure `each`'s
  `naturally_completed` flag — churn inside pinned mutex blocks with no intent gain.

Applied: C1 (initialize step-down to pool.rb-style validators); C2 (redundant freeze removal)
Rejected: C3 (behavioral); C4/C5 (pinned concurrency ordering); C6 (over-machinery); C7 (churn)

Diff stat: 1 file changed, 33 insertions(+), 15 deletions(-) (net +18, under the ~60-line rail)
Commands:
- `ruby -c gems/tamoz-concurrency/lib/tamoz/stream_sink.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- `ruby -Itest test/core_stream_test.rb` → 12 runs, 46 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips
- Behavior proof: HEAD version swapped in place vs edited version over a 5-case validation
  script (each invalid arg + precedence case `capacity: "x", cancellation: Object.new`) —
  class+message output byte-identical; scratch files removed, working tree holds only this edit.
Deviations: none. No rescue sites touched; emit/each/close/finish lock structure and ordering
untouched; public API (initialize kwargs, attr_readers, method set) unchanged.
FLAG (owner decisions): C3 — `emit(run_id:)` falls back to the default on `false`
(`run_id || @default_run_id`) but `emit(task_id:)` does not (`task_id.nil? ? … : task_id`);
if unintentional it is a behavior fix, out of scope for this pass.
