# Combined pass — rank 230 gems/tamoz-core/lib/tamoz/core/capability/registry.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down: extracted `enforce_built_in_sources!` + `enforce_descriptor_ownership!` from `self.build`, bodies verbatim); C5 (`|s|` → `|source|` in the moved shape check); C2 (`built_in_prefix?` triple clause collapsed to one prefix iteration with `!= prefix`, equivalence proven)
Rejected: C3 (error-precedence change on doubly-invalid input — pinned semantics); C4 (style churn only); C6 (already minimal); C7 (adds state machinery for a ≤4-element scan)
Diff stat: gems/tamoz-core/lib/tamoz/core/capability/registry.rb | 72 ++++++++++++---------- (40 insertions, 32 deletions; net +8 lines)
Commands:
- BEFORE/AFTER `rubocop --cache false <file>` → 0 offenses / 0 offenses (no increase)
- `ruby -c <file>` → Syntax OK
- `ruby -Itest test/capability_registry_test.rb` → 9 runs, 28 assertions, 0 failures, 0 errors
- `ruby -Itest test/capability_closed_world_test.rb` → 3 runs, 14 assertions, 0 failures, 0 errors
- `ruby -Itest test/capability_host_test.rb` → 21 runs, 91 assertions, 0 failures, 0 errors
- `ruby -Itest test/agent_phase4_capability_test.rb` → 7 runs, 31 assertions, 0 failures, 0 errors
Deviations: gross changed-line count is 72 (> ~60 rail) while net is +8; the excess is verbatim relocation of the two pinned gate blocks into the private helpers (same checks, same order, byte-identical error strings), not new logic. Applied anyway because every gate is green and each candidate was proven behavior-preserving; a plain `git checkout` of the file reverts it wholesale if the orchestrator reads the rail as gross churn.
FLAG (owner decisions): none beyond the Deviations note above. Registration semantics untouched: sealed `register`, private `new`, closed-world gates, admission intersection, and all error bytes are exactly as before.

## Phase 1 — ANALYZE

BEFORE state: `rubocop --cache false` → 0 offenses. File is 142 lines; one public
constructor (`Registry.build`), four instance readers/predicates, private `new`
(sealed), five private class helpers. Callers proven by repo-wide grep:
`tamoz-tools/capability_host.rb`, `tamoz-agent-capabilities/capability_binding.rb`
(`descriptors`, `declared_descriptors`, `admitted?`, `source_for`),
`test/capability_registry_test.rb`, `test/capability_closed_world_test.rb`,
`test/capability_host_test.rb` (`build`, `register` refusal, `new` refused).
Registration semantics are PINNED per binding note — closed-world gates, error
bytes, and ordering stay untouched.

Candidates:

- C1 — `self.build` is ~27 code lines (> 20 normal ceiling) and mixes two
  refusal gates with assembly. Step-down: extract the two validation loops into
  private `enforce_built_in_sources!` and `enforce_descriptor_ownership!`
  (`enforce_*` = policy refusal per §3.1, matching `enforce_parent_capabilities!`
  in tamoz-agent-capabilities). Bodies move verbatim (same checks, same order,
  byte-identical errors); invariant comments move with them. APPLY.
- C2 — `built_in_prefix?` repeats the `start_with?(p) && length > p.length`
  trick three times. Collapse to one iteration over the literal prefixes with
  `source_id != prefix`. Equivalence proof: given `start_with?(p)`,
  `id.length > p.length ⟺ id != p`; prefix set {skill:, mcp:, websearch:} and
  bare "local"/"websearch" branches unchanged; "local:x" still rejected
  (local is not in the prefixed list). APPLY.
- C3 — merge the ownership loop into `build_registry` to avoid the second
  nested iteration. REJECTED: interleaving would change which error surfaces
  for input violating both ownership and id-collision — error precedence is
  pinned registration semantics.
- C4 — merge the two adjacent `class << self` blocks into one. REJECTED:
  pure style churn; the split isolates the `private :new` pin with its
  rationale comment. No functional gain.
- C5 — rename block param `|s|` → `|source|` in the moved shape check.
  Folded into C1's verbatim move.
- C6 — unify `normalize_admission` Hash/Array branches. REJECTED: already
  minimal; the case reads clearly.
- C7 — store a source-by-id map so `source_for` avoids the linear scan.
  REJECTED: adds a Data field for a ≤4-element scan — more machinery, not
  simpler (owner directive).

Planned net delta: ≈ +10 lines (two def/end wrappers + moved comments), well
under the 60-line stop rail.

## Phase 2 — APPLY

Applied C1+C5 and C2 as planned; all gates green. Verdict above.
