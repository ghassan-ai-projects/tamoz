# Fix report — rank 40 gems/tamoz-mcp/lib/tamoz/mcp/server_config.rb
Applied: C1 (`validate_endpoint_security!` triple-negative compound → three sequential guards, raise condition and message bytes verified identical over an 11-case truth table incl. loopback/IPv6/private-IP/opt-in/public combinations); C2 (`validate_process_surface!` 6 positional params → required keywords; grep-proved sole caller at line 139 in-file, call site uses identically named locals so body/order/messages unchanged); C3 (inline budgets type-check extracted to `validate_budgets!` beside the other validators, byte-identical message "budgets must be a Tamoz::Mcp::ServerConfig::Budgets"; `initialize` now a uniform validator pipeline); C4 (`describe` budgets 10-line hand-synced literal → `Budgets.members.to_h { |name| [name.to_s, budgets.public_send(name)] }`, key order == members order verified, values/state byte-identical); C5 (`MAX_HEADER_VALUE_BYTES = 4096` constant next to `MAX_ARGUMENT_BYTES`, same number same behavior)  |  Rejected: none
Diff stat: gems/tamoz-mcp/lib/tamoz/mcp/server_config.rb | 34 +++++++++++++-------------- 1 file changed, 16 insertions(+), 18 deletions(-)
Commands: `ruby -c` owned file → Syntax OK · `rubocop --cache false` BEFORE → 0 offenses · `rubocop --cache false` AFTER → 0 offenses (count not increased, nothing suppressed) · `ruby -Itest test/mcp_server_config_test.rb` → 39 runs, 321 assertions, 0 failures/errors · `ruby -Itest test/mcp_http_test.rb` → 2 runs, 11 assertions, 0 failures · `ruby -Itest test/mcp_elicitation_test.rb` → 19 runs, 71 assertions, 0 failures (covers the `#describe` render path C4 touched) · `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures (public surface incl. frozen `credential_env_name?`) · ad-hoc equivalence probe under test_helper's load path → 11-case endpoint-security truth table identical with exact message bytes; budgets preview keys/values/frozen-state identical; C3 message byte-identical
Deviations: C4 applied WITHOUT the candidate's proposed inner `.freeze` on the generated budgets hash — every sibling container in `describe` (`env_allowlist.dup`, `protocol_range.dup`, `primitives.map`) is handed out unfrozen, and freezing would be an observable object-state change on the pinned operator-preview surface; outer `.freeze` kept as committed. Notes for orchestrator: (1) probing surfaced a PRE-EXISTING Ruby `Data.new` quirk — calling `ServerConfig.new(dynamic_hash)` instead of literal kwargs raises "missing keyword: :transport"; it predates this diff (constructor signature untouched), all real callers and tests use literal kwargs and pass, no action taken per no-rare-cases rule. (2) Working tree also holds concurrent edits by other agents (docs/README.md, session_adaptive.rb, session_context_controls.rb, scoreboard.rb, an evals runner); my footprint is ONLY the owned file. (3) Both FLAG items respected: `credential_env_name?` untouched; https-message bytes exactly as committed ("endpoint must use https unless it targets loopback" asserted verbatim in the probe).
