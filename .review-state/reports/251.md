# Combined pass — rank 251 gems/tamoz-graph/lib/tamoz/graph/execution_support.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (open_writer: local `actual_ttl` → `resolved_ttl`, §3.1 resolve_ vocabulary states the provenance chain argument → checkpointer policy → DEFAULT_WRITER_TTL)
Rejected: C2 (compatible_latest! as `writer&.latest || checkpointer.latest` — BEHAVIOR CHANGE: a truthy writer whose latest is nil must raise CheckpointConflictError 'thread does not exist'; `||` silently falls back to the checkpointer. Ternary form rejected as pure churn); C3 (extract shared writer-accepts predicate from bind_writer_context — executor-family precedent pins method-level only; adds indirection to an 11-line flat method); C4 (validate_concurrency! via `value.to_sym` — behavior change: nil/non-symbolizable raises NoMethodError instead of ConfigurationError bytes); C5 (hoist `%i[inline threads]`/`%w[inline threads]` to frozen constants — micro-gain on cold path, churn); C6 (vocabulary renames `ensure_ephemeral_public!`→assert-family, `stream_error_data`→build_ — all methods are re-exported by Compiled's endless forwarders compiled.rb:237–247 and consumed via __send__ in request_staleness, writer_run_executor, fork_executor, lifecycle_executor, state_operations, durable_request_executor, run_coordinator; rename = multi-file + public API touch); C7 (flip build_context guard order — no gain); C8 (`error.respond_to?(:original) && error.original || error` one-liner in stream_error_data — precedence-obscure; if/else identical and clearer)

Diff stat: `gems/tamoz-graph/lib/tamoz/graph/execution_support.rb | 12 ++++++------` (6 insertions, 6 deletions; net 0; confined to open_writer)
Commands: ruby -c → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE baseline also 0, no increase) · ruby -Itest test/sqlite_stale_request_test.rb → 26 runs, 176 assertions, 0 failures, 0 errors · ruby -Itest test/graph_durable_runner_test.rb → 19 runs, 39 assertions, 0 failures, 0 errors · ruby -Itest test/graph_stream_test.rb → 7 runs, 70 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

## Phase 1 audit notes (written before any edit)

Surface: ExecutionSupport has exactly ONE consumer — Compiled forwards all 10 public methods
via endless delegations (compiled.rb:237–247). No test references the class directly; coverage
flows through Compiled/executor tests. Tamoz::Core::Context (tamoz-core) is constructed in
build_context — cross-gem type, untouched.

Constraint noted: replay/error semantics pinned by executor family precedents; method-level
naming/step-down only, no extraction.

Criteria walk:
- (1) names state intent: validate_concurrency!, bind_writer_context, merge_resume_values read
  true; ensure_ephemeral_public!/stream_error_data would prefer assert_/build_ vocabulary but
  renames are multi-file (C6).
- (2) length: longest method open_writer ~14 lines; all ≤ Q6 ceiling of 20.
- (3) one level of abstraction: each method either delegates or guards+forwards; no mixing.
- (4) public surface reads as a small DSL over Compiled; forwarder symmetry preserved.
- (5) step-down: private section holds only attr_reader :compiled; nothing to sink.
- §4: no boolean params; kwargs used correctly. §5: instance frozen after initialize.
- Header reek suppressions document the recorded boundary-cohesion exception — left intact.

Candidates: C1 above; C2–C8 rejected with reasons above.
