# Combined pass — rank 254 gems/tamoz-core/lib/tamoz/configuration.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract `concurrency_mode!` / `instrumenting_notifier!` private validators mirroring the existing `bounded_integer!` seam so `initialize` reads at one abstraction level; byte-identical errors, same validation order concurrency→pool→recursion→stream→notifier, freeze last, zero renames) | Rejected: C2 (dedupe "is finalized" raise — helper would hide a mutex-held precondition inside pinned CAS protocol); C3 (Builder accessor narrowing — no defect, mutability required by `yield builder`, exposure already capped by `private_constant`); C4 (move module-level state init into a method — adds machinery over the simple load-time idiom, defaults pinned)
Diff stat: gems/tamoz-core/lib/tamoz/configuration.rb | 28 ++++++++++++++++++---------- → 1 file changed, 18 insertions(+), 10 deletions(+)
Commands: ruby -c gems/tamoz-core/lib/tamoz/configuration.rb → Syntax OK (before & after) · rubocop --cache false <file> → 0 offenses BEFORE, 0 offenses AFTER (no increase) · ruby -Itest test/core_instrumentation_test.rb → 9 runs, 29 assertions, 0 failures, 0 errors · ruby -Itest test/graph_execution_test.rb → 11 runs, 43 assertions, 0 failures, 0 errors · ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

---

## Supporting audit detail

Cross-gem contract verified read-only before editing: `Tamoz.configure`, `Tamoz.configuration`,
`finalize_configuration!`, `configuration_finalized?`, `Configuration#to_builder`, and all five
attr_readers untouched; defaults (`:threads`, 8, 200, 32, `Notifier::Null::INSTANCE`) and the
mutex + generation-CAS precedence untouched; renames forbidden per binding — none performed.
Repo-wide grep found no reflective (`send`/`public_send`) callers of this class's internals and
no external references to `Configuration::Builder`. Focused-test coverage confirmed in
core_instrumentation_test.rb (configure round-trip, `Configuration.new(pool_size: 0)` raises),
graph_execution_test.rb (ConfigurationError paths, live `Tamoz.configuration.pool_size`),
public_api_test.rb (finalized-predicate manifest).

Phase-1 candidates (recorded before any edit):
- C1 APPLY — see Applied above. Provable behavior-preserving by inspection: identical exception
  classes, message expressions copied verbatim, evaluation order unchanged; new helpers are
  private, ≤10 lines each, named after what they return per CODING_STANDARD §3/§3.1.
- C2 REJECT — visible duplicate raise states the lock-scoped invariant where it applies.
- C3 REJECT — nothing to improve on Builder.
- C4 REJECT — declarative init is the simple solution.

Known-red lanes not chased; sqlite boundary_source_audit extra gate N/A (not tamoz-sqlite).
