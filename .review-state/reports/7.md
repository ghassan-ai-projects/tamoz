# Fix report — rank 7 gems/tamoz-stream/lib/tamoz/stream/situation_request.rb

Applied: C1 (split-finalize-stream, amended naming); C2 (execution_id := envelope.request_id, byte-identical);
C3 (`return nil unless reconsider?` — equivalent for ALL inputs: unknown wire kind maps `kind` to nil ≠ :reconsider,
not merely post-`validate!`); C4 (kwargs for `episode_content_for` [5 params] + `open_verification_for_intent` [4];
all callers grep-proven in-file); C5 (`ensure_genuine_provider!` → `enforce_genuine_provider!`, sole in-file caller,
precedent `enforce_result_bounds!` capability_host.rb:180 same gem) | Rejected: none outright; C1 name amended
(candidate's `emit_produced_artifacts` misnames the path — the checkpoint branch also runs on FAILED-after-call runs
whose receipts must still be witnessed (B4), so extracted as `emit_terminal_outputs`; exact emit order preserved:
manifest → retain → model events → budget → decision event → verifications → terminal; manifest still built exactly
once — returned by the emit step, `||=` fallback unchanged).

C2 identity-seam review (per orchestrator caution): no error-message bytes change and no join semantics differ —
`request_id` is `"episode.#{episode_id}.#{attempt_id}.#{fence}"` over the same three wire readers, so the substituted
execution_id is byte-identical today. Drift stays caught independently: test/stream_episode_crash_matrix_test.rb
builds the fetch id from raw wire fields (:170, :180), not via the envelope.

C6 verification note: candidate's evidence ("every StreamError subclass defines CATEGORY, errors.rb:9–45") was
INCOMPLETE — subclasses also live in artifact_store.rb, notification_contract.rb, sse_transport.rb, worker_server.rb,
outcome_subscriber.rb, approval_relay.rb, reconsideration.rb, verification_store.rb, tamoz-sqlite/artifact_store.rb.
All 9 were checked: every one defines CATEGORY. With StreamError < Tamoz::Error < StandardError
(tamoz/core/error.rb:26) verified, implemented C6 as ONE StandardError rescue calling the new `wire_code_for`
(rather than two rescues sharing it) — byte-identical codes on every real path, duplication gone.

Diff stat: gems/tamoz-stream/lib/tamoz/stream/situation_request.rb | 60 +++++++++++++--------- (1 file changed,
37 insertions(+), 23 deletions(+); net +14)

Commands:
1. rubocop --cache false <owned> BEFORE edit → "1 file inspected, 4 offenses detected" (baseline recorded)
2. ruby -c <owned> AFTER → Syntax OK
3. rubocop --cache false <owned> AFTER → "1 file inspected, 4 offenses detected" — count NOT increased; identical
   cops as baseline (Style/Documentation :22; Style/GuardClause :217→216, :278→277, :687→701, shifted only by edits
   above them). No new lines introduce offenses; nothing suppressed.
4. ruby -Itest test/public_api_test.rb BEFORE → 3 runs, 1015 assertions, 0 failures, 0 errors (pre-edit baseline)
5. ruby -Itest test/public_api_test.rb AFTER → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips

Test-lane note (gate 4): every direct behavioral test of EpisodeRequestEnvelope/EpisodeRunner lives in
stream_*_test files (stream_situation_request, stream_invariants, stream_episode_crash_matrix, stream_episode_witness,
stream_artifact_manifest, stream_decision_builder, stream_token_custody, …) — all excluded SLOW/SERIAL lanes per the
brief, plus the test/support/episode_composition.rb helper. The only non-excluded reference is public_api_test.rb
(pinned public-constant inventory; my diff renames privates only — green before AND after). So no runnable focused
test exercises this file's behavior; gates rest on syntax + uncached RuboCop + the per-candidate equivalence proofs.

FLAG items (owner decisions only — no action taken):
- Filename lies: file holds EpisodeRequestEnvelope + EpisodeRunner, nothing named SituationRequest (cross-gem rename).
- validate_identity_fields!: divergent emptiness messages ("episode identity is incomplete…" vs "#{field} is
  required") — collapsing changes error bytes.
- wall_time enforced in validate_budget! but never propagated by budget_hash (G6 carry-forward; behavior change).
- identity_for mints an ad hoc Struct per failure; missing-abstractions.md:103 already requests a named identity value.

Deviations: (1) C1 method names differ from proposal for the correctness reason above; (2) C6 collapsed to a single
rescue clause instead of two rescues calling the helper — identical exception→code mapping, verified hierarchy;
(3) ENVIRONMENT: at session start `git status` was clean except untracked .review-state/; during the session six
OTHER gem files acquired modifications from sibling fixers (tamoz-agent worker.rb/worker_runtime.rb, tamoz-comms
gateway.rb, tamoz-evals openclaw_comms_runner.rb, tamoz-sqlite comms_store.rb/migrator.rb). I touched ONLY my owned
path; orchestrator must attribute commits per file. My owned file's diff contains exactly the edits described here.
