# Combined pass — rank 137 gems/tamoz-sqlite/lib/tamoz/sqlite/lease_operations.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (`backend_time` double negative → `value.is_a?(Integer) && value >= 0`; raise message bytes unchanged); C2 (`validate_lease_in_transaction!` binds `row.fetch(2)` once as `current_expiry` instead of two fetches in one condition)
Rejected: C3 (pre-init `expires_at_ms = nil` / `fence = nil` pattern — identical idiom across acquire/validate/renew; churn without clarity gain); C4 (rename `guard_backend_clock!` toward §3.1 `enforce_*` vocabulary — invoked via `adapter.__send__(:backend_time, ...)`-style dispatch from ~13 sibling files incl. `store.rb:186`, `thread_purge.rb:31`, `effect_preparation.rb:71`, and cited in docs/P14_STREAM_PLAN.md; multi-file rename outside this file's ownership); C5 (rename `validate_lease*` family per §3.1 validate-vs-verify table — `validate_lease_in_transaction!` is symbol-referenced from 8+ collaborators (`effect_preparation.rb:73`, `checkpoint_appender.rb:59`, `effect_reconciler.rb:83`, `request_inbox_recovery.rb:168`, `checkpoint_store.rb:90`, …) and lease.rb `__send__`s validate/renew/release; pinned cross-gem API + Lease/CAS semantics pinned); C6 (extract shared `(ttl * 1_000).ceil` expiry arithmetic from acquire/renew — recorded cohesion exception forbids extraction in durable transaction cores); C7 (named-column accessors over `row.fetch(0..3)` indices — ripples through audited helper bodies for zero behavior change)

Phase 1 candidate list (recorded before any edit): C1–C7 as above; nothing else surfaced.
The file is a durable transaction core under the recorded cohesion exception: methods run long (acquire_lease ~72 lines) but are single-transaction CAS sequences — method-level improvements only, no extraction. Names/signatures are pinned cross-file via `__send__` dispatch and boundary-source-audit symbol references.

Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/lease_operations.rb | 7 ++++--- (1 file, +4/-3, net +1)
Commands:
- ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/lease_operations.rb → Syntax OK
- rubocop --cache false <file> → BEFORE 0 offenses, AFTER 0 offenses (no increase)
- ruby -Itest test/sqlite_boundary_source_audit_test.rb → 7 runs, 55 assertions, 0 failures, 0 errors (mandatory extra gate)
- ruby -Itest test/sqlite_checkpoint_test.rb → 6 runs, 23 assertions, 0 failures, 0 errors
- ruby -Itest test/sqlite_boundary_registry_test.rb → 5 runs, 1576 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none
