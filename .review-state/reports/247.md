# Combined pass — rank 247 gems/tamoz-evals/lib/tamoz/evals/benchmark/baselines.rb
Verdict: CLEAN
Applied: (none) | Rejected: C1 (extract the `cell.merge("primary_code" => …, "probabilities" => …)` prediction line repeated in all 7 predictors into a shared helper — structural extraction; binding note restricts this file to method-level naming/step-down only and recorded benchmark outputs are pinned; zero behavioral gain for a 7-site diff); C2 (inline/remove the `one_hot_probabilities` forwarder to `Metrics.one_hot` — module_function public surface of a gem API, public/cross-gem API untouchable per brief; repo-wide grep finds no external caller but removal is pure churn with API risk); C3 (rename any strategy or kwarg — names are dispatched via `Baselines.public_send(name, …)` from the `baselines` array of documentation/benchmark/BENCHMARK_PROTOCOL.json and kwargs are matched by reflection against family keys in report.rb#run_baseline (`Baselines.method(name).parameters`), so they are protocol wire surface); C4 (`facts.fetch(metric, nil)` → `facts[metric]` and simplify the series guard in `metric_value` — equivalent but no readability gain on a rubocop-clean file, style churn); C5 (hoist the invariant `majority_for(cells, codes)` out of the per-cell loops in fixed_threshold/z_score/first_difference/moving_median/nearest_symptom — provably output-identical but a perf micro-opt, not a clean-code gain under the five criteria; the inline call keeps each cell's decision self-contained); C6 (name or alter the `(mad.zero? ? 0.1 : mad)` floor in `moving_median` — the fallback is protocol-frozen tuning baked into recorded outputs; untouchable)
Diff stat: no diff — file untouched (git diff --stat empty for owned path)
Commands: ruby -c gems/tamoz-evals/lib/tamoz/evals/benchmark/baselines.rb → Syntax OK · rubocop --cache false <file> → 1 file inspected, 0 offenses (BEFORE = AFTER = 0, no edit made) · ruby -Itest test/benchmark_harness_test.rb → 25 runs, 67 assertions, 0 failures, 0 errors, 0 skips (exercises every baseline incl. determinism + fixed_threshold lt/gt/invalid-operator + run_baseline dispatch)
Deviations: none. EXEMPT was considered and rejected: the file is algorithmic predictor logic, not mostly declarative data. Audit result: names state intent and match protocol vocabulary; longest method body ≈10 lines (Q6 ≤20 met); one level of abstraction per method; the eight module_function entry points read as a DSL of preregistered predictors over (cells, codes, domain params); step-down holds via metric_value/metric_series/majority_for/one_hot_probabilities. Header and provenance comments ("never defaults here", seed from protocol statistics) are §11 why-comments tied to the domain-leak fix and stay.
FLAG (owner decisions): none
