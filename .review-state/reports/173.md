# Combined pass — rank 173 gems/tamoz-evals/lib/tamoz/evals/harness/heuristic_paired_evaluation.rb

## Phase 1 — ANALYZE (candidates written BEFORE any edit)

File: 191 lines, single class `HeuristicPairedEvaluation`, paired baseline/holdout
evaluation harness (P12-I2 extension, not a second harness). Recorded-verdict file:
byte-identical output required; method-level naming/step-down only. BEFORE rubocop: 0 offenses.

- C1 — `evaluate` is 23 lines (over the ≤20 normal ceiling): partition scoring, artifact-body
  assembly, sealing, writing, freezing all inline. Extract the pure body-hash literal into a
  private `report_body(heuristic, development, holdout)` so `evaluate` reads as orchestration.
  Byte-identical body (same literal, same key order, same evaluation order dev→holdout);
  no side effects move. APPLY (high confidence, mechanical).
- C2 — rename private `arm(tasks, digest)` to e.g. `score_arm`/`build_arm`.
  REJECT: "arm" is this file's domain vocabulary (report keys `"baseline"`/`"candidate"`
  arms, header "Two arms"); the call sites sit directly inside the `"arms" =>` literal and
  read correctly against it. Rename adds noise, not clarity.
- C3 — fold the unknown-oracle guard in `satisfies?` into a `case ... else raise`, deleting
  the then-orphaned `ORACLES` constant (repo-wide grep: zero external references).
  REJECT: `ORACLES` is declared public surface on the class and the brief forbids touching
  public/cross-gem API; saving two duplicated lines does not justify that.
- C4 — extract the output-filename ternary `report.#{regression ? ... }.json` into a helper.
  REJECT: one call site, self-explanatory; churn.
- C5 — rename local `read` → `reads` in `read_before_patch?`.
  REJECT: style churn inside an untouched method.
- Comments: the long header/oracle/regression comments are §11 "why" comments (determinism
  requirement, dependency-isolation constraint, why the injected regression is not contrived);
  not narrative bug history. No trims.

## Phase 2 — APPLY

Verdict: COMMITTED-CANDIDATES
Applied: C1 (extracted private `report_body(heuristic, development, holdout)` from `evaluate`; evaluate now 12 lines of orchestration; body hash byte-identical)
Rejected: C2 (domain term "arm", reads correctly at both call sites); C3 (would orphan public `ORACLES` constant — public API untouchable); C4 (single-site churn); C5 (style churn in untouched method)
Diff stat: 1 file changed, 18 insertions(+), 14 deletions(-) — net +4 lines
Commands:
- `ruby -c <file>` → Syntax OK
- `rubocop --cache false <file>` → "1 file inspected, no offenses detected" (BEFORE 0, AFTER 0)
- `ruby -Itest test/improvement_candidate_test.rb` (only test file touching the class) → 14 runs, 284 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
