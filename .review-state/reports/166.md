# Combined pass — rank 166 gems/tamoz-evals/lib/tamoz/evals/harness/memory_cell.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract unnamed 10-term correctness conjunction → private `injection_correct?`, name matches emitted key `"injection_correct"`); C2 (dedupe nil-safe audit fallback ×4 → private `audit_count(audit, key)`); C3 (hoist duplicated injected-id capture scan shared by `build_measurement` + `marks_missing_but_injection_expected?` → private `injected_ids`); C4 (dedupe `@execution ? @execution.events : []` ×2 → private `execution_events`)
Rejected: C5 (`seed_store` → private: zero external callers repo-wide, but public/cross-gem API is untouchable per brief); C6 (§4 BooleanParameter on `outcome(correct)`/`reason(correct)`: fix requires re-plumbing five id sets into two guard ladders whose ordering encodes outcome/reason precedence on byte-recorded output — risk > gain); C7 (merge double rescue in `run_execution`: branches produce different recorded error-message bytes, untouchable)
Diff stat: 1 file changed, 37 insertions(+), 18 deletions(-) (55 changed lines, net +19)
Commands:
- `ruby -c gems/tamoz-evals/lib/tamoz/evals/harness/memory_cell.rb` → Syntax OK (before AND after)
- `rubocop --cache false gems/tamoz-evals/lib/tamoz/evals/harness/memory_cell.rb` → BEFORE 0 offenses, AFTER 0 offenses (no increase)
- `ruby -Itest <all gem lib dirs> test/memory_treatment_profile_test.rb` (baseline, pre-edit) → 14 runs, 218 assertions, 0 failures, 0 errors, 0 skips
- same command post-edit → 14 runs, 218 assertions, 0 failures, 0 errors, 0 skips (identical)
Deviations: The sole test touching this file is `test/memory_treatment_profile_test.rb`, parked in SLOW_TESTS (Rakefile L40–50, measured 15.7s weight). Instead of skipping it as known-red, I baselined it green pre-edit and re-ran it post-edit (~8s actual): identical pass counts prove the non-regression directly. Behavior preserved by construction: all extracted helpers recompute the same expressions in the same short-circuit order over pure reads; no visibility, signature-of-public-method, key-order, message-byte, or digest changes; DeepFreeze output byte-identical.
FLAG (owner decisions): none

## Phase 1 candidate list (written before any edit)

Audit basis: name-states-intent, ≤20/≤30 length, one level of abstraction, public DSL,
step-down; CODING_STANDARD §3/§3.1/§4/§5/§6/§11. Binding note: recorded outputs
byte-identical; method-level naming/step-down only. BEFORE gates: `ruby -c` OK;
rubocop --cache false = 0 offenses; focused baseline test/memory_treatment_profile_test.rb
= 14 runs, 218 assertions, 0 failures, 0 errors, 0 skips (SLOW-lane parked gate, 8s actual,
run pre-edit to prove non-regression).

- C1 APPLY — `build_measurement` (56 lines): extract the unnamed 10-term correctness
  conjunction into private predicate `injection_correct?` (name matches the emitted key
  `"injection_correct"`; the predicate is called out by name in
  docs/reviews/codebase-review-2026-08-20 §6). Same expressions, same short-circuit
  order, pure reads → byte-identical hash.
- C2 APPLY — dedupe `(audit ? audit.fetch(k) : 0)` (4 occurrences: 2 in the correctness
  conjunction, 2 in the hash) behind private `audit_count(audit, key)`.
- C3 APPLY — `marks_missing_but_injection_expected?` recomputes
  `@captures.flat_map { |c| c.fetch("injected_ids") }.uniq`, duplicating `build_measurement`;
  hoist to private `injected_ids`.
- C4 APPLY — dedupe `@execution ? @execution.events : []` (2 sites) behind private
  `execution_events`.
- C5 REJECT — make `seed_store` private (zero external callers repo-wide). Public/cross-gem
  API is untouchable per brief; visibility change alters the exported surface.
- C6 REJECT — §4 BooleanParameter on `outcome(correct)` / `reason(correct)`: removing it
  means re-plumbing five id sets into both ladders whose ordering encodes outcome/reason
  precedence on byte-recorded output; risk exceeds the cosmetic gain.
- C7 REJECT — merge the double rescue in `run_execution`: the two branches produce
  different recorded error-message bytes (`error.message` vs `"Class: message"`); error
  bytes are untouchable.
- C8 NOTE (named exception) — `build_measurement` stays >30 lines after step-down: the
  remaining body is the 30-key `DeepFreeze.call` literal plus gather lines; splitting the
  literal off adds indirection for zero insight (§4: never split merely to satisfy a number).
- C9 NOTE — `run` threads seven ivars through the cell lifecycle; stateful-cell shape is
  load-bearing for `MemoryTreatmentProfile`; any change is extraction, out of scope for a
  method-level pass.
