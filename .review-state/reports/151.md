# Combined pass — rank 151 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_selector_control_stopper.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (finish!: manual `@state = :failed` + `raise ExecutionError` replaced with the existing in-file seam `fail_control!`, literal equivalence, message bytes preserved); C2 (call: same conversion on the post-SIGSTOP resume path — `fail_control!` sets `:failed` before raising exactly as the removed pair did, so the `rescue ExecutionError` clause stays a provable no-op) | Rejected: C3 (shared armed-check helper for call/finish! — extraction is out of scope for this pass per binding "naming/step-down only", and the two messages differ by design); C4 (split the ~68-line call into per-phase helpers — kill/intervention semantics pinned and §4 forbids splitting cohesive protocol steps to satisfy a line count; named exception stands); C5 (hoist repeated `metadata.fetch("operation")` into a local — churn on pinned wire-comparison code for marginal gain); C6 (renames of ensure_owner!/validate_hook!/validate_control_stat!/write_control!/normalize_point/fail_control! — already match §3.1 vocabulary and are byte-identical to names in the passed sibling intervention file; tests additionally pin private names via `send(:write_control!)`); C7 (drop `rescue ExecutionError; raise` clauses as redundant — load-bearing: ExecutionError < Error < StandardError, so without them `rescue StandardError` would rewrap control failures)
Diff stat:  gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_selector_control_stopper.rb | 9 ++++----- (4 insertions, 5 deletions; net −1 LOC)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/sqlite_selector_control_test.rb → 19 runs, 125 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
