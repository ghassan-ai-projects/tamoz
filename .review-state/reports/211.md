# Combined pass — rank 211 gems/tamoz-approval/lib/tamoz/approval/evaluator.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (`build_decision` positional → keyword args); Rejected: C2 (unify deny/ask-allow finds behind a predicate param — the proc/flag param is the two-behaviors shape §4 bans, indirection across 2 sites to save ~2 lines); C3 (drop `else false` in `match?` — validate_rules! already rejects unknown matchers, so it is dead, but plain `case` without else returns nil→falsy identically and the explicit else documents matcher closedness; zero-gain edit on matching semantics); C4 (`entry.fetch(:grant_scopes, nil)` → `[]`-style reads in tool_scopes_for/key_argv — cannot prove identical for hand-built documents passed straight to Evaluator.new in tests under an explicit-nil field; churn on gate logic); C5 (extract the required_evidence ternary — single-use one-liner inside pinned evidence semantics); C6 (shed grant_key_for's field case into a helper — ~19 code lines, within the ≤20 ceiling, and its nil/empty short-circuits are load-bearing for durable grant-key identity)
Diff stat: gems/tamoz-approval/lib/tamoz/approval/evaluator.rb | 7 +++--- (4 insertions, 3 deletions; net +1)
Commands: ruby -c evaluator.rb → Syntax OK · rubocop --cache false evaluator.rb → 0 offenses (BEFORE also 0, no increase) · ruby -Itest test/approval_grant_key_test.rb → 4 runs, 8 assertions, 0 failures, 0 errors · ruby -Itest test/approval_engine_test.rb → 26 runs, 95 assertions, 0 failures, 0 errors · ruby -Itest test/approval_policy_document_test.rb → 23 runs, 66 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

## Candidate audit (written before any edit)

Context proven before listing: every private method below has ZERO callers outside
evaluator.rb (repo-wide grep incl. send/__send__/public_send — only dynamic dispatch in
the gem is PolicyDocument's unrelated `base.send(:apply_profile)`; the target_root/
key_argv/decision_id/build_decision hits elsewhere are policy-YAML keys and same-named
methods in other gems). Public surface is exactly `initialize`, `evaluate`, and
CLOSED_MATCHERS — the constant is mirrored by PolicyDocument::CLOSED_MATCHERS and is
untouchable. All four comment blocks pass §11 (each states a why/safety invariant:
fallback-tier scope sourcing, durable key stringification, targets-folded-into-key,
deny-first ordering).

- C1 APPLY — `build_decision(request, tier, verdict, reason, rule_id)`: 5 positional
  params, sitting at the Metrics/ParameterLists Max:5 ceiling, against §4 "keyword
  arguments beyond one or two positional parameters; no long positional lists". Convert
  to kwargs; both call sites (`decision_from`, `decision_for_tier`) are in-file and
  proven private-only. Labels make the authority-relevant fields visible at the call
  site (which string is the refusal reason vs the rule id). Identical Decision
  construction, byte-identical output.
- C2 REJECT — `matching_deny_rule` / `matching_ask_allow_rule` near-duplication: unify
  behind a verdict-predicate parameter. Rejected: that param is precisely the "named
  policy" split §4 forbids collapsing into a flag-like argument; a lambda indirection
  across two find calls saves nothing and hides which verdict tier runs first — the
  ordering IS the gate semantics.
- C3 REJECT — `match?`'s `else false`: dead by construction (PolicyDocument#validate_rules!
  rejects unknown matchers at load). Rejected: removing it changes nothing observable
  and the explicit else keeps the matcher set visibly closed inside the evaluator.
- C4 REJECT — fetch-with-default style in `tool_scopes_for` / `key_argv`: rejected as
  unprovable for exotic documents (tests construct documents directly); scope fallback
  order here decides whether a grant can carry :session — not a style playground.
- C5 REJECT — extract `required_evidence` ternary from build_decision: single-use,
  evidence requirement is pinned policy ("ask ⇒ evidence[:approve]"); extraction adds a
  hop on a gate for no readability gain.
- C6 REJECT — decompose `grant_key_for`: longest method (~19 code lines incl. its §11
  comment) but within ceiling; it computes durable grant-key identity where each nil/
  empty short-circuit demotes the offer to [:once]. Restructuring risks subtle drift;
  the flat field-case reads as a table.
