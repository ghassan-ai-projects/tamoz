# Combined pass — rank 201 gems/tamoz-approval/lib/tamoz/approval/decision_log.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (append: name the decision-identity comparison — frozen NON_IDENTITY_KEYS + Hash#except replaces two duplicated inline reject lambdas)
Rejected: C2 (record_mode_switch `stored` temp var removal — pure churn, zero intent gain, touches audit conflict path); C3 (`values.reverse.find` → `reverse_each.find` — micro-perf churn, not a cleanliness criterion, ordering semantics pinned); C4 (consolidate three ConflictingResolutionError guards into shared helper — per-site message bytes untouchable, shared machinery couples audit-evidence paths, simple-over-complicated); C5 (`record && record[:created_at_ms]` → `&.` on aref — reads worse, no cop offense); C6 (port-class mixed positional/kwargs signatures — public/cross-gem API untouchable, sqlite sibling implements these exact signatures); C7 (record_mode_switch ~22 lines vs ≤20 normal — within 30 hard ceiling; extracting the conflict guard would touch pinned conflict semantics, rails say method-level only)
Diff stat: gems/tamoz-approval/lib/tamoz/approval/decision_log.rb | 5 +++-- (3 insertions, 2 deletions; net +1)
Commands: ruby -c decision_log.rb → Syntax OK (before + after) · rubocop --cache false decision_log.rb → 0 offenses before, 0 after (no increase) · ruby -Itest test/approval_mode_switch_test.rb → 11 runs, 30 assertions, 0 failures/errors · ruby -Itest test/approval_engine_test.rb → 26 runs, 95 assertions, 0 failures/errors · ruby -Itest test/approval_resolve_test.rb → 16 runs, 62 assertions, 0 failures/errors
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE (candidates written before any edit)

Audit basis: five clean-code criteria + CODING_STANDARD §3/§3.1/§4/§5/§6/§11. Extra rail:
decision log rows/digests/ordering are audit evidence → naming/step-down only, CLEAN acceptable.

- C1 (APPLY): `MemoryDecisionLog#append` repeats `{ |key, _| key == :step_scope || key == :created_at_ms }`
  twice inline (lines 76–77). The concept — compare logged content ignoring provenance/clock fields —
  is unnamed. Replace with `NON_IDENTITY_KEYS = %i[step_scope created_at_ms].freeze` +
  `existing.except(*NON_IDENTITY_KEYS) == record.except(*NON_IDENTITY_KEYS)`.
  Proof of behavior preservation: only producer of append records is `engine.rb#log_decision`
  (plain symbol-keyed hash); `Hash#except` returns the identical remaining pairs in identical
  order, so equality results are bit-identical; error bytes untouched; constant is class-internal
  (public_api_test pins only top-level gem symbols). Repo precedent for the exact idiom:
  tamoz-observability/signal_catalog.rb:166 `existing.to_h.except(:name, :since) == entry.to_h.except(:name, :since)`.
- C2 (REJECT): `record_mode_switch` assigns `stored = @mutex.synchronize { … }` then returns `stored`;
  block value could be returned directly. Churn with no readability/intent gain on an audit path.
- C3 (REJECT): `latest_decision_for` scans `@decisions.values.reverse.find`. `reverse_each.find`
  avoids one array copy but changes nothing observable; insertion-order-dependent "latest" match is
  pinned behavior. Not a cleanliness violation.
- C4 (REJECT): append / record_resolution / record_mode_switch each raise ConflictingResolutionError
  with site-specific guard + message. A shared guard helper would couple three audit-evidence paths
  and must preserve three distinct message byte strings. Rejected.
- C5 (REJECT): `decision_created_at_ms` — `record && record[:created_at_ms]`; safe-nav does not apply
  cleanly to `[]`, current form is clear, RuboCop silent.
- C6 (REJECT): DecisionLog port mixes positional (`lookup`, `lookup_resolution`, `latest_mode_switch`,
  `decision_created_at_ms`) and kwargs signatures. Public/cross-gem API — untouchable.
- C7 (REJECT): `record_mode_switch` ≈22 body lines (>20 normal). Under the 30 hard ceiling; the only
  split candidate is the identity-conflict guard, whose extraction would reshuffle pinned audit
  conflict semantics for marginal gain. Leave.
- Comments: all existing comments carry contract "why" (idempotency contracts, provenance carve-out,
  replay/pinning rationale) — appropriate under §11, kept. New code adds none.
