# Combined pass — rank 49 gems/tamoz-sqlite/lib/tamoz/sqlite/store.rb

Verdict: COMMITTED-CANDIDATES

Applied: C1 (`put` → extracted `validate_sensitive!`, step-down + §3.1 vocabulary); C2
(`each` → hoisted predicate/binds assembly above the read transaction so the closure is
query execution only; `predicate` → `key_filter`); C3 (`materialize` 35→24 lines →
extracted live-value branch as `decode_canonical_value`, corruption checks/messages/order
byte-identical)
Rejected: C4 (get/read_version wrapper dedupe — 2-line duplication around distinct
queries; obscures more than it saves); C5 (unify materialize's dual row shape via SELECT
changes — touches three query contracts, beyond method-level); C6 (rename private
`bytes_for_decode` — name already states purpose, churn); C7 (split/rename
`append_version_in_tx` pair — durable transaction core, recorded cohesion exception, no
extraction; CAS sequencing untouched); C8 (`normalize_expected_version` message says
"if_version" on read paths — error/message bytes untouchable); C9 (redundant `.freeze` on
PROTECTED_FORMAT — style churn, rubocop clean as-is)

Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/store.rb | 49 ++++++++++++++++------------
1 file changed, 29 insertions(+), 20 deletions(-)  [net +9]

Commands:
- ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/store.rb → Syntax OK (before and after)
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/sqlite_store_test.rb → 6 runs, 68 assertions, 0 failures, 0 errors
- ruby -Itest test/memory_store_test.rb → 16 runs, 112 assertions, 0 failures, 0 errors
  (second focused file: MemoryStore is the heaviest consumer of Store's seam/read paths)

Deviations: none. Transaction boundaries (`adapter.__send__(:transaction/:read)`) verbatim;
P11/DC-3 internal seam methods untouched (callers proven in memory_store.rb,
circuit_store.rb, durable_subscriber_store.rb, approval_receipt_store.rb,
agent/worker_runtime.rb, evals memory_repository_adapter.rb).

FLAG (owner decisions): none.

## Phase 1 — ANALYZE (candidate list)

File: 416 lines, storage composition facade (`Tamoz::SQLite::Store`). Public DSL
(put/get/delete/each/search + protocol_version/storage_identity) reads clean; the five
P11/DC-3 internal seam methods (`open_transaction`, `append_in_transaction`,
`protect_bytes`, `read_version`, `head_version`) have live callers in `memory_store.rb`,
`circuit_store.rb`, `durable_subscriber_store.rb`, `approval_receipt_store.rb`,
`agent/worker_runtime.rb`, `evals/harness/memory_repository_adapter.rb` → no public/seam
renames. Transaction boundaries (`adapter.__send__(:transaction/:read)`) stay verbatim.
BEFORE gates: ruby -c OK; rubocop --cache false = 0 offenses.

- C1 APPLY: `put` — the inline `unless sensitive == true || sensitive == false` type check
  interrupts the happy path mid-method; extract to `validate_sensitive!(value)` following
  the file's existing `validate_protection!` naming precedent (§3.1 validate_ vocabulary,
  step-down). Call stays at the same position, so raise precedence for multi-invalid input
  is unchanged; message bytes identical.
- C2 APPLY: `each` — the read-tx closure builds the key-range predicate string and the
  binds array next to the `tx.rows` call (two abstraction levels inside the transaction
  block). Hoist that pure assembly above `adapter.__send__(:read ...)` so the closure is
  only query execution; rename local `predicate` → `key_filter`. Pure locals on
  already-normalized values, evaluated once either way — no ordering or tx interaction.
- C3 APPLY: `materialize` — 35-line method (over the 30 hard ceiling) mixing row-shape
  decoding with the live-value decode/canonicality invariant. Extract the non-deleted
  branch into `decode_canonical_value(payload, digest, sensitive, address)`; checks, order,
  exception classes and messages preserved byte-for-byte; the why-comment travels with it.
- C4 REJECT: dedupe the read→nil-check→materialize wrapper shared by `get` and
  `read_version` — two lines of duplication around genuinely different queries; a shared
  closure-based helper obscures more than it saves.
- C5 REJECT: unify materialize's dual row shape (7-col rows carrying h.key vs 6-col) by
  changing the SELECT column lists — touches three queries and their column contracts;
  beyond method-level scope for this facade.
- C6 REJECT: rename private `bytes_for_decode` — name already states purpose (bytes handed
  to the state codec); no clarity win worth diff churn.
- C7 REJECT: split `append_version_in_tx` / rename `append_version` pair — durable
  transaction core is a recorded cohesion exception: method-level only, no extraction;
  CAS/conflict/head-upsert sequencing untouched.
- C8 REJECT: `normalize_expected_version` error text says "if_version" when reached via
  `read_version`/`append_in_transaction` — error message bytes are untouchable.
- C9 REJECT: redundant `.freeze` on `PROTECTED_FORMAT` under frozen_string_literal — style
  churn; rubocop is clean as-is.

## Phase 2 — APPLY

Applied C1, C2, C3 as specified above; all gates pass (see Commands).
