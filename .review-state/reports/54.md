# Combined pass — rank 54 gems/tamoz-concurrency/lib/tamoz/pool.rb

Verdict: COMMITTED-CANDIDATES

## Phase 1 — ANALYZE (candidate list, written before any edit)

BEFORE gates: `ruby -c` Syntax OK; `rubocop --cache false` = **0 offenses**.
Context proven by repo-wide grep: `record_stuck!`, `cancellation_for`, `Pool::Base/Inline/Threads`
have zero callers outside this file; no `send/__send__/public_send` reaches pool symbols;
`test/core_pool_test.rb` pins error identity from `Threads#initialize` and pins the file SOURCE
(no kill/raise/Timeout tokens); the worker-loop `rescue Exception` at line 255 is the recorded
inline-disabled pattern per CODING_STANDARD §7 — off-limits.

- C1 Threads#initialize is 41 lines (> 30 hard ceiling): four independent field contracts
  drown state setup. APPLY — extract to named private validators (`validate_size!`,
  `validate_queue_capacity!`, `validate_cancellation_grace!`, `validate_stuck_worker_limit!`),
  same conditions, same message bytes, same evaluation order after `super`. §4 step-down,
  §3.1 `validate_*` = shape check named by field. High confidence, low risk.
- C2 `record_stuck!` mutates with no safe twin — §3: bang only when a safe twin exists. APPLY —
  rename to `record_stuck`; single in-file caller proven. Low risk.
- C3 `Pool.for` (28 lines) split into build_inline/build_threads helpers. REJECT — flat mode
  table IS the public DSL entry point (criterion 4); under the 30 hard ceiling; splitting adds
  indirection without removing a mixed abstraction level.
- C4 Base#execute (23 lines) split run/interpret. REJECT — pinned interrupt/error ordering
  (catch :tamoz_interrupt + FatalRuntimeFailure-vs-Failed classification); already reads
  top-to-bottom; marginal gain vs pinned-core risk.
- C5 execute_threads (46) / collect (50) extraction of join/backfill/tail-mapping steps.
  REJECT — mutex/queue discipline byte-stable per directive; backfill order
  (stuck → submitted-cancelled → not_scheduled) feeds pinned result ordering; ensure-block
  shares locals with the body; churn risk for zero observable gain.
- C6 build_workers / worker loop. OFF-LIMITS — recorded inline-disabled `rescue Exception`
  region (§7), directive forbids touching it. Not counted as a rejection.
- C7 local `completed_normally` → `completed_normally?`. REJECT — reads plainly; ? on locals
  unidiomatic; churn inside pinned collect.
- C8 `ensure_circuit_closed!` → `enforce_circuit_closed!` (§3.1 enforce_). REJECT — raising
  guard-bang is established idiom, method does not mutate, single caller, nothing gained.
- C9 drop unreachable-by-construction `fetch { ... "not_scheduled" }` fallback in
  execute_threads tail. REJECT — sits in the pinned result-ordering bytes; unobservable gain.
- C10 add public-API doc comment to `module Pool` / `Pool.for` (§11). NOT APPLIED here —
  out of method-level mandate ("comments default none"); flagged for owner.

## Phase 2 — APPLY

Applied: C1 (Threads#initialize → four private validate_*!, message bytes and order preserved);
C2 (record_stuck! → record_stuck, §3 bang rule)
Rejected: C3 (factory table reads as the DSL entry point); C4 (pinned interrupt/error ordering);
C5 (byte-stable mutex/queue discipline, pinned result ordering); C7 (unidiomatic ? on locals);
C8 (raising guard-bang idiom, no mutation); C9 (pinned result-ordering bytes)

Diff stat: gems/tamoz-concurrency/lib/tamoz/pool.rb | 57 ++++++++++++++++++--------------
32 insertions(+), 25 deletions(-) — net +7 LOC; committed by the orchestrator as 0ae13d3
(single-file commit, matches my working diff byte-for-byte).

Commands:
- BEFORE `ruby -c pool.rb` → Syntax OK; `rubocop --cache false pool.rb` → 0 offenses
- AFTER `ruby -c pool.rb` → Syntax OK; `rubocop --cache false pool.rb` → 0 offenses (no increase)
- `ruby -Itest test/core_pool_test.rb` → 11 runs, 137 assertions, 0 failures, 0 errors, 0 skips
  (includes test_pool_implementation_contains_no_asynchronous_thread_termination source pin and
  test_task_count_and_pool_configuration_are_bounded exercising all four validators)
- `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips

Deviations: none in process (no commits made by me, no forbidden paths touched, no suppressions).
Note: the orchestrator committed the finished diff mid-session (0ae13d3), so the working tree is
clean and Diff stat cites that commit.

FLAG (owner decisions): C10 — Tamoz::Pool / Pool.for carry no doc comment while sibling
concurrency.rb documents its surface at the entry file; §11 calls public-API docs required, but
adding them was outside this pass's method-level mandate.
