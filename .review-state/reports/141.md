# Combined pass — rank 141 gems/tamoz-sqlite/lib/tamoz/sqlite/thread_purge.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (group public entry points — moved `receipt` next to `call`, dropped mid-class `public` re-open and the now-redundant second `private`); C2 (named `row.fetch(3)` as `purge_after_ms` local in `ensure_purgeable!`, removing the duplicate call)
Rejected: C3 (rename `ensure_purgeable!` → `enforce_purgeable!`: tamoz-sqlite's established lexicon for refusing guards is `ensure_*!` — `ensure_process!`, `ensure_thread!`, `ensure_namespace!`, `ensure_no_earlier_request!`; 35 vs 36 split repo-wide, so the rename adds inconsistency instead of removing it); C4 (consolidate the duplicated receipts SELECT in `existing_receipt`/`receipt`: different op labels (`thread.purge.receipt` vs `thread.deletion_receipt`) and different contexts (write tx vs read path) — sharing would be extraction across a durable transaction core, which stands as recorded exception); C5 (compress `delete_comms_rows!` per-table DELETEs into an interpolated loop: Invariant 54 comment pins one explicit statement per table as the deletion contract; op labels become dynamic strings; recorded exception stands, method-level only); C6 (convert `build_receipt` positional params to kwargs: recorded LongParameterList exception states row fields and transaction arguments are the durable purge contract); C7 (add `max_nesting:` to `JSON.parse` in `build_final_report`: behavior change; bytes are digest-verified immediately before parse so tampering is excluded; corruption-only case — do not cover rare cases); C8 (replace `receipt = nil` block-assignment in `call` with direct assignment from `adapter.__send__(:transaction)` return: depends on the transaction wrapper's return contract across fault-hook paths; zero gain); C9 (rename `purge_new_thread`/`existing_receipt`/`active_tombstone`: alternatives equally ambiguous or pure churn; single-caller context disambiguates)
Diff stat: see Commands section below (recorded after apply)
Commands: recorded after apply
Deviations: none
FLAG (owner decisions): none

---

## Phase 1 candidate list (written before any edit)

Audit axes: (1) name states intent · (2) ≤20 lines normal / 30 hard · (3) one level of abstraction · (4) public entry points read as a small DSL · (5) step-down. Recorded cohesion exceptions honored (durable transaction core; header reek suppressions pin row fields/transaction args as the purge contract). TAMOZ-SQLITE rail: no endless-method conversions, explicit parameter lists kept on forwarders, extra gate after any edit.

- **C1** — Public surface is split: `call` at top, then ~150 lines of privates, then a `public` flip for `receipt`, then another `private` for `attr_reader`. Criterion (4)/(5): move `receipt(tombstone_id:)` up beside `call`; drop the lone `public` and the now-redundant second `private`. Behavior-preserving by construction: visibility sections are lexical, both methods stay public, only external callers are `Adapter#purge_thread`/`#deletion_receipt` (adapter.rb:191–198), verified repo-wide including `send`/`__send__`/`public_send`.
- **C2** — `ensure_purgeable!` calls `row.fetch(3)` twice inside one condition. Name it `purge_after_ms` (the column selected in `active_tombstone`); duplicate-call reek suppression exists but the local states intent. Pure: fetch is side-effect-free.
- **C3** — rename to `enforce_purgeable!`. REJECT (evidence above).
- **C4** — dedupe receipts SELECT. REJECT (extraction across transaction core).
- **C5** — loop the comms table DELETEs. REJECT (Invariant 54 explicit-statement contract).
- **C6** — kwargs for `build_receipt`. REJECT (recorded exception).
- **C7** — `max_nesting` on parse. REJECT (behavior change; digest authenticates first).
- **C8** — direct-assign from transaction block. REJECT (return-contract coupling, zero gain).
- **C9** — helper renames. REJECT (churn).

BEFORE gates: `ruby -c` Syntax OK; `rubocop --cache false` → 0 offenses. Focused coverage: no test file names `ThreadPurge` directly; behavior is exercised through the adapter facade by `test/sqlite_deletion_test.rb` (idempotent receipt, Invariant 54 comms-row deletion). Extra gate: `test/sqlite_boundary_source_audit_test.rb`.

Verdict: COMMITTED-CANDIDATES
Applied: C1 (moved public `receipt` beside `call`, removed mid-class `public` re-open + redundant second `private`); C2 (named `row.fetch(3)` local `purge_after_ms` in `ensure_purgeable!`, guard rewritten in multiline form after modifier-form line-length offense — behavior identical)
Rejected: C3–C9 with reasons above
Diff stat: 1 file changed, 20 insertions(+), 21 deletions(-)
Commands:
- `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/thread_purge.rb` → Syntax OK (before and after)
- `rubocop --cache false gems/tamoz-sqlite/lib/tamoz/sqlite/thread_purge.rb` → BEFORE 0 offenses; AFTER 0 offenses (one transient IfUnlessModifier/LineLength pair from my own C2 line was fixed, not suppressed)
- `ruby -Itest test/sqlite_deletion_test.rb` → 3 runs, 19 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/sqlite_boundary_source_audit_test.rb` (extra gate) → 7 runs, 55 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
