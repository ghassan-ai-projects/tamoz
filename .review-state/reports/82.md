# Combined pass — rank 82 gems/tamoz-core/lib/tamoz/circuit/registry.rb
Verdict: CLEAN
Applied: none | Rejected: C1 (Condition#initialize is a ~32-raw-line guard ladder, over the 20-normal prose ceiling — but it is ONE thing: a flat, uniform-abstraction validation ladder mirroring the DR-2 §3 constraint table, and the enforced gate (rubocop Metrics/MethodLength Max: 20) already passes it with 0 offenses; extracting enforce_* helpers would insert indirection between each frozen ConfigurationError message and its condition, trading local auditability of byte-frozen error text for a metric the toolchain blesses — owner directive: simple solution over machinery); C2 (Scope#conditions_for does three selects over conditions — a partition-based rewrite is provably equivalent but pure churn on a 13-line method whose arbitration contract (specific wins → catch-all fallback → rates always observe) is documented inline at the site and consumed by Record#with_failure; zero behavioral gain, real drift risk next to untouchable registration semantics); C3 (the `budget_exceeded` rate Condition literal appears in both RULE_TARGET and SCHEDULE — hoisting a shared constant would couple two independent scope registrations; the file header declares the scope table "verbatim" from DR-2 §3, so row-level duplication is deliberate transcription, not smell); C4 (Registry.fetch passes through Scope instances, so the name slightly under-describes a resolve/coerce seam — rename to `resolve` touches cross-gem public API: record.rb ×3, evidence.rb ×2, circuit.rb ×2, tamoz-sqlite/circuit_store.rb ×2 all call fetch; API is untouchable); C5 (Condition#with_threshold lacks the integer>=1 validation its Scope counterpart performs — adding it creates a new raise path, a public behavior change; asymmetry is by design: Scope#with_threshold is the validated rebinding entry point); C6 (`consumes?` could reuse `specific?` as `!specific? || failure_kinds.include?(...)` — micro-churn, no intent gain));
Diff stat: (empty — file untouched; `git diff --stat` and `git status --porcelain` clean for the owned path)
Commands: ruby -c gems/tamoz-core/lib/tamoz/circuit/registry.rb → Syntax OK · rubocop --cache false gems/tamoz-core/lib/tamoz/circuit/registry.rb → BEFORE 0 offenses / AFTER 0 offenses (1 file inspected) · ruby -Itest test/circuit_record_test.rb → 13 runs, 25 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Candidate list (Phase 1 audit, written before any edit)

Audit lens: (1) names state intent, (2) short/one thing, (3) one abstraction level,
(4) entry points read as a small DSL, (5) step-down. Grounded in docs/CODING_STANDARD.md
§3/§4/§6/§11 and the Q6 ceilings (.rubocop.yml).

- Naming sweep (rule 1): CLEAN. Predicates end `?` (`specific?`, `consumes?`,
  `observes_every_outcome?`, `provisional?`); no get_/set_; render_/parse_,
  encode_/decode_, build_/normalize_/resolve_ vocabulary not applicable here;
  finder `condition(id)` and `consecutive_condition` state intent exactly.
- Length (rule 2): only `Condition#initialize` exceeds 20 raw lines (~32 incl.
  kwargs signature) → candidate C1 above, rejected.
- Abstraction level (rule 3): every method holds one level; guard ladders are
  uniform; no mixed levels found.
- DSL (rule 4): module surface is `fetch / scope_types / provisional?` plus the
  frozen SERVER/RULE_TARGET/SCHEDULE/EGRESS/SCOPES constants — reads as a small
  registry DSL.
- Step-down (rule 5): holds everywhere; `conditions_for` decomposes into named
  selections that map to the documented arbitration order.
- Comments: load-bearing spec anchors (DR-2/SELF_HEALING_DESIGN/SCHEDULER_DESIGN/P17
  section mappings) — §11-compliant "why", not narration; left untouched.

Blast radius proven before deciding: repo-wide grep shows `fetch` called from
gems/tamoz-core (record.rb ×3, evidence.rb ×2, circuit.rb ×2) and tamoz-sqlite
(circuit_store.rb ×2); `conditions_for`/`observes_every_outcome?` from record.rb;
direct construction in test/circuit_record_test.rb. No send/__send__/public_send
dynamic dispatch on these seams was found.
