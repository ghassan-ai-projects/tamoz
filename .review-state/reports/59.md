# Combined pass — rank 59 gems/tamoz-stream/lib/tamoz/stream/evidence_client.rb

Verdict: COMMITTED-CANDIDATES

## Phase 1 — ANALYZE (candidate list, written before any edit)

Audit basis: five loop criteria (intent-naming · ≤20/30 length · one abstraction level ·
public DSL · step-down) plus CODING_STANDARD §3/§3.1, §4, §5, §6, §11. File is 355 lines,
three classes (`EvidenceClient`, `EvidenceToolAdapter`, `EvidenceUnavailableAdapter`);
behavior constraints honored: error/message bytes, result/event key ordering
(`result_hash` key order, `EvidenceToolCall` field order), approval semantics, public API
untouched. Recorded cohesion exceptions (wire codecs) respected — no extraction attempted.

- C1 — RENAME private `result_hash(result, parsed)` → `build_result_hash(result, parsed)`
  (§3.1 Construction: "`build_*` returns a new value"; the current name states the return
  type, not the action). Single in-file caller (line 86); repo-wide grep finds no other
  occurrence of either name anywhere; no `send`/`__send__`/`public_send` in the gem's lib
  and zero references in the (excluded) direct test reach it. Pure rename.
- C2 — RENAME private `artifact_hash(artifact)` → `build_artifact_hash(artifact)` (same
  §3.1 rule; constructs the artifact sub-hash or nil). Single caller (line 255); repo-clean.
- C3 — RENAME private `timestamp(value)` → `build_timestamp(value)` (constructs
  `Google::Protobuf::Timestamp`; reads as a noun getter today). Single caller (line 160);
  the only same-named code is an unrelated private `timestamp` in `episode_stream.rb`
  (different class, not owned here).
- C4 — REJECT: extract/restructure `initialize` (26 lines, over the 20 normal ceiling).
  Inside the hard 30 ceiling; the flat validate+assign sequence's order IS behavior
  (first-error message precedence is pinned), and grouping adds indirection without an
  abstraction gain (§4 forbids splitting cohesive logic merely to satisfy a number).
- C5 — REJECT: repair pre-existing Style/GuardClause at line 105 (`require_channel!`).
  Converting both checks to guard clauses pushes the capability-token branch to ~126
  chars > the 120 LineLength cap — the file's two pre-existing offenses cannot be jointly
  fixed within a surgical diff; gate requires parity, not reduction.
- C6 — REJECT: repair pre-existing Layout/LineLength at line 136 (126/120,
  `bounded_tool_name!`). Predates this pass ("fix anything your lines introduce" — none
  introduced); fixing alone is style churn on an untouched method.
- C7 — REJECT: convert `result_hash`'s string-keyed return to a `Data.define` value (§5).
  That hash is exactly the opaque/wire-boundary payload handed across the adapter seam to
  the capability host — §5 permits hash-key protocols there — and reshaping it changes the
  public/cross-gem surface (untouchable).
- C8 — REJECT: unify `bounded_tool_name!` with `identity!`. They differ by the control-char
  check; adding `identity!`'s control-char rejection to tool names changes accepted inputs
  — a behavior change that cannot be proven preserving.
- C9 — REJECT: trim the class doc, `build_stub`, or `verify!` comment blocks. §11 blesses
  precisely these why-comments (token-custody model, channel-connect segfault invariant,
  replay/cross-result refusal invariant).
- C10 — REJECT: normalize `arguments: arguments` explicit form in `EvidenceToolAdapter#call`
  to shorthand — pure style churn, clean under rubocop either way.

## Phase 2 — APPLY

Applied: C1, C2, C3 (pure private-method renames, 2 lines each, all callers proven in-file)
Rejected: C4 (within hard ceiling; order-is-behavior); C5/C6 (pre-existing offenses, not jointly fixable surgically); C7 (wire-boundary hash sanctioned by §5; cross-gem API); C8 (behavior change); C9 (sanctioned why-comments); C10 (style churn)

## Gates

- Test lane note (gate rule 4): the direct tests for this file live in
  `test/stream_evidence_client_test.rb`, which belongs to the SERIAL_TESTS excluded lane
  (`stream_*_test`) per the brief's known-red list — NOT RUN. Coverage claim rests on:
  `ruby -c` syntax, uncached rubocop parity vs the recorded BEFORE count, and pure-rename
  discipline (every renamed symbol proven single-owner by repo-wide grep including
  `send`/`__send__`/`public_send`, with zero references in the excluded test itself).
- Commands:
  - `ruby -c <owned file>` → Syntax OK (before and after)
  - `rubocop --cache false <owned file>` → BEFORE: 2 offenses (both pre-existing:
    L105 Style/GuardClause, L136 Layout/LineLength) | AFTER: 2 offenses, identical set —
    parity held, nothing introduced
- Diff stat: see below

Diff stat:
```
 gems/tamoz-stream/lib/tamoz/stream/evidence_client.rb | 12 ++++++------
 1 file changed, 6 insertions(+), 6 deletions(-)
```
Net LOC delta: 0. Every hunk is one of the three renames (def + its single call site);
no formatting, comment, or ordering changes.

Commands:
- `ruby -c gems/tamoz-stream/lib/tamoz/stream/evidence_client.rb` → Syntax OK (before AND after)
- `rubocop --cache false gems/tamoz-stream/lib/tamoz/stream/evidence_client.rb` → BEFORE: 2
  offenses | AFTER: 2 offenses, byte-identical offense list (L105 Style/GuardClause,
  L136 Layout/LineLength) → count not increased, gate satisfied; both pre-date this pass
- Focused tests: NOT RUN per gate rule 4 — see Test lane note above (direct lane
  `stream_evidence_client_test` is SERIAL_TESTS-excluded; syntax + uncached-rubocop parity +
  proven pure renames are the evidence of safety for this pass)

Deviations: FLAG row only.

FLAG (owner decisions): `episode_stream.rb:152` carries its own private `timestamp(epoch_seconds)`
builder of the same protobuf concept; if C3's `build_*` vocabulary is adopted repo-wide for
timestamp construction, that file (not owned by rank 59) should follow.
