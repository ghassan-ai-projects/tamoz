# Fix report — rank 33 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_trace_recorder.rb
Applied: C1 (record! step-down: extracted `accepted_hook_fields` returning the frozen [point, operation, statement, attempt] tuple, `next_occurrence!` with the MAX_OCCURRENCES refusal, `build_event`; every statement kept in its current order so identical inputs raise the identical first error); C2 (finish step-down: extracted `ensure_trace_complete!`, `ensure_registry_stable!`, `derive_selectors` with the MAX_SELECTORS refusal beside derivation, `freeze_results!`; sequencing preserved — events dup.freeze before SelectorDeriver, manifest built before collections freeze, `@state = :finished` last); C3 (`validated_subject_body(normalized)` performs id/version/revision/tree validations then the dirty check last, returning the pre-digest body; normalize_subject reads shape → body → content_digest → DeepFreeze); C4 (`enforce_phase2_operation!` lifted out of initialize between ivar assignment and state init, position unchanged); C5 (`with_failure_tracking(wrap_prefix)` replaces the two identical rescue pairs in call/finish; prefixes passed verbatim including the trailing ": " so wrapped bytes and `cause:` are unchanged; the fresh-state early return in `call` stays outside the wrapper); C6 (renamed `exact_hash` → `validate_exact_hash`, def plus both call sites at normalize_scenario/normalize_subject)
 |  Rejected: C3-partial (the optional `boolean(value, name:)` twin — its sibling-family message would replace the pinned "subject dirty state must be boolean" bytes; bespoke check kept verbatim instead)
Diff stat: 1 file changed, 107 insertions(+), 83 deletions() — net +24 lines (543 → 567 LOC)
Commands: `ruby -c <owned>` before → Syntax OK; after → Syntax OK · `rubocop --cache false <owned>` BEFORE → 0 offenses ("1 file inspected, no offenses detected"); AFTER → 0 offenses (count did not increase; nothing suppressed) · `ruby -Itest test/sqlite_trace_recorder_test.rb` → 11 runs, 952 assertions, 0 failures, 0 errors, 0 skips (covers manifest build + canonical-digest verification, pinned error fragments, wrong-thread/failure paths)
Deviations: C1 amended — the two admission guards ("second operation" / MAX_EVENTS) stayed inline in `record!`: one extracted name would have to cover two distinct policies (protocol completeness vs event capacity), and `record!` lands at 18 non-blank lines either way; step-down order guard → hook acceptance → occurrence accounting preserved as pinned. No other test file references SQLiteTraceRecorder (grep-verified); sqlite_scenario_driver lane not run (known-red list). FLAG items recorded only, untouched: validator vocabulary fragmentation (`exact_hash` here vs `exact_hash!` in sqlite_scenario_registry/sqlite_trace_manifest_verifier vs `validate_exact_hash` in sqlite_selector_control) and `deeply_frozen?` triplication are cross-file owner decisions; `"manifest_version" => 1` literal next to `VERSION = 1` left as-is pending owner intent on schema/recorder version decoupling.
