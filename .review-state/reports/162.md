# Combined pass — rank 162 gems/tamoz-stream/lib/tamoz/stream/episode_worker.rb

## BEFORE state

- `ruby -c` → Syntax OK. `rubocop --cache false <file>` → **0 offenses**.
- Focused-test baselines (recorded before any edit):
  - `test/stream_episode_worker_test.rb` → 8 runs, 24 assertions, 0 failures, 0 errors
  - `test/p8_rollout_test.rb` → 8 runs, 38 assertions, 0 failures, 0 errors
    (invokes `validate_request!` via `send` — name is externally pinned by test)
  - `test/stream_episode_stream_test.rb` → 8 runs, **3 pre-existing failures**, 0 errors
    (known-red `stream_*_test` lane; after-comparison must match exactly)

Binding note honored: event ordering and retry semantics are pinned — method-level
naming/step-down only. Repo-wide grep (incl. send/__send__/public_send via literal-name
search) shows the only external reference to any private validator is p8_rollout_test's
`send(:validate_request!)`; that name is not touched.

## Candidates (Phase 1 — ANALYZE, written before any edit)

- C1 APPLY — Step-down + DRY: `validate_contract!` and `validate_request!` open with a
  byte-identical 5-line protocol-version check. Extract private
  `validate_protocol_version!(request)` (name follows repo vocabulary: cf.
  `situation_request.rb#validate_protocol_version!`, mcp `validate_protocol_version_shape!`),
  called first by both validators. Exception class (`GRPC::FailedPrecondition`), message
  bytes, and check order preserved exactly; private, in-file callers only.
- C2 APPLY — Naming: rename private `ensure_terminal(state)` → `assert_terminal!(state)`.
  §3.1: it checks an invariant this code owns whose failure is a runner bug → `assert_*`;
  "ensure" is outside the lexicon and misreads as synthesizing/forcing a terminal event,
  which the code never does (it raises `GRPC::Internal`). Single caller, in file
  (`validate_stream`, line 170); repo-wide grep proves no other references.
- C3 APPLY (with C2) — Comment accuracy: `validate_stream` header says "the terminal is
  forced when the runner forgets it", but nothing is forced — a missing terminal raises.
  §11: a doc describing behavior the code does not have is worse than none. Reword to
  "a missing terminal is a typed failure, not a synthesized event".
- C4 REJECT — Replace mutable `state` hash with an immutable value (§5). It is mutable
  iteration state threaded through an Enumerator by design, not a stable value shape;
  conversion touches pinned stream-validation machinery for zero behavioral gain.
- C5 REJECT — Extract handshake's inline empty-identity check into a helper. `handshake`
  is already ≤20 lines and reads linearly top-to-bottom; extraction adds indirection
  without fixing any rubric violation (churn).
- C6 REJECT — Rename `refuse_fixture_fallback!` to a strict §3.1 `enforce_*` spelling.
  The current name states intent plainly and matches its P8 comment; no competing second
  spelling exists nearby, so this is churn, not a lexicon repair.
- C7 REJECT — `initialize`: validate `provider_mode` from the local parameter instead of
  `@provider_mode`. Zero behavioral or readability difference (style churn).

## Verdict: COMMITTED-CANDIDATES

Applied: C1 (extract shared `validate_protocol_version!` from the two validators); C2 (rename private `ensure_terminal` → `assert_terminal!`, §3.1 assert_* = owned-invariant check whose failure is a bug); C3 (reword stale `validate_stream` header comment — nothing is forced/synthesized, a missing terminal raises)
Rejected: C4 (state hash → value object: mutable Enumerator iteration state by design, pinned stream machinery, zero gain); C5 (extract handshake identity check: method already ≤20 lines and linear, churn); C6 (`refuse_fixture_fallback!` → `enforce_*`: name already plain, no second-spelling conflict, churn); C7 (`initialize` local-vs-ivar guard: style churn)
Diff stat: 1 file changed, 15 insertions(+), 12 deletions() — net +3 lines

Commands:
- `ruby -c gems/tamoz-stream/lib/tamoz/stream/episode_worker.rb` → Syntax OK (before & after)
- `rubocop --cache false <file>` → 0 offenses BEFORE, 0 offenses AFTER (no increase)
- `ruby -Itest test/stream_episode_worker_test.rb` → BEFORE 8 runs/0 failures → AFTER 8 runs, 24 assertions, 0 failures, 0 errors
- `ruby -Itest test/p8_rollout_test.rb` → BEFORE 8 runs/0 failures → AFTER 8 runs, 38 assertions, 0 failures, 0 errors (exercises `send(:validate_request!)`)
- `ruby -Itest test/stream_episode_stream_test.rb` → pre-edit baseline hit the known-red lane's flakiness (8 runs, 3 failures, 25 assertions); post-edit AND a control run against pristine HEAD both give 8 runs, 76 assertions, 0 failures — HEAD control proves the earlier red was environmental, not caused by the edit

Deviations: none — event ordering, retry semantics, error/message bytes, wire contract, and public API untouched; all edits are method-level naming/step-down plus one comment-accuracy fix within the binding note.

FLAG (owner decisions): none

## Note for the orchestrator

`test/stream_episode_stream_test.rb` is nondeterministic in this environment (fixture
model endpoint): same tree produced 25 assertions/3 failures and 76 assertions/0 failures
on consecutive runs, and the HEAD control matched the edited-tree pass exactly. Do not
attribute its red/green flips to episode_worker changes without a HEAD control run.
