# Combined pass — rank 242 gems/tamoz-comms/lib/tamoz/comms/admission.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename `decision_disposition` → `callback_disposition`, def + sole in-file call site)
Rejected: C2 (hoisting `envelope.fetch('kind')` local — reek suppressions on the decision table are deliberate; pinned logic, zero intent gain); C3 (shared allowlist/pairing authorization predicate across command/text paths — beyond method-level scope, entangles two policy paths carrying distinct design-§7 rationale; decisions pinned); C4 (inline `authorized` temp — churn); C5 (kwargs for `Decision.new` — positional Data constructor idiomatic at 5 short fields)
Diff stat:  gems/tamoz-comms/lib/tamoz/comms/admission.rb | 4 ++-- (2 insertions(+), 2 deletions(-))
Commands:
- ruby -c gems/tamoz-comms/lib/tamoz/comms/admission.rb → Syntax OK
- rubocop --cache false --format simple <file> BEFORE → "no offenses detected" (0)
- rubocop --cache false --format simple <file> AFTER → "no offenses detected" (0, not increased)
- ruby -Itest test/comms_admission_test.rb → 15 runs, 51 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/comms_gateway_test.rb (exercises Admission.decide via Gateway) → 33 runs, 190 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE candidates (written before any edit)

Baseline: ruby -c OK; rubocop --cache false = 0 offenses (BEFORE). Branch verification-again.
Repo-wide grep (incl. send/__send__/public_send patterns, all file types): `decision_disposition`
is referenced ONLY inside the owned file (def line 124, call line 41). Other hits are worktree
copies (.worktrees/*) and generated .enola facts.jsonl — not callers.

- C1 APPLY — rename `decision_disposition` → `callback_disposition`. Inside `Admission.decide`
  the name reads as tautology ("the disposition of the decision"); every sibling disposition
  builder is named by its inbound kind (`command_disposition`, `text_disposition`,
  `request_disposition`), so the callback branch should read
  `return callback_disposition if envelope.fetch('kind') == 'callback'`. Method-level naming
  only, zero behavior change, sole caller proven in-file.
- C2 REJECT — hoist repeated `envelope.fetch('kind')` into a local in `decide`: the reek
  suppressions on this exact surface are deliberate ("the metric smells measure the decision
  table"); admit/refuse logic is pinned, and the hoist buys no intent.
- C3 REJECT — extract the allowlist/pairing authorization shared by `command_admission` and
  `text_disposition` into one predicate: beyond method-level scope, entangles two policy paths
  whose branches carry different design-§7 rationale comments; decisions are pinned.
- C4 REJECT — inline the `authorized` temp in `command_admission`: pure churn.
- C5 REJECT — convert `Decision.new(...)` positional construction to kwargs: Data constructors
  are idiomatic positional; churn with no readability gain at 5 short fields.

Method sizes all within Q6 ceilings (longest ≈18 code lines); step-down order holds; predicates
end in ?; no get_/set_ vocabulary; comments are §11 why-comments tied to design §5/§7/§9 — kept.
