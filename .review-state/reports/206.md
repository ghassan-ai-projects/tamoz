# Combined pass — rank 206 gems/tamoz-comms/lib/tamoz/comms/delivery.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (delete duplicated class-header rationale block — lines stating "the delivery is one validated value … splitting them would fragment the outbox row" restated lines 18–19 verbatim in substance; §11 bans narration that restates; comment-only, zero runtime effect)
Rejected: C2 (rewrap mangled multiline `unless Shapes.member?(operation, OPERATIONS)` at L143–144 — cosmetic only; single-line form measures 127 chars > line-length ceiling, so the wrapped alignment is already the compliant shape; pure churn with new-offense risk); C3 (decompose private `validate!` into per-field assert helpers à la decision_record.rb — recorded cohesion exception stands: repo-quality-audit names Delivery/SurfaceDescriptor persisted/wire value contracts whose whole remains together, orchestrator note scopes this file to method-level naming/step-down, and all 13 error-message bytes are pinned, so extraction is risk without structural gain); C4 (extract shared `wire_time` helper for `from_wire`'s expires_at parse mirroring decision_record.rb — same extraction-scope concern; the inline `wire['expires_at'] && Time.parse(...)` reads clearly and the deliberate `:reek:DuplicateMethodCall` suppression already covers it); C5 (naming audit of public DSL — no change needed: `build`/`wire`/`from_wire`/`ephemeral?` read as a small DSL per §3/§3.1, predicate ends `?`, no `get_`/`set_`, constants well-named)
Diff stat: `gems/tamoz-comms/lib/tamoz/comms/delivery.rb | 3 --- · 1 file changed, 3 deletions(-)` (net −3 LOC)
Commands: `ruby -c gems/tamoz-comms/lib/tamoz/comms/delivery.rb` → Syntax OK · `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase) · `ruby -Itest test/comms_values_test.rb` → 20 runs, 78 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/comms_gateway_test.rb` → 33 runs, 190 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Candidate list (written before any edit)

Audit basis: name-states-intent / ≤20-line methods / one abstraction level / DSL entry points /
step-down; CODING_STANDARD §3/§3.1 vocabulary, §4, §5, §6, §11. File context: frozen value
object; `wire` hash key order and `Canonical.hexdigest` identity inputs are receipt-pinned
(orchestrator note: method-level naming/step-down only). Baseline: `ruby -c` OK,
rubocop --cache false = 0 offenses BEFORE.

- **C1 — remove duplicated header rationale (L22–24).** Lines 18–19 ("The delivery's thirteen
  fields ARE the value; splitting them would fragment the row the store persists.") and L22–24
  ("The delivery is one validated value; the smells below are the per-field rule set … splitting
  them would fragment the outbox row.") make the identical why twice around the same cop-disable
  block. Keep the first statement plus the functional `rubocop:disable`/`:reek:` lines. §11:
  narration that restates is noise. Provable behavior-preserving (comments do not compile).
  Confidence high, diff −3 lines. APPLY.
- **C2 — rewrap L143–144 condition split.** The aligned continuation is ugly but valid and
  currently offense-free; single-line form is 127 chars (> ceiling), so no compliant improvement
  exists that isn't churn. REJECT.
- **C3 — step-down decomposition of `validate!`.** Sibling `decision_record.rb` does extract
  helpers, but this file carries an explicit, recorded cohesion exception (whole-value contract;
  docs/repo-quality-audit-2026-08-20) plus disabled complexity cops with a stated why, and every
  raise message byte is pinned by the brief. Net change would be ~50–60 lines against a ~60-line
  rail for a purely stylistic win. REJECT.
- **C4 — `from_wire` wire_time helper.** Same extraction concern at smaller scale; no reader
  confusion today. REJECT.
- **C5 — public DSL naming sweep.** `Delivery.build` / `.wire` / `.from_wire` / `#ephemeral?`
  conform to §3/§3.1; `KINDS`/`OPERATIONS`/`DIGEST_DOMAIN`/byte ceilings well-named; private
  bang `validate!` matches house convention. No rename candidates. CLEAN.
