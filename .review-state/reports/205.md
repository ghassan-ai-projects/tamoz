# Combined pass — rank 205 gems/tamoz-graph/lib/tamoz/graph/fork_executor.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (remove §4 boolean param `terminal` from private `append_checkpoint`; status derived as `frontier.empty? ? :completed : :running` inside, where the frozen frontier is already in scope — definition + sole call site changed atomically in this file); C2 (rename `checkpoint_attributes` → `build_checkpoint_attributes` — §3.1 construction verb, gem precedent `build_checkpoint`/`build_context`×4/`build_pools`); C3 (rename `execution_context` → `build_execution_context` — same verb, and it ends the shadowing against `execution.context`, which the method body itself reads)
Rejected: C4 (collapse dual-key `update` if/elsif/else to a `fetch(:update, {})` ternary → proven equivalent but trades visible case analysis for fetch-on-nil subtlety; method is 17 lines < ceiling and the explicit branches mirror the `checkpoint_id` string/symbol tolerance two lines above); C5 (dedup `continue_after_claim` with its near-twin in durable_request_executor.rb → cross-file edit outside ownership and the twins genuinely differ (`mark_request_running: request.status == :claimed` vs `false`), so a shared home touches a pinned replay branch); C6 (split `build_checkpoint_attributes`, body exactly 20 lines → declarative kwargs literal for one protocol operation; §4 limits are diagnostic and never split cohesive logic; class-level reek exception on record); C7 (drop now-vacuous `:reek:ControlParameter` from the class annotation after C1 → reek is not a runnable gate here and `mark_request_running:` still reads control-ish at the Compiled boundary; unverifiable suppression edits risk an unrunnable check); C8 (rename `reset_frontier`/`fork_outcome`/`fork_state` → names state intent and the `fork_` family reads as one coherent protocol)
Diff stat: 1 file changed, 7 insertions(+), 7 deletions(+)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → no offenses (BEFORE 0, unchanged) · ruby -Itest test/sqlite_request_inbox_test.rb → 8 runs, 46 assertions, 0 failures, 0 errors, 0 skips (fork-request E2E incl. `test_fork_binds_new_execution_to_an_explicit_historical_checkpoint`) · ruby -Itest test/graph_identity_test.rb → 8 runs, 30 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/graph_history_test.rb → 7 runs, 30 assertions, 0 failures, 0 errors, 0 skips
Deviations: REDO of rank 205 after a prior agent died mid-edit; its partial diff was already reverted — file verified pristine vs 8c1edd5 (`git status`) before this pass. This audit was performed independently; it converges with the abandoned draft only on C1–C3.
FLAG (owner decisions): none

## Audit detail (Phase 1 candidate list, written before any edit)

Shape: 170 lines, one public entry point (`execute`, reading as the fork protocol: claim-continue
→ fork source/update → state → frontier → checkpoint → snapshot-or-run), nine private helpers
each ≤20 normal lines. No declarative-data exemption applies.

Caller proof (repo-wide grep incl. send/__send__/public_send, `.worktrees/` excluded):
- `ForkExecutor` is constructed only at `compiled.rb:221` (`fork_with_writer`) and reached only
  via public `execute`; `durable_request_executor.rb:124` __send__s `fork_with_writer`. No
  external `__send__` reaches ForkExecutor privates.
- Private `append_checkpoint(execution, source, state, frontier, terminal)`: sole call site is
  line 28 of this same file. All other repo hits are distinct methods with kwargs signatures
  (`Compiled#append_checkpoint`, `StateOperations#append_checkpoint`, sqlite writers/stores);
  `test/agent_session_test.rb:436` __send__s the Compiled kwargs form, not this one. → C1's
  atomic definition+call-site edit is fully contained in the owned file.
- Renames collision-free: `execution_context` occurs nowhere else in the main tree;
  other `checkpoint_attributes` defs (tamoz-evals runtime, graph_checkpoint_codec_test) are
  unrelated classes' own methods, unreachable by bare-name dispatch from here.

C1 equivalence proof: in `execute`, `frontier = reset_frontier(source)` (frozen result)
immediately precedes `terminal = frontier.empty?` and the call; nothing mutates or reassigns
`frontier` between, so `status = terminal ? :completed : :running ≡ status = frontier.empty? ?
:completed : :running` inside the callee. Status bytes, event ordering, checkpoint attributes,
and the early `compiled.snapshot(checkpoint)` return are untouched.

Untouchable-by-brief items left byte-identical: error messages ('fork payload must be a Hash',
'fork source checkpoint does not exist'), `%w[fork update]` path / `task_id: 'fork.update'`
event identity, existence-before-compatibility raise order, remaining_steps clamp, and all
Compiled-private seams reached via `__send__` (`continue_with_writer`, `compatible!`,
`build_context`, `bind_writer_context`, `append_checkpoint`).
