# Fix report — rank 29 gems/tamoz-graph/lib/tamoz/graph/executor.rb
Applied: C1 (step-down of `run`'s terminals: extracted move-only `append_failed_checkpoint` shared by the errors branch and `non_interactive_interrupt`, plus `append_paused_checkpoint`; hoisted the advance-branch conditional transition into `completion_transition` BETWEEN `writer.check!` and the append — exact evaluation order kept; DR-2 append→emit→RunResult order intact at every terminal) — with one modification: `RunResult` construction stays at the call sites because the two failed sites report different interrupts expressions (errors branch: appended checkpoint's `failed.interrupts`; non-interactive: the in-memory `interruptions` array; the sqlite writer rehydrates checkpoints from rows, so folding them could observably change `RunResult.interrupts`); C2 (shadowing local `cancelled` → `pool_cancelled` in `run`, extended to the identical shadowing local inside the classifier); C3 (`classify` → `classify_pool_results`, local `expected` → `scheduled_by_id`; grep proof: only def + one call site in-file, zero `.send(`/`__send__`/`const_get`/`instance_eval` references to any of these names repo-wide; remaining `classify` hits are unrelated Healing/evals classes) | Rejected: C4 sub-proposal (shared event-payload builder for the two 3-key emit hashes — indirection without removed complexity); FLAG (`validate_outcome!` rename — FROZEN, pinned via `__send__` at test/graph_interrupt_test.rb:131; owner decision)
C4 main body applied (extracted `invoke_node(task, checkpoint, task_context)` covering input-resolution → node call → normalize → Outcome; statement order verbatim: `task_start` emit → invoke → `task_end` emit → `append_writes` → outcome).
Diff stat: gems/tamoz-graph/lib/tamoz/graph/executor.rb | 186 +++++++++++++++++---------- 1 file changed, 117 insertions(+), 69 deletions(-) (568 → 616 LOC, net +48)
Commands:
- ruby -c executor.rb — before AND after: Syntax OK
- rubocop --cache false executor.rb — BEFORE: 0 offenses / AFTER: 0 offenses (no increase; nothing suppressed)
- ruby -Itest test/graph_execution_test.rb — 11 runs, 43 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/graph_interrupt_test.rb — 9 runs, 59 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/graph_determinism_property_test.rb — 2 runs, 480 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/graph_subgraph_test.rb — 4 runs, 28 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/graph_durable_request_executor_test.rb — 2 runs, 5 assertions, 0 failures, 0 errors, 0 skips
Deviations:
- enola baseline/diff not re-pinned: no MCP tool access in this session. The change is intra-file private-scope refactoring of a `private_constant` class; no cross-gem or public surface moved.
- Working tree also carries modified files owned by other agents (gems/tamoz-agent-profile/lib/tamoz/agent/profile.rb, gems/tamoz-evals/.../openclaw_comms_fixture.rb, gems/tamoz-evals/.../sqlite_convergence_probe.rb). Not mine, untouched per the ownership contract — orchestrator should attribute them elsewhere before committing.
