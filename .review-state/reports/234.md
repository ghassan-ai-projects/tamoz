# Combined pass — rank 234 gems/tamoz-sqlite/lib/tamoz/sqlite/connection_pool.rb
Verdict: CLEAN
Applied: (none) | Rejected: C1 (self.open split — under §4 hard ceiling, linear
construction→configure→verify reads step-down already, extraction adds indirection for no
defect; migrator.rb:1384 consumes this exact entry point); C2 (rename `ensure_process!`
to a §3.1 checking verb — `ensure_*` is an established repo-wide spelling, 34 `def ensure_*`
sites; renaming here would introduce the second-spelling drift §3.1 forbids); C3 (drop `&.`
in initialize's rescue cleanup `@available&.each` — load-bearing: `String(path)` on line 9
can raise before `@available = []`, so removing `&.` would swap the original TypeError for
NoMethodError, a behavior change); C4 (remove unused-public readers `path`/`limits`/`pid` —
public/cross-gem API is untouchable per brief, `pid` is read in-file by checkin, narrowing
is an API decision not a refactor); C5 (rewrite `database&.close unless database&.closed?`
in self.open's rescue — equivalent form is pure churn inside a recorded §7 rescue-Exception
site with zero behavioral gain); C6 (any rewrite of checkout/checkin/close synchronized
blocks, e.g. the `return false if @closed` non-local return or closing outside the mutex in
checkin — binding rail pins checkout/checkin ordering and mutex discipline byte-stable;
tail risk with no defect to fix).
Candidate notes (Phase 1 audit): both `rescue Exception` sites are the §7-recorded admitted
pattern verbatim — release resource, re-raise, inline `# rubocop:disable Lint/RescueException`
(naming Tamoz::Pool and this class explicitly). Public DSL (`open`, `with_connection`,
`close`, `closed?`, `stats`) matches §3/§3.1: `with_connection` is a true §3.1-scope
yield-and-restore; predicates end `?`; kwargs throughout; error identity/message bytes
(ClosedError, BusyError, ConfigurationError strings) left untouched. Longest method
(`self.open`) ≈28 lines incl. rescue, under the 30 hard ceiling. Repo-wide grep (incl.
send/__send__/public_send) shows no dynamic dispatch to private checkout/checkin and no
external caller of the pool's attr_readers.
Diff stat: (no changes — owned file byte-identical to HEAD)
Commands: ruby -c connection_pool.rb → Syntax OK · rubocop --cache false → 0 offenses
(BEFORE 0 / AFTER 0, file unedited) · ruby -Itest test/sqlite_kernel_test.rb → 8 runs,
61 assertions, 0 failures, 0 errors, 0 skips (this file exercises the real pool:
with_connection, busy checkout, fork fail-closed, stats) · ruby -Itest
test/sqlite_boundary_source_audit_test.rb → 7 runs, 55 assertions, 0 failures, 0 errors,
0 skips (extra tamoz-sqlite rail; connection_pool.rb is not among the audit's SOURCE_PATHS)
Deviations: none
FLAG (owner decisions): none
