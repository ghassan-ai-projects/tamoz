# Combined pass — rank 104 gems/tamoz-evals/lib/tamoz/evals/harness/heuristic_corpus.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (dedupe encoding-critical corpus write into private `write_corpus_file`; three verbatim copies of `CanonicalJSON.dump` + "\n" + `encoding: Encoding::UTF_8` + `File.chmod(FILE_MODE, …)` collapsed to one — bytes, modes, return values identical); | Rejected: C2 (create's tmpdir leak on raise between mktmpdir and begin block — rare-case handling owner directive forbids pre-emptively, error-path semantics change); C3 (`File::SEPARATOR` → `File.join(…, "")` in outside_grant? — byte-equivalent churn, zero gain); C4 (resolver lambda each+return → find/filter_map — equivalent flow, not worth touching a security-annotated resolver); C5 (zip build's 3×mkdir_p + 3×chmod — obscures the explicit mode posture that mirrors secure?); C6 (rename evaluator_report_resolver → build_ per §3.1 — public gem API untouchable); C7 (any corpus-constant edit — digest-recorded provenance, recorded outputs byte-identical)
Diff stat:  gems/tamoz-evals/lib/tamoz/evals/harness/heuristic_corpus.rb | 18 ++++++++++--------  (1 file changed, 10 insertions(+), 8 deletions(-))
Commands:
- `ruby -c gems/tamoz-evals/lib/tamoz/evals/harness/heuristic_corpus.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- A/B byte-identity proof: HEAD version reloaded over edited class; snapshot hash of every file under the corpus root (relative path + octal mode + content SHA-256) plus train_digest/holdout_digest plus a write_evaluator_output artifact → BEFORE == AFTER == 24f249c6f66b1ec780b5ffbbb66291565e743cfb1ab3265ff6b128af5ecf5964
- `grep -l "HeuristicCorpus" test/*.rb | head` → test/improvement_candidate_test.rb
- `ruby -Itest test/improvement_candidate_test.rb` → 14 runs, 284 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): docs/code-quality-baseline.json records 9 offenses for this file while live `rubocop --cache false` reports 0 — stale baseline entry, outside my ownership, left untouched.

## Phase 1 — ANALYZE detail

File shape: ~180 lines of live code (sandbox scaffolding, capability assertions,
digests, evaluator output/resolver) + ~84 lines of declarative corpus constants.
NOT mostly data → full audit applies. Constants byte-frozen (they feed
train_digest/holdout_digest, recorded provenance). BEFORE tree clean on branch
verification-again. Callers (repo-wide grep): evals.rb require +
test/improvement_candidate_test.rb only; its one public_send targets
Improvement::Provenance axes, not this class.

Five-principle sweep: all methods ≤12 lines ✓ · names state intent, predicates end ?
(`secure?`, `outside_grant?`) ✓ · public surface reads as a small DSL
(create/build/secure?/…/cleanup) ✓ · step-down holds (public → private
write_*/digest_of; now tighter after C1) ✓ · class-level "why" comment (holdout
isolation as capability restriction) is exactly the §11 kind that earns its place —
untouched.
