# Fix report — rank 15 gems/tamoz-graph/lib/tamoz/graph/checkpoint_codec.rb

Applied: C1 (`load_outcome` step-down: extracted private `outcome_writes_in_order!`, `decode_pending_channel_write`, `decode_pending_routes_write`; pure move, statement order + every raise/message byte preserved); C2 (`load` step-down: extracted `decoded_attributes(wire, strict:)` with the 14-field hash verbatim + `attributes[:state]` line; first-failure semantics unchanged); C3 (named wire tags `OUTCOME_FORMAT`/`ROUTES_FORMAT`/`REQUEST_FORMAT` with exact existing literals, used at all five dump/load sites, added to `private_constant`; identical bytes); C4 (private `enforce_strictly_ascending!(current, previous, message)` returning current, replacing five inline copies in `decode_pending`/`decode_interrupts`/`decode_attempts`/`decode_resume_values`; same condition, class, and per-site message, check position unchanged at each site); C5 (renamed `validate_input` → `validate_input_bytes`, matching `StateCodec#validate_input_bytes`; sole caller line 97 verified repo-wide via grep — no test/send references); C6 (renamed `canonical_value_bytes` → `verify_canonical_value_bytes`, established repo verify_-verb family; callers lines 70/301 both in-file) | Rejected: none (all six candidates survived review; FLAG items untouched — `validate_identity:` boolean is owner-only cross-gem surface)

Diff stat: gems/tamoz-graph/lib/tamoz/graph/checkpoint_codec.rb | 191 ++++++++++++--------- — 1 file changed, 111 insertions(+), 80 deletions(+)

Commands:
- `ruby -c gems/tamoz-graph/lib/tamoz/graph/checkpoint_codec.rb` — before: Syntax OK; after: Syntax OK — PASS
- `rubocop --cache false gems/tamoz-graph/lib/tamoz/graph/checkpoint_codec.rb` — before: 1 file inspected, no offenses detected (0); after: 1 file inspected, no offenses detected (0) — count NOT increased, no offenses introduced or suppressed — PASS
- `ruby -Itest test/graph_checkpoint_codec_test.rb` — 4 runs, 22 assertions, 0 failures, 0 errors, 0 skips — PASS
- `ruby -Itest test/sqlite_checkpoint_seams_test.rb` — 7 runs, 14 assertions, 0 failures, 0 errors, 0 skips — PASS
- `ruby -Itest test/sqlite_checkpoint_test.rb` — 6 runs, 23 assertions, 0 failures, 0 errors, 0 skips — PASS

Deviations:
- C1 signature adapted: candidate proposed `decode_pending_channel_write(write, update)`, but that drops the "pending channel write appears after routes" guard because `routes_seen` is not derivable from `update` (a decoded `routes` value can legitimately be nil). Implemented as `(write, update, routes_seen)` reading the flag; fold state still owned by `load_outcome`.
- C6 leaves two comment-only mentions of the old name in `test/agent_non_ascii_session_test.rb` (~184, ~244). Tests are read-only under my ownership contract, so I did not touch them; they are not callers and all tests pass. Orchestrator may refresh those comments or revert C6 if the staleness is unacceptable.
- Net +31 LOC (719 → 750): new named methods add def/end scaffolding while removing duplicated logic; largest method bodies shrank (`load_outcome` body roughly halved).
