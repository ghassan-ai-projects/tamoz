# Combined pass — rank 109 gems/tamoz-agent-session/lib/tamoz/agent/session_plan_attempt.rb
Verdict: COMMITTED-CANDIDATES

Applied: C2 (rename `increment` → `next_ambiguity` — §3 "names say what they return"; pure
computation on immutable Data, both callers in-file); C3 (rename `plan_record` →
`build_plan_record` — §3.1 construction vocabulary, removes near-anagram collision with
`record_plan`; one in-file caller); C4 (step-down: hoist tool-surface merge out of `semantic_call`
into `visible_tool_descriptions`, dropping the Metrics/AbcSize disable pair — fixes the real
one-level-of-abstraction mix; kwarg evaluation and effect/call_index ordering unchanged).

Rejected: C1 (`details.plan_digest` in `parse_review` rescue is a latent NoMethodError — see FLAG;
fix is behavior-changing on an error path, out of scope for a behavior-preserving pass);
C5 (consolidate duplicated protocol-review builds across the two rescues — differing review_id /
digest source / rationale bytes; wire bytes untouchable, drift risk exceeds gain); C6 (keyword-ize
`Acceptance`/`Clarification` positional constructors — lives in session_plan_outcomes.rb, not owned,
cross-file API); C7 (default `result`'s `feedback:` kwarg — four explicit call sites read fine,
pure churn). Also verified untouched by design: structural review records its accept/revise row
before the emptiness check (event-ordering critical), and attempt/effect ordering (`attempt*2`,
`(attempt*2)+1`) is preserved.

Diff stat: 1 file changed, 16 insertions(+), 14 deletions(-) — net +2 LOC

Commands:
- ruby -c gems/tamoz-agent-session/lib/tamoz/agent/session_plan_attempt.rb → Syntax OK
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/agent_runtime_test.rb → 11 runs, 54 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_digest_resolution_test.rb → 17 runs, 95 assertions, 0 failures, 0 errors,
  0 skips
- No test names SessionPlanAttempt directly; coverage is via the deliberation loop
  (runtime/digest-resolution suites above). The semantic-review parse-failure lane has no test.

Deviations: none. Net change well under the 60-line cap; no suppressions added (one pair removed).

FLAG (owner decisions): C1 — session_plan_attempt.rb:204 (`parse_review` rescue) reads
`details.plan_digest`, but `SessionPlanAttempt::Details` defines no such member (12 members listed
above; never reopened; sole construction at session_deliberation.rb:119 passes exactly those). Any
malformed semantic-review document reaches this rescue via `Deliberation.parse_review` raising
ProtocolError and then crashes with NoMethodError instead of recording the `.semantic` protocol
review ("the reviewer did not return a usable review document") and returning revise feedback.
Probable fix: pass `plan_data.plan_digest` into `parse_review`. Left unfixed: error-path behavior
change, outside this pass's behavior-preserving mandate; lane currently untested.
