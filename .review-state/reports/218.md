# Combined pass — rank 218 gems/tamoz-agent-cli/lib/tamoz/agent/cli_comms_doctor.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (doctor_surface: bind the token check to a local `token_check` so the early return no longer reads through `checks.last.last`); C2 (comms_doctor: rename OptionParser block param `value` → `parser`, the object is a parser, not a value)
Rejected: C3 (extract `collect_doctor_checks(options)` from comms_doctor's begin/rescue — churn; the branching is the recorded rubocop-disable rationale and the exit-code derivation at line 52 is pinned); C4 (drop unused `_options` from comms_doctor_bootstrap — zero-gain signature churn, caller stays symmetric with sibling commands); C5 (simplify poller_ok? guard / unify state[] vs fetch — nil-check on poller_expires_at_ms is deliberate (0 ms is a valid timestamp) and lease semantics are behavior-pinned); C6 (restructure bot-id if/else in doctor_surface — message bytes are pinned, no gain); C7 (tls_ok? respond_to?(:origin) dispatch — fixture-client seam documented in-file, reek suppression already recorded); C8 (method lengths of comms_doctor / doctor_surface — inside the recorded :reek:/rubocop-disable exception, method-level only, no extraction)
Diff stat: 1 file changed, 8 insertions(+), 8 deletions(+) — net 0 LOC
Commands: ruby -c <file> = Syntax OK · rubocop --cache false <file> BEFORE = 0 offenses · rubocop AFTER = 0 offenses · ruby -Itest test/comms_cli_test.rb = 12 runs, 60 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 1 candidate audit (written before any edit)

File: 156 lines, module mixin (`include CLICommsShared`), one public entry point
`comms_doctor(options, argv)` consumed by `cli_comms_commands.rb:28`; every other method is
private with in-file callers only (repo-wide grep incl. send/__send__/public_send patterns:
doctor_surface, render_doctor, authenticated_identity, credential_present?, tls_ok?,
webhook_ok?, poller_ok?, comms_doctor_bootstrap appear nowhere else; `render_doctor_outcome`
in cli_worker_commands.rb is a different method).

- C1 · naming/intent · doctor_surface L62–63: `return checks unless checks.last.last == true`
  forces the reader to trace what was appended last. Bind the pair to `token_check`.
  Behavior-preserving: credential_present? is a pure read of @env/descriptor, evaluated once
  either way. → APPLY.
- C2 · naming/intent · comms_doctor L28–35: OptionParser block param named `value` misstates
  the object. Rename to `parser`. Block-local, zero blast radius. → APPLY.
- C3 · step-down · extract the begin/with_comms_runtime/rescue into collect_doctor_checks.
  → REJECT: pinned exit-code derivation + recorded disable rationale; extraction adds a seam
  for nothing.
- C4 · signature · drop `_options` param of comms_doctor_bootstrap. → REJECT: churn.
- C5 · clarity · poller_ok? guard mixing state[] and fetch, `(Time.now.utc.to_r * 1000).to_i`.
  → REJECT: boundary semantics pinned; any "simplification" risks ms-boundary behavior for
  zero readability gain.
- C6 · bot-id branch bytes. → REJECT: output bytes pinned.
- C7 · tls_ok? manual dispatch. → REJECT: documented fixture seam, suppression recorded.
- C8 · method length ceilings under recorded exception. → RESPECT EXCEPTION, no extraction.

Behavior pins honored: error/message bytes, exit codes (1 on any non-true detail), check
ordering (token → adapter → tls → token validity → bot id → webhook → poller), bootstrap
never persists the id.
