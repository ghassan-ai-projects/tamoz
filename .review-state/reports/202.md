# Combined pass — rank 202 gems/tamoz-otel/lib/tamoz/otel/http_exporter.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (open: drop prologue resets duplicated by the rescue clause — rescue owns the failure reset, end state provably identical on success and on any StandardError; pinned by test_http_exporter_clears_state_after_credential_failure); C2 (otel_trace_id/otel_span_id: rename parameter `value` → `raw_id`, which shadowed the private `value` helper name)
Rejected: C3 (reuse MAX_RESPONSE_BODY_BYTES for the request-body cap at export: request and response caps are distinct limits that coincidentally equal 16 MiB, constant is named for responses, and it sits inside the pinned export ordering); C4 (export is ~48 lines > hard 30: extraction rejected — export/error ordering pinned per binding, wire-codec cohesion exception allows method-level only); C5 (rename private resource_spans → build_ vocabulary: referenced by name via send in test/otel_test.rb which I do not own, and the name mirrors the OTLP wire term resourceSpans); C6 (close(deadline_ms:) ignores its kwarg: public/cross-gem API untouchable); C7 (rename tolerant lookup helper value(document, key): reads fine at every call site, rename is churn — the real confusion was the shadowed parameter, fixed by C2)
Diff stat: 1 file changed, 8 insertions(+), 11 deletions(-) (net -3 lines)
Commands: ruby -c → Syntax OK (BEFORE and AFTER) · rubocop --cache false → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/otel_test.rb → 6 runs, 17 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Candidate list (written before any edit)
- C1 APPLY — `open`: `@opened = false; @headers = {}; @descriptor = nil` in the prologue duplicate exactly what the `rescue StandardError` clause resets. Success path assigns all three anyway; failure path ends at identical ivar state via the rescue. No observer exists between statements.
- C2 APPLY — `otel_trace_id(value)` / `otel_span_id(value)`: parameter named `value` shadows the private helper method `value`, so inside these methods the identifier means two different things. Pure local rename, callers pass positionally.
- C3 REJECT — line 45 `body.bytesize > 16 * 1_024 * 1_024` vs `MAX_RESPONSE_BODY_BYTES`: same number today, different limits semantically; coupling them via the response-named constant would make a future response-cap change silently move the request cap. Inside pinned region.
- C4 REJECT — `export` mixes policy checks, encoding, connection setup, streaming read, response mapping (~48 lines). Extraction would reorder the pinned export/error sequence; binding restricts to method-level naming/step-down only.
- C5 REJECT — `resource_spans` rename: `test/otel_test.rb:38` calls it via `send(:resource_spans, ...)`; tests are outside my ownership. Name also mirrors OTLP `resourceSpans`.
- C6 REJECT — `close(deadline_ms:)` unused kwarg is part of the exporter interface (`include Tamoz::Observability::Exporter`); public API frozen.
- C7 REJECT — helper `value(document, key)` is terse but unambiguous at its ten call sites; renaming buys nothing behavioral.
