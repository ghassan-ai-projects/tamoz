# Combined pass — rank 75 gems/tamoz-agent-session/lib/tamoz/agent/session_steps.rb

## Phase 1 — ANALYZE (candidate list, recorded before any edit)

Baseline: ruby -c OK · rubocop --cache false = 0 offenses · repo-wide grep proves all
touched private helpers have zero external callers (no send/__send__/public_send hits;
only a doc-comment mention of `denied_update` in tamoz-agent runtime/step_execution.rb).

- C1 APPLY — extract pre-flight observation-budget guard out of
  `prepare_step_without_rescue` into `ensure_observation_headroom(effects, state, tool)`.
  Byte arithmetic sits below the orchestration level; mirrors the existing sibling
  `ensure_observation_budget`. Statement order inside the guard preserved exactly
  (`maximum_effect_output_bytes` before the bytes sum; raise still propagates uncaught,
  as before — prepare_step rescues only ToolArgumentError). Brings the 34-line body to
  the hard ceiling of 30 without splitting the cohesive gate sequence itself.
- C2 REJECT — inline the single-use `iteration` temp: churn inside an ordering-critical
  method, no clarity gain.
- C3 APPLY — dedupe the two nil-receipt `SessionEvidence::ToolFailure` constructions
  (`denied_update`, `argument_failure`) behind `record_tool_failure(state, step:, tool:,
  error_class:, reason:)`. `ToolFailure` is `Data.define` (kwargs, order-free); message
  bytes are built by the callers unchanged; `failed_update` keeps its own construction
  (non-nil receipt).
- C4 APPLY — unify `failed_receipt`/`successful_receipt` (identical except status string)
  into `build_effect_receipt(step, intent, outcome, status:)`; 'failed'/'succeeded'
  passed verbatim so record bytes are identical; matches §3.1 `build_*` lexicon, no name
  collision repo-wide.
- C5 REJECT — rename `prepare_step_without_rescue`: the suffix documents why the thin
  rescue wrapper exists (ADR §8 — decide must never run twice for one gated step); a
  prettier name loses that.
- C6 REJECT — split the `approval_descriptor` / `successful_observation` literal hashes:
  declarative wire payloads are already one thing; sub-builders add indirection, not
  step-down (§6 tolerates long declarative shapes).
- C7 REJECT — mixed Hash-vs-Preparation returns on the gate path (`prepared.is_a?(Hash)`):
  inherent to the node-update protocol consumed by SessionNodes/session.rb; cross-file
  API change, outside this file's ownership.
- C8 REJECT — `record && record['decision']` → `record&.[]('decision')`: style churn,
  less readable.
- C9 REJECT — thread `output` from `successful_update` into `successful_observation` to
  avoid one deterministic recompute: signature noise outweighs a free String() call.

## Phase 2 — APPLY (final)

Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract `ensure_observation_headroom`, exact statement order kept);
C3 (shared `record_tool_failure` for the two nil-receipt ToolFailure sites);
C4 (unified `build_effect_receipt(status:)` replacing failed/successful_receipt twins)
Rejected: C2 (churn in ordering-critical method); C5 (suffix documents the rescue
boundary); C6 (declarative wire payloads already one thing); C7 (cross-file update
protocol, not method-level); C8 (style churn); C9 (signature noise > free recompute)
Diff stat: 31 insertions(+), 42 deletions(-) — net −11 LOC, file 320 → 309 lines;
no reformatting outside the three candidate sites
Commands:
- ruby -c gems/tamoz-agent-session/lib/tamoz/agent/session_steps.rb → Syntax OK
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/agent_runtime_test.rb → 11 runs, 54 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_tool_error_recovery_test.rb → 13 runs, 116 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_session_test.rb → 11 runs, 72 assertions, 0 failures, 0 errors, 0 skips
Deviations: none. (Gross diff touches 73 lines because C4 removes two 16-line twin
bodies wholesale; net change is −11, far under the ~60 rail.)
FLAG (owner decisions): none
