# Combined pass — rank 118 gems/tamoz-sqlite/lib/tamoz/sqlite/checkpoint_store.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (`append_checkpoint` pure-delegation shim → anonymous forward-all `(...)`, matching the file's existing RequestInbox delegation idiom; committer signature verified identical, all kwargs required, no defaults to shift, no block/yield); C2 (`verify_existing_writes!` → `@appender.verify_pending_writes!(...)` same treatment; appender signature `(tx, lease:, execution_id:, task_id:, writes:)` verified identical); C3 (`validate_commit_arguments!` → `@committer.validate_commit_arguments!(...)`; committer signature verified identical); C4 (`validate_commit_mode!` → `@committer.validate_mode!(...)`; committer signature verified identical)
Rejected: C5 (split `open_writer`, 33 lines > 30 hard ceiling — cohesion exception forbids extraction, and its rescue/ensure cause-chaining invariant (`raise primary_error if primary_error`) is subtle and commented; cannot personally prove a restructure preserves `#cause` chaining in every path, so rejected per rail); C6 (drop trailing `public :active_execution_id!, :latest_checkpoint_in_transaction` — provably a visibility no-op but reads as an intentional cross-collaborator seam marker; removal is churn, zero readability win); C7 (rename `verify_existing_writes!`/`validate_commit_mode!` for §3 alignment with collaborator names — callers live in other files (checkpoint_appender.rb, checkpoint_committer.rb), which are read-only; public/cross-gem API untouchable); C8 (convert remaining short shims `latest`/`find`/`history`/`materialize`/`pending_outcomes`/`append_writes` to `(...)` — their explicit signatures carry the facade's DEFAULTS at the public boundary (verified byte-identical to collaborator defaults today); moving to forward-all shifts default authority into collaborators and weakens the self-describing DSL for marginal gain = churn); C9 (extract `prune`'s keep-validation into a named step — extraction barred by recorded transaction-core exception; current normalize→validate→delegate reads fine)
Diff stat: `gems/tamoz-sqlite/lib/tamoz/sqlite/checkpoint_store.rb | 60 ++---` — 1 file changed, 4 insertions(+), 56 deletions(-); net −52 LOC (247 → 195 lines)
Commands: `ruby -c checkpoint_store.rb` → Syntax OK · `rubocop --cache false <file>` → BEFORE 0 offenses, AFTER 0 offenses (no increase) · `ruby -Itest test/sqlite_checkpoint_seams_test.rb` → 7 runs, 14 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/sqlite_checkpoint_test.rb` → 6 runs, 23 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/graph_durable_runner_test.rb` → 19 runs, 39 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): `open_writer` reaches `adapter.__send__(:acquire_lease, ...)` and CheckpointCommitter likewise uses `adapter.__send__(:transaction/:backend_time/:validate_lease_in_transaction!)` — the adapter's persistence seam is private-API-dispatched across classes in this gem. Promoting those few methods on Tamoz::SQLite::Adapter would delete every ManualDispatch site; adapter-side, outside this file's ownership.
