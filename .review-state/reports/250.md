# Combined pass — rank 250 gems/tamoz-graph/lib/tamoz/graph/state_manager.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (drop dead kwarg `allow_managed:` from `normalize_update` — managed-channel guard now unconditional; simplified internal call in `initial`); C2 (step-down: extracted pure grouping loop out of `apply_writes` into private `group_writes(records)`, pure code motion) | Rejected: R3 (rename `initial`: all callers in read-only files outside ownership); R4 (remove boolean kwargs per §4: `allow_immutable:` is alive and pinned to initial-state semantics); R5 (collapse managed/immutable guards: churn, message bytes must stay verbatim); R6 (ternary in seed block: style churn); R7 (split reducer/conflict branch: mutating 5-param helper adds indirection, §4 forbids splitting for a number — residual ~27-line transaction core accepted and named); R8 (`Data.define` write records: cross-file seam redesign, not in-file)
Diff stat: gems/tamoz-graph/lib/tamoz/graph/state_manager.rb | 28 +++++++++++++---------- (16 insertions, 12 deletions; net +4 LOC — under the ~60-line stop rail)
Commands: ruby -c state_manager.rb → Syntax OK · rubocop --cache false → 1 file inspected, no offenses detected (BEFORE baseline also 0; not increased) · ruby -Itest test/graph_state_manager_test.rb → 4 runs, 16 assertions, 0 failures, 0 errors · ruby -Itest test/graph_immutable_recall_channels_test.rb → 1 runs, 2 assertions, 0 failures, 0 errors · ruby -Itest test/graph_checkpoint_codec_test.rb → 4 runs, 22 assertions, 0 failures, 0 errors
Deviations: apply_writes remains ~27 body lines after step-down (>20 normal, ≤30 hard) — durable reducer/conflict/managed-refresh core kept whole per recorded cohesion exception and §4 "never split merely to satisfy a number"; dead-kwarg drop follows the pinned state_operations precedent (snapshot API frozen, no signature removed that any caller uses)
FLAG (owner decisions): none

## Appendix — phase 1 candidate record (written before any edit)

BEFORE baseline: rubocop --cache false = 0 offenses (file carries Metrics debt exclusions in
`.rubocop_todo.yml`). 131 lines; `StateManager` is `private_constant`; every public entry point
has callers outside the owned file (initial_checkpoint_builder.rb:64, lifecycle_executor.rb:40,
fork_executor.rb:77,91, executor.rb:122,239,258,264, state_operations.rb:105,143,146).

- C1 proof: repo-wide grep shows `allow_managed` exists ONLY inside the owned file; zero passers
  anywhere (lib/test/apps); all external calls positional-only; `initial` passed explicit `false`
  equal to the default → unconditional guard is behavior-identical on every existing path.
- C2 shape: `group_writes(records)` returns `[grouped, writers]`; hash default-blocks, insertion
  order, error bytes ("conflicting writes to X from A, B"), and reducer call order untouched.
