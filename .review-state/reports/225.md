# Combined pass — rank 225 gems/tamoz-sqlite/lib/tamoz/sqlite/database_file.rb

Verdict: COMMITTED-CANDIDATES

## BEFORE state

- rubocop --cache false (owned file): 0 offenses.
- Callers (repo-wide grep): adapter.rb:90 (instantiates; prepare!/verify!/verify_sidecars!), backup.rb:25/164/165 (create_secure_temporary!/verify_backup!/secure_mode). No test references `DatabaseFile` directly; coverage arrives through Adapter (sqlite_kernel_test.rb, sqlite_backup_test.rb).
- boundary_source_audit parses only the 12 registered store/query paths — database_file.rb is not among them; gate run kept anyway per rail.

## Candidates (Phase 1 audit)

- C1 APPLY: `verify_sidecars!` and `create_secure_temporary!` repeat the same guard verbatim (`stat.file? && !stat.symlink? && stat.mode.nobits?(UNSAFE_MODE_MASK)`). Extract one private predicate `secure_regular_file?(stat)`; callers keep their own distinct error bytes ('SQLite sidecar permissions are unsafe' / 'backup temporary file is unsafe'). Behavior-preserving by expression identity and evaluation order.
- C2 APPLY: `create_database_file!` and `create_secure_temporary!` build the same exclusive flags (`WRONLY|CREAT|EXCL`, `|= NOFOLLOW` when defined). Extract private `exclusive_write_flags` so both security-critical creation sites provably stay identical. Plain def with explicit list, not endless (tamoz-sqlite rail).
- R1 REJECT: drop/rename `secure_mode` — public collaborator API consumed by backup.rb publish path; API untouchable.
- R2 REJECT: remove boolean kwarg `repair_permissions:` (§4 no-boolean-params) — pinned by the class contract as the explicit secure-file API; public shape mirrored by private `repair_permissions!`.
- R3 REJECT: dedupe `normalize_path` with `Backup#resolve_destination_path` (near-identical) — consolidation crosses file ownership; flagged for orchestrator only.
- R4 REJECT: rewrite `database&.close unless database&.closed?` in `verify_backup!`'s ensure — exact idiom already used in backup.rb#copy_backup; cosmetic churn.
- R5 REJECT: extract `%W[#{path}-wal #{path}-shm]` sidecar list — single use site; indirection without dedup value.
- R6 REJECT: any endless-method conversion of forwarders — forbidden by the tamoz-sqlite rail (boundary_source_audit break reverted in fc3878a).

## Applied / Rejected

Applied: C1 (shared `secure_regular_file?` guard for sidecars + backup temp); C2 (shared `exclusive_write_flags` for both exclusive creations).
Rejected: R1–R6 above.

Security posture untouched: every File.open keeps its block and EXCL/NOFOLLOW/0600 semantics; all error messages byte-identical; refusal order in prepare!/verify! unchanged.

## Diff stat

```
 .../tamoz-sqlite/lib/tamoz/sqlite/database_file.rb | 27 +++++++++++-----------
 1 file changed, 14 insertions(+), 13 deletions(-)
```

Net +1 line.

## Commands

- BEFORE rubocop: `rubocop --cache false gems/tamoz-sqlite/lib/tamoz/sqlite/database_file.rb` → 0 offenses.
- `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/database_file.rb` → Syntax OK.
- AFTER rubocop: `rubocop --cache false gems/tamoz-sqlite/lib/tamoz/sqlite/database_file.rb` → 0 offenses (no increase vs BEFORE).
- `ruby -Itest test/sqlite_kernel_test.rb` → 8 runs, 61 assertions, 0 failures, 0 errors, 0 skips.
- `ruby -Itest test/sqlite_backup_test.rb` → 3 runs, 16 assertions, 0 failures, 0 errors, 0 skips.
- Extra rail gate: `ruby -Itest test/sqlite_boundary_source_audit_test.rb` → 7 runs, 55 assertions, 0 failures, 0 errors, 0 skips.

## Deviations

none

## FLAG (owner decisions)

none
