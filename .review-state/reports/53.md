# Combined pass — rank 53 gems/tamoz-agent-kernel/lib/tamoz/agent/deliberation.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract inline 15-line `"argument_rule"` prose literal from `planning_input` into module constant `ARGUMENT_RULE` beside the other prompt constants — string proven byte-identical vs HEAD, method drops from ~38 body lines to ~19); C2 (`check_order_issues`: double-indexing `each_index.select { plan.steps[index].tool … }` ×2 → `each_with_index.filter_map`; identical integer arrays); C3 (`routing_prompt`: name `(toolbox.names + capability_descriptions.keys).uniq` once as `work_tools`, reuse for hash value + slice — identical JSON bytes); C4 (`verification_prompt`: verification_context guard aligned to the modifier form used by all three sibling builders) | Rejected: R1 (if/elsif→case/when in `planning_phase_instruction` — pure idiom churn, no payoff); R2 (split `placeholder_arguments?` keyed/universal into helpers — provable-equivalent but touches the D-8 false-positive-sensitive heuristic; minimal-touch wins); R3 (trim D-8/P10/P15-W/P16 comment narratives — review-ID traceability and §11 "why" live there); R4 (dedupe `parse_review`/`parse_verification` extraction — different shapes, machinery > payoff); R5 (restructure `step_issues` — inside hard ceiling at 22 lines, one rule per push); R6 (any rename — cross-gem surface, no name misstates intent)
Diff stat: 1 file changed, 34 insertions(+), 33 deletions(-)
Commands: BEFORE `ruby -c` → Syntax OK · BEFORE `rubocop --cache false <file>` → 0 offenses · AFTER `ruby -c` → Syntax OK · AFTER `rubocop --cache false <file>` → 0 offenses (no increase) · `ruby -Itest test/agent_digest_resolution_test.rb` → 17 runs, 95 assertions, 0 failures/errors · `ruby -Itest test/agent_skills_toolbox_test.rb` → 18 runs, 152 assertions, 0 failures/errors · `ruby -Itest test/agent_mcp_capability_source_test.rb` → 14 runs, 102 assertions, 0 failures/errors · `ruby -Itest test/agent_runtime_test.rb` → 11 runs, 54 assertions, 0 failures/errors
Deviations: gross churn 67 lines (34+/33−) nominally exceeds the ~60 rail while the NET delta is +1; kept all four candidates under the rail read on net change and flagging both numbers here for orchestrator judgment — every hunk maps to a named candidate, nothing drive-by. No dedicated deliberation-only test file exists; coverage came via the four focused files above (digest-pinning lane included and green). Untouched by design: `action_signature`/`canonicalize_apply_patch_arguments`/`canonical` (pinned digest inputs), all public signatures.
FLAG (owner decisions): none

## Phase 1 — ANALYZE candidates (written before any edit)

Audit basis: name/intent · ≤20 normal / ≤30 hard · one level of abstraction · DSL-ish entry points · step-down. File is pure deliberation logic (constants + module_function predicates/builders); not exempt (real logic, 372 lines).

Candidates:

- C1 (apply): `planning_input` was ~38 body lines — over the 30 hard ceiling — almost entirely due to the inline 15-line `"argument_rule"` prose literal sitting inside the input-hash assembly (one-level-of-abstraction violation). Extract byte-identical string to module constant `ARGUMENT_RULE` beside the other prompt/rule constants. JSON output unchanged.
- C2 (apply): `check_order_issues` used awkward double indexing twice: `plan.steps.each_index.select { |i| plan.steps[i].tool == ... }`. Replace with `each_with_index.filter_map` yielding the index directly. Same integer arrays.
- C3 (apply): `routing_prompt` computed `(toolbox.names + capability_descriptions.keys).uniq` twice (hash value + slice arg). Name it once (`work_tools`). Identical JSON bytes.
- C4 (apply): `verification_prompt` used a multiline `unless` block for `verification_context` while all three sibling builders use the modifier form for `planning_context`. Align (line fits 120-col budget).
- R1 (reject): convert `planning_phase_instruction` if/elsif chain to case/when — pure idiom churn; rubocop flags nothing, behavior and clarity gain negligible.
- R2 (reject): split `placeholder_arguments?` keyed/universal logic into helpers — equivalence provable, but this is the D-8 false-positive-sensitive heuristic with its scoping documented in-place; minimal-touch beats marginal readability.
- R3 (reject): trim the long D-8/P10/P15-W/P16 comment narratives — they carry review-ID traceability and the §11 "why" (failure model, rejected alternative); deletion risks losing design record, no code win.
- R4 (reject): deduplicate `parse_review`/`parse_verification` extraction scaffolding — different document shapes; machinery exceeds payoff (simple-over-complicated).
- R5 (reject): restructure `step_issues` (22 lines, multiple guarded pushes) — inside hard ceiling, cohesive, each push maps to one rule; churn only.
- R6 (reject): any rename — public/cross-gem surface (session gem, runtime gem, evals all reference these names) and no name misstates intent.

Untouched by design: `action_signature`/`canonicalize_apply_patch_arguments`/`canonical` (pinned digest inputs), effect-routing seams (none in this file — pure module, no EffectDispatcher usage).
