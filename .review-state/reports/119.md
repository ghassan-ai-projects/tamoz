# Combined pass — rank 119 gems/tamoz-graph/lib/tamoz/graph/compiled.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (removed dead DEFAULT_WRITER_TTL constant + its private_constant — zero references repo-wide, execution_support.rb has its own identical copy; private_constant blocks all external lookup); C2 (step-down in initialize: extracted private resolve_checkpointer / build_pools, called pre-freeze in identical order — body 23→17 code lines, under Q6 ceiling of 20) | Rejected: C3 (dedup LifecycleExecutor.new(self) across invoke/stream/resume/continue/retry_failed — accessor must stay non-memoized, saves tokens not clarity; churn), C4 (rename terse private shims invoke_at/open_writer/compatible_latest!/… — live cross-file contract, collaborators call them via compiled.__send__(:name) from unowned files), C5 (attr_reader vs endless-accessor placement inconsistency — cosmetic churn on pinned surface), C6 (tighten call guard to chained safe-nav — needs runtime local anyway, no gain)
Diff stat: 1 file changed, 17 insertions(+), 13 deletions(-)
Commands: ruby -c → OK · rubocop --cache false compiled.rb → 0 offenses BEFORE, 0 AFTER · ruby -Itest test/graph_execution_test.rb → 11 runs, 43 assertions, 0 failures, 0 errors · ruby -Itest test/graph_stream_test.rb → 7 runs, 70 assertions, 0 failures, 0 errors · ruby -Itest test/graph_durable_runner_test.rb → 19 runs, 39 assertions, 0 failures, 0 errors
Deviations: none — replay/digest/approval surfaces untouched; public signatures, error bytes, event ordering unchanged; helper names verified collision-free repo-wide before use
FLAG (owner decisions): none

## Candidate evidence (Phase 1, written before edits)

- C1: grep over whole repo (incl. send/__send__/public_send surfaces) for DEFAULT_WRITER_TTL:
  only compiled.rb:8/:243 itself plus execution_support.rb:13/:83 which defines and uses its own
  constant. No reader can exist through Compiled's namespace (private_constant).
- C2: initialize mixed the assembly list with two one-level-lower details (conditional
  checkpointer binding via respond_to?(:bind_graph); inline pool table). Both helpers are pure
  mechanical movements; the with_checkpointer(self.class.new(...)) re-init path runs the same code.
- File rides .rubocop_todo.yml Exclude entries (out of my ownership, untouched); gate held at 0.
