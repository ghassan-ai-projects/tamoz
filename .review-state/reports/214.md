# Combined pass — rank 214 gems/tamoz-agent-improvement/lib/tamoz/agent/improvement/heuristic.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename `seen` → `read_targets` in apply: the list holds targets already read, "seen" does not say by what); C2 (hoist `tool = String(step["tool"])` once per loop iteration in apply — it was computed identically in both the subject-tool and precursor-tool branches)
Rejected: C3 (rename public `assert_bounded!` to a validate_/enforce_ spelling per §3.1 — it checks externally supplied constructor values, so assert_ is arguably off-lexicon; PUBLIC API, untouchable); C4 (delete explicit keyword-only `initialize` that only calls super — Data's default initializer also accepts positional args, so removal WIDENS the API; not behavior-preserving); C5 (extract the 4-conjunct insert condition into a private predicate — needs a 4-parameter helper and adds indirection for an 18-line method already under the Q6 ceiling; machinery, not clarity); C6 (rename `inserted` counter / `result` accumulator — cosmetic churn next to MAX_INSERTIONS); C7 (trim the §11 policy header comment block — it documents real hard bounds: insert-only, read-only tools, snapshot byte cap)

## Audit detail (Phase 1)
- Names: public surface `assert_bounded! / snapshot / digest / to_h / apply` reads as a small DSL. Locals in `apply`: `seen` vague → C1.
- Length: longest method `apply` ≈ 18 code lines (< Q6 ≤20 normal), `assert_bounded!` ≈ 16. No extraction warranted or in scope (binding note: method-level naming/step-down only).
- One level of abstraction: `apply` loop computes `String(step["tool"])` twice (subject branch L127, precursor branch L133) → C2.
- Step-down: apply → target_of / precursor_step_for ✓.
- Constants frozen ✓; no boolean params; kwargs used ✓; comments are §11 "why" (policy bounds) ✓.

## Behavior-preservation argument
C1/C2 touch locals and expression hoisting only. `snapshot`, `digest`, `to_h`, emitted precursor steps, insertion order, and guard semantics are byte-identical; `String(step["tool"])` is deterministic on the same object, evaluated before either branch as before.

Diff stat: 1 file changed, 6 insertions(+), 5 deletions(-) (heuristic.rb only)
Commands: ruby -c heuristic.rb → Syntax OK · rubocop --cache false heuristic.rb → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/improvement_candidate_test.rb → 14 runs, 284 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/agent_improvement_lifecycle_test.rb → 6 runs, 13 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): C3 — if the owner ever wants `assert_bounded!` re-spelled under §3.1 (`validate_shape!`/`enforce_policy!`), that is a cross-gem rename needing its own pass.
