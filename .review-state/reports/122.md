# Combined pass — rank 122 gems/tamoz-agent-kernel/lib/tamoz/agent/witness_gateway.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (named bare upstream timeouts 300/60 as UPSTREAM_READ_TIMEOUT_SECONDS / UPSTREAM_OPEN_TIMEOUT_SECONDS, mirroring READ_TIMEOUT_SECONDS; values unchanged); C2 (corrected factually wrong comment on `record_payload` — "the tests read it back" disproven by repo-wide grep; comment-only)
Rejected: C3 (local sha256-digest helper for the duplicated `"sha256:#{...}"` literal — inline idiom is the repo's established spelling ~10× across gems incl. sibling episode_model_transport.rb; new local name adds vocabulary without removing cross-file duplication, sits on evidence-adjacent pinned lines); C4 (step-down split of `witness_request` at 27 lines — under the 30 hard ceiling; tail is pinned evidence semantics, KeyError fetch order/message bytes feed the 400 path; split needs ~9-kwarg plumbing introducing §4 long-parameter-list smell); C5 (rename private `handle` — churn without intent gain, single caller); C6 (`attr_reader :records` hands out live mutable array, §5 — public API untouchable, flagged); C7 (rename public `signature_ok?` to verify_* vocabulary — public API pinned by tests); C8 (`stop` begin/rescue-nil → modifier rescue — style churn)
Diff stat: gems/tamoz-agent-kernel/lib/tamoz/agent/witness_gateway.rb | 9 +++++---- (5 insertions, 4 deletions)
Commands: ruby -c witness_gateway.rb → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE 0, no increase) · ruby -Itest test/benchmark_controls_test.rb → 14 runs, 42 assertions, 0 failures, 0 errors · ruby -Itest test/stream_episode_witness_test.rb → 14 runs, 48 assertions, 0 failures, 0 errors (both match pre-change baselines captured before any edit)
Deviations: none
FLAG (owner decisions): (1) `attr_reader :records` exposes the live mutable array (§5 freeze rule says freeze what you hand out) — changing return semantics is a public-API decision, not taken here. (2) `record_payload` is public with zero external callers repo-wide (only in-file `append_log` uses it; its old comment claimed tests read it back) — removing/deprecating it is an API-surface decision for the owner.

---

## Phase 1 — ANALYZE (written before any edit)

BEFORE state: branch verification-again; owned file clean; `ruby -c` Syntax OK;
`rubocop --cache false` = 0 offenses; both focused tests GREEN as baselines:
`ruby -Itest test/benchmark_controls_test.rb` 14 runs / 0 failures;
`ruby -Itest test/stream_episode_witness_test.rb` 14 runs / 0 failures.
Binding note honored: evidence semantics pinned — record build→sign→store→log
sequence, error/message bytes, wire envelope, and public/cross-gem API untouched;
only method-level naming/step-down candidates considered. Renames of public
methods excluded by rule; private rename candidates rejected on value/churn.

### Candidates (as written pre-edit)

- C1 APPLY: name the bare upstream timeouts in `forward` (300/60) as constants,
  mirroring the existing `READ_TIMEOUT_SECONDS`; bare numbers next to a named
  client-side timeout invite misreading which side the 15s applies to. Values unchanged.
- C2 APPLY: fix factually wrong comment on `record_payload` ("the log line and the
  tests read it back") — repo-wide grep proves zero test callers; only in-file
  `append_log` reads it. Comment accuracy, no code change.
- C3 REJECT: extract a local sha256-digest helper for the twice-repeated
  `"sha256:#{Digest::SHA256.hexdigest(x)}"`. The inline idiom is the repo's
  established spelling (~10 sites across gems, including sibling
  episode_model_transport.rb); a new local name adds vocabulary without removing
  cross-file duplication, and sits on evidence-adjacent pinned lines.
- C4 REJECT: step-down split of `witness_request` (27 lines > 20 normal). Under the
  30 hard ceiling; the tail IS the pinned evidence semantics (KeyError fetch order
  and message bytes feed the 400 path); a split needs ~9-kwarg plumbing, introducing
  the §4 long-parameter-list smell to fix a diagnostic-limit exceedance. Standard:
  never split cohesive logic merely to satisfy a number.
- C5 REJECT: rename private `handle(client)` — generic but single-caller and clear
  in context; churn without measurable intent gain.
- C6 REJECT (change): `attr_reader :records` hands out the live mutable array (§5
  freeze rule). Public/cross-gem API untouchable → flagged above instead.
- C7 REJECT (change): rename public `signature_ok?` per §3.1 `verify_*` vocabulary —
  public API pinned by tests (`assert @gateway.signature_ok?(record)`).
- C8 REJECT: `stop`'s begin/rescue-nil → modifier rescue — style churn, rubocop clean.
