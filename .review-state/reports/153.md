# Combined pass — rank 153 gems/tamoz-sqlite/lib/tamoz/sqlite/thread_tombstone.rb

Verdict: COMMITTED-CANDIDATES

Applied: C1 (`call` returns the transaction block value directly; dropped `report = nil`
pre-declaration + trailing local — proven by `DatabaseKernel#transaction`
`result = yield tx … return result`, database_kernel.rb:44–48);
C2 (renamed private `report_bytes` → `build_report`; it returns `[bytes, digest,
purge_after]`, not bytes — §3.1 `build_*`; sole def + one in-file call site, repo-wide
grep incl. send/__send__/public_send clean); C3 (`validate_live_leases!` reads the
namespace row via named block destructure instead of opaque `row.fetch(3)/fetch(4)/
fetch(0)/fetch(2)`; column order pinned by the SELECT in `validate_namespaces!`,
values identical, SQL bytes untouched)
| Rejected: R1 (unify `create_tombstone!`'s values_at/fetch mix — cosmetic churn only);
R2 (inline `abandon_effect!` forwarder — rail zone: explicit-parameter delegation
forwarders deliberately kept after boundary_source_audit broke once, fc3878a);
R3 (rename `existing_report` → `decode_existing_report` — pairs with `existing_tombstone`;
decode verb already owned by DeletionReportCodec; churn);
R4 (replace string-keyed `context` Hash with a Data value threaded through three methods —
structural, recorded durable-transaction-core cohesion exception stands);
R5 (trim class-level `:reek:` disables — most listed smells still plausibly fire;
gate noise for zero reader value);
R6 (reorder methods for strict step-down — movement-only diff).

Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/thread_tombstone.rb | 15 +++++++--------
1 file changed, 7 insertions(+), 8 deletions(-)

Commands:
- `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/thread_tombstone.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE: 0 offenses / AFTER: 0 offenses (not increased)
- `ruby -Itest test/sqlite_deletion_test.rb` → 3 runs, 19 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/sqlite_boundary_source_audit_test.rb` (TAMOZ-SQLITE extra gate) →
  7 runs, 55 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/agent_session_operations_test.rb` → 6 runs, 36 assertions, 0 failures, 0 errors, 0 skips

Deviations: none. Transaction root shape kept byte-identical
(`adapter.__send__(:transaction, operation: 'thread.tombstone') do |transaction|`);
no endless-method conversions; delegation signatures untouched; error/message bytes,
wire/digest bytes, and event ordering unchanged; net delta 1 deleted line (budget ≤ ~60).
Note: this file is NOT among the audit's SOURCE_PATHS (test pins checkpoint_*/request_inbox_*/
lease_operations/effect_census), but its transaction block is the same pattern the rail
statically resolves, so it was preserved exactly and the audit test was still run green.

FLAG (owner decisions): none.
