# Combined pass — rank 236 gems/tamoz-agent-memory/lib/tamoz/agent/memory/surface.rb

Verdict: COMMITTED-CANDIDATES
Applied: C2 (header comment corrected: owner/path said `tamoz-agent` / `gems/tamoz-agent/lib/tamoz/agent/memory/`; actual owner gem since 2f01925 "P2-A: extract tamoz-agent-memory" is `tamoz-agent-memory` — verified against gemspec summary and absent target dir)
Rejected: C1 (remove dead `@protection` ivar — ATTEMPTED then reverted: removal trips `Lint/UnusedMethodArgument` on the `protection:` kwarg; renaming the kwarg breaks public callers passing it (test/memory_engine_test.rb:45, test/stream_learning_loop_test.rb:419,614, test/stream_invariants_test.rb:112) and suppression is forbidden; no clean in-file fix exists); C3 (inline `collapsed` local in `searchable_text` — style churn, zero intent gain); C4 (hoist double `record.sensitive?` in `index_for` — micro-churn); C5 (`Engine#caller` shadows `Kernel#caller` — rename is a public-API break, shape pinned); C7 (extract shared tenant-normalize from `Surface.memory_namespace` + `Engine#initialize` — adds machinery for 2 call sites)
Diff stat: 1 file changed, 2 insertions(+), 2 deletions(-) — comment lines only, zero code change
Commands: ruby -c surface.rb → Syntax OK · rubocop --cache false surface.rb → BEFORE 0 offenses, AFTER 0 offenses · ruby -Itest test/memory_engine_test.rb → 25 runs, 162 assertions, 0 failures, 0 errors · ruby -Itest test/memory_session_integration_test.rb → 4 runs, 24 assertions, 0 failures, 0 errors · ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): C6 — `searchable_text` comment says "first 512 bytes" but `text[0, 512]` slices 512 *characters*; multibyte statements can exceed 512 bytes in the indexed `statement_search`. Fixing the bound changes index content (out of scope here); fixing the comment would bless current behavior. Owner decides which side is intended. Related: C1's dead `@protection` can only be resolved by wiring protection into the engine (its natural consumer) or dropping the `protection:` kwarg repo-wide — both multi-file, orchestrator territory.

## Phase 1 — ANALYZE (candidates written before any edit)

File: 139 lines, module `Surface` (codec, memory_namespace, secret_shaped?) + class `Engine`
(wiring + caller/index_for/searchable_text). Not declarative data → EXEMPT does not apply;
per binding note, method-level naming/step-down only.

- C1: `@protection = protection` — written, never read anywhere in repo (full-repo grep over
  gems/ + test/: no `.protection` reader, no reflection reader; real protection flows via
  adapter `store_protection`). Dead state (§5). Removal attempted in Phase 2, reverted at gate.
- C2: Header comment claimed all P11 memory code lands under
  `gems/tamoz-agent/lib/tamoz/agent/memory/`; that dir no longer exists and the file itself
  lives in `gems/tamoz-agent-memory`. Factual correction applied.
- C3: `collapsed = text.gsub(...).strip[0, 512]; collapsed` → inline return. Rejected: churn,
  named local reads fine next to the invariant comment.
- C4: `record.sensitive?` twice on adjacent lines in `index_for`. Rejected: micro-churn.
- C5: `Engine#caller(user:, project:, ...)` shadows `Kernel#caller`. Real smell, but renaming
  is a public-API break. Rejected.
- C6 (FLAG): bytes-vs-characters mismatch between bounded-projection comment and
  `String#[0, 512]`. No behavior-preserving edit possible. Flagged.
- C7: tenant normalization duplicated in `Surface.memory_namespace` and `Engine#initialize`.
  Rejected: extraction adds machinery; shapes pinned.
- Step-down/naming sweep: endless accessors (admission..wisdom) are consistent style; the
  anti-endless rail is tamoz-sqlite-only, not applicable here. `initialize` ≈18 body lines and
  `index_for` ≈21 declarative IndexRow lines are within ceilings. Nothing else qualifies.

## Phase 2 — APPLY

C2 applied as a two-token comment edit. C1 attempted (`@protection = protection` deleted),
caught by gate 2 (`Lint/UnusedMethodArgument`, offense count 0 → 1), reverted per rails;
offense count back to 0. All other candidates rejected above. Net changed lines well under the
~60-line stop threshold.
