# Fix report — rank 16 gems/tamoz-evals/lib/tamoz/evals/benchmark/readiness.rb

Applied: C6 (merged twin missing/unexpected mission-id reasons into `expected_mission_id_reasons`, missing-then-unexpected order and reason bytes unchanged); C1 (extracted `surface_record_shape_valid?` shared by `validate_surface_execution!` and `valid_surface_map?`; dropped that method's now-unneeded AbcSize/CyclomaticComplexity/PerceivedComplexity disables); C5 (private `non_empty_string?` replacing eight verbatim `is_a?(String) && !empty?` spellings: manifest strings, catalog goal, string-list entries, trace_id/run_id/thread_id, safe-path head, durable-mission binding); C2 (stepped `artifact_document_reasons` down to parse + secret check + rescues with `first_document_reason` as a lazy `||` chain — short-circuit order identical); C3 (moved real-provider receipt/trace forensics into `real_provider_trace_reason`; same check order, same reason strings; dropped its AbcSize disable); C4 (verify-lexicon renames `real_provider_mission_verified?`, `durable_mission_verified?`, `provider_trace_digest_verified?`, `independent_trace_verified?`; own grep proof before applying: all four old names occur only in this file at the claimed def/call-site pairs in the main tree — `.worktrees/*` hits are sibling checkouts of the same file, not callers; no dynamic dispatch in the file).

Rejected: none.

Diff stat:
 .../lib/tamoz/evals/benchmark/readiness.rb | 106 ++++++++++-----------
 1 file changed, 48 insertions(+), 58 deletions(-)
(711 → 701 LOC, net −10)

Commands:
- ruby -c gems/tamoz-evals/lib/tamoz/evals/benchmark/readiness.rb — Syntax OK (before and after).
- rubocop --cache false <owned file> — BEFORE: 0 offenses; AFTER: 0 offenses (count did not increase; no suppressions added — three stale disable blocks removed instead). NEVER ran it without --cache false.
- ruby -Itest test/openclaw_benchmark_readiness_test.rb — 10 runs, 41 assertions, 0 failures, 0 errors.
- ruby -Itest test/openclaw_mission_runner_test.rb — 9 runs, 39 assertions, 0 failures, 0 errors.
- ruby -Itest test/openclaw_benchmark_readiness_cli_test.rb — 1 run, 4 assertions, 0 failures, 0 errors.
- ruby -Itest test/benchmark_comms_b0_test.rb — 13 runs, 150 assertions, 0 failures, 0 errors.
- Behavioral probes (inline ruby, not test edits): validate_surface_execution! valid → no raise, shape-valid/content-bad → SchemaError "benchmark surface provenance is invalid:m1:cli", shape-bad → SchemaError "benchmark surface execution is invalid:m1:cli"; expected_mission_id_reasons(["a","b"],["b","c"]) → ["mission_missing:c","mission_unexpected:a"], nil → [], [] → unexpected-only (matches legacy truthiness of []).

Deviations:
- Self-caught mid-application regression, fixed before final gates: my first C1 edit turned `unless shape … end` into `return if shape …`, which skipped the provenance-content check for every shape-valid record (existing suites do not cover that path, so tests stayed green). Restored the unless-flow around the predicate and proved all three paths byte-identical via probe. The committed diff contains the correct version.
- Kept `independent_trace_verified?`'s existing complexity disables (still earning their keep after C5 slimming); removed only the two disable blocks genuinely obsoleted by C1/C3.
- FLAG items recorded only, untouched per owner decision: schema_error noun-vs-raise (~26 call sites, error bytes untouchable) and misleading 'invalid status' message in validate_mission! (error-byte change forbidden this pass).

Working tree: only gems/tamoz-evals/lib/tamoz/evals/benchmark/readiness.rb changed by me. Other modified files present in the tree (gems/tamoz-agent-cli/lib/tamoz/agent/cli.rb, gems/tamoz-graph/lib/tamoz/graph/checkpoint_codec.rb, gems/tamoz-sqlite/lib/tamoz/sqlite/schedule_store.rb) were already modified by sibling agents at my baseline; I did not touch them.
