# Combined pass — rank 89 gems/tamoz-agent-capabilities/lib/tamoz/agent/mcp_capability_source.rb
Verdict: PENDING

## Phase 1 — ANALYZE (candidates, written before any edit)

Baseline: ruby -c OK · rubocop --cache false 0 offenses · test/agent_mcp_capability_source_test.rb green
(14 runs, 102 assertions, 0 failures). All private helpers proven in-file-only by repo-wide grep
(no hits outside the owned file, so no send/__send__/public_send dispatch exists).

- C1 APPLY: `assert_descriptor_interface!` → `validate_descriptor_interface!`. Caller-supplied
  descriptor shape check ("must respond to id, name, …") raises ArgumentError = bad input named by
  field → §3.1 validate_ semantics, matching this file's own top-level guards
  (validate_constructor_args!, validate_snapshot!) and repo precedent (validate_identity! ×9,
  validate_shape!, validate_inputs!). "assert_" claims a broken Tamoz invariant; a malformed caller
  descriptor is not one.
- C2 APPLY: `assert_descriptor_identity!` → `validate_descriptor_identity!`. Same category: the
  source-qualified id convention (`mcp:<source>/<name>`) is declared shape of a supplied value,
  checked at construction with ArgumentError.
- C3 APPLY: `assert_descriptor_pinning!` → `verify_descriptor_pinning!`. This is exactly §3.1's
  verify_ row — "claim holds against evidence: a digest, a binding" (definition_digest vs the pinned
  snapshot entry). Repo already spells this family verify_: verify_digest!, verify_receipt_binding,
  verify_mcp_binding!/enforce_mcp_binding_ in tamoz-agent-session (the standard's own example).
  One step here, so no separate enforce_ half.
- C4 REJECT: rename `record_unique_descriptor!`. "record_" states its seen-index bookkeeping, which
  is real state mutation, not just a check; no repo-wide spelling conflict to align with. Weak gain.
- C5 REJECT: de-bang `normalize_arguments!`. No safe twin exists anywhere for it; the bang reads as
  the raising normalizer convention used across the guard family. Pure churn.
- C6 REJECT: extract `build_source_digests(source_digests)` for symmetry with build_catalog_digests.
  A one-line key/value coercion does not need a named method; adds indirection for zero clarity.
- C7 REJECT: effect_intent calls descriptor_for!(name) twice (once inside validate). Micro-
  redundancy with no observable effect; restructuring risks call-order drift for nothing.
- LEAVE `assert_depth!`: byte-identical twin in gems/tamoz-mcp/lib/tamoz/mcp/invocation.rb pins the
  name repo-wide ("one concept, one verb"); renaming only this copy would CREATE a second spelling.

Behavior-preservation proof for applied set: pure private-method renames; all callers are the three
call sites inside validate_descriptors! (grep-proven); no string/symbol dispatch anywhere; error
bytes, ordering, wire shapes, public API untouched. Diff ≈ 6 lines (< 60 cap).

## Phase 2 — APPLY

(applied after this section was written)

Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename assert_descriptor_interface! → validate_descriptor_interface!);
C2 (rename assert_descriptor_identity! → validate_descriptor_identity!);
C3 (rename assert_descriptor_pinning! → verify_descriptor_pinning!)
Rejected: C4 (record_unique_descriptor! — name states the bookkeeping, weak gain);
C5 (de-bang normalize_arguments! — raising-normalizer convention, churn);
C6 (extract build_source_digests — indirection for a one-liner);
C7 (effect_intent double lookup — micro-redundancy, no observable gain)
Diff stat: 1 file changed, 6 insertions(+), 6 deletions(-)
Commands:
- ruby -c gems/tamoz-agent-capabilities/lib/tamoz/agent/mcp_capability_source.rb → Syntax OK
- rubocop --cache false <file> → 0 offenses (BEFORE 0, no increase)
- ruby -Itest test/agent_mcp_capability_source_test.rb → 14 runs, 102 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): public reader `mcp_catalogs` actually holds {server_id => snapshot_digest}
(see build_catalog_digests) — name says catalogs. Consumed cross-gem by tamoz-agent-session
(session.rb:359, session_bindings.rb:39, session_effects.rb:124) and duck-typed in
test/legacy_session_resume_test.rb:83, so it is public/cross-gem API and untouchable in this pass;
a rename belongs to an owner decision spanning both gems.
