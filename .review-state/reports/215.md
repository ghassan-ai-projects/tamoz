# Combined pass — rank 215 gems/tamoz-agent-profile/lib/tamoz/agent/profile/secure_file.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract duplicated non-regular-file refusal — lines 57 and 107 raised the byte-identical pinned message `PermissionError "#{@path}: profile must be a regular file"` on a non-regular fstat; both call sites now use one private `refuse_non_regular_file!(stat)` in the existing refuse_*! vocabulary. Behavior-proof: same predicate on the same fstat result, same class, same message bytes, evaluated at the same points — line 57 still runs unconditionally before the permissions branch, so FIFO rejection without permission verification is preserved); Rejected: C2 (unify sibling-helper signatures `refuse_writable_directory!(dir, mode)` vs `refuse_readable_directory!(dir, stat)` — each takes exactly what its check consumes: mode-only bit test vs mode+ownership; symmetry would be cosmetic churn on pinned code); C3 (rename `read_bytes` — it returns decoded UTF-8 text, but it is public API via the `Profile.read_bytes` wrapper at profile.rb:431 and caller profile.rb:401); C4 (`verify_permissions!` explicit `{ nil }`/trailing nil — deliberate nil-return semantics, pinned); C5 (boolean `permissions:` param → named policy — documented Q4 API change, suppressed in-file); C6 (`decode`'s defensive `bytes.dup` — preserves non-mutation of the read buffer, zero cost); C7 (reorder private helpers for strict step-down — current order already groups read-path then verify-path helpers; churn).
Diff stat: gems/tamoz-agent-profile/lib/tamoz/agent/profile/secure_file.rb | 10 ++++++++-- (8 insertions, 2 deletions; net +6 lines)
Commands: ruby -c secure_file.rb → Syntax OK · rubocop --cache false → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/agent_profile_test.rb → 44 runs, 130 assertions, 0 failures, 0 errors · ruby -Itest test/agent_profile_adoption_seams_test.rb → 15 runs, 42 assertions, 0 failures, 0 errors · ruby -Itest test/agent_profile_transition_test.rb → 11 runs, 49 assertions, **1 failure — proven pre-existing**: identical failure with pristine secure_file.rb stashed (git stash push/pop round-trip); backtrace never enters secure_file.rb.
Deviations: none
FLAG (owner decisions): test/agent_profile_transition_test.rb#test_from_authority_rejects_tampered_snapshots is RED on the current tree, independent of this pass: commit b2707ce ("clean-code pass on check_spec_validator.rb") left `check_spec_validator.rb:137` raising `"#{named} is a shell or interpreter wrapper…"` where no `named` local or method exists in scope (the method binds `basename` and `prefix`) → NameError escapes where callers expect Profile::ValidationError. One-line owner decision (likely `#{program}` vs `#{basename}`), but the file belongs to another owner so I did not touch it. Also note: working tree currently carries unrelated modifications by sibling agents (cli_comms_doctor.rb, heuristic.rb, session_status_projection.rb, fork_executor.rb, effect_record_reader.rb) — unstashed, unowned by me.
