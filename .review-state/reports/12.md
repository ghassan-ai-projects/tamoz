# Fix report — rank 12 gems/tamoz-mcp/lib/tamoz/mcp/invocation.rb

Applied: C1 (extracted private `open_pinned_channel` — the four shared preamble steps
`verify_pinned_digest!` → `ensure_available!` → `build_client` → `ensure_connected!` in current
order; `call` keeps `validate_arguments!` ahead of it, `reissue` keeps `Elicitation.answer`
after it; retry path still calls `ensure_connected!` directly); C2 (split `round_trip`: the
read-only retry loop with its `Timeout::Error, MCP::Client::RequestHandlerError` arm moved
intact into private `transport_round_trip`; `round_trip` keeps ServerError / ValidationError /
InputRequired arms wrapped around it; inline ValidationError message folded into byte-identical
private `malformed_response_error(descriptor)` beside its builder siblings, deliberately without
stderr metadata exactly as before); C3 (renamed `transport_failure` → `record_transport_failure`,
sole in-file caller updated); C4 (renamed `classify_response` → `succeeded_outcome`, sole
in-file caller updated)  |  Rejected: none of C1–C4 (all verified before applying)

Verification behind acceptance:
- Order-of-operations proof for C1: both public bodies execute identical call sequences
  before/after (diff is a pure hoist); wire/effect ordering untouched.
- Escape-semantics check for C2 against gems/tamoz-mcp/lib/tamoz/mcp/errors.rb: everything
  raisable inside the moved arm (`record_transport_failure`, `retry_eligible?`, `restart`,
  `ensure_connected!`, `raise_classified_transport`) surfaces Tamoz-typed errors
  (`Tamoz::Mcp::Error` family), which match none of the three remaining MCP-client rescue arms;
  Ruby does not consult sibling rescues for exceptions raised inside an arm, so today those same
  exceptions also escaped `round_trip` uncaught. Residual theoretical window (a raw
  `MCP::Client::InputRequiredError` out of `client.connect` during retry reconnect) cannot arise
  by construction — input_required detection applies to tools/call results, not initialize — and
  is uncovered per repo convention.
- Sole-caller claims for C3/C4 re-grepped in the main tree: only invocation.rb:359/385 + defs.
  The telegram `client.rb#transport_failure` is a different class; the test hit at
  test/mcp_invocation_test.rb:537 is a test method name only. `.worktrees/` copies are separate
  checkouts, untouched.

Diff stat:
 gems/tamoz-mcp/lib/tamoz/mcp/invocation.rb | 84 ++++++++++++++---------
 55 insertions(+), 29 deletions(+) → net +26 LOC (743 → 769 lines)

Commands (environment block sourced before every ruby/rubocop invocation):
1. ruby -c <owned file>            → Syntax OK (before AND after edits)
2. rubocop --cache false <owned file> BEFORE edits → 0 offenses (baseline recorded)
   rubocop --cache false AFTER first pass → 2 offenses, both Layout/HashAlignment introduced by
   my new multi-line keyword args at lines 89/110 → FIXED (one keyword per line), not suppressed;
   rubocop --cache false FINAL → 0 offenses. Gate: count did not increase ✓
3. ruby -Itest test/public_api_test.rb        BEFORE: 3 runs, 1015 assertions, 0F/0E
                                              AFTER:  3 runs, 1015 assertions, 0F/0E ✓
4. ruby -Itest test/mcp_elicitation_test.rb   BEFORE: 19 runs, 71 assertions, 0F/0E
                                              AFTER:  19 runs, 71 assertions, 0F/0E ✓
5. timeout 120 ruby -Itest test/websearch_invocation_test.rb BEFORE: 11 runs, 61 assertions, 0F/0E
                                                             AFTER:  11 runs, 61 assertions, 0F/0E ✓

Test-lane note: the file's own suite test/mcp_invocation_test.rb and
test/agent_mcp_adversarial_test.rb are named known-red/excluded lanes in this brief (SLOW_TESTS /
SERIAL_TESTS policy) and were NOT run or chased, per gate instructions. The three suites above
are non-excluded lanes that reference/exercise Invocation (public API surface, elicitation/reissue,
websearch call path) and were baselined BEFORE editing so no failure could be misattributed.
No test was edited; no file other than the owned one was touched by me.

Deviations:
- Concurrent work observed: gems/tamoz-sqlite/lib/tamoz/sqlite/memory_store.rb shows as modified
  in git status (306 changed lines). The tree contained only untracked .review-state/ when I
  started; I did not touch memory_store.rb — it belongs to another rank working in this checkout.
  Orchestrator should expect it in the tree alongside my owned-file change.
- FLAG items not actioned (owner decisions, per binding): strict_schema/deep_strictify/
  strictable_object? duplication with elicitation.rb left alone (elicitation.rb untouched);
  `headless:` boolean on public `call`/`reissue` unchanged (public signature owner-only).
- New methods meet Q6 ceilings: open_pinned_channel 8 lines / 4 params;
  transport_round_trip ≤20 lines / 5 params; round_trip ≤20 code lines (params pre-existing);
  succeeded_outcome 5 params; malformed_response_error 1 param.
