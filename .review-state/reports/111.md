# Combined pass — rank 111 gems/tamoz-agent-profile/lib/tamoz/agent/profile/transition_registry.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (renamed private `document` → `read_document`, def + 3 in-file call sites; §3 read_ vocabulary states the IO+verify+parse+normalize action and stops the noun colliding with `write_document`'s `document` param)
Rejected: C2 (`dead_candidates` digest pair → kwargs — public/cross-gem API untouchable); C3 (split `consume_if_candidate!` — explicitly documented atomic-RMW exception, extraction forbidden by binding); C4 (extract row→Transition / hash plumbing from `candidates`/`record` — keep-whole precedent, zero gain); C5 (hoist `field_rules` to constant — documented load-order reason stands: PROFILE_ID_PATTERN lives in Profile, loads later); C6 (delete `LEGACY_REGISTRY_SCHEMA_VERSION` — public constant, owner decision, see FLAG); C7 (trim header/reek comments — they are the recorded cohesion exceptions, §11 why-content); C8 (dedupe `String(thread_id)` coercion via helper — two sites, helper is more machinery than the duplication)

Diff stat: gems/tamoz-agent-profile/lib/tamoz/agent/profile/transition_registry.rb | 8 ++++---- (4 insertions(+), 4 deletions(-))

Commands:
- ruby -c <file> → Syntax OK
- rubocop --cache false <file> BEFORE → 0 offenses · AFTER → 0 offenses (no increase)
- ruby -Itest test/agent_profile_transition_test.rb → 11 runs, 72 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_profile_machinery_test.rb → 24 runs, 230 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_phase4_capability_test.rb → 7 runs, 31 assertions, 0 failures, 0 errors, 0 skips

Deviations: none (~25-min budget kept; no commits; no bundle install; tests one file per command; net change 8 lines ≪ 60-line stop rail)

FLAG (owner decisions): `TransitionRegistry::LEGACY_REGISTRY_SCHEMA_VERSION` is referenced nowhere in the repo beyond its own definition (repo-wide grep, tests included) — probable dead public constant. Removal would touch public surface, so rejected here and left to the owner.

---

## Audit detail (Phase 1, written before any edit)

Baseline: `ruby -c` Syntax OK · rubocop --cache false BEFORE = 0 offenses · branch
verification-again · owned file clean before this pass.

Shape audit: 253 lines; public verbs (`candidates`, `candidate?`,
`dead_candidates`, `record`, `consume_if_candidate!`) over a private locked
write path (`with_registry_lock`, `write_document`, `validate!`, `field_rules`,
`document`). Methods ≤20 lines except `consume_if_candidate!`, which carries an
explicit in-file §4 exception (one atomic flocked RMW) plus recorded reek
suppressions — the binding note's keep-whole precedent applies. Public names end
in `?` where predicate; kwargs carry the identity triples; bangs have documented
raise-only contracts. Step-down holds: each verb reads as verb → lock →
read_document/write_document.

Rename safety proof for C1: callers only at the def plus lines 53/102/136, all
in-file; repo-wide grep found no `send/__send__/public_send(:document)` and no
subclass of TransitionRegistry. Zero-behavior rename.

Candidates as listed at analysis time:

- C1 APPLY — rename private `document` → `read_document`.
- C2 REJECT — positional digest kwargs on `dead_candidates` (public API).
- C3 REJECT — split `consume_if_candidate!` (documented atomic exception).
- C4 REJECT — extract builders/plumbing (keep-whole).
- C5 REJECT — constant-hoist `field_rules` (load-order reason stands).
- C6 REJECT — delete dead public constant (owner FLAG instead).
- C7 REJECT — strip reek/header comments (they are the recorded exceptions).
- C8 REJECT — helper for the shared thread-id coercion (over-machinery).
