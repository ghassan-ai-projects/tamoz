# Combined pass — rank 84 gems/tamoz-sqlite/lib/tamoz/sqlite/circuit_store.rb

## Phase 1 — ANALYZE (candidates, written before any edit)

BEFORE state: `ruby -c` OK; `rubocop --cache false` = 0 offenses; file 295 lines; branch
verification-again; working tree clean for the owned file.

- C1 — Merge the copy-paste pair `normalize_namespace`/`normalize_key` (identical
  `SafeText.normalize` params, differ only in the `name:` label string) into one private
  `normalize_name(value, label:)`; call sites in `repair_corrupt` pass the same labels, so
  error-message bytes are preserved. APPLY.
- C2 — Remove duplicate public reader `def scope = @scope_type` (shadows `attr_reader
  :scope_type`). REJECT: it is a documented consumer-contract method — the class header
  lists `scope` in "the one interface both consumers already use", and
  `gems/tamoz-agent-healing/lib/tamoz/agent/healing/remediation/session.rb#circuit_guard`
  calls `@circuit.scope` on caller-injected stores (`circuit ||=` accepts any duck-typed
  store). Cross-gem API is untouchable per brief.
- C3 — Replace hand-rolled `secure_digest_equal?` with `Wire.secure_compare` (same-gem
  duplicate capability). REJECT: `Wire` is a `private_constant` and `secure_compare` a
  `private_class_method` of wire.rb, which I do not own; reaching it requires `send`,
  which is worse than the duplication, and its semantics differ slightly (no `String`
  coercion), so equivalence is not provable without editing an unowned file.
- C4 — Deduplicate the get→load-or-initial logic shared by `read_record` and `cas`
  (extract a loader helper, or have `cas` call `read_record`). REJECT: `cas` is the
  bounded-CAS transaction core under the recorded cohesion exception (no extraction or
  split); reusing `read_record` would add a second `store.get` inside the CAS loop,
  changing concurrency behavior (not behavior-preserving).
- C5 — Share a private `last_failure` reader between `last_failure_kind` and
  `last_failure_context`. REJECT: churn; the guard+delegate shape is deliberate and
  parallel across the whole read family (`failures`, `reset_evidence`,
  `conditions_digest`); asymmetry for ~2 saved lines reduces scanability.
- C6 — Narrow public surface beyond the documented contract (`store`, `now_ms`,
  `read_record`, `corrupt?`). REJECT: public/cross-gem API untouchable per brief.
- C7 — Split `repair_corrupt` (62 lines, above the hard ceiling). REJECT: recorded
  cohesion exception for this exact atomic repair transaction; boundaries stay visible;
  no extraction/split proposals. Method-level review found nothing to rename — locals
  already read step-down clean.
- Also considered renaming `cas`: REJECT — matches the DR-2 doc vocabulary ("CAS append",
  "bounded CAS"); rename is churn.

Cohesion exception honored: transaction core kept whole; only method-level changes inside.

## Phase 2 — APPLY

Verdict: COMMITTED-CANDIDATES
Applied: C1 (merge normalize_namespace/normalize_key into normalize_name(value, label:))
Rejected: C2 (documented cross-gem consumer contract, live caller in healing session.rb);
C3 (shared helper is private in unowned wire.rb, needs send to reach); C4 (CAS core under
recorded cohesion exception; reuse would add a racy second get); C5 (churn vs deliberate
parallel shape); C6 (public/cross-gem API untouchable); C7 (recorded repair-transaction
cohesion exception)
Diff stat: 1 file changed, 4 insertions(+), 13 deletions(-) (net −9)
Commands:
1. `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/circuit_store.rb` → Syntax OK
2. `rubocop --cache false <file>` → 0 offenses (BEFORE: 0 — no increase)
3. `ruby -Itest test/sqlite_circuit_store_test.rb` → 11 runs, 48 assertions,
   0 failures, 0 errors, 0 skips
4. `ruby -Itest test/websearch_circuit_test.rb` → 8 runs, 33 assertions,
   0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): C3 — if the owner ever wants one canonical constant-time compare,
the move is to make `Wire.secure_compare` internal-but-shared inside tamoz-sqlite (owner's
call; touches unowned wire.rb).
