# Fix report — rank 23 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_selector_control.rb

Applied: C1 (normalize_selector split into six in-order helpers: selector_point, enforce_phase2_operation!, optional_statement, enforce_selector_binding!, iteration_class_value, verify_selector_digest! — validation order and every message byte preserved); C2 (prepare! step-down: extracted create_control_directory!(root_path:, directory_name!) for join → absent-guard → mkdir 0700 → chmod → fsync — with the creation-failure rollback INSIDE the helper, see deviations); C3 (build_expectation now orchestrates; assembly-through-size-guard extracted to build_control_record returning [record, bytes]); C4 (trusted-path prologue deduped into resolve_real_path(value, name:) used by private_directory and filesystem_anchor_stat; syscall sequence expand→lstat→realpath→stat and all refusal bytes identical — the existing `name:` parameterization makes the wrapper templates byte-identical); C5 (renamed in-file only: validate_exact_hash, validate_identifier, validate_digest, validate_positive_integer, validate_nonnegative_integer, build_expected_hook).

Rejected: none of C1–C5 outright. FLAG items not acted on (owner decisions): the seven send-reached "private" methods (validate_layout!, build_expectation, ensure_control_absent!, deeply_frozen?, validate_registry_unchanged!, sync_directory, preflight_record!) are untouched — all seven names verified still present with unchanged signatures; repo-wide validator-spelling unification is owner-level.

Diff stat: 1 file changed, 139 insertions(+), 93 deletions (+46 net; 598 → 644 LOC)

Commands:
- `ruby -c <owned file>` — Syntax OK (before and after)
- `rubocop --cache false <owned file>` — BEFORE: 0 offenses; AFTER: 0 offenses (not increased; no suppression added). Note: this file is clean uncached; the ~1000 legacy offenses are elsewhere in the repo.
- Rename-safety greps before any rename: `(send|__send__|public_send)` + string-form `send("...")`/`respond_to?`/`define_method`/`method` across gems/, test/, apps/, bin/ for all six C5 names — zero dynamic references outside the owned file; identically named helpers in sqlite_scenario_registry.rb / sqlite_trace_recorder.rb / tamoz-core / otel / mcp are other classes' own privates. The seven frozen send-targets confirmed referenced ONLY from sqlite_selector_control_stopper.rb, sqlite_selector_control_intervention.rb, test/sqlite_selector_control_test.rb:735.
- `ruby -Itest test/sqlite_selector_control_test.rb` — 19 runs, 125 assertions, 0 failures, 0 errors, 0 skips. Other files referencing the class (sqlite_raw_oracle_test, sqlite_scenario_driver_test) are in the do-not-chase SLOW/SERIAL list per brief, so not run.

Deviations:
- C2: candidate wanted the rescue-rollback kept solely in prepare!. That is not behavior-preserving: with `created = true` set only after the helper returns, a chmod/fsync failure would leak the created directory; setting it before the call would rmdir a foreign directory on an mkdir race (current code skips cleanup when mkdir itself fails). The helper therefore owns the mkdir→chmod→fsync failure cleanup locally with its own created flag (mirroring the original flag timing exactly); prepare!'s rescue keeps rollback for the post-creation phase only. Guard refusal ("must not pre-exist") stays ExecutionError, unwrapped, no rmdir — byte-identical.
- C4 helper named resolve_real_path (candidate: resolved_real_path) per CODING_STANDARD §3.1 (`resolve_*` turns a reference into the thing it names).
- C3 helper named build_control_record (candidate: control_record) per §3.1 (`build_*` returns a new value), consistent with C5's build_expected_hook.
- normalize_selector lands at 27 body lines of pure one-step-per-line sequencing (candidate promised ≤20): keeping the 9-line body literal inline beats a 7-keyword helper; §4's hard ceiling is 30 and forbids splitting merely to satisfy a number. All new helper methods are ≤14 lines.
- No comments added anywhere.
- Working tree: my changes touch ONLY the owned file. git status also shows concurrent modifications by sibling agents NOT mine (gems/tamoz-agent/lib/tamoz/agent/runtime.rb, gems/tamoz-evals/lib/tamoz/evals/harness/subprocess_runner.rb, gems/tamoz-agent-cli/.../cli_worker_commands.rb) — present in the shared tree, untouched by me.
