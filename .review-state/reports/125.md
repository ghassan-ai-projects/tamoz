# Combined pass — rank 125 gems/tamoz-sqlite/lib/tamoz/sqlite/adapter.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down in `initialize`: extract `enforce_configuration!` — all four config validations, identical raise order + message bytes); C5 (`integrity_check`: extract pure predicate `integrity_clean?` so the entry point reads gather → assert → freeze)
Rejected: C2 (rename `guard_open!`/`ensure_process!` to §3.1 vocabulary — both proven file-local, but current names already read clearly; churn without clarity gain); C3 (dedupe the repeated `clock: -> { Time.now }` literal across four bind_* methods — a shared constant would share one Proc identity across stores; per-call literal is the correct idiom); C4 (strip "P13-A"/"Slice A/C"/"P3" ticket tags from comments — cosmetic; the comment bodies carry real why-content: the shared-enqueue-primitive constraint on bind_schedule_store, row homes, reload-loop reader); C6 (public `attr_reader` exposing pool/kernel internals — cross-gem API, and it feeds the documented `__send__` reach-in seams; adapter-side promotion is an explicit owner decision, out of scope per rails); FULL-C1 (extract engine bootstrap — Migrator/pool/kernel/backup wiring — into a second private step to bring `initialize` fully under the 30-line hard ceiling: provable, but ~100 changed lines busts the ~60-line stop rail; escalated as residual below)

Diff stat: `gems/tamoz-sqlite/lib/tamoz/sqlite/adapter.rb | 44 +++++++++++++++----------` — +24/−16 changed lines (40 touched, net +8; inside the ~60-line stop rail)
Commands: `ruby -c adapter.rb` → Syntax OK · `rubocop --cache false <file>` → 0 offenses before, 0 after (one transient Style/GuardClause from my own lines was fixed by converting the two short checks to guard-clause raises, not suppressed) · `ruby -Itest test/sqlite_store_test.rb` → 6 runs, 68 assertions, 0 failures, 0 errors · `ruby -Itest test/sqlite_backup_test.rb` → 3 runs, 16 assertions, 0 failures, 0 errors · `ruby -Itest test/sqlite_kernel_test.rb` → 8 runs, 61 assertions, 0 failures, 0 errors (expected sqlite3 fork-safety warning; that suite exercises the fork guard)
Deviations: none
FLAG (owner decisions):
- Residual debt: `initialize` is still ~51 lines after C1 (was 60) — over the 30-line hard ceiling. The remainder is mechanical engine bootstrapping (Migrator → ConnectionPool → DatabaseKernel → Backup); extracting it is behavior-provable but was rejected solely on the ~60-changed-line rail of this pass. Escalation candidate for the two-agent pipeline.
- The three cops that flag `initialize` today (Metrics/AbcSize, CyclomaticComplexity, MethodLength) are suppressed via `.rubocop_todo.yml` entries for this file — that file is forbidden to this pass, so the suppression survives even though C1 shrinks the method.
- Standing FLAG confirmed, not acted on: `transaction`/`read` private delegators plus LeaseOperations' `acquire_lease`/`backend_time`/`validate_lease_in_transaction!` remain the documented `__send__` reach-in seams (~20 call sites across sibling stores). Untouched here by rail; adapter-side promotion would retire them.

## Candidate audit (Phase 1, written before any edit)

File shape: 239 lines; 11 `bind_*` entry points reading as a small DSL (each `guard_open!` → construct store), lifecycle (`initialize`/`close`/`closed?`/`stats`/`integrity_check`/`backup`), thread-deletion delegation, then a small private section holding the fork guard, open guard, and the lease/transaction seams. Baseline rubocop: 0 offenses (1 file inspected).

| # | Finding | Rule | Disposition |
|---|---------|------|-------------|
| C1 | `initialize` is 60 lines (hard ceiling 30; TODO-suppressed) and mixes three abstraction levels: config validation, ivar wiring, engine bootstrapping | §Q6(2)/(3) | Apply validation extraction; bootstrap extraction rejected on line-budget rail (FULL-C1 above) |
| C2 | `guard_open!`/`ensure_process!` not exact §3.1 vocabulary (`assert_`/`enforce_`) | §3.1 | Reject — names clear, renames are churn |
| C3 | `clock: -> { Time.now }` repeated in 4 bind_* signatures | DRY-ish | Reject — sharing a Proc instance changes identity semantics |
| C4 | Ticket-tag comment prefixes ("P13-A:", "Slice A/C:", "P3:") | §11 | Reject — content is load-bearing why; tag-stripping is cosmetics |
| C5 | `integrity_check` validates with a 4-line inline `unless` between fetch and freeze | Q6(3) | Apply predicate extraction |
| C6 | Public readers expose `pool`/`kernel` internals | §5/§6 | Out of scope — cross-gem API + documented seam surface |

Behavior-preservation proofs applied before editing:
- C1: raise order preserved (limits → repair_permissions → notifier → fault_injector); moving `@notifier`/`@pid`/`@fault_injector` assignments above validation is unobservable because a failed construction re-raises through `rescue Exception` and the half-built object never escapes; `@pool&.close` rescue semantics unchanged; fault_injector normalization (`|| ->…`) still precedes its own check, so a nil stays legal.
- C5: `integrity_clean?` is a pure function of the result hash; raise class/message bytes unchanged ("SQLite integrity check failed").
- Repo-wide grep (incl. send/__send__/public_send): `guard_open!` has zero references outside adapter.rb; `ensure_process!` in connection_pool.rb is an independent definition; no subclasses of `Adapter` exist.
