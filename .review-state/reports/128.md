# Combined pass — rank 128 gems/tamoz-sqlite/lib/tamoz/sqlite/checkpoint_queries.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (history before_sequence guard → De Morgan positive form); C2 (pending_outcomes:
hoist `payload = write.fetch(3)` so verified bytes and emitted bytes share one binding)
Rejected: C3 (pinned nil/false return guard in `latest`); C4 (extraction from
`pending_outcomes` forbidden — recorded durable-transaction-core cohesion exception);
C5 (`decode_latest_checkpoint_in_transaction` signature is positional cross-file API +
boundary_source_audit contract); C6 (latest/find guard asymmetry encodes two different
pinned contracts); C7 (binds one-liner = churn, current form mirrors bind order);
C8 (conditional `durable_pending` fetch — read count/order is the documented contract)

Diff stat: 1 file changed, 4 insertions(+), 3 deletions(+)
Commands: ruby -c → OK · rubocop --cache false → BEFORE 0 offenses / AFTER 0 offenses ·
ruby -Itest test/sqlite_checkpoint_seams_test.rb → 7 runs, 14 assertions, 0 failures,
0 errors · ruby -Itest test/sqlite_checkpoint_test.rb → 6 runs, 23 assertions, 0 failures,
0 errors · ruby -Itest test/sqlite_boundary_source_audit_test.rb → 7 runs, 50 assertions,
1 error — PRE-EXISTING: reproduced identically with the pristine HEAD version of this file
(checkout → rerun → restore); failing helper `verify_pending_writes!` is defined in another
audited boundary file, not here; not chased.
Deviations: none
FLAG (owner decisions): pre-existing red outside my ownership:
SQLiteBoundarySourceAuditTest#test_real_capability_sources_exactly_match_the_registry fails
on HEAD ("boundary helpers are unreachable: verify_pending_writes!") independent of this pass;
orchestrator should route to whoever owns the audited boundary files.

## Phase 1 — ANALYZER candidates (written before any edit)

BEFORE state: `ruby -c` OK; `rubocop --cache false` = 0 offenses. All 7 public methods are
called by `CheckpointStore` (`checkpoint_store.rb` lines 42–180) — no dead surface; every
public signature is cross-file API and untouchable. Recorded cohesion exception applies
(durable transaction core / separate reconciliation reads): method-level improvements only,
no extraction. SQL heredoc bytes and row shapes pinned — no SQL text touched.

- C1 `history` before_sequence guard: `!(before_sequence.is_a?(Integer) &&
  !before_sequence.negative?)` is a triple negation. De Morgan rewrite to
  `!before_sequence.is_a?(Integer) || before_sequence.negative?` — provably identical
  truth table and identical evaluation order (is_a? first; negative? only reached when
  Integer; falsy input skips the branch), same ConfigurationError message bytes. APPLY.
- C2 `pending_outcomes` inner write loop: `write.fetch(3)` appears twice (digest verify +
  row literal); hoisting `payload = write.fetch(3)` binds the verified bytes to the emitted
  bytes and drops the duplicate fetch. Pure-read refactor, no ordering change. APPLY.
- C3 `latest` nil/false guard `return nil unless row && row.fetch(0)` — comment pins the
  original semantics; changing it alters return bytes for a falsey first column. REJECT
  (pinned behavior).
- C4 Extract per-task write loader / metadata builder out of `pending_outcomes`.
  REJECT — recorded cohesion exception forbids extraction across this read seam; the
  separate read barriers are the documented contract.
- C5 Rename/reorder params of `decode_latest_checkpoint_in_transaction(tx, …, label)`.
  REJECT — called positionally by `CheckpointStore` (#decode_latest_checkpoint… line 149)
  and the tx-named-helper shape is asserted by `sqlite_boundary_source_audit_test`;
  cross-file API + audit contract.
- C6 Align `find`'s missing-row handling with `latest`'s guard (or vice versa). REJECT —
  the two guards encode different pinned contracts (find: nil row ⇒ nil; latest: nil/false
  first-column guard); row shapes pinned.
- C7 Alternative `binds` constructions in `history` (splat-interpolated one-liner).
  REJECT — churn; incremental build mirrors bind order in the SQL and is clearer.
- C8 Drop the conditional `durable_pending` fetch in `materialize` (always reconcile).
  REJECT — changes the number and order of durable reads; documented as deliberate.
