# Fix report — rank 31 gems/tamoz-evals/lib/tamoz/evals/verifier.rb

Applied: C1 (verify_provenance! → components/structure/citations step-downs); C2 (verify_evidence_process! → status-consistency/termination-action/kill-signal/streams step-downs); C3 (resolve_reference_path! extracted out of verify_references! loop); C4 (validate_citations! replaces triplicated "cites missing evidence" loops in verify_semantics!/verify_evidence_semantics!/provenance citations); C5 (both renames: assert_unique_ids!→validate_unique_ids!, assert_no_diagnostic_errors!→validate_no_diagnostic_errors!); C6 (three-arm case dispatch in verify_semantics! + verify_result_semantics! extraction); C7 (expand_artifact_path extracted from verify; TypeError/ArgumentError rescue travels with it); C8 (verify_evidence_processes! + verify_evidence_timing! out of verify_evidence_semantics!)
Rejected: FLAG mirror-table merge (`verify_evidence_status!`/`verify_decision_evidence!`) — owner-only decision, verdict/message bytes untouchable; FLAG public-surface narrowing (DIGEST_DOMAINS reached by test/release_evaluation_manifest_test.rb:150, RESULT_DECISIONS/Verification narrowing needs cross-file/test edits) — outside owned path

Diff stat: gems/tamoz-evals/lib/tamoz/evals/verifier.rb | 223 ++++++++++++++++----------- 1 file changed, 129 insertions(+), 94 deletions(-) (561 → 596 lines, net +35)

Commands:
1. `ruby -c <owned>` — before: Syntax OK; after: Syntax OK
2. `rubocop --cache false <owned>` — before: 1 inspected, 0 offenses; after first edit pass: 5 offenses (all Style/GuardClause on tail raises that became last expressions in my new methods) — fixed by guard-clause conversion, none suppressed; final: 1 inspected, no offenses detected
3. `ruby -Itest test/evals_verifier_test.rb` — baseline AND post-edit: 23 runs, 294 assertions, 0 failures, 0 errors, 0 skips
4. `ruby -Itest test/release_evaluation_manifest_test.rb` — post-edit: 9 runs, 149 assertions, 0 failures, 0 errors, 0 skips (exercises Verifier.verify end-to-end incl. DIGEST_DOMAINS)
5. `ruby -Itest test/benchmark_controls_test.rb` — post-edit: 14 runs, 42 assertions, 0 failures, 0 errors, 0 skips

Safety proofs:
- Check ORDER identical: every validation runs in its original sequence inside the step-downs; the only intra-method reorder candidate (kill-signal vs termination case) was deliberately kept as a separate leaf placed AFTER the case so first-error semantics are unchanged.
- Fetch-order safety: keys re-fetched across siblings (provenance, attempts, termination, termination_reason, timing, processes) are all schema-required (gems/tamoz-evals/schemas/{evidence,result}.schema.json $defs verified), and Schema.validate! gates entry, so re-fetches cannot surface a different error for any reachable input.
- Message bytes: string-literal diff HEAD↔worktree shows only the three split citation-message fragments reassembled into the byte-identical `"#{kind} #{record.fetch("id").inspect} cites missing evidence #{missing.inspect}"` template ("hard gate"/"claim"/"attempt"); all raise classes unchanged; error bytes pinned by 294+149 assertions passing.
- C5 grep proof: repo-wide grep for both old names matches only this file (def + call sites); failures raise InvalidArtifactError = §3.1 validate_* (bad input named by field), matching existing `Schema#validate!` vocabulary.
- Constants untouched: DIGEST_DOMAINS, RESULT_DECISIONS, Verification, MEMORY_EVAL_SUITE_ID byte-identical.

Deviations:
- C1 realized as three leaves (components / structure / citations), not the proposed two: the two-leaf shape leaves a 29-line body, breaking Q6 ≤20 for new code.
- C2 realized as four leaves, not two: two-leaf shape leaves ~24-line bodies over ceiling; four named checks keep the parent reading as the domain checklist.
- C8 keeps `document.fetch("timing")` inside verify_evidence_timing! (candidate signature passed timing down, which would have front-run the fetch past the process checks); fetch order preserved exactly.
- C4 helper signature `(kind, records, cited_field:, reference_ids:)` — keywords per §4 beyond 1–2 positionals; cited ids key differs by site ("evidence_ids" vs "reference_ids").
- One one-line why-comment added above the `else` arm of verify_semantics! (§11 load-bearing: else provably "result" via DIGEST_DOMAINS gating).
