# Combined pass — rank 257 gems/tamoz-agent-healing/lib/tamoz/agent/healing/classification/matrix.rb

## Phase 1 — ANALYZE (written before any edit)

Not mostly declarative data (it is tally/report computation, ~60 lines of logic over
declarative bucket shapes), so the corpus EXEMPT precedent does NOT apply; the binding
note's second branch governs: recorded outputs pinned (`test/healing_matrix_test.rb`
asserts exact denominators/rates/score), method-level naming/step-down only, no
extraction. File state: rubocop BEFORE = 0 offenses; slice 33
(`docs/QUALITY_PROGRAM_STATE.md` #33) extracted this exact method set at 0 smells.

### Candidates

- **C1 — rename `tally!` → `tally`.** §3: "Mutating methods end in `!` only when a safe
  twin exists." There is no safe twin and no `Matrix.tally` anywhere. Sole caller is
  line 29 in this file; repo-wide grep for `tally!` outside the owned file (gems, apps,
  bin, test) finds nothing, and send/__send__/public_send dynamic dispatch finds
  nothing. The module header references the name once and moves with it.
  Behavior-preserving by construction.
- **C2 — `quality(correct_rate, never_denominator, over_rate, handled_denominator)` →
  kwargs.** §4: "Keyword arguments beyond one or two positional parameters; no long
  positional lists." Four positionals interleaving two rates with two denominators is a
  transposition hazard; kwargs put each rate beside its denominator label. Sole caller
  is lines 94–95 in this file; repo-wide grep proves no external caller and no dynamic
  dispatch. Output hash bytes identical; pinned test still gates it.
- **C3 (rejected) — rename `empty_tally` → `build_*`.** Noun factory name states its
  return plainly and reads as a small DSL next to `report`; churn without conformance
  gain.
- **C4 (rejected) — trim the 18-line module reek-justification header.** It is the
  site-written exception record per CODING_STANDARD §1/§11 ("an exception is written at
  the site"); removing recorded rationale for line-count aesthetics is not surgical.
- **C5 (rejected) — mixed positional/kwarg on `abstention_quality(per_category,
  rule:)`, `run(cases:, rule:)`, `tally(per_category, entry, rule)`.** Two params or
  fewer — within §4 tolerance; renaming the positional accumulators would be churn.
- **C6 (rejected) — FeatureEnvy/DuplicateMethodCall/DataClump "fixes" in `tally!`
  (bucket[...] += 1 ×5).** Header documents the exception (the data is the subject);
  any value-object extraction is forbidden by the note and outlives its one `run`.

## Phase 2 — APPLY

# Combined pass — rank 257 gems/tamoz-agent-healing/lib/tamoz/agent/healing/classification/matrix.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (`tally!` → `tally`, §3 bang-only-with-safe-twin, header ref moved with it); C2 (`quality` 4 positional params → kwargs, §4 no long positional lists, each rate now sits beside its denominator label)
Rejected: C3 (rename `empty_tally` → `build_*` — noun factory states its return plainly; churn without conformance gain); C4 (trim 18-line module reek-justification header — site-written recorded exception per §1/§11; not surgical); C5 (mixed positional/kwarg on `run`/`tally`/`abstention_quality` — ≤2 params, within §4 tolerance); C6 (FeatureEnvy/DuplicateMethodCall/DataClump fixes in the tally buckets — documented exception, extraction forbidden by the binding note)
Diff stat: 1 file changed, 6 insertions(+), 6 deletions(-) (net 0 LOC)
Commands:
- `ruby -c gems/tamoz-agent-healing/lib/tamoz/agent/healing/classification/matrix.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses
- `ruby -Itest test/healing_matrix_test.rb` → 4 runs, 44 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/healing_failure_contract_test.rb` → 35 runs, 264 assertions, 0 failures, 0 errors, 0 skips
Deviations: none. No test file touches the helper methods directly; `test/healing_matrix_test.rb` pins `Matrix.run` report values exactly and passes unchanged.
FLAG (owner decisions): none
