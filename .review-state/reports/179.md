# Combined pass — rank 179 gems/tamoz-agent-capabilities/lib/tamoz/agent/child_task_dispatcher.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract_task → validate_task!); C2 (extract_capabilities → validate_capabilities!); C3 (assert_capabilities_within_authority! → enforce_capabilities_within_authority!); C4 (assert_budget_available! → enforce_delegation_budget!); C5 (assert_durable_identity! → enforce_durable_identity!)
Rejected: R1 (rename assert_descriptor!: descriptor mismatch is a mis-wiring invariant — "a bug in Tamoz" — so assert_* is the correct verb per §3.1, not a policy refusal); R2 (rename assert_no_secrets!: it asserts an owned invariant over already-validated values; sibling child_task.rb keeps secret-shape checks inside validate_* bodies, no standalone enforce_ precedent — churn on the security path); R3 (extract initialize's respond_to guards into helpers: two guard clauses read coherently inline as init-time dependency contracts; extraction is indirection without clarity gain, §4); R4 (reorder private methods into strict call order: pure churn, no intent change); R5 (pass normalized `capabilities` instead of re-fetching raw in validate: changes the secret-check input from raw to dedup'd for zero readability gain — security-path edit not worth proving again); R6 (rename child_context → delegation_context: marginal clarity, not a lexicon violation)
Diff stat: 1 file changed, 10 insertions(+), 10 deletions(-)
Commands:
- BEFORE: `ruby -c` Syntax OK; `rubocop --cache false` 0 offenses.
- `ruby -c <file>` → Syntax OK; `rubocop --cache false <file>` → 0 offenses (no increase).
- `ruby -Itest test/agent_phase4_capability_test.rb` → 7 runs, 31 assertions, 0 failures, 0 errors, 0 skips.
- `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips.
- Other candidate tests (`agent_child_task_runtime_test`, `agent_child_task_test`,
  `child_environments_test`) reference neither ChildTaskDispatcher nor delegate_child_task —
  no coverage touches this file beyond the two run above.
Deviations: none
FLAG (owner decisions): none

## Phase 1 audit (recorded before any edit)

Public surface = generic tool interface consumed polymorphically by
capability_binding.rb (`dispatcher.effect_intent/maximum_effect_output_bytes/safety`) and
session effects; plus `attr_reader :parent_profile`. All pinned — untouched.

Candidates:
- C1/C2 `extract_task` / `extract_capabilities`: fetch + shape-validate + normalize and return
  the value. §3.1: that is `validate_*` ("value matches its declared shape", returns canonical
  form); `extract_` is outside the lexicon. Sibling precedent: ChildTask#validate_task!.
- C3 `assert_capabilities_within_authority!`: raises ToolPolicyError 'child capabilities exceed
  parent authority' on caller input — policy refusal where authority lives here → `enforce_*`.
  Sibling precedent: ChildTask#enforce_parent_capabilities! raises the identical message.
- C4 `assert_budget_available!`: ToolPolicyError refusal when delegation budget exhausted —
  caller-state refusal, not an internal-bug invariant → `enforce_delegation_budget!`.
- C5 `assert_durable_identity!`: ToolPolicyError refusal when the caller context lacks durable
  thread/request identity → `enforce_durable_identity!`.

Rename safety proof (repo-wide grep incl. test/, apps/, bin/): each renamed name occurs only in
child_task_dispatcher.rb; zero `send`/`__send__`/`public_send` dynamic references. Bodies are
byte-identical after rename → error/message bytes, wire shapes, digests unchanged.
