# Combined pass — rank 120 gems/tamoz-sqlite/lib/tamoz/sqlite/request_inbox_enqueuer.rb

## Phase 1 — ANALYZE (candidates, written before any edit)

BEFORE gates: `ruby -c` OK · `rubocop --cache false <file>` = 0 offenses.

Scope note (binding block): inbox enqueuer; claim/CAS family siblings already passed;
error bytes pinned; method-level naming/step-down only. Cross-class callers proven by
repo-wide grep (incl. `send`/`__send__`/`public_send`):
- `enqueue_request_in_transaction!` → called by `request_inbox.rb:75` (P13-A scheduler seam) and
  `checkpoint_store.rb:71`; public API, untouchable.
- `ensure_namespace_for_enqueue!` → reached dynamically by the facade's private seam
  (`request_inbox.rb:172`, `@enqueuer.__send__(__method__, ...)`); name load-bearing, untouchable.
- `existing_row`, `next_request_sequence`, `persist_new_request!` → zero external references.

### Candidates

- **C1 — APPLY. Rename private `existing_row(row, input)` → `verify_existing_row!(row, input)`.**
  Criterion 1 (name states intent) + §3.1 checking vocabulary: the method verifies a claim
  ("this request id is bound to the same input") against evidence (stored digests
  `row.fetch(4)/(5)/(6)/(9)`) and raises `CheckpointConflictError` — exactly `verify_*`
  ("this claim holds against evidence — a digest"), not a row query as the current name implies.
  Same-gem precedent names the identical concept `verify_existing_writes!`
  (`checkpoint_store.rb:199`) and digest checks are `Wire.verify_digest!`. One in-file caller,
  no dynamic sends; error message bytes untouched.
- **C2 — REJECT. `row = nil` + block assignment in `enqueue_request` could use the transaction's
  return value.** Cannot personally prove `adapter.__send__(:transaction)` returns the block result
  across adapters; behavior pinned. Also identical pattern survives in the passed sibling
  `request_inbox_claimer.rb#claim_next_request`.
- **C3 — REJECT. `build_input` is 24 lines (> 20 normal ceiling).** Recorded exception already on
  the method (`Metrics/MethodLength` disable with reason: normalization mirrors the durable enqueue
  API); it is a linear normalize pipeline and splitting is extraction, which the cohesion note and
  "method-level only" scope exclude.
- **C4 — REJECT. `next_request_sequence` / `persist_new_request!` are public but internal-only.**
  Visibility is public/cross-gem API — untouchable per brief. The class-level `# :nodoc:` (line 5)
  already keeps the documented surface honest, so adding method-level markers is redundant churn;
  adding the sibling's "kept public for the facade" comment would be false documentation (the facade
  does not call these).
- **C5 — REJECT. Opaque column indices (`row.fetch(4/5/6/9)`).** Header records the family stance
  ("durable SQL stays explicit"); claimer/recovery read raw indices identically; named-index
  constants or a row value object would be extraction/machinery outside method-level scope, and the
  column order is wire-pinned material.
- **C6 — REJECT. Rename `encode_payload` (returns `[payload_bytes, payload_digest, input_digest]`).**
  Name conforms to the §3.1 durable-codec lexicon (`encode_*`); digesting the encoded payload is
  intrinsic to it, the return order feeds the pinned `input_digest` domain `'tamoz.sqlite.request'`,
  and a longer name adds a second spelling without clarity.
- **C7 — REJECT. Compact def grouping (missing blank lines between some defs).**
  `Layout/EmptyLineBetweenDefs` is disabled at class scope with a recorded reason ("compact grouping
  keeps the writer under 250 lines"); reformatting is mass style churn.
- **C8 — REJECT. Dense guard `(operation == 'redirect') == (delivery == 'redirect')` in
  `validate_redirect_pair!`.** Correct, one line, name already carries intent; any rewrite adds
  branches or churn with real risk of flipping boolean semantics guarding pinned validation errors.

Step-down check (criterion 5): `enqueue_request` reads build → transact → materialize;
`enqueue_request_in_transaction!` reads time → ensure namespace → existing-or-new → persist →
re-read. Public entry points read as a small DSL (criteria 3–4): `enqueue_request`,
`enqueue_request_in_transaction!(tx, input:)` mirror the facade verbatim. No violations beyond C1.

## Phase 2 — APPLY

Applied C1 (rename private `existing_row` → `verify_existing_row!`, caller at line 46 updated).
Net change: 2 lines. Everything else rejected above.

## Gates

1. `ruby -c <file>` → Syntax OK (before and after).
2. `rubocop --cache false <file>` → 0 offenses before, 0 offenses after (no increase; nothing to fix).
3. Focused test `ruby -Itest test/sqlite_request_inbox_test.rb`:
   - BEFORE edit: 8 runs, 46 assertions, 0 failures, 0 errors.
   - AFTER edit: first run failed at **load time** with `NameError: constant
     Tamoz::Graph::Compiled::DEFAULT_WRITER_TTL not defined` from
     `gems/tamoz-graph/lib/tamoz/graph/compiled.rb:249` — a concurrent out-of-scope edit by another
     agent (working tree shows `compiled.rb` mid-refactor: constant definition removed while its
     `private_constant` remains). The enqueuer file is not on that load path's error; retrying after
     a settle delay. Result recorded below.
   - AFTER edit (retry): **8 runs, 46 assertions, 0 failures, 0 errors, 0 skips** — pass.

# Combined pass — rank 120 gems/tamoz-sqlite/lib/tamoz/sqlite/request_inbox_enqueuer.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename private `existing_row` → `verify_existing_row!`, matching §3.1 verify-vocabulary and same-gem precedent `verify_existing_writes!`; caller updated in file); Rejected: C2 (transaction return value unprovable across adapters + family pattern), C3 (`build_input` length is a recorded disable; split = extraction, out of scope), C4 (visibility of `next_request_sequence`/`persist_new_request!` is public API, untouchable; class-level `:nodoc:` already covers marking), C5 (raw row indices are family convention, wire-pinned order; constants = machinery), C6 (`encode_payload` conforms to encode_* lexicon; return order feeds pinned input_digest domain), C7 (compact def grouping has recorded class-scope disable), C8 (dense redirect guard correct; rewrite risks boolean semantics guarding pinned error)
Diff stat: 1 file changed, 2 insertions(+), 2 deletions(-) (owned file only)
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses before, 0 after · `ruby -Itest test/sqlite_request_inbox_test.rb` → before 8 runs/46 assertions/0 failures; after 8 runs/46 assertions/0 failures/0 skips
Deviations: one transient gate-3 failure at load time caused by a concurrent out-of-scope agent edit to gems/tamoz-graph/lib/tamoz/graph/compiled.rb (missing DEFAULT_WRITER_TTL mid-refactor); identical command passed on retry with no change to this file
FLAG (owner decisions): `next_request_sequence` and `persist_new_request!` are public but have zero external callers (repo-wide grep incl. send/__send__/public_send); narrowing them to private would tighten §6 "narrow public APIs" but is a public-API decision left to the owner
