# Combined pass — rank 88 gems/tamoz-agent/lib/tamoz/agent/runtime/step_execution.rb

Verdict: COMMITTED-CANDIDATES

## Phase 1 — ANALYZE (candidate list, written before any edit)

Baseline: `ruby -c` OK; `rubocop --cache false` = 107 offenses (94 pre-existing autocorrectable:
StringLiterals ×~70, Metrics on execute/gate_step/observation_for_denial/tool_failure_observation,
2× CollectionLiteralInLoop, ExplicitBlockArgument ×10 — the forwarding boilerplate).
`observation_bytes` is LIVE (runtime.rb:297,497) — not dead code. No recorded cohesion exception
covers this file. Method line counts: execute 73 counted (ceiling 20), gate_step 30,
resolve_ask 21, observation_for_denial 25, tool_failure_observation 25.

Candidates:

- C1: Decompose `execute` (73 lines, AbcSize 65.7/20, cyclo 12/8) into per-step /
  per-outcome helpers. REJECTED: a faithful decomposition rewrites ~200 changed lines — far
  beyond the ~60-line rail; the loop threads four locals across three `break` sites with pinned
  effect/event ordering, so partial extraction cannot be proven behavior-preserving cheaply.
  Escalate to the two-agent pipeline.
- C2: Merge `observation_for_denial` + `tool_failure_observation` into one observation builder.
  REJECTED as a whole (~110 changed lines > rail). Safe core applied instead as C3.
- C3: Extract the duplicated failure-signature recipe (SHA256 over JSON of kind/tool/reason/
  canonical-arguments digest) shared verbatim by both evidence builders into
  `build_failure_signature(tool, reason, arguments)`. The two pipelines must never disagree on
  bytes (per the file's own mirror comments); one recipe enforces that. Both methods drop under
  the 20-line ceiling. APPLY.
- C4: Name the twice-repeated `%i[action repair].include?(phase)` terminal-phase test as
  predicate `owns_repair_budget?(phase)` (vocabulary lifted from the file's own comment). Also
  clears both Performance/CollectionLiteralInLoop offenses. APPLY.
- C5: Extract `approval_request_event` builder from `gate_step` (30 → ~24 lines). REJECTED:
  gate_step sits at the 30-line hard ceiling, not beyond it; diff budget is better reserved and
  the gain is marginal.
- C6: `{ |event| yield event }` event-forwarding boilerplate (~40 sites here, shared shape with
  plan_review.rb). REJECTED: owner FLAG already covers this cross-file dedup — owner decision.
- C7: Pre-existing autocorrectable lint (StringLiterals family, EmptyLineAfterGuardClause ×2,
  RescuedExceptionsVariableName `error`, SpaceInsideHashLiteralBraces). REJECTED: none introduced
  by this pass; mass style churn is forbidden.

## Phase 2 — APPLY

Applied: C3 (`build_failure_signature(tool, reason, arguments)` — the SHA256-over-JSON
failure-signature recipe duplicated verbatim by `observation_for_denial` and
`tool_failure_observation` moved into one method; JSON payload key order kind/tool/reason/
arguments_digest preserved exactly, so digest bytes are identical); C4 (`owns_repair_budget?(phase)`
predicate replacing both `%i[action repair].include?(phase)` sites — vocabulary lifted from the
file's own comment).
Rejected: C1 (~200-line decomposition of `execute`, over the ~60-line rail; three `break` sites
thread four locals against pinned effect/event ordering — escalate to two-agent pipeline);
C2 (whole-builder merge ~110 lines, folded into C3); C5 (`approval_request_event` extraction from
`gate_step` — sits AT the 30-line hard ceiling, not beyond; marginal vs churn); C6 (event-forwarding
boilerplate — owner FLAG covers it, cross-file with plan_review.rb); C7 (pre-existing autocorrectable
lint — mass style churn forbidden, none of it introduced by this pass).

Diff stat: 1 file changed, 23 insertions(+), 20 deletions(-) — 43 changed lines, net +3.

Commands:
- `ruby -c gems/tamoz-agent/lib/tamoz/agent/runtime/step_execution.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 107 offenses / AFTER 98 offenses (no new cops, no new
  occurrences on changed lines; −2 Performance/CollectionLiteralInLoop, −2 Metrics/MethodLength —
  observation_for_denial and tool_failure_observation now under the 20-line ceiling; moved recipe
  lines carried their pre-existing StringLiterals offenses unchanged)
- `ruby -Itest test/agent_digest_resolution_test.rb` → 17 runs, 95 assertions, 0 failures, 0 errors
- `ruby -Itest test/agent_runtime_test.rb` → 11 runs, 54 assertions, 0 failures, 0 errors
- `ruby -Itest test/agent_repair_evaluation_test.rb` → 8 runs, 55 assertions, 0 failures, 0 errors
- `ruby -Itest test/agent_change_evaluation_test.rb` → 2 runs, 30 assertions, 0 failures, 0 errors

Behavior preservation: signature helper receives the same three values each site computed inline and
builds the identical hash literal in identical order before hexdigest; evaluation point within the
failure hash unchanged. `owns_repair_budget?` is the same pure expression; both call sites sit at
identical control-flow positions, so denial/failure continue-vs-break semantics are untouched.
Event emission order untouched (no emit site moved).

Deviations: none.

FLAG (owner decisions): none new. The standing event-forwarding-boilerplate flag (~40 `{ |event| yield event }`
sites here sharing shape with plan_review.rb) remains the one open cross-file decision; C6 was
rejected under it.
