# Combined pass — rank 135 gems/tamoz-core/lib/tamoz/core.rb
Verdict: CLEAN
Applied: (none) | Rejected: C1 (collapse `normalize_reconsideration`'s two block-form
unless-raise guards + reject-block to modifier form, 21→14 lines — Style/IfUnlessModifier
is deliberately excluded for this exact file in .rubocop_todo.yml, gates already clean at
0 offenses; pure churn on the cross-gem root namespace); C2 (extract `name = String(value)`
local in `serialized_tool_error_name` to avoid double coercion — single-expression idiom is
clear, zero functional gain); C3 (reorder module_functions into cohesive families for a
tighter top-down read — no step-down defect exists: `strings` already sits below its
`string` callee and every other function is a leaf; reordering public surface = mass churn);
C4 (rename `string`/`strings` toward §3.1 validate_* vocabulary — renames forbidden, every
constant/method here is cross-gem contract); C5 (drop the `@loader = loader` retention ivar
— cannot personally prove loader-GC/tooling neutrality, hard rail rejects it).
Diff stat: (no changes — git diff empty for owned file)
Commands:
- `ruby -c gems/tamoz-core/lib/tamoz/core.rb` → Syntax OK (before and after)
- `rubocop --cache false gems/tamoz-core/lib/tamoz/core.rb` → 1 file inspected, 0 offenses
  (BEFORE == AFTER; recorded BEFORE count 0). Legacy per-file excludes for this file in
  .rubocop_todo.yml are stylistic only (ModuleLength, RescuedExceptionsVariableName,
  Documentation, IfUnlessModifier, StringLiterals×2) — none mask a metric violation found.
- `ruby -Itest test/core_jcs_vectors_test.rb` → 14 runs, 73 assertions, 0 failures, 0 errors
- `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures, 0 errors
  (pins the Tamoz::Core module_function surface)
Deviations: none
FLAG (owner decisions): none

Audit notes (step-down lens per binding): constants → Zeitwerk setup → functions reads
top-down correctly. All methods ≤20 lines, one level of abstraction, no boolean params,
kwargs used properly, predicates end `?`. The long comments are §11 "why" material (homing
decisions, invariant numbers, frozen wire values) and stay.
