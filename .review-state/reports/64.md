# Combined pass — rank 64 gems/tamoz-agent/lib/tamoz/agent/runtime_directory.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (delete twin validator `ensure_legacy_schema!`; `migrate!` reuses existing `validate_schema_version!(document)` as its gate)
Rejected: C2 (un-send `self.class.send(:config_path, ...)` in `private_config_path` — instance deliberately delegates both `config_path` and `read_config_document` to the class-private defs; splitting the pattern keeps one send, and inlining `read_config_document` would duplicate the strict `safe_load_file(permitted_classes: [], aliases: false)` args whose drift is security-relevant); C3 (shared `config_section(key)` reader for sources/channels/stream tolerances — defaults differ (`[]` vs `{}`), and `source_settings`' `dig("sources", name)` currently raises TypeError for a non-Hash `sources:` where an `{}`-reading helper would silently disable the source; partial adoption = asymmetric churn); C4 (step-down extraction of `validate_channel!` into per-field validators — in-file annotation records the decision "one validation sequence per field, checked in the design's order"; binding pins layout contracts, method-level only); C5 (shared `configured_string_or(default)` for approval_policy_path/approval_profile — two short sites, saves nothing, obscures the readers); C6 (freeze `stream_bounds` result like sibling accessors — cannot prove no caller mutates the returned Hash); C7 (raise on unknown source even when `enabled` is false in `enabled_sources` — changes error semantics for existing operator configs)
Diff stat: 1 file changed, 1 insertion(+), 8 deletions(-) (net −7)
Commands: ruby -c PASS · rubocop --cache false BEFORE 0 / AFTER 0 offenses · ruby -Itest test/runtime_directory_config_test.rb → 6 runs, 43 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/comms_cli_ops_test.rb → 7 runs, 53 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Candidate list (Phase 1, written before any edit)

Audit frame: naming/vocabulary (§3), length/one-thing (≤20/30), one level of abstraction,
public-entry DSL readability, step-down; binding constraint — runtime-directory path/layout
contracts are PINNED, so only method-level naming and step-down moves are eligible. Recorded
cohesion exceptions stand. RuboCop BEFORE count: **0 offenses**; branch `verification-again`.

- **C1 — APPLY.** `migrate!` gates through private `ensure_legacy_schema!(version)` while an
  existing `validate_schema_version!(document)` carries a byte-identical error message and a
  superset membership check (`SCHEMA_VERSIONS`). After `migrate!`'s `already_current` early
  return (`version == SCHEMA_VERSION`), the old condition `version != LEGACY` is exactly
  equivalent to `version ∉ SCHEMA_VERSIONS`, so the swap is provably behavior-preserving:
  same Error class, same message bytes, same position in the sequence (before any backup or
  write). Deletes 6 lines of near-duplicate machinery and removes the one off-vocabulary name
  (`ensure_` here validates, it does not act). Repo-wide grep incl. send-by-name:
  `ensure_legacy_schema` occurs ONLY in this file (def + sole caller).
- **C2 — REJECT.** See header. Lateral churn; the remaining delegation is consistent and
  single-sources the strict loader args.
- **C3 — REJECT.** See header; not provably behavior-preserving at the malformed-config edge.
- **C4 — REJECT.** Recorded decision stands; complexity annotations already in place.
- **C5 — REJECT.** Churn.
- **C6 — REJECT.** Unprovable caller-mutation assumption.
- **C7 — REJECT.** Error/wire semantics untouchable; closed set plausibly governs what can be
  turned ON, not what may sit disabled in operator data.

## Gates (after edit)

- `ruby -c` → Syntax OK
- `rubocop --cache false <file>` → 0 offenses (BEFORE 0 — no increase)
- `ruby -Itest test/runtime_directory_config_test.rb` → 6 runs, 43 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/comms_cli_ops_test.rb` (migrate! via CLI) → 7 runs, 53 assertions, 0 failures, 0 errors, 0 skips

## Diff stat

```
 gems/tamoz-agent/lib/tamoz/agent/runtime_directory.rb | 9 +--------
 1 file changed, 1 insertion(+), 8 deletions(-)
```
