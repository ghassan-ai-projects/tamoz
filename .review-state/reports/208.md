# Combined pass — rank 208 gems/tamoz-graph/lib/tamoz/graph/state_operations.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (drop dead `namespace:` kwarg from private `compatible_latest`; sole caller line 54 never passes it and manual updates are pinned to root namespace `[]` — forwarded call identical)
Rejected: C2 (rename `snapshot` → `build_snapshot` per §3 build_ vocabulary — it IS the body of public `Compiled#snapshot` (compiled.rb:173, file not owned); cross-gem API untouchable); C3 (rename private `compatible_latest` → `compatible_latest!` for raising convention — identical non-bang forwarder idiom exists in writer_run_executor.rb:150; renaming one copy diverges the sibling seams, churn); C4 (rename `update`'s positional param shadowing the method name — domain vocabulary calls this object an "update" (`normalize_update`), no recursive call inside, rename is pure churn); C5 (step-down extraction of `state`'s find-or-latest branch and of `update`'s fork/advance middle — durable transaction core, extraction explicitly out of scope for this binding); C6 (restructure `append_checkpoint`'s delete-based attribute massaging — writer protocol/wire semantics pinned).

## Phase 1 audit notes (recorded before editing)

- Public surface state/history/update/append_checkpoint/snapshot are all 1-line delegations
  from Compiled (compiled.rb:141/145/160/169/173) — every public name is pinned API.
- Lengths: update ~39 lines (over the 30 hard ceiling) and append_checkpoint 26 — both stand
  under the recorded cohesion exception (checkpoint protocol keeps transitions explicit;
  reek/rubocop suppressions at top say the same). No extraction permitted.
- Names read as a small DSL; predicates/error bytes (`CheckpointConflictError 'checkpoint not
  found'`, history-limit errors) are pinned.
- Only genuine defect found: speculative `namespace: []` parameter on the private
  `compatible_latest` wrapper. Manual updates cannot target a namespace by construction
  (`open_writer(thread, [])`, `namespace: []` in update) — the knob invites misuse and its
  twin in writer_run_executor genuinely needs it, this one does not.

## Gates

- BEFORE rubocop --cache false: 0 offenses. AFTER: 0 offenses.
- ruby -c: Syntax OK before and after.
- Focused tests: test/graph_history_test.rb, test/graph_execution_test.rb.

## Diff stat

```
 gems/tamoz-graph/lib/tamoz/graph/state_operations.rb | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)
```
Net LOC delta: 0.

## Commands

- `ruby -c gems/tamoz-graph/lib/tamoz/graph/state_operations.rb` → Syntax OK (before and after)
- `rubocop --cache false <file>` → BEFORE 0 offenses; AFTER "1 file inspected, no offenses detected"
- `ruby -Itest test/graph_history_test.rb` → 7 runs, 30 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/graph_execution_test.rb` → 11 runs, 43 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/graph_durable_runner_test.rb` → 19 runs, 39 assertions, 0 failures, 0 errors, 0 skips

Deviation: none

FLAG (owner decisions): none
