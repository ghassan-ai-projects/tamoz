# Combined pass — rank 172 gems/tamoz-observability/lib/tamoz/observability/catalog.rb
Verdict: EXEMPT
Applied: none | Rejected: C1 (stray `private_class_method :comms_events` sits at line 183 below `metric_catalog` instead of after `comms_events` at line 146 — relocation is runtime-equivalent (declaration executes before CATALOG init either way; repo-wide grep shows zero callers) but purely cosmetic; file is ~85% pinned declarative registry data → EXEMPT means change nothing); C2 (`metric_catalog` is public while its three sibling seeders are private — privatizing changes the public API surface of `Tamoz::Observability::Catalog`, which test/public_api_test.rb pins; public/cross-gem API untouchable without owner sign-off); C3 (`model_events` 54 / `comms_events` 31 / `metric_catalog` 35 lines exceed §6 ceilings — they are flat literal tables of pinned signal names; splitting adds machinery with no logic to step down into); C4 (`metric_catalog` naming asymmetry vs `*_events` siblings — rename needs proven callers and buys nothing; name already reads clear)
Diff stat: (no change — `git diff --stat` empty)
Commands: `ruby -c catalog.rb` → Syntax OK · `rubocop --cache false catalog.rb` BEFORE → 0 offenses · AFTER → 0 offenses · `ruby -Itest test/observability_catalog_test.rb` → 10 runs, 26 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): (1) one-line cosmetic fix available: move `private_class_method :comms_events` up to directly after `comms_events` (line ~147). (2) decide whether `seed` and `metric_catalog` should be private like their siblings — that is an API-surface decision, not a clean-code edit.
