# Combined pass — rank 103 gems/tamoz-sqlite/lib/tamoz/sqlite/effect_preparation.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (name `row.fetch(11)` as `current_attempt_number` local — one assignment at the top of the prepared/running branch, three use sites: attempt lookup, abandon_unstarted params, unknown_attempt params); C2 (name attempt-row indices `attempt.fetch(4)`/`attempt.fetch(2)` as `attempt_deadline`/`attempt_fence` locals in the wait decision)
Rejected: C3 (extract duplicated `EffectAttemptLedger.grant_next!` blocks into a helper — extraction is out of scope under the recorded durable-transaction-core exception; method-level only); C4 (split/step-down `prepare` into smaller methods — same recorded cohesion exception, the Metrics rubocop disables are deliberate); C5 (rename `logical_key_text` / rework the logical-vs-built key duality — the name accurately describes the stored column in both branches; churn without provable intent gain); C6 (drop "duplicate" validation before `EffectJournalKey.build` — outer validations run first and their order decides which error/message bytes fire; pinned surface); C7 (table-driven status→action map — restructuring of visible branch ordering inside the transaction core)
Diff stat: 1 file changed, 7 insertions(+), 4 deletions(-)
Commands:
- ruby -c effect_preparation.rb → Syntax OK
- rubocop --cache false <file> → 0 offenses BEFORE, 0 after (no increase)
- ruby -Itest test/sqlite_effect_journal_test.rb → 8 runs, 35 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/effect_identity_test.rb → 5 runs, 12 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/sqlite_crash_recovery_test.rb → 3 runs, 30 assertions, 0 failures, 0 errors, 0 skips
Deviations: none. No test file references EffectPreparation directly (private_constant); coverage is via sqlite_effect_journal_test.rb, which drives every prepare branch, plus effect_identity_test.rb and the digest-pinning sqlite_crash_recovery_test.rb — all green.
FLAG (owner decisions): none
