# Combined pass — rank 106 gems/tamoz-agent-cli/lib/tamoz/agent/cli_comms_commands.rb
Verdict: CANDIDATES-ONLY
Applied: none | Rejected: C1 (single-use outcome set — a named constant adds indirection, inline reads clearer and the exit-code mapping bytes are pinned); C2 (dedupe the identical thread rescue blocks in run_gateway_loops — needs an extraction, barred by this module's recorded cohesion exception, and restructures Thread/ensure supervision semantics around pinned stderr bytes and exit codes); C3 (hoist per-iteration `codec = ... Memory::Surface.codec` in with_delivery_drainers — hoisting would share one codec instance across adapters where today each iteration gets its own call result; equivalence unproven, gain negligible); C4 (`poll['next_offset']` / `row['next_offset']` → fetch for consistency — churn on wire-summary projection; [] vs fetch differ on missing keys); C5 (usage-string verb order in cmd_comms differs from case order — error-message bytes are pinned); C6 (trim narrative why-comments above/inside run_gateway_loops — they record real invariants: trap-context ThreadError on lease release, supervised stop semantics; removal is §11 information loss)
Diff stat: (no diff — owned file untouched)
Commands: ruby -c gems/tamoz-agent-cli/lib/tamoz/agent/cli_comms_commands.rb → Syntax OK · rubocop --cache false BEFORE → 0 offenses · ruby -Itest test/comms_cli_test.rb → 12 runs, 60 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/comms_serve_supervision_test.rb → 1 runs, 6 assertions, 0 failures, 0 errors, 0 skips · rubocop --cache false AFTER → 0 offenses
Deviations: none
FLAG (owner decisions): none

## Candidate notes (Phase 1 audit, written before any edit)

Audit basis: name-states-intent · ≤20/30-line methods · one level of abstraction · public DSL
· step-down, plus docs/CODING_STANDARD.md §3/§3.1 vocabulary, §4 kwargs, §5 values/state, §6
design, §11 comments.

- Structure is sound: `cmd_comms` is a flat dispatch table to `comms_serve`/`comms_list` here and
  to `comms_doctor`/`comms_pair`/`comms_delivery`/`comms_request` defined in sibling modules
  (`cli_comms_doctor.rb`, `cli_comms_ops.rb`) mixed into the same CLI class via `cli.rb:26`;
  everything below `private` steps down into `CLICommsShared` helpers
  (`with_comms_runtime`, `build_transport`, `credential`, `ms_to_iso`, `GATEWAY_POLLER_PREFIX`).
- The long command methods sit under this file's own recorded metric-disable annotations with a
  stated reason ("one operator command per method; json-vs-text branching is the CLI's global
  convention") — per brief, recorded exceptions stand: method-level improvements only, no extraction.
- Every stderr line, usage string, JSON projection key and exit code in this file is operator- or
  wire-visible output; the binding note pins prompt bytes and exit codes, so C4/C5-class polish is
  rejected rather than risked.
- Test coverage exists and passes: test/comms_cli_test.rb (dispatch, serve --once --json bytes,
  exit codes, list rendering) and test/comms_serve_supervision_test.rb (run_gateway_loops
  supervision). Neither file is in SLOW_TESTS/SERIAL_TESTS per Rakefile.
