# Combined pass — rank 235 gems/tamoz-evals/lib/tamoz/evals/duplicate_key_detector.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (hoist SCALAR_TERMINATORS constant, single terminator check in parse_scalar); C2 (name magic bytes 0x5c/0x22 as BACKSLASH_BYTE/QUOTE_BYTE in parse_string)
Rejected: C3 (cross-method extraction of object/array comma-loops — out of method-level scope, machinery for zero output change); C4 (rename `current` — churn); C5 (regex/String#index rewrite of parse_string — risk to pinned escape/control/unterminated outcomes); C6 (split parse_object pair body — ~15 statements, under Q6 ceiling, split adds state-carrying param); C7 (expand keys.add? idiom — correct as written)
Diff stat: 1 file changed, 6 insertions(+), 3 deletions(-) (net +3)
Commands:
- ruby -c gems/tamoz-evals/lib/tamoz/evals/duplicate_key_detector.rb → Syntax OK (before and after)
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/duplicate_key_detector_test.rb → 2 runs, 35 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Phase 2 — APPLY result
Both edits are literal-equivalent renames/hoists: same membership set and eof? short-circuit
in parse_scalar; same byte comparisons in parse_string. No error message, verdict, ordering,
or API byte changed (`validate!` and `MAX_NESTING` untouched — MAX_NESTING is consumed by
harness/sqlite_selector_control_intervention.rb:151).

## Phase 1 — ANALYZE (written before any edit)

Context: hand-rolled JSON scanner whose sole job is detecting duplicate object keys
(JSON.parse silently keeps the last). Detection outputs are pinned: callers
(schema.rb:29, verifier.rb:85, harness/memory_store.rb:179,
harness/sqlite_selector_control_intervention.rb:147) use only
`DuplicateKeyDetector.validate!`, plus `MAX_NESTING` (sqlite_selector_control_intervention.rb:151).
Test test/duplicate_key_detector_test.rb pins accept/reject verdicts and the
"nesting exceeds" message fragment. BEFORE gates: ruby -c OK, rubocop --cache false = 0 offenses.

Candidates:

- C1 (naming/step-down, APPLY): `parse_scalar` line 112 packs two concepts — whitespace
  vs structural terminators — into one dense per-byte condition that rebuilds
  `[",", "]", "}"]` every iteration. Hoist `SCALAR_TERMINATORS = (WHITESPACE + [",", "]", "}"]).freeze`
  next to the other scanner constants and reduce the line to
  `@index += 1 until eof? || SCALAR_TERMINATORS.include?(current)`.
  Provable behavior-preserving: identical membership set, identical `eof?` short-circuit,
  loop still stops at the first terminator.
- C2 (naming, APPLY): `parse_string` compares raw bytes against magic `0x5c` / `0x22`
  in its escape/quote state machine. Name them `BACKSLASH_BYTE = 0x5c`, `QUOTE_BYTE = 0x22`
  alongside MAX_NESTING/WHITESPACE. Pure rename of literals; the `byte < 0x20` control
  check stays literal — its own raise message names the intent one line below.
- C3 (extraction, REJECT): unify the parse_object / parse_array comma-loops behind one
  parameterized sequence parser. Cross-method extraction of two distinct grammar
  productions; error paths ("expected }"/"expected ]"), empty-container shortcuts, and
  the key-collection step differ. Brief scopes this file to method-level changes and
  detection outputs are pinned — machinery added for zero output change. No.
- C4 (rename, REJECT): rename private `current` (e.g. to `peek`). `current` is accurate
  and idiomatic for a cursor scanner; rename is churn with no reader benefit.
- C5 (rewrite, REJECT): replace the byte-loop in `parse_string` with String#index/regex
  scanning. Would touch the exact logic that produces pinned unterminated-string,
  unescaped-control, and escape-handling outcomes; regex equivalence across multibyte +
  escapes is exactly where subtle drift lives. Reject.
- C6 (split, REJECT): extract the key/value pair body out of `parse_object` into a
  helper taking the mutable keys Set. Method is ~15 statements, well under the Q6
  ceiling; splitting adds a state-carrying parameter for no clarity gain.
- C7 (style, REJECT): expand `keys.add?(key)` into include?+add. Standard idiom, correct
  as written.

## Phase 2 — APPLY (filled in after gates)
