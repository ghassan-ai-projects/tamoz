# Combined pass — rank 192 gems/tamoz-tools/lib/tamoz/tools/skills/frontmatter.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (`description`: collapsed redundant double-regex control-char condition to one positive match — `A && B ≡ B` proven logically and empirically over ASCII 0x00–0x7F + NEL/LS samples; refusal bytes unchanged); C2 (`validate_metadata_pair!`: split the `return unless key == 'tamoz.risk' && !DECLARED_RISKS.include?(entry)` double-negative into two forward-reading guards; reject condition identical)
 |  Rejected: C3 (rename `string!` — reads as intentional field-schema DSL documented in the class header; rename drags reek-suppression comments along for zero ambiguity removed); C4 (unify `valid_string?`/`valid_metadata_value?` — extraction out of scope at method-level-only, and the shared helper needs an allow-empty boolean param, §4-forbidden); C5 (`metadata` detail hardcodes "32 pairs" vs `@limits.fetch(:max_metadata_pairs)` — fixing it changes pinned refusal bytes); C6 (inline `scan!` delegation — wrapper names pass one and keeps the two-pass step-down); C7 (hoist `key == 'name'` special case out of `string!` — moves pinned `skill_name_invalid` path across a validator boundary for no gain); C8 (reorder private methods — current order already follows `call`'s evaluation order with `reject!` last; reshuffle is churn)
Diff stat:  gems/tamoz-tools/lib/tamoz/tools/skills/frontmatter.rb | 5 +++— ; 1 file changed, 3 insertions(+), 2 deletions(-)
Commands: ruby -c frontmatter.rb → Syntax OK · rubocop --cache false → BEFORE 0 offenses / AFTER 0 offenses (one transient Style/GuardClause from C2's first form was fixed by adopting the two-guard form, not suppressed) · ruby -Itest test/agent_skills_adversarial_test.rb → 37 runs, 252 assertions, 0 failures, 0 errors · ruby -Itest test/agent_skills_test.rb → 18 runs, 96 assertions, 0 failures, 0 errors · ruby -Itest test/agent_skills_toolbox_test.rb → 18 runs, 152 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): C5 — `skill_metadata_invalid` detail "metadata must be a mapping of at most 32 pairs" hardcodes 32 while the ceiling is data-driven (`max_metadata_pairs`); byte-pinned so left untouched. Latent only if that limit ever moves off 32.

## Phase 1 candidate audit (recorded before any edit)

Audit basis: name-states-intent, ≤20-line methods (all private methods are 4–15 lines), one level of abstraction, public surface = `initialize`/`call` command pair, step-down order follows `call`. Constraints honored: parse errors and refusal message bytes untouched; no extraction, no signature changes, no renames applied.

- **C1** `description` L71 — redundant conjunct: every char matched by `\A[^\x00-\x08\x0B-\x1F\x7F]*\z` failing implies `[[:cntrl:]]` matches (the excluded ranges are a subset of cntrl), so `match?(cntrl) && !match?(negated)` ≡ `!match?(negated)` ≡ positive `match?(/[\x00-\x08\x0B-\x1F\x7F]/)`. Verified by brute force over all single ASCII codepoints embedded in text plus "", "abc", tab/LF/CR runs, U+0085, U+2028, U+0000, "é". Invalid encodings cannot reach this line (`valid_string?` gates on `valid_encoding?`). APPLY.
- **C2** `validate_metadata_pair!` L119 — compound inverted guard forces two mental flips; split into `return unless key == 'tamoz.risk'` then `return if DECLARED_RISKS.include?(entry)` so the final `reject!` states its own trigger. Truth-table identical for all key/entry combinations ('tamoz.eval-suite' still returns early; undeclared 'tamoz.risk' still refuses). APPLY.
- **C3–C8** REJECTED with reasons above.
