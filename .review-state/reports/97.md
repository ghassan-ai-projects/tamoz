# Combined pass — rank 97 gems/tamoz-observability/lib/tamoz/observability/metrics.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (`positive_integer` → `validate_positive_integer!`: noun-named raiser aligned with the file's own `validate_*!` vocabulary per §3; private, sole caller `initialize`, proven in-file by repo-wide grep incl. send/__send__/public_send)
Rejected: C2 (dedup triplicated `rescue StandardError → count_violation → :rejected` into a `with_` guard block: saves ~4 lines but hides the `:rejected` contract behind block-pass indirection over three dead-simple rescues — churn > clarity); C3 (rename `definition` → verb-led: marginal intent gain on a 3-line method whose call site already reads clean — style churn); C4 (make `validate_labels!` return normalized labels to drop the second normalization in `series_key`: cannot consolidate without either changing emitted `'labels'` key types (pinned wire shape) or keeping both transforms, which defeats the dedup); C5 (`percentile` sorts per call / `from_signals`–`from_documents` near-dupes: perf nit and public-API-adjacent dedup — out of scope for behavior-preserving pass)
Diff stat: 1 file changed, 3 insertions(+), 3 deletions(-) (owned file only; net 0 LOC)
Commands: `ruby -c metrics.rb` → Syntax OK · `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses · `ruby -Itest test/observability_runtime_test.rb` → 12 runs, 49 assertions, 0 failures, 0 errors · `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures (pinned public surface unchanged)
Deviations: none
FLAG (owner decisions): label VALUES are not normalized — `labels: {outcome: :ok}` and `labels: {outcome: 'ok'}` create two distinct series (`to_h` shows `:ok` vs `"ok"`) that render IDENTICALLY in `prometheus` output (duplicate exposition lines). Merging them is a behavior change (series identity), so untouched here.

## Phase 1 — ANALYZE (written before editing)

BEFORE state: `ruby -c` OK · rubocop --cache false = **0 offenses** · branch verification-again, file unmodified · focused suite `test/observability_runtime_test.rb` green (12 runs, 49 assertions, 0 failures).

Surface constraints honored: public DSL (`increment`/`observe`/`set`/`add_signal`/`add_document`/`to_h`/`prometheus`/`health` + `from_signals`/`from_documents`) is pinned cross-gem API; metric names, label keys, `DURATION_METRICS` map, and all serialized shapes are dashboard-pinned — audit restricted to method-level naming/step-down inside the class.

Audit against the five criteria:
1. **Name states intent** — validators uniformly follow `validate_*!`; the one outlier is `positive_integer(value, name)` (C1, apply). Other noun-led privates (`definition`, `series_key`, `histogram_summary`, `counter_lines`) read fine as data shapers at their call sites.
2. **Length** — longest method 10 lines (`initialize`); all ≤20. No violations.
3. **One level of abstraction** — `prometheus`/`to_h` compose line/serialize helpers; `record` chains definition → validation → keying → registration. Clean.
4. **Public entry points as small DSL** — reads well; pinned anyway.
5. **Step-down** — no layer jumps found.

Observed-but-rejected warts (see Rejected above): triplicated rescue policy (C2), double normalization of label keys (C4), symbol-vs-string label values creating duplicate prometheus-exposition series (FLAG).
