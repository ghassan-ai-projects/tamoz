# Combined pass — rank 126 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_selector_control_intervention.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (extract private validate_poll_request! from poll); C2 (extract private
assert_control_unchanged! + assert_intentional_kill! from verify_result!, called in original
order); C3 (step-down helper placement under private)
Rejected: C4 (read_stable_control! split — atomic TOCTOU sequence, each step consumes the prior
stat, Errno mapping spans the whole body; zero behavioral gain in the riskiest region);
C5 (normalize state-guard raises to fail_control! — outcome-identical via rescue clause, churn);
C6 (merge dual UTF-8 checks — predicates differ: own-encoding validity vs forced-UTF-8 validity
with BINARY short-circuit; merge risks behavior drift); C7 (rename fingerprint(stat) — stat
param carries meaning, rename churn)

Diff stat: 1 file changed, 35 insertions(+), 25 deletions(-) (net +10)

Commands:
- BEFORE: ruby -c <file> → Syntax OK; rubocop --cache false --format simple <file> → 0 offenses
- ruby -c <file> → Syntax OK
- rubocop --cache false <file> → 0 offenses (one Style/GuardClause introduced by C2 was fixed
  in place as guard clause, not suppressed; count back to BEFORE baseline of 0)
- ruby -Itest test/sqlite_selector_control_test.rb → 19 runs, 125 assertions, 0 failures,
  0 errors, 0 skips (covers poll authorize + every failure branch and verify_result!
  accept/wrong-result paths touched by this change)

Behavior-preservation proof: all moved statements are verbatim (conditions, message bytes,
order); helpers raise into poll/verify_result!'s existing inline rescue clauses unchanged;
@state/@authorized_fingerprint transitions and fail_control! semantics untouched; no public or
cross-gem signature changed (Intervention remains private_constant, reached only via
SQLiteSelectorControl#intervention).

Deviations: none. Known-red lanes (scenario driver, sqlite_raw_oracle) intentionally not run;
subprocess_runner_test.rb defines its own unrelated local Intervention and does not exercise
this class. No test outside sqlite_selector_control_test.rb touches this file.

FLAG (owner decisions): none
