# Combined pass — rank 232 gems/tamoz-sqlite/lib/tamoz/sqlite/comms_routes.rb
Verdict: CANDIDATES-ONLY
Applied: (none) | Rejected: C1 (delete duplicate private `transaction`/`read` forwarders — NOT provably behavior-preserving: removing the private overrides makes the inherited public `CommsStoreRows#transaction/read` visible, flipping `respond_to?(:transaction)` on CommsRoutes instances from false to true, an API-surface change; and the identical per-class re-declaration is the deliberate family idiom — `CommsStore` also includes `CommsStoreRows` yet re-declares both privately at comms_store.rb:1117/1121, as does `CommsOutbox` at 325/329); C2 (hoist duplicated `#{BINDING_COLUMNS.join(', ')}` in `binding` to a local — pure churn, zero gain in a registry-pinned SQL lane); C3 (name `current[0]/[1]/[2]` indices in `revoke_binding` via locals — positional SELECT-columns-to-binds mirroring is the documented §13 storage vocabulary of CommsStoreRows; localizing only here diverges from gem idiom for marginal gain); C4 (extract `bind_conversation_in_transaction!` mirroring the correspondent seam — no second consumer exists; unlike `approve_pairing`, which needs its seam, this adds machinery without need)
Diff stat: (no changes — owned file untouched)
Commands: `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/comms_routes.rb` → Syntax OK · `rubocop --cache false <file>` → BEFORE/AFTER 0 offenses (no edit) · `ruby -Itest test/sqlite_comms_store_test.rb` → 42 runs, 219 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/sqlite_boundary_source_audit_test.rb` (extra TAMOZ-SQLITE gate) → 7 runs, 55 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): C1 stands if the owner ever wants forwarder dedup — it requires accepting a visibility widening (or adding per-class `private :transaction, :read`) across the whole Comms family at once, so it is a cross-file decision, not an in-file cleanup. Non-blocking observations, no action needed: `binding`/`conversation` shadow Kernel methods but are domain DSL vocabulary and cross-gem API (untouchable); `bind_correspondent_in_transaction!` uses `!` for "runs inside the caller's transaction", documented by its §7 comment.
