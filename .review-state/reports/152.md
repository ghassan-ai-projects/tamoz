# Combined pass — rank 152 gems/tamoz-tools/lib/tamoz/tools/capability_host.rb
Verdict: CLEAN
Applied: (none) | Rejected: C1 (step-down extraction of `inventory`'s per-descriptor row build — method is over the 30-line hard ceiling but carries a recorded in-file cohesion exception, lines 43–46: keeping the full gate vector together prevents an omitted dimension becoming an implicit success path; emitted row shape is byte-pinned, extraction churns ~45 authority-adjacent lines for zero behavioral gain); C2 (split `inventory_reason`'s precedence chain — same recorded exception, line 176 "reason selection is the closed inventory protocol"; within the hard ceiling at 24 lines and the reason ordering is semantics-bearing); C3 (`dispatch`: `rescue ... => error; raise error` → bare `raise` — pure style churn on the error-identity boundary; the explicit re-raise documents the invariant-17 pass-through that pins class+message bytes); C4 (rename private `gate` — "gate" is this file's established vocabulary per its own comments; rename against pinned-shape code is churn); C5 (mutable default `source_reasons: {}` — only ever read via `key?`/`fetch`, never mutated; no hazard by construction, owner directive forbids preemptive coverage); C6 (`bind_dispatcher` membership check via `@registry.source_for` instead of `sources.any?` — `source_for`'s miss semantics live outside this file; swapping lookup mechanics on the sealed-surface guard without proof violates the behavior-preservation rail)
Diff stat: (no diff — file untouched)
Commands:
- ruby -c gems/tamoz-tools/lib/tamoz/tools/capability_host.rb → Syntax OK
- rubocop --cache false gems/tamoz-tools/lib/tamoz/tools/capability_host.rb → 0 offenses (BEFORE == AFTER == 0)
- ruby -Itest test/capability_host_test.rb → 21 runs, 91 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/capability_inventory_test.rb → 3 runs, 18 assertions, 0 failures, 0 errors, 0 skips
- grep send/__send__/public_send across gems/apps/test → no dynamic dispatch reaches this file's methods
Deviations: none
FLAG (owner decisions): none
