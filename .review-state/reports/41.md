# Combined pass — rank 41 gems/tamoz-agent-memory/lib/tamoz/agent/memory/admission.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (2 pre-existing Layout/ArgumentAlignment offenses corrected); C2 (store_rejected delegates to append — verbatim-duplicate persistence body deduped); C3 (dead `gate:` kwarg dropped from private reject_reason + its sole caller); C4 (episode_evidence reuses normalized_outcome; unreachable is_a?(Hash) tolerance removed)
Rejected: R1 (hides explicit policy ladder; near-extraction); R2 (churn, private single-caller); R3 (ordered policy ladder, split = indirection for a number); R4 (name reads at call site, builder-naming consistent); R5 (deliberate durability semantics, not touched); R6 (changes limits refresh semantics)
Diff stat: 1 file changed, 6 insertions(+), 13 deletions(-)
Commands:
- ruby -c gems/tamoz-agent-memory/lib/tamoz/agent/memory/admission.rb → Syntax OK
- rubocop --cache false <file> BEFORE → 2 offenses (pre-existing Layout/ArgumentAlignment ×2)
- rubocop --autocorrect --cache false <file> → 2 offenses corrected
- rubocop --cache false <file> AFTER → 0 offenses (count did not increase; decreased 2→0)
- ruby -Itest test/memory_engine_test.rb → 25 runs, 162 assertions, 0 failures, 0 errors, 0 skips
- Test coverage note: test/memory_engine_test.rb exercises all three gates (episode, owner_request, consolidation_candidate) incl. duplicate_identity, oversized, secret-shaped paths; stream_* tests touching admission are in the known-red/do-not-chase lane and were not run.
Deviations: none
FLAG (owner decisions): REJECT_REASONS (line 22) is declared but referenced nowhere in gems/, apps/, bin/, lib/, or test/. Removing it would change the class's public constant surface (forbidden here); owner to consume or remove deliberately.

## Phase 1 — ANALYZE

Scope note: memory admission policy is a recorded owning-class cohesion exception —
no extraction proposals; method-level naming/step-down only. Cross-gem API confirmed
untouchable via repo grep: `Admission.new(engine)` (surface.rb:66), `admit_episode`
(session_memory.rb:88, live_learning_handlers.rb:89, tests), `admit_owner_request`,
`admit_consolidation_candidate` (consolidation.rb:61), `MemoryRecordDigest.identity`
(consolidation.rb:273), `AdmissionResult`.

### Candidates

- C1 [apply] Fix 2 pre-existing `Layout/ArgumentAlignment` offenses (lines 232, 239).
  Mechanical layout correction; lowers offense count 2→0.
- C2 [apply] `store_rejected` duplicates `append`'s body verbatim (same
  `repository.append` kwargs). Delegate: `append(record)` inside the existing
  `rescue StandardError`. Behavior-preserving: identical persistence call; arg
  computation stays inside the rescue scope. Step-down dedup.
- C3 [apply] `reject_reason(record, gate:)` never uses `gate:` (dead kwarg). Drop it;
  sole caller is line 236, proven by repo-wide grep incl. send/__send__/public_send
  (no external hits).
- C4 [apply] `episode_evidence` guards `observed.is_a?(Hash)` — unreachable by
  construction: its only caller path runs `build_from_episode` → `normalized_outcome`
  first, which raises `MemoryPolicyError` for non-Hash and fetches the key (so it is
  always present). Reuse `normalized_outcome(episode)["outcome"]` instead of a second,
  more tolerant spelling of the same canonicalization (one spelling per idea).

### Rejected candidates (with reasons)

- R1 [rejected] Extract a `refusal(reason)` helper to DRY the six inline
  `AdmissionResult.new(rejected: true, reason: …)` sites across the three gates.
  The explicit per-gate ladders are the policy; hiding refusals behind a shared
  helper trades gate-level visibility for brevity (simple solution rule). Also
  borders on extraction beyond method-level scope.
- R2 [rejected] Convert `build_statement(task, observed, episode, kind)` to kwargs
  (§4 prefers kwargs past 1–2 positional). Private, one call site, argument order
  mirrors data flow; conversion is churn without clarity gain.
- R3 [rejected] Split `reject_reason` (~29 statements, at the hard ceiling). Each
  clause is exactly one ordered policy rule of a decision ladder whose ORDER is
  observable in refusal bytes; splitting adds indirection merely to satisfy a number
  (§4: limits are diagnostic).
- R4 [rejected] Rename `oversized_rejected`. Reads correctly at its single call site
  ("return oversized_rejected(...)"); noun-builder naming is consistent with the
  file's other builders (`episode_evidence`, `transition`, `rejected_version`).
- R5 [rejected] Make the `duplicate_identity` branch in `admit` store the rejected
  version like the other branches. The omission looks deliberate: the original
  candidate is already durable in the repository, so appending a second rejected row
  would duplicate durable evidence and change rerun-idempotency semantics.
- R6 [rejected] Hoist `MemoryLimits.fetch(:max_statement_bytes)` (4 call sites) into
  a constant. Would freeze a dynamic limits lookup for the process lifetime — a
  semantic change, not a cleanup.

### FLAG (owner decisions)

- `REJECT_REASONS` (line 22) is declared but referenced nowhere in gems/, apps/,
  bin/, lib/, or test/. Removing it would change the class's public constant surface
  (forbidden here); leaving it means an unmaintained list a reader may trust. Owner
  to consume or remove deliberately.

Verdict: COMMITTED-CANDIDATES (see contract block above)

