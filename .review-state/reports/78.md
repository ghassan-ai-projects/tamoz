# Combined pass — rank 78 gems/tamoz-sqlite/lib/tamoz/sqlite/verification_store.rb

Verdict: CANDIDATES-ONLY

## Candidate list (Phase 1 audit, written before any edit)

Audit basis: name-states-intent · ≤20/≤30 lines · one abstraction level · public DSL · step-down;
CODING_STANDARD §3/§3.1, §4, §5, §6, §11. File is a recorded durable transaction core —
method-level improvements only, no extraction.

- **C1 — rename private `canonical_episode` → `encode_episode`.** §3.1 names `encode_*`/`decode_*`
  as the durable-codec pair for SQLite rows; this file already uses `decode_episode` for the
  read-back half, so the write half is a second spelling of the same idea. REJECTED: the identical
  concept and identical name live in the paired contract implementation
  `gems/tamoz-stream/lib/tamoz/stream/verification_store.rb` (`canonical_episode`, def line 211,
  caller line 49) which is outside my ownership. Renaming only this side would leave two spellings
  of one concept across twin stores — worse than status quo under §3.1's repository-wide rule.
  In-file callers were proven single (line 196), so it is safe, but only as a BOTH-files rename.
- **C2 — `open` (~29 body lines) and `reconcile` (~45 body lines) exceed the ≤20 normal ceiling.**
  Extraction of the INSERT/UPDATE SQL or the idempotency decision out of the transaction block is
  barred by the recorded cohesion exception: the row transition and its idempotency decision belong
  in one SQLite transactional unit. REJECTED.
- **C3 — duplicated validation prologues** (`text!`/`digest!` runs opening `record_outcome`,
  `reconcile`, `fetch`) could share one validator. Cross-method machinery with error-message bytes
  at risk; beyond method-level scope. REJECTED.
- **C4 — `row_from_values` (22 lines)** is a declarative column→kwarg map; splitting adds
  indirection over data, not logic. REJECTED.
- **C5 — `#{intent_id}` vs `values.fetch(:intent_id)` inconsistency in `open`'s conflict raise
  (line 40)** — byte-identical after `text!`; unifying is pure churn. REJECTED.
- **C6 — `%i[observed reconciled].include?(row.state)`** could become a `Row` predicate, but `Row`
  is defined in the stream gem (not owned) and the set literal reads clearly here. REJECTED.
- **C7 — `find_in_transaction` re-validates already-validated ids via `text!`** (line 205).
  Defensive, idempotent, zero-cost on the failure-free path; removal buys nothing and touches the
  transactional read path. REJECTED.

No candidate met the apply bar: every finding is either covered by the recorded transaction-core
exception (C2) or requires touching the paired stream-gem implementation (C1); the rest are churn.

Applied: none
Rejected: C1 (cross-gem name symmetry — needs both stores, stream gem not owned); C2 (recorded
transaction-core cohesion exception — no safe method-level shrink exists); C3 (extraction beyond
method-level scope, error-byte risk); C4 (indirection over declarative data); C5 (churn,
identical bytes); C6 (predicate lives on non-owned Row / reads fine); C7 (defensive re-validation
removal has no value)

Diff stat: no changes to the owned file (`git diff --stat` empty; working tree clean).

Commands:
- `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/verification_store.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (unchanged)
- `ruby -Itest test/stream_situation_request_test.rb` → 11 runs, 55 assertions, 0 failures, 0 errors
  (default-lane test driving this store through `Adapter#bind_verification_store` + real runner)
- `ruby -Itest test/memory_store_test.rb` → 16 runs, 112 assertions, 0 failures, 0 errors
  (asserts `tamoz_stream_verifications` schema)
- SLOW/SERIAL lanes not run per brief known-red list; sqlite gem itself ships no own test dir —
  coverage above is the repo's only direct exercise of this class.

Deviations: none

FLAG (owner decisions): C1 is worth doing as an owner-approved cross-gem rename —
`canonical_episode` → `encode_episode` in BOTH `gems/tamoz-stream/lib/tamoz/stream/
verification_store.rb` and this file completes the §3.1 durable-codec pair against the existing
`decode_episode`. Single in-file caller each side, trivially provable; needs one coordinated commit.
