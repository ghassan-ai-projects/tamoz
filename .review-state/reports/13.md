# Fix report — rank 13 gems/tamoz-sqlite/lib/tamoz/sqlite/memory_store.rb

Applied: C1 (dead `address = normalize_memory_address(index)` + helper deleted; dead
`version_rows = nil` / `index_rows = nil` pre-assignments in `purge` dropped);
C2 (`MemoryStore::ELIGIBLE_STATES`, `MemoryStore::SEARCH_COLUMNS` deleted — verified zero
references repo-wide incl. string/const_get forms and docs/public-api.json; tamoz-agent-memory's
same-named constants are its own locally defined copies); C3 (`scan_matched_restricted`,
`expired_tombstones`, `pending_tombstones` demoted to `private` — special acceptance proof PASSED:
no direct or send/__send__/public_send reference anywhere in gems/, apps/, bin/, script/, test/;
test/public_api_test.rb does not pin MemoryStore at all; docs/public-api.json has no MemoryStore
entries); C4 (byte-identical tombstone SELECT deduped into private `tombstone_query` returning
[sql, binds] — heredoc strip yields identical SQL bytes; each caller keeps its own
open_transaction, statement label, and retention comparison); C5 (`validate_situation_identity!`
extracted from `validate_caller!`; validation order and all error bytes unchanged; T0.3 comment
moved with the code); C6 (private `retention_ms` replaces three inline
`(deletion_retention * 1_000).ceil` computations); C7 (`text_value` → `validate_text` (12 sites),
`normalize_limit` → `validate_limit` — both private, all callers in-file, no dynamic sends);
C8 (`search` local `now_ms = now_ms()` renamed `as_of_ms`, removing self-shadowing).
Rejected: C9 (INDEX_COLUMNS constant driving INSERT/binds/SELECT: adds name-mangling indirection
(klass→"class", searchable boolean special case) and interpolates SQL text in a durability-critical
path, while the 16-column search ladder stays hand-synced regardless — discipline rule (c); the
positional lists are covered by the passing tests. IndexRow constructor untouched per binding.)
FLAGs untouched for owner: `fetch` delete-vs-keep; whether ELIGIBLE_STATES/SEARCH_COLUMNS are
reserved P11 surface (if so, restore by wiring them into the SQL, not as inert constants).
Diff stat: 1 file changed, 147 insertions(+), 159 deletions(-) [net −12 LOC]
Commands:
- rubocop --cache false <file> BEFORE edit: "1 file inspected, no offenses detected" (baseline 0)
- ruby -c <file> → Syntax OK
- rubocop --cache false <file> AFTER edit: "1 file inspected, no offenses detected" (0 ≥ 0, not increased)
- ruby -Itest test/memory_store_test.rb → 16 runs, 112 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/memory_engine_test.rb → 25 runs, 162 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/sqlite_deletion_test.rb → 3 runs, 19 assertions, 0 failures, 0 errors, 0 skips
Deviations: none in my own scope. Working-tree note for the orchestrator: the tree also carries a
modification to gems/tamoz-mcp/lib/tamoz/mcp/invocation.rb that predates/exceeds my ownership
(another rank's in-flight change) — I did not touch it; my diff is limited to memory_store.rb.
Transaction boundaries (append/search/purge/purge_expired open_transaction calls) are visible and
unchanged; IndexRow's constructor signature untouched.
