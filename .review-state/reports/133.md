# Combined pass — rank 133 gems/tamoz-agent-session/lib/tamoz/agent/session_nodes.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (remove six unread construction ivars: @model, @memory, @memory_owner, @profile_roles, @profile_budgets, @profile_narrowed); C2 (demote @plan_outcomes/@effects/@evidence to locals — composed into NodeServices only, no delegation reads them); C3 (demote @graph_version to a local — read only during initialize)
Rejected: C4 (extract step-down helpers from ~68-line initialize body: kwargs plumbing does not decompose without giant pass-through signatures, extracted blocks would exceed the Q6 ≤20-line new-code ceiling, and the body already reads as ordered declarative wiring verify→capabilities→config→collaborators→freeze); C5 (rename collaborator methods for node-name symmetry, e.g. @adaptive.decide vs @steps.step_gate: collaborators live in other files, cross-file rename forbidden); C6 (add require_relative for GraphVersions/CapabilityBinding/ProfilePolicyError: constants resolve via tamoz-agent-kernel load order; adding requires risks load cycles and touches cross-gem wiring — ask-first rule); C7 (restructure verify_profile_roles! guard clauses: error/message bytes are untouchable and the form is already guard-style churn); C8 (trim attr_reader list: public API surface, untouchable)

## Phase 1 audit (written before any edit)

File shape: 227 lines. Frozen graph-node façade left after the chartered extraction of the
former 1,450-line god object; each phase is owned by one collaborator
(session_memory/session_bindings/session_planning_context/session_plan_outcomes/
session_effects/session_evidence/session_deliberation/session_routing/session_adaptive/
session_steps/session_lifecycle). Constants BEHAVIOR_VERSION/MAX_* are consumed by other
gems/files (runtime.rb, session_steps.rb, session_memory.rb) — untouchable.

Extra-rail check (durable effect journal): this file contains NO non-deterministic calls,
no EffectDispatcher.run sites, no effect keys, no journal writes — it is pure delegation.
Node boundaries = the 22 endless delegation methods (lines 160–206): untouched. Key identity
and journal writes live in session_effects.rb / EffectDispatcher: not in scope, not altered.

Candidates:
- C1 apply: `@model`, `@memory`, `@memory_owner`, `@profile_roles`, `@profile_budgets`,
  `@profile_narrowed` assigned in initialize, never read afterwards (NodeConfiguration is fed
  from the kwarg locals). Proof of absence of readers: repo-wide grep over gems/apps/test/bin
  for `.model`-style accessors on nodes (none), `instance_variable_get(:@…)` for each name
  outside the collaborator files' own ivars (none), send/__send__/public_send reach-ins into
  SessionNodes privates in tests (none), SessionNodes subclasses or second constructor call
  sites (only session.rb lines 170–173), Marshal/.dump of session objects in the gem (none),
  `.instance_variables` enumeration (none). Q5 values/state: carry no unread state.
- C2 apply: same proof standard; `@plan_outcomes`, `@effects`, `@evidence` have no delegating
  reader in the façade — their consumers take them from NodeServices. Demote to locals so the
  remaining ivars (@memory_nodes/@bindings/@planning_context/@routing/@adaptive/
  @deliberation/@steps/@lifecycle) exactly equal "has a delegated public node".
- C3 apply: `@graph_version` read at lines 125/131 only, both inside initialize; local is
  equivalent (Data fields compare by ==, frozen-string identity not depended on).
- C4 reject: see header.
- C5 reject: see header.
- C6 reject: see header.
- C7 reject: see header.
- C8 reject: see header.

Diff stat: 1 file changed, 9 insertions(+), 15 deletions(-) — net −6 lines, 24 changed total
Commands:
- ruby -c gems/tamoz-agent-session/lib/tamoz/agent/session_nodes.rb → Syntax OK
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors
- ruby -Itest test/agent_session_test.rb → 11 runs, 72 assertions, 0 failures, 0 errors
- ruby -Itest test/session_context_controls_test.rb → 11 runs, 105 assertions, 0 failures, 0 errors
Deviations: none. Post-edit repo-wide re-probe for readers of every removed ivar
(@nodes.effects/.evidence/.plan_outcomes/.model spellings included) returned zero matches.
enola note: no symbols, signatures, or coupling changed — private-state-only delta; no
structural regression possible, baseline re-pin not warranted.
FLAG (owner decisions): none
