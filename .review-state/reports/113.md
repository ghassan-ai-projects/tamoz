# Combined pass — rank 113 gems/tamoz-graph/lib/tamoz/graph/memory_checkpointer.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (initialize → private `validate_limits!`, validate-then-assign); C2 (append
construction tail → private `build_checkpoint(address, base, sequence, attributes)`,
verbatim move: freeze_value → digest id via unchanged `checkpoint_id_for` input →
frontier stamping → `Checkpoint.new`, evaluated at the same point under the same mutex
after `validate_append!`)
Rejected: C3 (SafeText validation-only call needs an assert-twin in tamoz-core — cross-gem
API addition, out of scope); C4 (descriptor_for case rewrite — churn, digest inputs
pinned); C5 (`base` local rename — churn, adjacent `expected_base_id` disambiguates);
C6 (kwargs-layout compaction / modifier-guard conversion — style churn, cop already
excluded debt for this file); C7 (literal `format_version: 1` vs
`CHECKPOINT_PROTOCOL_VERSION` — semantic coupling decision, flagged below)
Diff stat: 1 file changed, 34 insertions(+), 33 deletions(-) (net +1; well under ~60 cap)
Commands:
- ruby -c gems/tamoz-graph/lib/tamoz/graph/memory_checkpointer.rb → Syntax OK (before and after)
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (one transient
  Layout/FirstArgumentIndentation introduced by my lines was fixed in place, not suppressed)
- ruby -Itest test/graph_identity_test.rb → 8 runs, 30 assertions, 0 failures, 0 errors
- ruby -Itest test/graph_history_test.rb → 7 runs, 30 assertions, 0 failures, 0 errors
- ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors
Deviations: none. Checkpoint/replay semantics pinned as instructed — no signature,
digest-input, message-byte, ordering, or public-surface change; both applied candidates
are method-level step-down moves with byte-identical error messages, digest inputs, and
freeze/push ordering.
FLAG (owner decisions):
- `append` stores `format_version: 1` as a literal while `CHECKPOINT_PROTOCOL_VERSION = 1`
  is advertised via `#checkpoint_protocol_version`. Substituting the constant would couple
  protocol version to stored checkpoint format — left literal, owner call.
- Read-only observation outside my path: `memory_checkpointer/writer.rb` header comment
  still says "the block that `MemoryCheckpointer#transaction` yields"; the methods are
  `open_writer`/`synchronize`. Stale name only, no behavior impact.
