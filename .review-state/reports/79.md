# Combined pass — rank 79 gems/tamoz-stream/lib/tamoz/stream/outcome_subscriber.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down: `handle_control` now returns a continue? boolean like `handle_event`, so `run`'s frame loop is uniform and the protocol string `"subscriber_too_slow"` no longer leaks into the loop; each branch ends in an explicit true/false so the break never depends on `cursor_store.write`'s return value)
Rejected: C2 (rename `bounded_frame_data!` → `enforce_*`: `bounded_*!` is the established repo family for "refuse oversized input" — `bounded_string!` graph/checkpoint_codec, `bounded_integer!` core/state_codec, `bounded_tool_name!` this gem's evidence_client — renaming would fight the lexicon, not serve §3.1); C3 (unify poison/dedupe key spaces `["frame", cursor]` vs `[source, id]`: would change ack semantics for redelivered-after-poison events — not provably behavior-preserving); C4 (freeze/dup the `skipped`/`resnapshots` arrays per §5: public API identity change; pinned untouchable); C5 (memoize `notification_digest`: pure function called ≤2×/event; adds machinery for nothing); C6 (rename `refuse_*!`/`ensure_*!`/`require_*!`: all three are consistent repo-wide families — refuse_ ×8 gems, ensure_ ×12 gems, require_! incl. situation_snapshot/artifact_store in this same gem)
Diff stat: 1 file changed, 5 insertions(+), 3 deletions(-)
Commands: ruby -c PASS · rubocop --cache false: BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/stream_learning_loop_test.rb → 17 runs, 84 assertions, 0 failures, 0 errors (baseline pre-edit identical) · ruby -Itest test/stream_notification_contract_test.rb → 2 runs, 11 assertions, 0 failures, 0 errors (baseline pre-edit identical)
Deviations: none
FLAG (owner decisions): none

## Phase 1 candidate list (written before any edit)

Audit basis: name-states-intent · ≤20-line methods · one level of abstraction · public
entry points as small DSL · step-down · CODING_STANDARD §3/§3.1/§4/§5/§6/§11.
Constraint from binding block: event ordering pinned; method-level naming/step-down only.

- C1 APPLY — step-down/uniformity: `run` handles `:control` frames by calling
  `handle_control` then re-reading `frame.control == "subscriber_too_slow"` in the loop,
  while `:event` frames already use the "return false ends the pass" pattern. Give
  `handle_control` the same contract (`false` only on subscriber_too_slow) with explicit
  boolean tails per branch. Same operations, same order, same break condition → provably
  behavior-preserving. In-file caller proven (grep repo-wide incl. send/public_send/__send__:
  zero external references to handle_control/handle_event/remember/record_audit).
- C2 REJECT — rename `bounded_frame_data!`. See above; family idiom matches semantics exactly.
- C3 REJECT — key-space unification. Changes dedupe outcomes, not just names.
- C4 REJECT — reader exposure hardening. Public API pinned.
- C5 REJECT — digest memoization. Speculative optimization.
- C6 REJECT — checking-verb renames. Existing usage IS the lexicon.

Non-candidates examined and found clean: `parse_cloud_event`/`utf8_frame_document`
(parse_ vocabulary correct), `validate_notification!` (validate_ correct),
`acknowledge_in_memory_duplicate!`/`acknowledge_durable_duplicate!`/
`acknowledge_unhandled_type!` (state intent + return contract clearly),
`durable_event_state`/`mark_durable_event` pair, ring-bounded `remember`/
`record_audit`, all comments (protocol-invariant why-comments per §11, none narrate
code). RuboCop Metrics already at configured ceilings (0 offenses before edit).
