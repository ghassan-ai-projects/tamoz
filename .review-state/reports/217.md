# Combined pass — rank 217 gems/tamoz-sqlite/lib/tamoz/sqlite/effect_record_reader.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (name the receipt columns at the attempt-row call site — result_bytes/result_digest = fetch(6)/fetch(7), error_bytes/error_digest = fetch(9)/fetch(10) — same convention sibling effect_completion.rb uses for its receipt row; removes the bytes-vs-digest ambiguity that only decode_receipt's parameter order resolved. Pure local naming inside one private method, behavior identical, explicit parameter lists untouched per the TAMOZ-SQLITE rail) | Rejected: C2 (name locals for row.fetch(8)/row.fetch(9) in materialize — checked_symbol!'s label arguments already name the values; intermediates add lines without information); C3 (attempts.map(&method(:materialize_attempt)) — style churn, current block reads identically); C4 (rename private materialize → materialize_effect for symmetry with materialize_record — zero intent gain under its single caller fetch); C5 (hoist %w[prepared running succeeded failed unknown abandoned] to a shared constant — the literal mirrors migrator.rb's schema CHECK and effect_reconciler's terminal set, both read-only for this pass; durable schema contract, not domain data); C6 (endless-method conversion of forwarders — forbidden by the TAMOZ-SQLITE boundary_source_audit rail, broke once in fc3878a)
Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/effect_record_reader.rb | 16 ++++++---------- (6 insertions, 10 deletions, net -4)
Commands: ruby -c → Syntax OK · rubocop --cache false → BEFORE 0 offenses / AFTER 0 offenses (no increase) · ruby -Itest test/sqlite_boundary_source_audit_test.rb → 7 runs, 55 assertions, 0 failures, 0 errors · ruby -Itest test/sqlite_effect_journal_test.rb → 8 runs, 35 assertions, 0 failures, 0 errors · ruby -Itest test/agent_non_ascii_session_test.rb → 7 runs, 39 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE (written before any edit)

Audit basis: positional row indices verified against the durable SELECTs this file
consumes — `EffectJournalRows.effect` (effect_journal_rows.rb) pins effect columns 0–14
(8=safety, 9=status), and `fetch_rows`' inline attempt SELECT pins attempt columns 0–13.
Public surface is one method (`fetch(key)`); file is rubocop-clean BEFORE (0 offenses).

Candidates:

- C1 APPLY — name the receipt columns in `materialize_attempt` before decoding:
  `result_bytes`/`result_digest` = fetch(6)/fetch(7), `error_bytes`/`error_digest` =
  fetch(9)/fetch(10). Today the four opaque indices are disambiguated only by the domain
  string and by knowing `decode_receipt(bytes, digest, ...)`'s parameter order — which
  index carries bytes vs digest is genuinely ambiguous at the call site. Same convention
  the sibling `effect_completion.rb` uses for its receipt row (stored_result_bytes /
  stored_result_digest / stored_error_bytes / stored_error_digest). Pure local naming,
  behavior identical, method stays ≤20 lines, explicit params untouched.
- C2 REJECT — name locals for `row.fetch(8)`/`row.fetch(9)` in `materialize`: unlike C1,
  each value feeds `checked_symbol!` whose third argument already names it ('effect
  safety' / 'effect status'); intermediates add two lines without removing ambiguity.
- C3 REJECT — `attempts.map(&method(:materialize_attempt))`: style churn; the explicit
  block reads identically and rubocop is silent on it.
- C4 REJECT — rename private `materialize` → `materialize_effect` for symmetry with
  `materialize_record`: zero intent gain under the single caller `fetch`; rename churn.
- C5 REJECT — hoist the attempt-status enum `%w[prepared running succeeded failed unknown
  abandoned]` into a shared constant: duplicates live in migrator.rb's schema CHECK and
  effect_reconciler.rb, both read-only for this pass; the literal mirrors the durable
  schema contract, not domain data.
- C6 REJECT — endless-method conversion of forwarders: explicitly forbidden by the
  TAMOZ-SQLITE rail (`boundary_source_audit` statically verifies registered helpers stay
  reachable from parsed source paths; broke once in fc3878a).
- C7 KEEP — all existing reek directives and the canonicality byte-encoding comment are
  §11 why-comments (positional wire contract, corruption boundary, encoding invariant);
  no comment edits needed.

Method ceilings: all methods ≤20 normal lines after C1 (`materialize_attempt` is 12 body
lines; largest, `materialize_record`, is a constructor mapping under an existing
ABC-size directive). Step-down order reads top-down; one level of abstraction per method.
No boolean params, no get_/set_, predicates n/a. Recorded cohesion exceptions (durable
transaction core / wire mapping) respected: method-level naming only, no extraction.
