# Combined pass — rank 102 gems/tamoz-stream/lib/tamoz/stream/verification_store.rb

Verdict: COMMITTED-CANDIDATES

## Candidate list (Phase 1 audit, written before any edit)

Audit basis: name-states-intent · ≤20/≤30 lines · one abstraction level · public DSL
(open / record_outcome / reconcile / reference / fetch / all — reads as a small lifecycle
DSL) · step-down (public methods → validators → row builders → fetch_locked).
CODING_STANDARD §3/§3.1, §4, §5, §6, §11. Recorded cohesion exception applies (durable
transaction core; twin of rank-78 sqlite store): method-level only, no extraction.
BEFORE gates: `ruby -c` Syntax OK · rubocop --cache false = 1 offense (pre-existing
Layout/HashAlignment at line 91, autocorrectable).

- **C1 — rename private `canonical_episode` → `encode_episode`** (def line 211, single
  caller line 49), completing the §3.1 durable-codec pair against the twin's existing
  `decode_episode`. REJECTED: a coordinated owner FLAG reserves this rename for ONE
  commit spanning BOTH stores (`gems/tamoz-sqlite/.../verification_store.rb` rank 78 +
  this file); explicitly out of scope per binding block. Recorded here so the owner's
  coordinated commit finds both sides confirmed safe (single caller each side).
- **C2 — `open` (~19 body lines) and `reconciled_row` (~21) at/over the ≤20 normal
  ceiling.** REJECTED: the state transition and its idempotency/conflict decision belong
  to one visible transactional unit (recorded durable-transaction-core exception;
  "transaction boundaries stay visible", §8). Splitting adds indirection inside the core;
  hard ceiling 30 is respected by both.
- **C3 — duplicated `text!`/`digest!` validation prologues** opening `open`,
  `record_outcome`, `reconcile`, `fetch` could collapse into one shared validator.
  REJECTED: cross-method machinery over byte-pinned error messages, and the identical
  prologue shape exists in the sqlite twin — changing one side alone breaks twin symmetry
  (same defect class as C1). Beyond method-level scope.
- **C4 — validator names (`text!`, `optional_text`, `digest!`) vs §3.1's `validate_*`
  vocabulary.** REJECTED: verified repo-wide that the sqlite twin uses the identical
  spellings (twin lines 278/288/292); renaming one store's private copies creates two
  spellings of one concept across twin stores. Any change belongs to a cross-gem naming
  decision, not this file.
- **C5 — `%i[observed reconciled].include?(row.state)` (line 133) could become a `Row`
  predicate.** REJECTED: unlike rank 78, `Row` IS defined here — but the predicate has a
  single call site and the set literal reads clearly; a one-use predicate is machinery,
  not clarity.
- **C6 — redundant identity check in `equivalent_open?`**: `row.intent_id ==
  values.fetch(:intent_id)` is always true when reached (the row is found under key
  `[tenant_id, intent_id]`). Provable but REJECTED: defensive symmetry documents the
  open-idempotency identity contract; removal buys nothing and touches the idempotent-
  redelivery path (same reasoning as rank-78 C7).
- **C7 — dead-by-construction clause in `reconciled_row`**: learnable computes
  `LEARNABLE_VERDICTS.include?(verdict) && !row.outcome_id.nil?`, but the preceding
  identity check plus `text!` already prove `outcome_id` is a non-empty String, so the
  second conjunct cannot fire. Provable behavior-preserving, REJECTED anyway: zero
  behavioral gain inside the durable transition core; churn where bytes matter most.
- **C8 — pre-existing `Layout/HashAlignment` offense at line 91** (`reconciliation_version:,
  source_authority:` continuation not aligned with the first hash key of the multiline
  `reconciled_row(...)` call). APPLY: whitespace-only, matches rubocop autocorrect output
  exactly (align to column 18), takes the file 1 → 0 offenses with zero behavior surface.

Public entry points read as a small DSL; error messages, verdict/state constants, Row
shape, and reference_hash key set are contract/pinned surfaces — none touched.

Applied: C8 (align multiline kwarg continuation at line 91 — whitespace-only rubocop fix)
Rejected: C1 (reserved owner FLAG: canonical_episode→encode_episode must land as one
cross-gem commit across both stores); C2 (durable transaction core, recorded cohesion
exception — no method-level shrink exists); C3 (shared validator = machinery + pinned
error bytes + twin asymmetry); C4 (twin uses identical spellings — local rename splits one
concept repo-wide); C5 (one-use predicate adds machinery over a clear set literal);
C6 (defensive identity check documents the redelivery contract — removal buys nothing);
C7 (provable dead clause, but churn inside the durable transition core)

Diff stat: `1 file changed, 1 insertion(+), 1 deletion(-)` (the owned file only; net
LOC delta 0 — one line re-indented)

Commands:
- `ruby -c gems/tamoz-stream/lib/tamoz/stream/verification_store.rb` → Syntax OK
- `rubocop --cache false <file>` → BEFORE 1 offense (Layout/HashAlignment, pre-existing)
  / AFTER 0 offenses
- `ruby -Itest test/stream_learning_loop_test.rb` → 17 runs, 84 assertions, 0 failures,
  0 errors, 0 skips (default-lane suite driving this store's awaiting → observed →
  reconciled lifecycle and admission references, T5.2/T5.3)
- SLOW/SERIAL lanes not run per brief known-red list (`stream_*_test` there maps to the
  four SERIAL_TESTS stream suites in the Rakefile: episode_worker, episode_end_to_end,
  evidence_client, worker_server — learning_loop is default-lane and was run)

Deviations: none

FLAG (owner decisions): C1 remains live exactly as flagged from rank 78 —
`canonical_episode` → `encode_episode` needs ONE coordinated commit touching BOTH
gems/tamoz-stream and gems/tamoz-sqlite verification_store.rb; this pass reconfirms each
side has a single private call site, so the rename is mechanically trivial when the owner
schedules it.
