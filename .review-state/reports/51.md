# Combined pass — rank 51 gems/tamoz-evals/lib/tamoz/evals/benchmark/cell_extractor.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (collapse `probabilities` intermediate vector/reassignment into `filter_map.first || fallback`); C2 (bind `spans = Array(trace['spans'])` once in `decision_at`/`first_observable_at`, which each computed it twice); C3 (rename private method `surface_record(surface, mission, artifact)` → `resolve_surface_record` — it shadowed the ubiquitous `surface_record` parameter name); C4 (`first_present` body → `values.compact.first`)
Rejected: R1 (inline `explicit_time` — its name carries the span-vs-explicit-timestamp distinction at both call sites; removal is churn); R2 (drop `build_cell` passthrough / thread precomputed terminal into `cell_context` — restructures the join flow; the recomputed `terminal_for` is pure/idempotent); R3 (`initialize` → `@manifest.dig('catalog', 'missions')` — NOT provably equivalent: for a truthy non-Hash catalog the original uses String#[] substring semantics while dig raises TypeError, so error-path bytes could differ); R4 (convert positional helpers like `truth_code(terminal, surface_record, artifact, mission, status)` to kwargs per §4 — ~10-call-site signature churn, no behavioral gain; §4 governs new code); R5 (normalize inconsistent parameter ordering across helpers — pure churn); R6 (`action_code_from_keys`/`effect_key` loops → `map.find` — equal clarity); R7 (extract modules out of the ClassLength-disabled class — cohesion comment records "one evidence join owns the cell contract"; method-level only); R8 (compose `valid_evidence_ids` without concat mutation — current form reads fine)
Diff stat: 1 file changed, 11 insertions(+), 11 deletions(-) — net 0 LOC
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` BEFORE 0 / AFTER 0 offenses ·
`ruby -Itest gems/tamoz-evals/test/comparison_executor_test.rb` → 5 runs, 36 assertions, 0 failures, 0 errors, 0 skips.
Focused-test note: comparison_executor_test.rb is the only test exercising CellExtractor (grep-proved); it asserts exact
cell hashes (cell_id/probabilities/decision_at/first_observable_at/evidence_refs) plus CanonicalJSON dump equality,
covering recorded-artifact bytes. scenario_driver/scorecard lanes touching this area are known-red per brief and were not run.
Deviations: none
FLAG (owner decisions): none

## Analysis notes (Phase 1)

BEFORE: `ruby -c` OK · rubocop --cache false = 0 offenses · branch verification-again, file clean.
Public API (`CellExtractor.extract(manifest:, artifact_base:)`) untouched; all changes private-method-level,
byte-preserving (cell hashes, canonical JSON, error messages unchanged).

Candidates above were written here BEFORE any edit. C1–C4 each proved equivalent by inspection:
C1 filter_map builds the same array, `.first` picks the same vector, nil falls back to the same
`{ primary_code => 1.0 }`; C2 `Array#reverse` is non-mutating so `spans.last`/`spans.first` see original order;
C3 repo-wide grep (incl. send/__send__/public_send patterns) finds the sole call site at line 79 in this file;
C4 `compact` preserves order and drops exactly nils.
