# Combined pass — rank 44 gems/tamoz-sqlite/lib/tamoz/sqlite/boundary_registry.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down of `validate_hook!` from 54 lines to a 13-line recipe over seven
private `assert_*` helpers — point, envelope shape+version, registered operation, attempt,
kind binding dispatch, transaction binding, statement binding; evaluation order and every
error-message byte preserved verbatim; public signatures untouched; OPERATIONS data and the
pinned digest untouched). Two Style/GuardClause offenses introduced by the relocation were
fixed in place with guard clauses, not suppressed.
Rejected: C2 (deriving transaction points from `POINTS` — the literal list keeps the
transaction boundary visible at the point of use per binding note); C3 (`phase_operations`
early-return rewrite — churn on an 8-line body); C4 (`.find` rewrite of
`resolve_statement` — loop carries both matched statement and instance index); C5 (private-
method declaration style normalization — style churn in a durable contract file); C6
(`match_template` rename — private, one caller, name adequate); C7 (memoizing
`digest`/`phase_operations`/`resolve_statement` — premature optimization; cached frozen
merged hashes invite aliasing bugs).
Diff stat: 1 file changed, 66 insertions(+), 27 deletions(-) (net +39)
Commands:
- BEFORE: `ruby -c` OK · `rubocop --cache false <file>` → 0 offenses
- AFTER: `ruby -c` OK · `rubocop --cache false <file>` → 0 offenses (no increase)
- `ruby -Itest test/sqlite_boundary_registry_test.rb` → 5 runs, 1576 assertions,
  0 failures, 0 errors, 0 skips (includes pinned REGISTRY_DIGEST tripwire and every
  `validate_hook!` rejection branch)
- `ruby -Itest test/sqlite_scenario_registry_test.rb` → 6 runs, 2035 assertions,
  0 failures, 0 errors, 0 skips (cross-gem consumer via verify_boundary_registry!)
- `ruby -Itest test/sqlite_trace_recorder_test.rb` → 11 runs, 952 assertions,
  0 failures, 0 errors, 0 skips (cross-gem consumer of resolve_statement + validate_hook!)
Deviations: none. Recorded cohesion exception honored: method-level step-down only, no
extraction, no data changes, transaction boundary literals left visible and untouched.
FLAG (owner decisions): none

## Analyzer candidates (as recorded before editing)

File profile: ~60% declarative data (OPERATIONS) behind a pinned digest; the remainder is a
small module-function API (`document`, `digest`, `operation`, `phase_operations`,
`resolve_statement`, `validate_hook!`) plus private helpers. Recorded cohesion exception
applies (durable boundary registry): method-level only, no extraction, data untouched.

- C1 `validate_hook!` was 54 lines (limit: 30 hard) mixing abstraction levels: point
  lookup, envelope shape/version, per-field checks, kind dispatch in one body. APPLIED.
- C2 Lines restate the four transaction points literally instead of deriving from
  `POINTS`. REJECTED — the literal list keeps the transaction boundary visible at the point
  of use; a `POINTS - sql` derivation obscures it for zero behavioral gain.
- C3 `phase_operations`: nested `nil? ||` guard inside `select` could early-return.
  REJECTED — churn; the 8-line body already reads top-to-bottom.
- C4 `resolve_statement` `.each` + early return could be `.find`. REJECTED — needs both the
  matched statement and its instance index; the loop expresses that without double matching.
- C5 Private-method declaration style is mixed (trailing `private_class_method :x` vs
  inline `private_class_method def`). REJECTED — normalizing five sites is style churn;
  new helpers follow the dominant trailing form.
- C6 `match_template` both matches and computes the index; a `resolve_index` rename might
  state intent better. REJECTED — private, single in-file caller, current name adequate.
- C7 `digest`/`phase_operations`/`resolve_statement` allocate per call; memoization would
  save allocations. REJECTED — premature optimization on a cold path; caching frozen merged
  hashes invites aliasing bugs.
