# Combined pass — rank 163 gems/tamoz-graph/lib/tamoz/graph/writer_run_executor.rb
Verdict: PENDING

## Phase 1 — ANALYZE (candidates, written before any edit)

BEFORE state: `ruby -c` OK · `rubocop --cache false` = **0 offenses** · branch verification-again.
Callers: public surface reached only via `Compiled#invoke_with_writer/resume_with_writer/
retry_failed_with_writer/continue_with_writer` (compiled.rb:204-207, endless forwarders);
all helpers private. Repo-wide grep (gems/ apps/ bin/ test/, includes send/__send__ forms)
shows `run_existing` referenced ONLY inside this file (2 call sites).
Tests touching this seam: test/sqlite_stale_request_test.rb (drives resume/continue/
retry_failed_with_writer via `__send__`, asserts Tamoz::StaleRequestError on all three
precondition sites); test/graph_durable_request_executor_test.rb (durable-request seam).
No test file greps `WriterRunExecutor` by name.

- C1 APPLY: `run_existing(expected_status, error_message, …)` — two anonymous positionals
  force readers to the definition to know what `:failed` / the string bind to. Convert to
  keywords `expected_status:` / `error_message:` at the def and both call sites. Proven
  behavior-preserving: private method, callers in-file only, identical values bind to the
  same locals, error-message bytes unchanged.
- C2 REJECT: route `resume` through `run_existing`. Resume interleaves
  `merge_resume_values` between the paused check and `mark_running` and passes
  `resume_values` into `run_checkpoint`; forcing it through the shared helper needs
  callback params (more machinery) and reshuffles pinned run/effect ordering.
- C3 REJECT: always pass `resume_values:` in `run_checkpoint` instead of the conditional
  options hash. Load-bearing: `Executor#run` defaults `resume_values:` to
  `checkpoint.resume_values` (executor.rb:18) — always passing nil would override the
  checkpoint default on invoke/continue/retry and change behavior.
- C4 REJECT: rename `mark_running` to advertise its no-op-without-durable_request_id
  guard. Name mirrors `writer.mark_request_running`; moving/duplicating the guard touches
  pinned effect-ordering lines for marginal naming gain.
- C5 REJECT: inline single-use `bind_writer_context` into `run_checkpoint`. The wrapper
  names the Compiled-boundary `__send__` crossing; inlining mixes one step of
  orchestration with a raw boundary send.
- C6 REJECT: drop the file-level `rubocop:disable Lint/UnusedMethodArgument` as stale.
  Not stale: `invoke` declares `request_id:` its body never reads, and the kwarg cannot be
  underscored without breaking the public forwarder signature (public API untouchable).
- C7 REJECT: split `merge_resume_values`' merge + stale-wrap policy into two methods.
  Four-line method; extraction adds machinery; boundary-mirroring exception stands.

Shape audit: every method ≤20 normal lines (largest body ≈15); step-down holds (public
methods name fetch→guard→mutate→run); §3 vocabulary respected (build_context,
merge_resume_values, mark_running); no boolean-param violations beyond the suppressed
`mark_request_running` flag already exempted in the header.

## Phase 2 — APPLY

Applied: C1 (`run_existing` anonymous positionals → `expected_status:` / `error_message:`
keywords at the def and both in-file call sites; values bind identically, error-message
bytes unchanged, proven private + caller-in-file by repo-wide grep).
Rejected: C2 (pinned ordering + needs callback machinery); C3 (proven load-bearing vs
Executor#run default); C4 (churn on ordering-sensitive line); C5 (names the boundary
crossing); C6 (disable still required by unread public `request_id:` kwarg); C7
(extraction machinery for a 4-line boundary method).

Diff stat: 1 file changed, 6 insertions(+), 6 deletions() — net LOC delta 0.

Commands:
- `ruby -c gems/tamoz-graph/lib/tamoz/graph/writer_run_executor.rb` → Syntax OK (before and after)
- `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses
- `ruby -Itest test/sqlite_stale_request_test.rb` → 26 runs, 176 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/graph_durable_request_executor_test.rb` → 2 runs, 5 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/graph_resume_answers_test.rb` → 2 runs, 7 assertions, 0 failures, 0 errors, 0 skips

Verdict: COMMITTED-CANDIDATES
Deviations: none
FLAG (owner decisions): `invoke` declares a `request_id:` kwarg its body never reads
(dead public-API parameter; removal is a cross-gem signature change, out of scope here —
the file-level Lint/UnusedMethodArgument disable exists for it).
