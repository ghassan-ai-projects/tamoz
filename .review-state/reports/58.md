# Combined pass — rank 58 gems/tamoz-evals/lib/tamoz/evals/harness/memory_repository_adapter.rb

## Phase 1 — ANALYZE (candidates, written before any edit)

Baseline: `ruby -c` OK; repo-config `rubocop --cache false` BEFORE = 0 offenses
(file sits on recorded `.rubocop_todo.yml` Exclude debt for Metrics/MethodLength,
Metrics/BlockLength, Lint/DuplicateBranch, Lint/UnusedMethodArgument,
Layout/SpaceInsideHashLiteralBraces, Style/HashLikeCase, Style/StringLiterals,
Style/StringLiteralsInInterpolation).

- APPLY C1: private `caller` shadows `Kernel#caller`; rename to `retrieval_caller`
  (single in-file call site at `scan`'s `caller:` kwarg; repo-wide grep incl.
  send/__send__/public_send shows no external reach-in; the engine-facing kwarg
  name stays `caller:`).
- APPLY C2: replace `klass_for`/`epistemic_kind_for` case methods with two frozen
  layer-map constants — same total map over :experience/:knowledge/:wisdom with
  nil fallback preserved via `[layer]`; resolves the recorded Lint/DuplicateBranch
  (two branches returning identical `:procedure`) and Style/HashLikeCase debt.
  In-file callers only (proven above).
- APPLY C3: extract `build_memory_record(fixture)` from `append_fixture` — pure
  code motion of the MemoryRecord construction literal; `append_fixture` drops
  from ~42 lines (over the 30 hard ceiling) to the repository-append contract at
  one level of abstraction. Digest-pinned tests guard every seeded byte.

- REJECT C4: `absorb(_fields, prompt_sourced:)` boolean kwarg violates §4 — but
  the signature mirrors the harness `MemoryStore` interface this adapter stands
  behind ("behind the identical MemoryStore interface"); public API untouchable.
- REJECT C5: `seed_intact?` returns nil (not false) when unseeded — changing it is
  an observable return-value change; not provably behavior-preserving.
- REJECT C6: trailing `@seed_digest` re-return line in `seed` — churn; reads fine.
- REJECT C7: `close` → safe-navigation restyle — churn.
- REJECT C8: CountingProtection#encrypt uses `bytes.b` while decrypt does not —
  byte-encoding subtleties feed store payloads and digests; recorded-artifact
  behavior must stay identical.
- REJECT C9: `digest` ~34 lines dominated by two pinned SQL heredocs — extracting
  per-query helpers adds names around literals, no clarity gain; ordering/bytes
  stay as recorded.
- REJECT C10: attr_reader exposes `engine`/`adapter` — consumed cross-file by the
  contamination test (`store.engine.repository.append`) and harness wiring;
  public surface untouchable.
- REJECT C11: `metadata` could use the `memory_id` param instead of re-fetching
  from fixture — micro-churn.

## Phase 2 — APPLY

Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename private `caller`→`retrieval_caller`, un-shadowing Kernel#caller; engine kwarg stays `caller:`); C2 (`klass_for`/`epistemic_kind_for` case methods → frozen `KLASS_BY_LAYER`/`EPISTEMIC_KIND_BY_LAYER` maps, nil fallback preserved via `[layer]`; resolves recorded Lint/DuplicateBranch + Style/HashLikeCase debt); C3 (extract `build_memory_record(fixture)` from `append_fixture`, pure motion; append_fixture now ~9 lines, over-30-hard-cap breach removed)
Rejected: C4 (boolean `prompt_sourced:` kwarg mirrors the harness MemoryStore interface — public API untouchable); C5 (seed_intact? nil-vs-false return is observable); C6/C7/C11 (churn); C8 (CountingProtection `.b` asymmetry feeds store payloads/digests — recorded-artifact behavior must stay identical); C9 (digest's SQL heredocs are pinned literals, extraction adds names not clarity); C10 (engine/adapter attr_readers consumed cross-file by tests/harness)
Diff stat: 1 file changed, 17 insertions(+), 27 deletions(-) — net −10
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses (BEFORE 0, no increase) · `ruby -Itest test/memory_repository_adapter_test.rb` → 10 runs, 104 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/memory_treatment_profile_test.rb` → 14 runs, 218 assertions, 0 failures, 0 errors, 0 skips
Deviations: none. Behavior guards: digest-pinned seed reproducibility (E5/E7), decrypt-count hard zeros (P11-25), and CI-profile ladder all pass unchanged on the real SQLite stack.
FLAG (owner decisions): none. (Note only: `.rubocop_todo.yml` Exclude entries for this file are now partly stale — DuplicateBranch/HashLikeCase no longer fire; TODO file is out of my ownership.)

