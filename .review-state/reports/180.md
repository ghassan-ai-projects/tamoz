# Combined pass — rank 180 gems/tamoz-agent-session/lib/tamoz/agent/session_plan_outcomes.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (deleted dead private `governed?` + its 2-line comment) | Rejected: C2 (privatize `plan_rejected_message` — public/cross-gem API is untouchable per brief → flagged); C3 (drop `details = <kwarg>` aliasing / rename generic `details` params to Acceptance/Clarification/Rejection concept names — ~60+ changed lines across 8 methods brushes the net-change rail for marginal gain); C4 (collapse double `.merge` chain in `repeated_action_update` — identical result/key order, zero intent gain, style churn); C6 (rename `validate_clarification` → `assert_*` — it raises on bad operator input, which §3.1 assigns to `validate_*`; correct as-is). No-action note: C5 (`clarify_update` at 21 lines vs ≤20 normal cap — within hard cap 30; extraction out of scope per orchestrator pin "method-level naming/step-down only", ordering pinned).

## Phase 1 candidate list (written before editing)
- C1 dead private method `governed?` (was lines 127–139): builds an approval request and simulates it, but no caller anywhere. Proof of deadness: repo-wide grep of gems/ apps/ test/ bin/ for `governed` hits only unrelated tamoz-evals mission names (`governed-mutation`, `mcp_governed_call`…); dynamic forms checked (`send`/`__send__`/`public_send`/`respond_to?`/`method_defined`/string-keyed `'governed'`) — zero ties to this class; `def governed?` exists nowhere else in the repo. Private ⇒ not API.
- C2 `plan_rejected_message` is public with zero external callers (only in-file use at line 66; the same-named method in gems/tamoz-agent/lib/tamoz/agent/runtime/plan_review.rb is that class's own copy).
- C3 entry points do `details = acceptance|rejection|clarification` and six helpers take generic `details` params, erasing the Data-class concept names at every use site.
- C4 `base.merge(a).merge(b)` double merge in `repeated_action_update`.
- C5 `clarify_update` body 21 lines (>20 normal guideline).
- C6 `validate_clarification` verb choice vs §3.1 vocabulary.

Diff stat:
```
 .../lib/tamoz/agent/session_plan_outcomes.rb | 14 --------------
 1 file changed, 14 deletions(-)
```
Commands:
- `ruby -c <file>` → Syntax OK (before and after)
- `rubocop --cache false --format simple <file>` → BEFORE: 1 file inspected, no offenses; AFTER: no offenses (0→0)
- `ruby -Itest test/agent_session_test.rb` → 11 runs, 72 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/agent_durable_routing_test.rb` → 4 runs, 33 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/agent_cli_test.rb` (clarify path) → 33 runs, 753 assertions, 0 failures, 0 errors, 0 skips

Deviations: No test file names SessionPlanOutcomes directly (grep over test/ is empty); coverage above is indirect via integration suites that exercise accept/clarify/routing paths — stated explicitly per brief. enola snapshot diff not graded: no pre-edit baseline was pinned in this session and re-pinning post-edit measures nothing (also avoids disturbing shared review state); the end-of-session enola hook remains the regression check. Net −14 lines, inside the ~60-line rail; no commits made.

FLAG (owner decisions):
1. `SessionPlanOutcomes#plan_rejected_message` is public but has zero callers outside the class — privatization candidate, blocked here only because the brief pins public API untouched.
2. Plan-rejection message logic is duplicated across gems: this class's `plan_rejected_message` and `gems/tamoz-agent/lib/tamoz/agent/runtime/plan_review.rb#plan_rejected_message` (whose comment references `SessionNodes#plan_rejected_message`) — cross-gem dedup is a two-agent-pipeline item, out of scope for a single-file pass.
