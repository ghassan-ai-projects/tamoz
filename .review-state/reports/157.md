# Combined pass — rank 157 gems/tamoz-agent-cli/lib/tamoz/agent/cli_comms_shared.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (`credential` now derives the name via the existing `credential_name` step instead of duplicating its expression; identical fetch chain, identical error bytes)
Rejected: C2 (ModuleLength 109/100 fix would need comment-stripping or helper splitting — recorded cohesion exception + method-level-only scope); C3 (`&&`→`&.` pure style churn in pinned-bytes seam); C4 (duplicate idempotent `require 'tamoz/telegram'` — removal trades load-order robustness for zero gain); C5 (extract build_descriptor default hashes — extraction beyond method-level scope under recorded disable comment); C6 (rename `symbolize` — name states intent, §3.1 offers no closer verb, rename needs proven callers for zero gain); C7 (with_comms_runtime 22 lines — explicit recorded exception naming the open→codec-parity→inbox ordering invariant; no extraction); C8 (comms_controls_source arity→kwargs would change mixed-in API consumed positionally by cli_comms_commands.rb:61 — cross-file API untouchable); C9 (comments audit vs §11: all are "why" comments — ADR ordering invariant, bounded-unavailable-reply failure model, fixture-client rule; none narrate code)
Diff stat: gems/tamoz-agent-cli/lib/tamoz/agent/cli_comms_shared.rb | 2 +- (1 insertion, 1 deletion; net LOC delta 0)
Commands: `ruby -c` → Syntax OK (before and after) · `rubocop --cache false` BEFORE 1 offense / AFTER 1 offense (same pre-existing Metrics/ModuleLength 109/100; not increased) · `ruby -Itest test/comms_cli_test.rb` → 12 runs, 0 failures, 0 errors (×4 consecutive; see Deviations) · `ruby -Itest test/context_control_exposure_test.rb` → 6 runs, 0 failures, 0 errors · `ruby -Itest test/comms_cli_ops_test.rb` → 7 runs, 0 failures, 0 errors
Deviations: first invocation of test/comms_cli_test.rb reported 1 failure + 1 error ("the durable offset must confirm the prefix", Expected 56 / Actual nil); it did not reproduce in four subsequent green runs of the same suite and cannot be produced by a one-expression delegation — consistent with shared-state interference from parallel review passes on sibling files. No revert needed.
FLAG (owner decisions): the module's pre-existing `Metrics/ModuleLength` (109/100) offense is either accepted or suppressed in config by the owner — every in-file remedy (splitting helpers, deleting rationale comments) is barred here by the file's recorded cohesion exception and this pass's method-level scope.
