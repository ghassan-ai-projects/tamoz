# Combined pass — rank 240 gems/tamoz-core/lib/tamoz/circuit/evidence.rb

## Phase 1 — ANALYZE (candidate list, written before any edit)

Audit vs the five criteria: `validate!` reads as the DSL entry (`Evidence.validate!(evidence, scope:)`),
dispatches on the registry's `evidence_rule` string to four per-rule validators, which step down to
`present_identity`/`present_value`/`digest_value`/`refuse!`. Longest method 14 lines (≤20 ✓); one
abstraction level per method ✓; step-down ladder clean ✓. Constants `EVIDENCE_DIGEST_DOMAIN`,
`DIGEST_PATTERN`, `Registry` resolve in sibling `circuit.rb`/`registry.rb` (both read-only here;
circuit.rb already audited CLEAN).

Candidates:

- C1 — Rename predicate helpers to `present_value?` / `digest_value?` / `present_identity?`
  (§3 predicates end `?`). REJECTED: they are module_function-public on a cross-gem contract
  module (tamoz-sqlite's `circuit_store.rb` consumes this surface); shapes are pinned for this
  pass. Repo-wide grep (incl. send/__send__/public_send) shows no external callers, so it would
  be *survivable* churn — but zero behavioral gain against real contract risk. RuboCop is at 0
  offenses under the repo config, so the standard does not demand it either.
- C2 — Unify `present_value`/`digest_value` behind one block-parameterized key scanner
  (`keys.any? { |k| yield evidence[k] }`). REJECTED: two 5-line parallel helpers are simpler
  than adding an extraction layer for two callers (owner directive: simple over complicated).
- C3 — Pass the fetched Scope entry into `validate_operator_command!` instead of re-fetching via
  `Registry.fetch(scope)`. REJECTED: fetch is a frozen-hash lookup that passes a Scope through
  unchanged; the change buys nothing measurable and breaks the symmetric `(evidence[, scope])`
  shape across the four validators.
- C4 — Inline `present_identity` (used once). REJECTED: it names intent at the call site
  ("identity present") and mirrors the helper family; removal is pure churn.
- C5 — In `validate!`, move the Hash-type guard ahead of `Registry.fetch(scope)`. REJECTED:
  changes which error surfaces first when both inputs are bad (error/message bytes untouchable),
  for no benefit.
- C6 — Name the inline `%w[operator_command_digest]` in `validate_operator_command!` as a
  constant. REJECTED: single use, adjacent to its comment; a constant adds machinery without
  clarity.

No candidate survives: every one is either pinned-surface churn or added machinery. Nothing was
edited; the file stands as-is.

Verdict basis: logic-bearing file (not EXEMPT), no risky findings (not CANDIDATES-ONLY), nothing
applied (not COMMITTED-CANDIDATES) → CLEAN.

---

# Combined pass — rank 240 gems/tamoz-core/lib/tamoz/circuit/evidence.rb
Verdict: CLEAN
Applied: none | Rejected: C1 (pinned cross-gem surface, zero gain); C2 (extra layer > two tiny helpers); C3 (no-op churn, breaks validator shape symmetry); C4 (names intent, removal is churn); C5 (changes first-surfaced error bytes); C6 (single-use constant = machinery)
Diff stat: (no changes — owned file untouched)
Commands: ruby -c gems/tamoz-core/lib/tamoz/circuit/evidence.rb → Syntax OK; rubocop --cache false <file> → BEFORE/AFTER 0 offenses, 1 file inspected; ruby -Itest test/circuit_record_test.rb → 13 runs, 25 assertions, 0 failures, 0 errors, 0 skips; ruby -Itest test/sqlite_circuit_store_test.rb → 11 runs, 48 assertions, 0 failures, 0 errors, 0 skips; ruby -Itest test/websearch_circuit_test.rb → 8 runs, 33 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
