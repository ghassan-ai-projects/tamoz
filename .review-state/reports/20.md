# Fix report — rank 20 gems/tamoz-core/lib/tamoz/circuit/record.rb

Applied: C1 (with_failure step-down: case dispatched to private per-kind appliers `count_consecutive_failure`/`count_immediate_failure`/`append_window_event`/`count_run_failure`/`append_rate_event` + `apply_failure_condition` dispatcher + `failed_candidate`; pure code motion, mutation order and raise points unchanged) ; C2 (validate! → ordered checklist `validate_key_set!`/`validate_scope!`/`validate_scalars!`/`validate_open_state!` + existing three validators; every corrupt! class/message byte-identical, evaluation order preserved — validate_fields! as proposed would have been 30 lines > Q6 20, so split one level further) ; C3 (local `print` → `normalized_fingerprint`; skipped optional `run` rename to keep the diff surgical) ; C4 (`admit!` → `enforce_admission!`, both callers in-file; grep-proven: only repo hits are the def + lines in with_failure/with_success; test/public_api_test.rb and docs/public-api.json contain zero `admit` references — their "Circuit" hits are PoolCircuitOpenError/MemoryCircuitStore) ; C5 (Record.repaired builds `initial(...).to_payload.merge(...)` → `new(scope:, payload:, now_ms:)`; exact path private `replace` takes — same deep-freeze discipline, byte-identical record, `__send__` bypass removed) ; C6 (`bound_counts`: no-op `counts.to_h { |k,v| [k,v] }` → `counts.dup`; counts is always a Hash per validate_sub_state! / `{}` init)
Rejected: none (all six candidates survived review). FLAG items untouched (owner-only): with_* naming, conditions_met/met_conditions near-collision, uncalled DR-2 surface, deep_copy consolidation.
Diff stat:  gems/tamoz-core/lib/tamoz/circuit/record.rb | 194 +++++++++++++++++-----------
             1 file changed, 117 insertions(+), 77 deletions(-)
Commands:
- ruby -c gems/tamoz-core/lib/tamoz/circuit/record.rb → Syntax OK (before and after)
- rubocop --cache false <owned file> BEFORE: 0 offenses / AFTER: 0 offenses (gate pass, no increase; per-file Metrics debt in .rubocop_todo.yml confirmed via YAML parse — new privates kept ≤20 lines regardless)
- ruby -Itest test/circuit_record_test.rb → 13 runs, 25 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/sqlite_circuit_store_test.rb → 11 runs, 48 assertions, 0 failures, 0 errors, 0 skips
Deviations: (1) window/rate appliers named `append_window_event`/`append_rate_event` instead of candidate's `count_window_failure`/`count_rate_failure` — they append bounded events, not counts (§3.1 verb accuracy); dispatcher drops the candidate's redundant `kind:` kwarg (dispatches on condition.kind directly); (2) C2's proposed validate_fields! split into validate_scope!/validate_scalars!/validate_open_state! to honor the Q6 ≤20-line ceiling on new methods; (3) first rubocop run after edit caught 3 offenses I had introduced (2× Style/GuardClause in the new validators — converted to guard clauses, predicates/messages/order unchanged; 1× Layout/HashAlignment on a shorthand-kwarg continuation) plus a stale `admit!` call left in with_failure before its rename landed — all fixed; final count 0. Note for orchestrator: other fixers' files are modified in the shared tree (agent-cli, agent-kernel, evals/readiness, graph/checkpoint_codec, sqlite/schedule_store) — not touched by this rank.
