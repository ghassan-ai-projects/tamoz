# Combined pass — rank 252 gems/tamoz-scheduler/lib/tamoz/scheduler/grant_intersector.rb
Verdict: CANDIDATES-ONLY
Applied: none | Rejected: C1 (allow_narrowed: kwarg is the §4 BooleanParameter case recorded as review M2; fix = split/rename effective_grant → public-API change on the pinned authority gate, forbidden here — owner decision); C2 (silent String() coercion of non-String grant members, review L1 with stale line refs; fix adds a new ConfigurationError path → behavior/message-bytes change, pinned by scheduler_values_test line 338); C3 (unify grant_intersection/removed_grant behind a set-operator helper; adds machinery for a two-operator difference, B5 shallow abstraction); C4 (rewrite effective_grant's case into early returns; explicit enumeration of :granted/:narrowed/:revoked IS the documented refusal DSL on this gate — churn without gain); C5 (guard-clause/modifier restyle of normalize_values raise; both forms already parked in .rubocop_todo.yml — Layout/EmptyLineAfterGuardClause, Style/IfUnlessModifier — style churn against recorded exclusions); C6 (INTERSECTION_DOMAIN has no in-repo consumer; removal is a public-surface change and it may be reserved for provenance stamping)
Diff stat: no changes to owned file (git diff --stat empty)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE == AFTER, no edits) · ruby -Itest test/scheduler_values_test.rb → 17 runs, 112 assertions, 0 failures, 0 errors · ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors
Deviations: none. File audited clean at method level: every method ≤8 lines under Q6 ceilings, names use repo vocabulary (normalize_/build_ + domain nouns maximum/policy), intersect reads as the BAR.md-required step-down sequence (normalize both grants → intersect → removed → classify → frozen result), and the module doc pins the same pure function at claim-time and execution-time. Per the hard safety rail, real issues exist (C1 above all) but every fix is API-touching or behavior-touching, so nothing was changed.
FLAG (owner decisions): C1 — review M2 stands unresolved: effective_grant(stored, current, allow_narrowed: true) violates §4, but any fix splits/renames a public cross-gem entry point that gates scheduling authority (callers today: sqlite/schedule_store.rb#enforce_claim_grant via default, agent/worker_runtime.rb uses intersect directly; NO caller anywhere passes allow_narrowed:, so a strict variant would have zero internal users). Owner should decide: keep as documented exception or schedule an API slice. Secondary note: C2/C6 from docs/reviews/codebase-review-2026-08/tamoz-scheduler.md remain open by design under behavior-preservation rails.
