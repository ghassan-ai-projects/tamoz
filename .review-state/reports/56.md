# Combined pass — rank 56 gems/tamoz-agent-cli/lib/tamoz/agent/cli_session_commands.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (reuse existing `control_value!` seam for the triplicated required-TASK/new-task argv parse in cmd_ask, cmd_follow_up, cmd_redirect); C2 (extract `submit_redirect` beside cmd_redirect, matching the module's established submit_<operation> convention: submit_follow_up, submit_cancel, submit_mode_switch)
Rejected: C3 (fold cmd_think/cmd_verbose into run_control_mutation — moves control_value! validation INSIDE the run_durable block, changing failure timing vs session acquisition; error-path side effects are pinned); C4 (dedupe cmd_resume/cmd_continue prologue — blocks differ (resume_options), extraction obscures the ordered operator flows the module header declares as its design); C5 (rename cancel_exit — sits in the exit_for_view/exit_for_cancellation family, rename is churn); C6 (reshape parse_resolution's [key, status, symbol] return — churn at its single call site); C7 (validate_cancellable! boolean param → kwarg — single-caller guard, cosmetic only); C8 (cmd_list if/else renderer dispatch → dispatch table — style churn on a byte-pinned render path)
Diff stat: 1 file changed, 14 insertions(+), 13 deletions(-) (net +1)
Commands:
- ruby -c <file> → Syntax OK (before and after)
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses ("1 file inspected, no offenses detected")
- ruby -Itest test/agent_cli_test.rb → 33 runs, 753 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/agent_phase3_context_lifecycle_test.rb → 13 runs, 62 assertions, 0 failures, 0 errors, 0 skips
- ruby -Itest test/context_control_exposure_test.rb → 6 runs, 58 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

Behavior-preservation notes for applied candidates:
- C1: `control_value!(argv, label)` is literally the same three lines being replaced (`argv.join(' ').strip`; raise `OptionParser::MissingArgument` with the same label bytes when empty; return the value). Call positions unchanged, so validation order relative to `extract_thread!`/`load_operator_profile`/`resolve_session_authority` and all error/exit bytes are identical. Caller proof: repo-wide grep (incl. send/__send__/public_send) shows `control_value!` referenced only inside the owned file in the live checkout.
- C2: mechanical move of the `durable_runner.submit` call into `submit_redirect(session, task, thread_id, request_id)` — same receiver, same payload hash, same keyword order → identical submit payload and ordering. Mirrors `submit_cancel` exactly; module header + reek note bless submission helpers beside their commands.
