# Combined pass — rank 94 gems/tamoz-agent-kernel/lib/tamoz/agent/intent_catalog.rb

## Phase 1 — ANALYZE (candidates, written before any edit)

BEFORE state: `ruby -c` OK · `rubocop --cache false` **0 offenses** · 280 lines.
Surface proof: repo-wide grep (incl. send/__send__/public_send patterns) shows external
consumers use only `verify_wire`, `from_list`, `.entry`, `.WATCH_TYPE`
(`episode_nodes.rb`, `decision_builder.rb`, stream tests). All `validated_entry_*`,
`build_entry`, `validate_presets!` references are in-file definitions +
`private_class_method` lists. Public API therefore frozen; only method-level work.

### Candidates

- C1 — `validated_parameter_schema`: drop the eager `schema_bytes = Core.jcs(schema) if
  schema` temp computed *before* presence validation; compute the canonical bytes at the
  single comparison point. Step-down repair: the method then reads presence → shape →
  digest claim, one concern per guard. Behavior-preserving by construction: `schema_bytes`
  was read only in the digest branch, which is unreachable when `schema.nil?` (the
  missing_schema raise precedes it), and `jcs` is pure — same inputs, same bytes, same
  error classes/messages/ordering. Pinned by `test_a_mismatched_schema_digest_is_refused`.
- C2 — `validated_entry_metadata`: flatten the nested unless ("if present, then must be
  valid") into one flat guard clause matching every sibling validator's style;
  `||`/`&&` precedence keeps identical short-circuit semantics; message bytes unchanged.

### Rejected (with reasons)

- R1 rename `validated_entry_*` → `validate_*`: verb family is already correct per
  standard §3.1 (validate = shape match); the participle form satisfies §3 "names say
  what they return" (they return the validated pieces). Pure churn, zero gain.
- R2 extract a dual-key fetch helper for the ~10 `raw[:x] || raw["x"]` sites: multi-site
  churn on a digest-pinned contract file; each site carries different defaults; violates
  surgical-diff discipline.
- R3 share the `normalize_digest(...)` expression between `build_entry` (L84) and the
  schema check (L120): two call sites; extraction adds a method to save one expression —
  machinery over simplicity.
- R4 inline the map in `from_list` into `new(...)`: zero-gain churn.
- R5 extract `schema.fetch("properties", {}).keys` (two sites): over-machinery.
- R6 split `validate_presets!` (~23 lines): within the hard ceiling (30); §4 forbids
  splitting a cohesive guard loop merely to satisfy a number.
- R7 `canonical`/`canonical_entry` vs the §3.1 `normalize_*` spelling: public API on the
  wire/digest path — untouchable by binding rails.

Verdict target: COMMITTED-CANDIDATES (C1+C2, net well under the 60-line rail).

## Final record

Verdict: COMMITTED-CANDIDATES
Applied: C1 (schema validator: lazy digest-bytes computation at point of comparison, guard ordering presence → shape → digest); C2 (rate-limit metadata validator: flattened guard, parenthesized &&-chain per Lint/AmbiguousOperatorPrecedence)
Rejected: R1–R7 as listed above (churn / machinery / out-of-scope API)
Diff stat: gems/tamoz-agent-kernel/lib/tamoz/agent/intent_catalog.rb | 23 ++++++++------------ (1 file changed, 10 insertions(+), 13 deletions(-))
Commands: ruby -c OK · rubocop --cache false: 0 offenses (BEFORE 0 → AFTER 0; two transient self-introduced offenses fixed, none suppressed) · ruby -Itest test/agent_intent_catalog_test.rb: 14 runs, 19 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/stream_decision_builder_test.rb: 18 runs, 52 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
