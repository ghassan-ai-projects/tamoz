# Combined pass — rank 212 gems/tamoz-comms/lib/tamoz/comms/delivery_drainer.rb

Verdict: CLEAN
Applied: (none)
Rejected: C1 (next_fence → advance_fence: same name/concept lives in gateway.rb Gateway#next_fence — main-tree lines 158/892; solo rename forks the §3.1 vocabulary, paired rename is outside ownership → FLAG); C2 (split send_row, ~58 lines > 30 ceiling: drain/retry order pinned by binding note and class-level `rubocop:disable Metrics/*` is the recorded exception; extraction restructures the pinned claim→reserve→bind→mark→send→record→activate sequence); C3 (claim → claim?: side-effecting action matching the store's `claim_delivery` seam, not a predicate; Naming/PredicateMethod already excepted at class scope); C4 (effect_key → build_effect_key: noun-phrase derivation over Comms::Canonical.hexdigest, adds no second spelling — §3.1 governs new spellings, not prefixes on existing ones); C5 (thread retry_after through drain_once's return instead of @retry_after_s ivar: changes public/cross-gem API consumed by gateway.rb:881 and supervision tests); C6 (drop class-level rubocop:disable Metrics/*: offense count would rise above the recorded BEFORE=0 without extraction, which C2 bars); C7 (@stopping → @stop_requested, local `wait` rename, ternary→case in serve_loop: churn, no intent gain)
Diff stat: (no change — see Commands)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → 0 offenses (BEFORE=AFTER) · ruby -Itest test/delivery_drainer_test.rb → 10 runs, 50 assertions, 0 failures, 0 errors · ruby -Itest test/callback_ack_crash_test.rb → 2 runs, 20 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): rename `next_fence` in BOTH DeliveryDrainer and Gateway (identical monotonic-fence helper, one concept one verb) in a single owner-approved pass; DeliveryDrainer#send_row stays long under the recorded class-scope Metrics exception + pinned ordering — revisit only if the pin is lifted.

## Phase 1 — candidates (written before any edit)

Audit vs (1) intent-naming (2) ≤20/30 lines (3) one abstraction level (4) DSL-ish entry points (5) step-down, plus CODING_STANDARD §3/§3.1/§4/§5/§6/§11:

- C1 `next_fence` (L156): computes AND installs `@fence` — name hides the mutation; `@fence` is later read bare in send_row. Rename is sound in isolation BUT the identical private helper exists in Gateway (same concept, same name); renaming only here forks the lexicon. → needs paired edit, out of ownership.
- C2 `send_row` (L70–127): ~58 lines, mixes pacing, journal bind, fence checks, send, recording, activation + 2 rescues. Real length violation EXCEPT the class carries a deliberate `rubocop:disable Metrics/ParameterLists, AbcSize, MethodLength, PredicateMethod` and the binding pins drain/retry ordering to method level. No extraction possible while the pin stands.
- C3 `claim` (L60): boolean return without `?`; but it performs the claiming action against `@store.claim_delivery` — action verb correct, predicate suffix wrong.
- C4 `effect_key` (L152): unprefixed derivation helper; acceptable noun-phrase, no vocabulary conflict.
- C5 `@retry_after_s`: ivar handoff drain_once→serve_loop; threading it through returns alters the public `drain_once` contract.
- C6 disable block (L10/L162): only removable by fixing the metrics underneath = C2 extraction.
- C7 micro-rename/style items: no clarity payoff.
- Comments (L89–90, L129–131): genuine why-comments (lost-fence invariant; receipt provenance §7.1) — keep per §11.
- Public surface `initialize/serve_loop/stop/drain_once`: reads as a small DSL; `drain_once(now:)` consumed cross-file by gateway.rb — untouched.
- Step-down otherwise clean: drain_once → claim/send_row → send_delivery/activate_after_receipt → store seams.

Grep proofs: no `send/__send__/public_send` reaches DeliveryDrainer privates in tests; `next_fence` outside worktrees = delivery_drainer.rb (64,156) + gateway.rb (158,892) only.

## Phase 2 — application

Nothing applied (all candidates rejected above; none both in-scope and net-positive). Gates re-run after no-op state; focused tests confirm green baseline.
