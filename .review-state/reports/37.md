# Fix report — rank 37 gems/tamoz-evals/lib/tamoz/evals/benchmark/scoreboard.rb

Applied: C1 (step-down split of `regression_check`: extracted private class helpers
`evaluate_regression`, `regression_drops`, `prior_intervals!`, and a shared
`regression_result` now also used by `passed_regression`; orchestrator reads as
read → validate → evaluate → pass/raise; all four Metrics disables on the method
removed); C5 (`interval_documents` step-down: extracted `manifest_referenced_paths`;
hoisted `INTERVAL_CONTAINER_KEYS` + `MANIFEST_REFERENCE_KEYS` frozen constants beside
`REPORT_FILENAMES`; same paths visited in same order; AbcSize disable removed);
C4 (`build_axis_verdicts` split: extracted `axis_verdict_source(report)` returning
`[source_hash, scalar]`; source-priority logic untouched; disables removed);
C3 (`metric_score` unwrap: extracted `metric_unavailable?` and `unwrap_metric_value`;
guard order preserved; disables removed); C2 (renamed `lower_score` →
`normalize_lower_score`, sibling of `normalize_higher_score`; sole call site line 452,
grep-proven in-file-only across repo incl. send variants)
| Rejected: visibility narrowing of `intervals_for_entry` / `acknowledged_note?`
(owner FLAG — changes the public method table; no owner acceptance proof exists;
left public); four `self.class.send(:...)` bridges (lines ~355/368/390/391) left
untouched per owner FLAG — not converted to a helper module)

Diff stat:
```
 .../lib/tamoz/evals/benchmark/scoreboard.rb        | 149 ++++++++++++---------
 1 file changed, 88 insertions(+), 61 deletions(-)
```

Commands:
- `ruby -c <owned file>` — before: Syntax OK; after: Syntax OK.
- `rubocop --cache false <owned file>` — before: **0 offenses**; after: **0 offenses**
  (count unchanged; four disable/enable comment pairs REMOVED, zero suppressions added).
- `ruby gems/tamoz-evals/test/scoreboard_test.rb` — before: 3 runs / 20 assertions /
  0 failures; after: 3 runs / 20 assertions / 0 failures.
- `ruby gems/tamoz-evals/test/scoreboard_cli_test.rb` (covers regression_check end to
  end via script/benchmark_openclaw_regression) — before: 3 runs / 21 assertions /
  0 failures; after: 3 runs / 21 assertions / 0 failures.
- Differential probe (extra proof, /tmp/probe_scoreboard.rb): HEAD variant vs edited
  variant over 41 observations — single-entry pass shape, unacknowledged-drop error
  bytes + full drop-row list, acknowledged-pass with regressions kept, incomplete-
  intervals error bytes in AXES order, manifest-referenced interval discovery
  (report_path → nested doc → 'axes' container → ci_low/upper), glob-sweep junk
  tolerance, 5 build_axis_verdicts source-priority variants, 7 find_interval shapes,
  higher/lower/cost scorer math incl. clamps, metric unavailable/normalized/score/value
  plumbing, first-match-wins. Result: **byte-identical output** (diff clean).

Deviations:
- C1 as proposed needed two helpers, but the literal two-helper split left the
  orchestrator above the Q6 20-line ceiling; applied a three-step decomposition
  (`evaluate_regression` composing `regression_drops` + `prior_intervals!` +
  `regression_result`) so every extracted method fits without disables. The
  candidate's named helpers `regression_drops` / `regression_result` are both present
  as specified; result-hash key order and all error-message bytes are unchanged
  (probe-proven).
- C5 additionally named the manifest-reference key list (`MANIFEST_REFERENCE_KEYS`);
  `manifest_referenced_paths(root)` reads manifest.json itself instead of taking it as
  a parameter, which keeps path-ordering (manifest path always included) inside one
  helper. Same paths, same order.

Notes for orchestrator:
- append / regression_check signatures untouched (script/benchmark_openclaw_regression
  call site verified); recorded-artifact bytes unaffected (CanonicalJSON key order
  identical; probe-proven byte equality incl. written scoreboard content paths).
- Working tree also contains OTHER agents' in-flight files (openclaw_mission_runner.rb
  earlier; docs/README.md, tamoz-agent-healing, tamoz-agent-session ×2, tamoz-mcp ×2 at
  report time). My change is confined to the owned file above; I did not touch them.
