# Combined pass — rank 197 gems/tamoz-agent-kernel/lib/tamoz/agent/episode_frame_builder.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (remove dead `bytes` local + dedupe canonical payload into one binding); C2 (extract private `build_evidence_ids` step-down helper) | Rejected: C3 (kwargs for `build_user` — no boolean params, private single-caller, pure churn); C4 (rewrite `build_facts` loop with `raw.first(MAX_FACTS)` — equivalent churn, explicit break-guard already states the truncation); C5 (`verify_prompt!` nil-when-unset return semantics — public API, contract documented, zero clarity gain); C6 (comment removal — all four blocks are §11 why-comments: design contract, Go cross-repo digest domain, untrusted-fencing rationale)
Diff stat: `1 file changed, 12 insertions(+), 11 deletions(-)` (net +1; well inside the ~60-line stop rail)
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses BEFORE, 0 AFTER (no increase) · `ruby -Itest test/benchmark_controls_test.rb` → 14 runs, 42 assertions, 0 failures, 0 errors, 0 skips. No dedicated episode_frame_builder test exists; the stream_episode_* suites that also touch this class are in the brief's do-not-chase lane and were not run.
Deviations: none
FLAG (owner decisions): none

## Phase 1 — candidates (written before any edit)

Audit basis: name-states-intent / ≤20-line methods (file carries MethodLength/AbcSize TODO debt
that hides `build` at ~22 body lines) / one level of abstraction / small-DSL entry points
(`initialize`, `verify_prompt!`, `build`, `prompt_digest`) / step-down. BEFORE: `ruby -c` OK,
`rubocop --cache false` = 0 offenses.

- **C1 — APPLY.** Lines 54–56 compute `bytes = Tamoz::Core.jcs({...})`; the local is never read.
  The identical payload hash is rebuilt inline at lines 68–69 for the digest. Proof of
  behavior preservation: `Tamoz::Core.digest(domain, value)` is `"sha256:" + SHA256(domain +
  canonicalize(value))` (gems/tamoz-core/lib/tamoz/core/jcs.rb:70–77) and `canonicalize` is pure,
  so the dropped JCS call has no observable effect; any JCS error it could raise on the payload is
  raised identically by the digest call over the same payload. Frame digest bytes are unchanged →
  pinned digests hold.
- **C2 — APPLY.** Extract the evidence-id concatenation (fact:/skill:/memory:/tool: prefixes) into
  private `build_evidence_ids(facts, skills, memory, tool_results)`; `build` drops to ~13 body
  lines and reads as verify → build parts → bind canonical → seal frame. Order of concatenation
  preserved exactly (asserted by test/benchmark_controls_test.rb Control 9 `"memory:memory:deadbeef"`
  evidence id, consumed by the grounding gate at episode_nodes.rb:645).
- **C3 — REJECT** (see verdict line).
- **C4 — REJECT** (see verdict line).
- **C5 — REJECT** (see verdict line).
- **C6 — REJECT** (see verdict line).

No renames proposed; no dynamic-dispatch audit needed beyond confirming no send/__send__/
public_send targets this class (repo-wide grep: only witness_verifier's unrelated receipt sends).
