# Combined pass — rank 43 gems/tamoz-agent-capabilities/lib/tamoz/agent/capability_binding.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename discovery-phase flag local `read_only` → `discovery`, de-collided from `effect_class == :read_only` two lines below); C2 (local `read_only` → `read_only_names`, aligned with callee's parameter — it holds a list); C3 (`source_id_for` now uses the existing private `websearch?` predicate instead of a raw duplicated comparison — byte-identical strings); C4 (frozen `MCP_CAPABILITY_KINDS` constant replaces per-call `%i[mcp_tool websearch]`, matching the file's own closed-set convention) | Rejected: C5 (`!include?` → `exclude?`: APPLIED then REVERTED — `Array#exclude?` is ActiveSupport-only and this gem does not load it; capability_host_test errored 13×, revert restored green); C6 (extract `routed(name)` helper for the 7 route sites — the explicit `host.route(String(name))` at each entry point IS the invariant-17 surface documented above `execute`; indirection adds nothing on an authority-adjacent path); C7 (locals named `effect_class` shadowing public `effect_class(name)` — unambiguous in context and they feed approval/retry/reconciliation kwargs; approval semantics untouchable); C8 (collapse `mcp_egress_policy` into `source_id_for`, byte-identical today — egress-policy naming feeds approval-engine digests, owner decision, see FLAG); C9 (split `mcp_descriptor`/`build_toolbox_descriptor` — sealed-descriptor field lists pin capability semantics; §6 admits declarative construction; both ≤27 lines)
Diff stat: gems/tamoz-agent-capabilities/lib/tamoz/agent/capability_binding.rb | 18 ++++++++---------- → 8 insertions(+), 10 deletions(-)
Commands: ruby -c <file> → Syntax OK (BEFORE + AFTER) · rubocop --cache false <file> → 0 offenses BEFORE, 0 offenses AFTER (no increase) · ruby -Itest test/capability_host_test.rb → 21 runs, 91 assertions, 0 failures, 0 errors · ruby -Itest test/agent_mcp_capability_source_test.rb → 14 runs, 102 assertions, 0 failures, 0 errors · ruby -Itest test/agent_phase4_capability_test.rb → 7 runs, 31 assertions, 0 failures, 0 errors
Deviations: none — net change 18 lines (−2 LOC), under the ~60 cap; zero method renames (all callers proven unaffected: touched symbols are file-private or locals; repo-wide grep incl. send/public_send found no external dispatch of `source_id_for`/`websearch?`; `mcp_capability?` name+signature untouched, sole caller session_effects.rb:271 unaffected)
FLAG (owner decisions): `mcp_egress_policy(descriptor)` returns byte-for-byte the same string as `source_id_for(descriptor)` today ("websearch:<server>" / "mcp:<server>"). Collapsing them would couple the egress-policy ref scheme (which seeds approval-engine egress digests) to the source-id construction scheme. Left separate deliberately; owner may want one defined as the other, or a comment pinning the coincidence.

---

## Phase 1 — ANALYZE record (written before any edit)

BEFORE gates: `ruby -c` OK · `rubocop --cache false` = 0 offenses · branch verification-again,
file unmodified. Surface constraint honored: authority-adjacent binding — method-level
naming/step-down only; no approval/capability semantics touched; zero method renames.

Audit sweep: all methods ≤27 lines (worst: `mcp_descriptor`, `build_toolbox_descriptor`, both
under the 30 hard ceiling); no boolean params; kwargs used; predicates end `?`; no get_/set_;
public entry points read as a coherent surface; step-down from `initialize` through the seven
`build_*` steps is one level deep throughout.

### Candidates as originally listed

APPLY: C1 discovery-flag rename · C2 read_only_names alignment · C3 websearch? reuse ·
C4 MCP_CAPABILITY_KINDS constant · C5 exclude? (later reverted — see Applied/Rejected above).
REJECT: C6 routed-helper indirection · C7 effect_class local shadow · C8 egress/source-id merge ·
C9 descriptor-block splits.
