# Combined pass — rank 116 gems/tamoz-tools/lib/tamoz/tools/tool_argument_validator.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (`validate_path_only` descriptor params → kwargs `allowed:`/`key:`/`default:`, §4)
Rejected: C2 (unify duplicated expected_sha256 shape check across validate_digest/validate_content_digest — pinned Rejection-contract bytes; header documents keeping per-operation checks inline so the accepted surface stays auditable; a 2-line dup does not justify new indirection); C3 (remove `empty:` boolean kwarg from validate_patch_text! by splitting — any split either duplicates the 5-check body or reorders it, and error precedence is pinned, e.g. a binary-encoded empty `before` must still report "must not be empty"); C4 (single-lookup dispatch `validator = VALIDATORS[name]` in validate_operation — guard+fetch is deliberate and equally clear; churn); C5 (`label = "replacements[#{index}]"` local in validate_replacement — interpolated names are self-describing at each site, message strings pinned; churn); C6 (hoist shared `Digest::SHA256.hexdigest(arguments.fetch('content'))` in validate_content_digest — branches read clearly as-is; micro-dedup); C7 (create_file mode ternary → unconditional merge — ternary states intent, normalize default only when absent; nil gain)
Diff stat: gems/tamoz-tools/lib/tamoz/tools/tool_argument_validator.rb | 8 ++++---- 1 file changed, 4 insertions(+), 4 deletions(-)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> BEFORE 0 offenses / AFTER 0 offenses (no increase) · ruby -Itest test/agent_toolbox_test.rb → 62 runs, 320 assertions, 0 failures, 0 errors, 0 skips · ruby -Itest test/agent_skills_toolbox_test.rb → 18 runs, 152 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): §4 forbids boolean parameters and `validate_patch_text!(value, name:, empty:)` uses one. Removing it cleanly conflicts with the pinned Rejection-contract error ordering (for `before`, the empty check precedes size/null/encoding checks; any de-booleanization reorders that or duplicates the body). Left as-is; split into before/after-specific validators only if the ordering contract is ever re-baselined.

## Phase 1 — ANALYZE (candidate list written before any edit)

Audit vs (1) intent-naming · (2) length ceilings · (3) one abstraction level ·
(4) public DSL · (5) step-down, grounded in docs/CODING_STANDARD.md §3/§3.1/§4/§11.
Baseline: branch verification-again; owned file clean in git before edit; `ruby -c`
OK; rubocop --cache false BEFORE = 0 offenses.

- Public entry points: exactly one — `validate(name, arguments)` plus the frozen
  constructor. Reads as a small DSL. The VALIDATORS symbol map + private per-op
  validators keep every accepted argument auditable in one class (documented
  protocol boundary). No change.
- Vocabulary (§3.1): all checks are `validate_*` / `reject_unknown!`; no get_/set_;
  predicates read as questions; rescue var `e` documented in the header. No change.
- Lengths (§4): largest method is `validate_content_digest` at 15 lines; all ≤20.
  No splits warranted ("never split cohesive logic merely to satisfy a number").
- Step-down: validate → validate_operation → per-op validators → leaf helpers
  (validate_path_argument!, validate_patch_text!, reject_unknown!, …), one level
  per hop. No change.

Candidate-by-candidate notes:
- C1 APPLY. `validate_path_only(arguments, allowed, key, default = nil)` had four
  positional params where only the first is the subject — §4 "keyword arguments
  beyond one or two positional parameters". Behavior-preservation proof: pure
  parameter-binding rename; identical values bind to identical locals; `default:
  nil` preserves the old default; control flow, raise order, message bytes
  untouched. Repo-wide grep (incl. send/__send__/public_send forms of the name)
  shows all three callers in-file (read_file/list_directory/search_text).
- C2–C7 REJECT with reasons in the header block above.
