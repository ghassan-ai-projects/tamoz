# Combined pass — rank 70 gems/tamoz-evals/lib/tamoz/evals/harness/memory_treatment_profile.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (dedup byte-identical digest-stamp+wrap tail of build_ci_report/run_live into private `final_report`); C2 (remove dead `else raise "unknown treatment mode"` in `run` — @mode assigned only in initialize and validated there, so unreachable; `.allocate` never used on this class repo-wide)
Rejected: C3 (aggregate_ci publishes one count under two keys — report surface bytes are product/digest-visible, flagged not touched); C4 (delta_summary recomputes pure control_identical? — signature churn for negligible win); C5 (gate(id, passed) boolean is the rendered verdict datum, not a behavior switch — §4 letter, no machinery warranted); C6 (build_ci_report ~52 L / run_live ~41 L over §4 ceiling — bodies are declarative document literals; named exception per §4 "never split cohesive logic", C1 removed their only true duplication); C7 (validate_mode! guard-clause restyle — churn)
Diff stat: 1 file changed, 10 insertions(+), 11 deletions(-) (net −1; rail ~60)
Commands:
- BEFORE `ruby -c <file>` → Syntax OK
- BEFORE `rubocop --cache false <file>` → 0 offenses
- BEFORE `ruby -Itest … test/memory_treatment_profile_test.rb` → 14 runs, 211 assertions, 1 failure (test_scorecard_environment_declares_scripted_no_attribution asserts on AgentSmokeScorecard — different file, SLOW known-red lane, does not reproduce after edit; assertion delta matches aborted-test accounting)
- AFTER `ruby -c <file>` → Syntax OK
- AFTER `rubocop --cache false <file>` → 0 offenses (no increase vs BEFORE)
- AFTER `ruby -Itest … test/memory_treatment_profile_test.rb` → 14 runs, 218 assertions, 0 failures, 0 errors (incl. E5 digest-identity across two runs — exercises the touched stamping path)
- AFTER `ruby -Itest … test/memory_repository_adapter_test.rb` → 10 runs, 104 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): aggregate_ci carries the same number under two keys — "attribution_incomplete" (~L207) and "attribution_incomplete_cells" (~L222). Retiring one changes emitted report bytes/content_digest, so it needs its own reviewed format change, not an inline cleanup.
