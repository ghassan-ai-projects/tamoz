# Combined pass — rank 66 gems/tamoz-stream/lib/tamoz/stream/decision_builder.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (reformat over-long modifier-form raise in apply_model_values! into a guarded block — error/message bytes and raise point unchanged); C2 (inline pure-delegation `diagnose_intents` into `intents` — both comments kept on the survivor); C3 (extract watch-only bindings from apply_episode_bindings! into apply_watch_bindings! — statements execute in identical order)
 |  Rejected: R1 (unify proposal_type/proposal_preset/proposal_parameters into one field-reader helper — three parallel two-line readers are clearer than added indirection); R2 (replace hand-rolled deep_dup with Marshal/Core helper — cannot prove identical handling of every catalog preset shape, e.g. non-dupable leaves); R3 (merge @catalog.include?+@catalog.entry in admissible_entry — depends on IntentCatalog semantics, low-value churn); R4 (rename confidence→clamped_confidence — clamping visible in the 4-line method, rename adds nothing); R5 (restructure the triple `return [watch_condition_intent]` guards — guard-clause form already reads as the demotion ladder); R6 (trim class/method contract comments — §11 "why" comments recording the authority model and fail-closed rationale)
Diff stat: gems/tamoz-stream/lib/tamoz/stream/decision_builder.rb | 16 ++++++++-------- → 8 insertions(+), 8 deletions(-), net 0
Commands:
- ruby -c gems/tamoz-stream/lib/tamoz/stream/decision_builder.rb → Syntax OK (before and after)
- rubocop --cache false <file> → BEFORE: 1 offense (Layout/LineLength 128/120 @243, pre-existing) · AFTER: 0 offenses
- ruby -Itest test/stream_decision_builder_test.rb → BEFORE 18 runs, 52 assertions, 0 failures, 0 errors · AFTER identical
- timeout 240 ruby -Itest test/stream_invariants_test.rb → BEFORE 9 runs, 212 assertions, 0 failures, 0 errors · AFTER identical
(public_api_test.rb also references DecisionBuilder but only asserts public API names; public surface untouched, not run)
Deviations: none
FLAG (owner decisions): none

## Candidate list (written before any edit)

- C1 APPLY: fix the file's only rubocop offense (Layout/LineLength in apply_model_values!) by restructuring to a guarded raise; bytes preserved.
- C2 APPLY: inline `diagnose_intents` into `intents`. Proven in-file: repo-wide grep shows no reference outside this file (only stale `.worktrees/` copies), `build` is the sole caller of `intents`, and tests use no send/__send__/public_send on privates.
- C3 APPLY: split watch-specific bindings out of `apply_episode_bindings!` into `apply_watch_bindings!(parameters)` — restores one level of abstraction in the mutator; ordering (entity_id, situation_id, situation_version, target, expires_at) unchanged.
- Rejected: R1–R6 above.

Constraints honored: hash-key/wire order untouched everywhere; public class API (`build`, `build_decision`, `episode_decision_id`, `with_digest`) untouched; sole external consumer `decision_node_builder.rb` uses only `build`/`build_decision`.
