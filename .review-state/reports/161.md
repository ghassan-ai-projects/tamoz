# Combined pass — rank 161 gems/tamoz-graph/lib/tamoz/graph/subgraph_runtime.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (drop unused `result` param from `failed_child_error`, sole in-file caller updated); C2 (`run_child` step-down: four duplicated 5-key kwarg blocks collapsed into private `dispatch_keywords(context, namespace)` helper, splatted per branch, `invoke_at` keeps its extra `execution_id:`/`new_execution:`); C3 (inline child-context derivation extracted to `child_context(child, context, call_index)`, joining the existing child_* derivation family)
Rejected: R1 (`Hash#slice` rewrite of `child_input` — NOT equivalent: slice matches original keys by equality, current code compares `key.to_s`, symbol-keyed inputs would project differently = behavior change); R2 (§3.1 renames `child_namespace`→`build_child_namespace`, `interrupt_descriptor`→`render_interrupt_descriptor` — names are clear, callers private and in-file, pure churn); R3 (`run_child` 6-positional-param reduction — ParameterLists exclusion already records the debt; kwargs form still counts 6; a Struct bundle is machinery for lint appeasement); R4 (Set membership in `child_input` — perf micro-change, no clean-code defect); R5 (per-status branch methods in `run_child` — after de-duplication each branch is 1–5 lines; more splitting adds indirection, §4 never-split rule)
Diff stat: 1 file changed, 26 insertions(+), 34 deletions(-) — net −8 lines, 60 changed lines (at the ~60 rail, not beyond)
Commands: `ruby -c gems/tamoz-graph/lib/tamoz/graph/subgraph_runtime.rb` → Syntax OK · `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase) · `ruby -Itest test/graph_subgraph_test.rb` → 4 runs, 28 assertions, 0 failures, 0 errors, 0 skips
Deviations: none — no commits, no bundle install, rubocop always --cache false, tests one file per command; file is not in gems/tamoz-sqlite so the boundary_source_audit extra gate does not apply
FLAG (owner decisions): `.rubocop_todo.yml` records Metrics/MethodLength + Metrics/ParameterLists exclusions for this file; C2 shrinks `run_child` from ~45 to ~27 code lines but it stays above the 20-line normal ceiling, so the MethodLength entry must remain. C1 removed what is almost certainly the file's only Lint/UnusedMethodArgument offense — that todo entry may now be regenerable away (forbidden file for this pass).

## Phase 1 — ANALYZE (candidates, written before any edit)

Audit basis: replay semantics pinned (user binding) → only method-level naming/step-down;
wire/digest bytes (`child_execution_id` domain string, `interrupt_descriptor` shape,
`child_namespace` ordering), error messages, event ordering, public API untouched.
BEFORE state: `ruby -c` OK · rubocop --cache false = 0 offenses (file carries per-file
exclusions in .rubocop_todo.yml: Layout/EmptyLineAfterGuardClause, SpaceInsideHashLiteralBraces,
Lint/UnusedMethodArgument, Metrics/AbcSize, Metrics/MethodLength, Metrics/ParameterLists,
Style/Documentation, Style/IfUnlessModifier, Style/StringLiterals).

- C1 APPLY — `failed_child_error(child, result)`: `result` is an unused parameter; the
  signature claims a dependency it does not have. Drop the param, update the sole caller
  (line 49). Proof: private method; repo-wide grep incl. send/__send__/public_send finds
  exactly one call site, inside this file (worktree copies are other branches' snapshots).
- C2 APPLY — step-down in `run_child`: four near-identical 5-key kwarg blocks
  (`thread:, namespace:, request_id:, concurrency:, context:`) collapse into one private
  helper returning that hash; branches splat it, invoke_at adds `execution_id:`/
  `new_execution:` on top. Behavior proof: Ruby kwargs are matched by name at the callee;
  all four targets are endless `(…)` delegators (compiled.rb:200–203) forwarding verbatim
  to RunCoordinator.invoke/resume/retry_failed/continue whose signatures take exactly these
  keywords (resume(answers, thread:, namespace:, request_id:, concurrency:, context:) etc.).
  No digest/wire/event surface involved. run_child ~45 → ~27 code lines (hard ceiling 30).
- C3 APPLY — step-down in `call`: the inline `context.child("subgraph.#{child.name}", …)`
  derivation becomes `child_context(child, context, call_index)`, joining the existing
  child_* derivation family (child_namespace, child_execution_id, child_input,
  child_output) — it was the only derived-per-call value left inline. Called at the same
  position in the sequence, so the side-effecting order (with_checkpointer → next_call_index
  → namespace → context derivation) is unchanged.
