# Combined pass — rank 177 gems/tamoz-mcp/lib/tamoz/mcp/websearch/egress_circuit.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down: constructor guards extracted to private `validate_configuration!` orchestrating `validate_threshold!` / `validate_scope_id!` / `validate_budget_breach!`, mirroring the file's existing `validate_evidence_*!` idiom; messages, order, exception class, and semantics byte-identical)
Rejected: C2 (`record_success` guard-clause inversion — pure churn); C3 (rename `transition_on_failure` — single in-file caller, no clarity gain); C4 (torn mutex reads in `conditions_digest` path — real observation but fix means lock-scope/digest change on owner-pinned security semantics); C5 (boolean kwarg `budget_breach:` vs §4 — operator-declared DR-2 policy switch on public API, untouchable); C6 (unused trailing `evidence` return in `validate_reset_evidence!` — churn on pinned authority-gate path)
Diff stat: 1 file changed, +29 −9 (38 changed lines; file 190→210)
Commands:
- BEFORE: `ruby -c <file>` Syntax OK · `rubocop --cache false <file>` = 0 offenses
- AFTER: `ruby -c <file>` Syntax OK · `rubocop --cache false <file>` = 0 offenses (parity held)
- `ruby -Itest test/websearch_circuit_test.rb` → 8 runs, 33 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/websearch_invocation_test.rb` → 11 runs, 61 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): C4 only — if torn-read immunity for `conditions_digest` is ever wanted, it is a deliberate lock-scope/digest-semantics decision, not a clean-code edit.

## Notes

- Trip/open/reset behavior untouched (owner-pinned): consecutive threshold, immediate
  budget-breach open, success-never-closes, operator-authority gate all as before HEAD.
- CanonicalJSON resolves lexically to `Tamoz::Mcp::CanonicalJSON`, required ahead of the
  websearch files by `lib/tamoz/mcp.rb` — established load seam, left alone.
- Header/inline DR-2 comment blocks kept: §11 why-comments on safety invariants.
- TAMOZ-SQLITE boundary_source_audit extra gate N/A (tamoz-mcp).
