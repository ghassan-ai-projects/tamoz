# Combined pass — rank 107 gems/tamoz-sqlite/lib/tamoz/sqlite/request_inbox_transitions.rb
Verdict: CANDIDATES-ONLY
Applied: (none)
Rejected: C1 (remove dead `@staleness` ivar — multi-file: the honest fix drops the `staleness:` kwarg, but the only constructor call lives in `request_inbox.rb`, outside my ownership; dropping just the assignment trips Lint/UnusedMethodArgument (defaults apply, AllowUnusedKeywordArguments false) and leaves a required-but-ignored kwarg, which lies about the contract worse than the dead store); C2 (replace the `case action.to_s` mapping in `request_transition` with an `%w[running completed failed]` include-check — ConfigurationError class/message bytes are pinned and the explicit mapping mirrors `RequestTransitionPlan::ACTIONS`; churn in an error-bytes-pinned path for zero gain); C3 (rename private `validate_transition_lease!` → `enforce_*` — the adapter seam it wraps is spelled `validate_lease_in_transaction!` and the already-passed claimer sibling uses `validate_claim_lease!`; `enforce_` would add a second spelling for one concept, against §3.1's own rule); C4 (convert `update_request_transition!`'s six positional params to keywords — the durable core's stated idiom is "fenced SQL writes stay explicit and auditable", class-level reek suppressions already declare this; signature churn in the CAS-critical UPDATE path, no behavioral gain); C5 (inline the one-use `transition_request_in_transaction!` wrapper — its name carries intent, checkpoint-less variant of the fenced apply; removal is neutral churn); C6 (extract the action-parse block out of `request_transition` to meet the ≤30 ceiling — the file's recorded cohesion exception forbids extraction and the inline Metrics/MethodLength disable is already justified at site per §1)
Diff stat: no change (git diff --stat empty for the owned file)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses · ruby -Itest test/sqlite_boundary_source_audit_test.rb → 7 runs, 55 assertions, 0 failures, 0 errors · ruby -Itest test/sqlite_request_inbox_test.rb → 8 runs, 46 assertions, 0 failures, 0 errors · ruby -Itest test/sqlite_stale_request_test.rb → 26 runs, 176 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): `RequestInboxTransitions#initialize` dead-stores `staleness:` into `@staleness`, never read anywhere in the file or via reflection; claimer and recovery both genuinely use their staleness collaborator, so this one is vestigial. One-line fix here plus dropping the argument at the `request_inbox.rb:25` construction site — cross-file, so left for the owner.
