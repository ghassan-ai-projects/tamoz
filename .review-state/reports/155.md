# Combined pass — rank 155 gems/tamoz-evals/lib/tamoz/evals/harness/sqlite_trace_manifest_verifier.rb

Verdict: COMMITTED-CANDIDATES
Applied: C2 (drop always-false boolean kwarg `allow_empty:` from bounded_array!, §4); C3 (`subject.reject { |k,_| k == "digest" }` → `subject.except("digest")`)
Rejected: C1 (`verify!` ~59 body lines > 30 hard ceiling — faithful step-down extraction needs ~80+ changed lines, alone trips the ~60-line rail → escalated as FLAG); C4 (`rescue ExecutionError; raise` provably redundant by ancestry but sits in the pinned refusal-wrapping path — explicit precedence worth more than 2 deleted lines); C5 (renames of `exact_hash!`/`scalar_hash!`/`scalar!` — style churn over ~14 call sites, bang-assertion naming already consistent); C6 (unify verify_recorder!/verify_registry! — adds machinery to two small obvious methods); C7 (`events.fetch(0)` → `events.first` — fetch(0) is deliberate fail-closed)
Diff stat:
```
 .../lib/tamoz/evals/harness/sqlite_trace_manifest_verifier.rb     | 8 +++-----
 1 file changed, 3 insertions(+), 5 deletions(-)
```
Commands:
- `rubocop --cache false <file>` BEFORE: 0 offenses
- `ruby -c <file>` → Syntax OK
- `rubocop --cache false <file>` AFTER: 0 offenses (no increase)
- `ruby -Itest test/sqlite_trace_recorder_test.rb` → 11 runs, 952 assertions, 0 failures, 0 errors, 0 skips (covers verify_manifest! round-trip with exact manifest/digest equality + 11 tamper-refusal treatments through ManifestVerifier)
- test/sqlite_scenario_driver_test.rb not run: declared known-red "scenario driver" lane in the brief; no other test touches this file.
Deviations: none
FLAG (owner decisions): C1 — `SQLiteTraceRecorder::ManifestVerifier#verify!` needs a real step-down extraction (replay_events! / record_event! / hook_payload, all under Q6 ceilings). Pure intra-class verbatim move, refusal bytes untouched, but ≈80+ changed lines exceeds the single-pass rail; recommend the two-agent pipeline.

## Phase 1 — ANALYZE (candidate list, written before any edit)

Baseline: rubocop --cache false → 0 offenses. Sole public entry point `verify!`; sole external
caller `SQLiteTraceRecorder.verify_manifest!` (sqlite_trace_recorder.rb:91); routed through by
sqlite_scenario_registry.rb:214. Focused tests: test/sqlite_trace_recorder_test.rb (full
verify_manifest! round-trip + 11 tamper-refusal treatments), test/sqlite_scenario_driver_test.rb
(known-red "scenario driver" lane per brief — not chased).

Behavior-preservation proofs for applied candidates:
- C2: both in-file callers passed `allow_empty: false`, so `(allow_empty || value.any?)` ≡ `value.any?`; method is private, repo-wide grep shows no send/public_send callers outside this file.
- C3: `Hash#except` (Ruby 3.3 core) returns a new hash minus `"digest"` with identical pairs in identical order — byte-identical recorder input, therefore identical content_digest/refusals. Round-trip test asserts `assert_equal manifest, verified`.
