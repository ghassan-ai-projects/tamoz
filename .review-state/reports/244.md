# Combined pass — rank 244 gems/tamoz-agent-healing/lib/tamoz/agent/healing/oracle.rb

## Phase 1 — ANALYZE (candidates, written before any edit)

BEFORE rubocop (`--cache false`): 0 offenses ("1 file inspected, no offenses detected").
Public API (untouchable): `Oracle.verify(rule:, toolbox:)`, `Oracle.digest_for(toolbox, check_name)`
— external callers proven: `gems/tamoz-evals/.../agent_smoke_corpus.rb:2661`,
`test/healing_fixtures.rb:48,100`, `gems/tamoz-agent-healing/.../remediation/effect_execution.rb:47`.
All other helpers are `private_class_method` with in-file-only callers.

- C1 — `verify` rescue path re-reads `rule.verification_oracle` although local `oracle`
  already holds it; the accessor is a plain `Data.define` reader on a deep-frozen hash
  (rule.rb:38, :138), so reuse is provably byte-identical. Low risk → APPLY.
- C2 — `digest_for` coerces `String(check_name)` twice (hash key + digest payload);
  hoist to one local. `String(String(x)) == String(x)`, digest input bytes unchanged.
  Low risk → APPLY.
- C3 — unify `absent_result` / `mismatch_result` / `unavailable_result` /
  `result_from_receipt` behind one parameterized builder → REJECT: extraction beyond the
  pinned method-level scope for this file (verdict semantics contract rail); the four
  explicit builders read as a reason-token table.
- C4 — rename `result_from_receipt` → `receipt_result` for symmetry with the other three
  constructors → REJECT: naming churn, current name already states intent.
- C5 — rename/reword `observe_digest` or its docstring → REJECT: name states intent
  (observes the configured check's digest, nil when absent); no defect.

Audit vs criteria: all methods ≤15 lines; one level of abstraction holds in `verify`
(step-down into observe → gate → execute → assert → build, plus a single rescue mapping to
the unavailable outcome); public surface reads as a two-verb DSL; §3 vocabulary respected
(`assert_receipt!` raise-on-fail, predicates via `receipt.passed?`). No boolean params, no
get_/set_. Verdict semantics untouched: `passed` remains the only gate on `recovered`.

## Phase 2 — APPLY

Applied: C1 (reuse local `oracle` in `verify`'s rescue instead of re-reading the frozen
`Data` accessor); C2 (hoist single `name = String(check_name)` in `digest_for`; error
message bytes unchanged — still interpolates original `check_name.inspect`).
Rejected: C3, C4, C5 as reasoned above.

```
Verdict: COMMITTED-CANDIDATES
Applied: C1 (verify rescue reuses local oracle hash); C2 (digest_for single String coercion)
Rejected: C3 (builder unification = extraction beyond pinned method-level scope); C4 (receipt_result rename = churn); C5 (observe_digest rename = churn)
Diff stat: 1 file changed, 4 insertions(+), 3 deletions(-)  (net +1 LOC)
Commands:
  ruby -c oracle.rb                                  → Syntax OK
  rubocop --cache false oracle.rb                    → BEFORE 0 offenses / AFTER 0 offenses
  ruby -Itest test/healing_remediation_test.rb       → 13 runs, 74 assertions, 0 failures, 0 errors, 0 skips
  ruby -Itest test/canonical_cross_surface_composition_test.rb → 1 runs, 106 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none
```
