# Combined pass — rank 200 gems/tamoz-scheduler/lib/tamoz/scheduler/occurrence.rb
Verdict: COMMITTED-CANDIDATES
Applied: C2 (dedup positive-epoch guards in `validate!` behind private `validate_time!(value, name)` mirroring sibling schedule.rb's seam; check order + message bytes preserved); C3 (inline single-caller private `transition(to,…)` into `claimed`, making all four due-guards uniform guard+`with_state`; guard message preserved verbatim)
 |  Rejected: C1 (to_h via super.transform_keys — diverges from passed sibling schedule.rb#to_h manual idiom for serialized value shapes; zero behavior gain); C4 (shared state-guard helper — per-site error-message bytes differ by design, helper needs format branches = more machinery than duplication saved); C5 (merge skipped/coalesced — same message-byte reasoning, two 4-line methods don't justify indirection); C6 (normalize guard phrasing — error bytes pinned, churn)
Diff stat: gems/tamoz-scheduler/lib/tamoz/scheduler/occurrence.rb | 26 ++— 1 file changed, 13 insertions(+), 13 deletions(-)
Commands:
- `ruby -c occurrence.rb` → Syntax OK
- `rubocop --cache false occurrence.rb` BEFORE → 0 offenses; AFTER → 2 Layout/EmptyLineAfterGuardClause introduced by my lines, fixed (blank line after remaining unless-guards), re-run → 0 offenses
- `ruby -Itest test/scheduler_values_test.rb` → 17 runs, 112 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/sqlite_schedule_store_test.rb` (cross-gem consumer: Occurrence.new/.identity/.claimed) → 21 runs, 73 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): none

## Candidate notes (written before any edit)

- C1 — derive `to_h` from Data's own `to_h`. Rejected: explicit member enumeration is the house
  idiom for these persisted string-keyed shapes (`schedule_store.rb`: `payload = schedule.to_h`);
  keeps which members are stringified visible.
- C2 — APPLIED. Two verbatim-duplicate guards (`nominal_fire_at_utc`, `created_at`): same predicate,
  same message template. Helper mirrors the gem's existing seam `schedule.rb#validate_time!(value, name)`
  ("#{name} must be a positive UTC epoch second"). Interpolation yields byte-identical messages;
  evaluation order unchanged (state → nominal → not_before → created_at → updated_at).
- C3 — APPLIED. Single caller proven by repo-wide grep including send/__send__/public_send; every
  other `transition(` hit belongs to unrelated classes. Message `"occurrence #{occurrence_id} is #{state}"`
  preserved; guard still fires before any state build.
- C4/C5/C6 — see Rejected line above.
