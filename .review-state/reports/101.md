# Combined pass — rank 101 gems/tamoz-agent-improvement/lib/tamoz/agent/improvement/candidate_lifecycle.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (validate_identity! — extracted `proposal_reader(key)` so the key→proposal-reader mapping leaves the comparison block); C2 (profile_activation/profile_rollback — positional→kwargs per §4, dropped unused `_approval` param)
Rejected: C3 (extract digest/evidence guards in approve! into verify_* helpers — guards already read plainly; extra indirection violates simple-over-complicated); C4 (apply! has no require_phase! unlike sibling stages — state transitions are PINNED by binding; any gate-order change alters lifecycle/approval semantics); C5 (extract `phase_for_operation` from approval_request's inline ternary — one local ternary is clear; a method for one branch is indirection); C6 (dedupe the field list shared by approval_digest and approval_request — touches the approval-digest wire bytes, untouchable); C7 (drop `_approval` alone — subsumed by C2); C8 (split run_effect's outcome check out — 15 lines, within ceiling, reads as one coherent step-down sequence)

## Audit (Phase 1, written before any edit)
- Lengths: longest methods are run_effect (15), approval_request (14), approve! (12) — all ≤20 normal ceiling. No length violations.
- Vocabulary §3/§3.1: validate_*/resolve_/ensure_* used consistently and correctly (validate! shape+policy, ensure_candidate_unchanged! tamper check, resolve_candidate turns reference into artifact); predicates end ? (succeeded?, unknown?); no get_/set_; no boolean params; kwargs throughout.
- Public DSL: propose · approval_digest · validate! · approval_request · approve! · apply! · restart_health_verify! · activate! · rollback! reads as the lifecycle in order. Untouched.
- Step-down: public stages delegate to private stage_effect/run_effect/stage_result_phase; ordering helper-after-caller respected.
- Findings: (a) validate_identity! line 151–153 embeds the key→reader ternary inside the all? comparison — two abstraction levels in one expression; (b) profile_activation takes 3 positionals with two adjacent same-kind registries (§4: kwargs beyond one or two positionals), profile_rollback leads with an unused `_approval`.
- Isolation proof: repo-wide grep (incl. send/__send__/public_send patterns) shows zero external callers of profile_activation/profile_rollback/validate_identity! outside this file (.worktrees copies and unrelated same-named methods in other classes excluded).
- Comments §11: class header explains the why (immutable until human binds digest) — appropriate; rubocop:disable annotation necessary; no narration comments. None added/removed.

## Diff stat
```
 .../tamoz/agent/improvement/candidate_lifecycle.rb | 20 +++++++++++++-------
 1 file changed, 13 insertions(+), 7 deletions(-)
```

## Commands
- `ruby -c <file>` → Syntax OK (before and after)
- `rubocop --cache false <file>` → BEFORE: 0 offenses; AFTER: 0 offenses (no increase)
- `ruby -Itest test/agent_improvement_lifecycle_test.rb` → 6 runs, 13 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips

## Deviations
none

## FLAG (owner decisions)
none
