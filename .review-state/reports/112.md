# Combined pass — rank 112 gems/tamoz-observability/lib/tamoz/observability/signal_catalog.rb
Verdict: CLEAN
Applied: none | Rejected: C1 (rename `validate_name` to a `normalize_*` name — §3.1 assigns `validate_*` exactly to "value matches its declared shape"; refusal on bad input is the contract and the incidental `to_s` coercion does not move it off the house lexicon); C2 (harmonize `!` on private validators — `validate_stability`/`validate_name` lack it, others have it — §3 reserves `!` for safe-twin mutators, these are refusers, so the suffix is convention; churn across ~10 names, zero intent gain); C3 (extract shared unknown-correlation-keys check from `validate_correlation_keys!` + `validate_signal_correlation!` — a 3-line duplication whose two contexts differ legitimately: registration-time normalizes/freezes/returns, emission-time only guards; a new helper is indirection for ~2 net lines); C4 (split `validate_signal_kind_and_version!` — 6 lines, one concern: emitted-signal vs entry-shape identity); C5 (guard a missing declaration key in `validate_attribute_values!` — unreachable by construction because `validate_unknown_attributes!` runs first in `validate_signal_attributes!`; writing it violates "do not cover rare cases"); C6 (reindent the `Entry.new(...)` continuation in `register` — style churn, rubocop already clean)
Diff stat: (no changes — owned file identical to HEAD; `git diff --stat` empty)
Commands:
- `ruby -c gems/tamoz-observability/lib/tamoz/observability/signal_catalog.rb` → Syntax OK
- `rubocop --cache false gems/tamoz-observability/lib/tamoz/observability/signal_catalog.rb` → 0 offenses (BEFORE = 0, Δ = 0)
- `ruby -Itest test/observability_catalog_test.rb` → 10 runs, 26 assertions, 0 failures, 0 errors, 0 skips
Deviations: none. EXEMPT deliberately not taken: this file is the catalog's validation engine, not declarative catalog data, so it was audited as logic. Signal-name/prefix/type constants (`PERMITTED_PREFIXES`, patterns, `ATTRIBUTE_TYPES`) left untouched — pinned dashboard contracts. The metrics.rb symbol-vs-string label-normalization FLAG was not acted on here per binding.
FLAG (owner decisions): none
