# Combined pass — rank 165 gems/tamoz-agent-memory/lib/tamoz/agent/memory/situation_recaller.rb
Verdict: COMMITTED-CANDIDATES
Applied: C3 (bind `identity` local in episode_provenance — removes double `ref["identity"].to_s` lookup+coercion per ref); C9 (name the over-fetch tuning pair in recall — `SEARCH_OVERFETCH_FACTOR = 8`, `SEARCH_RESULT_CAP = 512` — matching the class's own MAX_RECORDS/MAX_BYTES precedent and wisdom.rb's named-limit style)
Rejected: C1 (build_result ~31 body lines > soft 20: it is one cohesive fold threading projections/used_bytes/truncated; extracting the loop means passing mutable accumulator state through parameters — indirection for a number, which §4 explicitly forbids; MethodLength is already recorded debt for this file in .rubocop_todo.yml); C2 (local `projection = projection(record)` shadows the private method name: idiomatic Ruby, rename is churn); C4 (eligible_observed_episode? six-term chain: it reads as a flat eligibility checklist; grouping into sub-predicates adds a layer without adding meaning); C5 (rename validate_caller!/validate_snapshot! to verify_/enforce_ per §3.1: each is a single-step policy gate raising MemoryPolicyError before recall proceeds — the standard itself says keep one spelling when checking and refusing are one step, and renaming one half breaks the pair's symmetry); C6 (positive_limit → enforce_positive_limit: the name already states the validated value it returns, message bytes are pinned, rename is churn); C7 (situation_caller: noun phrase already says what it returns — allowed by §3); C8 (consolidate projection's first-`outcome:` find with verified_provenance's any-complete-outcome check into one helper: the two intentionally differ (`find` first vs `any?` complete) and recall semantics are pinned; equivalence rests only on admission building ≤1 outcome ref per record, so I cannot personally prove behavior preservation)
Diff stat: `1 file changed, 5 insertions(+), 3 deletions(-)` (net +2)
Commands: `ruby -c <file>` → Syntax OK · `rubocop --cache false <file>` → 0 offenses (BEFORE 0, no increase) · `ruby -Itest test/stream_learning_loop_test.rb` → 17 runs, 84 assertions, 0 failures, 0 errors, 0 skips · `ruby -Itest test/stream_situation_recall_contract_test.rb` → 2 runs, 4 assertions, 0 failures, 0 errors, 0 skips
Deviations: none
FLAG (owner decisions): observation, no action taken — `projection` picks the FIRST `outcome:` source_ref while `verified_provenance` accepts ANY complete one; they coincide only because admission (`episode_source_refs`) appends exactly one VerifiedOutcomeReference. If lifecycle edits ever introduce a second `outcome:` ref, `projection`'s `outcome.fetch("decision_id")` would raise an unrescued KeyError. Semantics are pinned, so left untouched.

## Phase 1 audit notes (recorded before any edit)

- BEFORE gates: `ruby -c` OK; `rubocop --cache false` = 0 offenses. File carries recorded todo excludes (Layout/EmptyLineAfterGuardClause, Layout/SpaceInsideHashLiteralBraces, Metrics/AbcSize, Metrics/MethodLength, Performance/CollectionLiteralInLoop, Style/StringLiterals x2).
- Public DSL (`recall(caller:, snapshot:, query:, limit:)`) reads clean; validation → search → projection step-down holds. No get_/set_, predicates end in ?, no boolean params, kwargs throughout.
- Real-recaller coverage: test/stream_learning_loop_test.rb T5.4 (real SQLite adapter, exercises recall incl. search-limit arithmetic and the episode:/outcome: provenance path). Downstream Result contract: test/stream_situation_recall_contract_test.rb.
