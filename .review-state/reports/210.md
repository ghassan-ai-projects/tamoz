# Combined pass — rank 210 gems/tamoz-sqlite/lib/tamoz/sqlite/approval_receipt_store.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (collapse `replace`'s duplicated versioned-CAS write onto `put`, explicit params kept); C5 (name `claim_delivery`'s bare retry count `3` as frozen `CLAIM_ATTEMPTS`)
Rejected: C2 (`fetch` guard shape — current form is already the clearest statement of fail-closed expiry); C3 (`transition` repeated `state.to_s` → local var — cosmetic churn, pure calls, no divergence risk); C4 (`reserve_requested` ~33 lines over ceiling — recorded cohesion exception: durable transaction core, extraction forbidden, method-level only); C6 (rename private `value` helper — cosmetic, reads fine in context); C7 (`text!` duplicated across three sibling stores — consolidation is a multi-file refactor outside ownership)
Diff stat: 3 insertions(+), 3 deletions(-) — see below
Commands: ruby -c OK · rubocop --cache false 0→0 · sqlite_approval_stores_test 10/45 pass · stream_learning_loop_test 17/84 pass · sqlite_boundary_source_audit_test 7/55 pass
Deviations: none
FLAG (owner decisions): none

---

## Phase 1 — ANALYZE (written before any edit)

File: 166 lines. Public surface (used cross-gem by tamoz-stream's live_learning_handlers and bound via
adapter.rb#bind_approval_receipt_store): fetch, reserve_requested, record_delivery, claim_delivery,
release_delivery, transition. Private: fetch!, expired?, now, value, put, replace, key, text!.

BEFORE state: `ruby -c` OK · rubocop --cache false = **0 offenses**.

Boundary-audit context (read-only inspection of boundary_source_audit.rb + its test): the audited
SOURCE_PATHS are checkpoint_*/request_inbox_*/lease_operations — approval_receipt_store.rb is NOT parsed
by the audit; its writes go through adapter store get/put/head_version, not tx.execute. Extra gate still run.

Candidates:

- **C1** — `replace(approval_id, value)` re-states, line for line, the compare-and-set write that `put`
  already owns (`head_version` read + `if_version:` guarded put). The CAS invariant exists twice; a future
  edit to one copy can silently miss the other. Collapse to a normal (non-endless) forwarder with an
  explicit parameter list: `put(key(approval_id), value)`. Proof of behavior preservation: identical
  receiver calls in identical order (`key(id)` → `@store.head_version` → `@store.put`), same arguments;
  private method, all callers in-file (record_delivery, claim_delivery, release_delivery, transition),
  repo-wide grep incl. send/__send__/public_send shows no external or dynamic reach into privates.
  Rail-compliant: explicit params, not an endless def, file outside audited paths.
- **C2** — REJECT. `fetch`'s `return nil if current && expired?(current); current` is already minimal.
- **C3** — REJECT. Hoisting `state.to_s` into a local in `transition` is churn with no gain.
- **C4** — REJECT. Length ceiling breach is covered by the recorded durable-transaction-core exception;
  the rail forbids extraction.
- **C5** — `claim_delivery`'s bounded retry is a bare `3.times`. Name it `CLAIM_ATTEMPTS = 3` alongside the
  existing declarative constants (NAMESPACE_PREFIX, STATES). Provable: loop count unchanged.
- **C6** — REJECT. Renaming private `value` gains nothing.
- **C7** — REJECT. `text!` idiom repeats in durable_subscriber_store.rb / verification_store.rb but with
  per-store error classes/shapes; deduplicating means editing files I do not own.

## Phase 2 — APPLY

Applied C1 + C5. Final diff (3 insertions, 3 deletions — net LOC delta 0, well under the 60-line stop):

```diff
@@ class ApprovalReceiptStore
       NAMESPACE_PREFIX = "tamoz.stream.approvals"
       STATES = %w[requested withdrawn resolved].freeze
+      CLAIM_ATTEMPTS = 3

@@ def claim_delivery
-        3.times do
+        CLAIM_ATTEMPTS.times do

@@ def replace(approval_id, value)
-        storage_key = key(approval_id)
-        @store.put(@namespace, storage_key, value, if_version: @store.head_version(@namespace, storage_key))
+        put(key(approval_id), value)
```

C1 rail compliance: `replace` keeps an explicit `(approval_id, value)` parameter list and a normal
(non-endless) body; it is not an endless forwarder and approval_receipt_store.rb is not among the audit's
SOURCE_PATHS — the extra gate still passes.

## Gates (all run on branch verification-again)

1. `ruby -c gems/tamoz-sqlite/lib/tamoz/sqlite/approval_receipt_store.rb` → Syntax OK
2. `rubocop --cache false <file>` → BEFORE 0 offenses / AFTER 0 offenses (no increase)
3. Focused tests, one file per command:
   - `ruby -Itest test/sqlite_approval_stores_test.rb` → 10 runs, 45 assertions, 0 failures, 0 errors, 0 skips
   - `ruby -Itest test/stream_learning_loop_test.rb` → 17 runs, 84 assertions, 0 failures, 0 errors, 0 skips
     (exercises the full receipt lifecycle through tamoz-stream's live_learning_handlers)
4. Extra TAMOZ-SQLITE gate: `ruby -Itest test/sqlite_boundary_source_audit_test.rb`
   → 7 runs, 55 assertions, 0 failures, 0 errors, 0 skips

Diff stat:
```
 gems/tamoz-sqlite/lib/tamoz/sqlite/approval_receipt_store.rb | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)
```

