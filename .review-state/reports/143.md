# Combined pass — rank 143 gems/tamoz-agent-session/lib/tamoz/agent/session_evidence.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (step-down layout)
Rejected: C2, C3, C4, C5, C6, C7 (reasons below)

## Phase 1 — candidates (written before any edit)

- **C1 — step-down layout (APPLY)**: the public entry points are interrupted by a
  `private` section (lines 75–103) holding `failure_record` + `failure_observation`,
  then a bare `public` resumes the DSL for nine more methods. Move both helpers below
  all public methods and drop the mid-class `private`/`public` sandwich. Pure code
  motion — Ruby method resolution is definition-order independent; both helpers are
  called only from this file (repo-wide grep incl. send forms: only lines 57, 61, 77,
  91, 99). Audit points (4) and (5).
- **C2 — merge `current_pass_tool_failure`/`current_pass_check` onto a shared keyed
  finder (REJECT)**: the twins are asymmetric — `current_pass_tool_failure` guards
  `phase` ∈ `action`/`repair`, `current_pass_check` deliberately does not (documented
  restriction). A shared helper hides that semantic fork to save ~6 lines and adds
  indirection on pinned evidence-selection logic. Fails the risk/benefit bar.
- **C3 — rebuild `observation_payload` via `record.slice(...)` (REJECT)**: the mix of
  `fetch` (keys that must exist) and `[]` (tolerant keys) is deliberate; slice would
  silently nil required keys, and hash insertion order feeds evidence bytes — pinned,
  untouchable.
- **C4 — hoist `accepted.fetch('plan_id')` out of the `find_intent` block (REJECT)**:
  micro-churn with no readability gain.
- **C5 — rename local `failure_record = failure_record(failure)` (REJECT)**: reads
  unambiguously; rename is churn.
- **C6 — restyle `bounded_repair` guard clauses (REJECT)**: rubocop is clean (0
  offenses BEFORE); churn on bounded-repair control flow.
- **C7 — make `tool_failure_signature` private (sole caller is in-file) (REJECT)**:
  shrinks the public/cross-gem API surface, which the brief pins as untouchable.

Naming audit: §3 vocabulary already holds (`find_`, `*_payload/_bytes/_update/_output/
_signature`, predicate `repairable_outcome?`; no `get_/set_`). No boolean params; kwargs
where optional (`blocked_update(step_id:, operation:)`). No comment issues (§11) beyond
the existing reek suppressions header, which stays.

## Gates

(To be filled after application.)

## Gates

1. `ruby -c gems/tamoz-agent-session/lib/tamoz/agent/session_evidence.rb` → Syntax OK.
2. `rubocop --cache false <file>` → BEFORE: no offenses (0); AFTER: no offenses (0). No suppressions added.
3. Focused tests, one file per command:
   - `ruby -Itest test/agent_tool_error_recovery_test.rb` → 13 runs, 116 assertions, 0 failures, 0 errors, 0 skips
   - `ruby -Itest test/agent_repair_evaluation_test.rb` → 8 runs, 55 assertions, 0 failures, 0 errors, 0 skips
   - `ruby -Itest test/agent_decision_flow_test.rb` → 5 runs, 23 assertions, 0 failures, 0 errors, 0 skips

## Diff stat

```
 gems/tamoz-agent-session/lib/tamoz/agent/session_evidence.rb | 58 +++++++++++-----------
 1 file changed, 28 insertions(+), 30 deletions(-)
```

Net LOC delta −2 (dropped the mid-class `public` keyword; everything else is verbatim code motion).
