# Fix report — rank 48 gems/tamoz-scheduler/lib/tamoz/scheduler/schedule.rb

Applied: C1 (`validate!` stepped down into ordered phase validators over a shared fields hash — validate_identity!/kind!/times!/policies!/artifacts!/lifecycle!, with `validate_expression!` hoisted to its own call in the exact slot it occupied inside `validate_times!`; raise order audited check-by-check against the old flat sequence, all 23 checks fire byte-identical messages in byte-identical order; profile-default normalization kept as an exact nil-check, not `||=`); C2 (`misfire_selection`: merged the three byte-identical arms into `when :skip, :latest, :fire_once`, re-verified identical myself against lines 135/140/146 pre-edit; one two-line why-comment; `:replay` untouched); C3 (extracted private `interval_anchor` = `start_at || created_at`, used in `next_interval_fire` and the `due_occurrences` dispatch; renamed `due_interval_occurrences` kwarg `start:` → `anchor:` — private method, zero external callers found repo-wide); C4 (`validate_budgets!` opens with `validate_hash!(value, "budgets")` — message bytes identical; trailing duplicate `deep_freeze` dropped) | Rejected: none of C1–C4; no candidate failed the review bar. FLAG (skip/latest/fire_once produce indistinguishable selections despite distinct documented policies) is an owner-only design question — semantics untouched, reported for the owner.

Diff stat: gems/tamoz-scheduler/lib/tamoz/scheduler/schedule.rb | 133 +++++++++++---------- 1 file changed, 67 insertions(+), 66 deletions(-)

Commands:
- ruby -c gems/tamoz-scheduler/lib/tamoz/scheduler/schedule.rb — Syntax OK (before AND after edit)
- rubocop --cache false gems/tamoz-scheduler/lib/tamoz/scheduler/schedule.rb — BEFORE: 0 offenses; AFTER: 0 offenses (no increase; nothing introduced, nothing suppressed). Note: this file carries none of the ~1000-repo legacy debt — clean baseline.
- ruby -Itest test/scheduler_values_test.rb — 17 runs, 112 assertions, 0 failures, 0 errors, 0 skips (green pre-change too)
- ruby -Itest test/scheduler_due_occurrences_test.rb — 9 runs, 24 assertions, 0 failures, 0 errors, 0 skips (green pre-change too)
- ruby -Itest test/scheduler_contract_test.rb — 3 runs, 23 assertions, 0 failures, 0 errors, 0 skips

Deviations:
1. C1 letter-vs-spirit: phases TAKE the fields hash but do not RETURN-and-chain it — nothing consumes the return values (same as today's discarded leaf returns); chaining would add noise without effect. Additionally `validate!(**fields)` replaces the explicit 22-kwarg signature (private method, sole caller `initialize` unchanged), which removes two of the four spellings of the field list the candidate flagged — more complexity removed than the proposal asked for.
2. C4 ordering nuance: `deep_freeze` now happens at shape-validation time (inside `validate_hash!`) instead of after the per-key loop. Success-path end state identical; only theoretical divergence is frozenness of an invalid budgets hash after a failed construction — unobserved by any test, and consistent with how capability_grant/delivery_policy already freeze.
3. Held-back items stayed held: `validate_times!` name kept (scope corrected by hoisting the expression check out, making the existing name accurate), `at_instant` untouched (test/scheduler_values_test.rb:80 calls it), no public-query changes.
4. Shared working tree: other modified files belong to sibling fixer agents; this agent's footprint is exactly the owned file (+67/−66, net +1 LOC).
