# Combined pass — rank 147 gems/tamoz-sqlite/lib/tamoz/sqlite/checkpoint_wire.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (private `checkpoint(row, attributes, pending)` → `build_checkpoint` — §3.1 construction vocabulary; bare noun did not say it returns a new Checkpoint)
Rejected: C2 (fold `request_status` into `persisted_enum_symbol` — error bytes differ: 'request status is invalid' vs 'stored request status is invalid', error identity untouchable); C3 (extract the response/terminal_error digest guard pairs from `materialize_request` — wire-codec cohesion exception: method-level only, no extraction); C4 (`validate_identity:` boolean kwarg on materialize/verified_attributes violates §4 no-boolean-params — public/cross-gem signature untouchable, split = multi-file API change); C5 (route `decode_checkpoint_row` through `materialize` to dedupe — decode lacks the active-checkpoint ArgumentError precondition, consolidation changes error semantics); C6 (rename public `enum_text` → `validate_enum_text` per §3.1 checking verbs — external callers at request_inbox_enqueuer.rb:131/136, public API untouchable); C7 (named constants for row column indices — churn on the pinned declarative mapping whose index list IS the wire contract); C8 (shrink `materialize_request` under the Q6 ceiling — recorded cohesion exception stands); C9 (comment audit — all are genuine §11 why-comments: byte-comparison encoding rationale, tri-state nil contract, reek suppression reasons; none restate code)
Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/checkpoint_wire.rb | 6 +++--- (3 insertions, 3 deletions; net LOC delta 0 — pure rename)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> BEFORE = 0 offenses / AFTER = 0 offenses (no increase) · ruby -Itest test/sqlite_boundary_source_audit_test.rb → 7 runs, 55 assertions, 0 failures, 0 errors (TAMOZ-SQLITE rail gate) · ruby -Itest test/sqlite_checkpoint_test.rb → 6 runs, 23 assertions, 0 failures, 0 errors · ruby -Itest test/sqlite_request_inbox_test.rb → 8 runs, 46 assertions, 0 failures, 0 errors · ruby -Itest test/agent_non_ascii_session_test.rb → 7 runs, 39 assertions, 0 failures, 0 errors (the file that pins this file's `.b ==` count at 2; count re-verified = 2 post-edit)
Deviations: none
FLAG (owner decisions): none

## Phase 1 — ANALYZE candidates (recorded before any edit)

BEFORE gates: ruby -c OK · rubocop --cache false = 0 offenses.
Scope pins found before editing:
- BYTE PIN: test/agent_non_ascii_session_test.rb:262 counts `.b ==` sites per file and
  pins this file at exactly 2 (lines ~50 and ~87, the canonicality comparisons). Both
  comparisons left byte-for-byte identical.
- COHESION EXCEPTION: wire codec — method-level improvements only, NO extraction
  (brief rail; fc3878a history).
- TAMOZ-SQLITE RAIL: explicit parameter lists kept on all methods; no endless-method
  conversion; boundary_source_audit gate run after the edit.
- PUBLIC SURFACE untouchable: enum_text / persisted_enum_symbol / request_status /
  canonical_state_value / materialize_request / decode_checkpoint_row / materialize /
  merge_pending + REQUEST_STATUSES / REQUEST_OPERATIONS / DELIVERY_MODES constants
  (REQUEST_OPERATIONS + DELIVERY_MODES read externally by request_inbox_enqueuer.rb:133/138).

- C1 [applied] Private `checkpoint(row, attributes, pending)` builds and returns a new
  Tamoz::Graph::Checkpoint but its bare-noun name says neither that nor which verb family
  it belongs to (§3 names say what they return; §3.1 build_* returns a new value).
  Renamed to `build_checkpoint`. Proof of safety: 2 in-file callers only (decode path and
  materialize path); repo-wide grep finds zero `.checkpoint(` receivers, zero
  send/__send__/public_send(:checkpoint…), zero boundary-registry references to the
  method, no reek/rubocop config naming it; `build_checkpoint` already means exactly this
  in tamoz-graph memory_checkpointer.rb:188, so the rename REUSES the existing lexicon
  instead of adding one. Explicit param list preserved (no endless method).
- C2 [rejected] `request_status(value)` duplicates `persisted_enum_symbol`'s shape
  (allow-list check + to_sym), but the failure bytes differ ('request status is invalid'
  vs 'stored request status is invalid') and the String-type guard differs; folding them
  changes error identity. Untouchable.
- C3 [rejected] The response-digest and terminal_error-digest guard pairs in
  `materialize_request` (rows 14/15 and 16/17) are structurally parallel; a shared
  verify_optional_blob helper would dedupe them. Forbidden: wire codecs get method-level
  work only, no extraction — and the second pair sits directly against the pinned
  canonicality comparison.
- C4 [rejected] `materialize(row, durable_pending:, validate_identity:)` carries a boolean
  kwarg (§4). Splitting into verified/unverified twins would change the public signature;
  callers outside this file set it. Public/cross-gem API untouchable.
- C5 [rejected] `decode_checkpoint_row` = verified_attributes + build_checkpoint, which
  `materialize` also does; routing one through the other fails because materialize raises
  ArgumentError for an active checkpoint without durable_pending while decode must not.
- C6 [rejected] `enum_text` validates bad input (ConfigurationError) so §3.1 suggests a
  validate_ verb — but it is a public entry point with external callers; renames of public
  API are out of scope for this pass.
- C7 [rejected] Raw column indices (row.fetch(0..20)) could become named constants. The
  declarative index mapping is the pinned wire contract and reads as such (§6 allows long
  declarative mappings); renaming indices everywhere is churn with no behavior gain.
- C8 [rejected] Bringing `materialize_request` under the ≤20-line ceiling requires
  extraction. Recorded cohesion exception stands; the header already documents why.
- C9 [rejected] Comment audit: every comment is load-bearing — the ASCII-8BIT byte-
  comparison rationale (twice), the tri-state retryable flag contract, the declarative-
  mapping defenses, and each :reek: suppression states its reason. Nothing restates code;
  removing or trimming any loses a non-recoverable why. No change.
