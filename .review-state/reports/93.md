# Combined pass — rank 93 gems/tamoz-agent-kernel/lib/tamoz/agent/reasoning_document.rb

Verdict: COMMITTED-CANDIDATES

Applied: C1 (fetch_bounded_string: `_missing` guard now precedes the `value = hash[key]`
read — presence proven before value is touched; all four error bytes unchanged);
C2 (reject_bad_utf8: encoding conditional collapsed to one
`dup.force_encoding(UTF_8).valid_encoding?` predicate — probe proved old/new agree on all 7
cases incl. valid/invalid × UTF-8/binary taggings × frozen inputs, caller string never
mutated).

Rejected: R1 (extract evidence-ref validation helper — single call site, adds a name without
removing an abstraction level); R2 (hoist duplicate-code detection out of
parse_probability_entry — moves rejection ORDER on docs with both duplicate and missing codes,
ordering pinned); R3 (Document 8-field shape with nil'd branch fields — shape pinned per
binding); R4 (select_argmax rebuilds order hash per call — perf micro-opt out of scope,
documented tie-break stays); R5 (inline `not_string` check in parse → helper — indirection,
not step-down); R6 (TERMINAL_KEYS loop in parse_tool_turn → named helper — churn on 3 lines).

Diff stat: 1 file changed, 3 insertions(+), 7 deletions(-)

Commands:
- ruby -c <file> → Syntax OK (before and after)
- rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase)
- ruby -Itest test/agent_reasoning_document_test.rb → 25 runs, 75 assertions, 0 failures, 0 errors
- ruby -Itest test/agent_diagnosis_catalog_test.rb → 15 runs, 53 assertions, 0 failures, 0 errors
- equivalence probe (ruby -e, old vs new utf8 predicate) → 7/7 match

Phase 1 candidate list was written into this report before any edit; behavior-equivalence
probe for C2 ran before that edit.

Deviations: none

FLAG (owner decisions): none
