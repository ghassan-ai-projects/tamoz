# Combined pass — rank 124 gems/tamoz-agent-profile/lib/tamoz/agent/profile/document_validator.rb
Verdict: COMMITTED-CANDIDATES

Phase 1 audit (recorded before editing): all methods within Q6 ceilings (longest body ~12
lines); step-down holds — each public entry point reads as a list of named checks stepping
down to per-field refusals; public class-level entry points mirror `Profile.validate_*!`
and are the cross-gem seam, left untouched. Error/message bytes and refusal ordering are
pinned (test/agent_profile_test.rb, test/agent_profile_schema_seams_test.rb).

Candidates:
- C1 APPLY: rename private `normalized_settings!` → `validate_normalized_settings!`
  (§3 validate_ vocabulary; sole caller is `validate_role!` in-file — repo-wide grep
  incl. send/__send__/public_send finds no other caller, no symbolic refs in configs/docs).
- C2 APPLY: rename private `credential_ref!` → `validate_credential_ref!` (same proof as C1).
- C3 APPLY: extract `optional_section!(hash, name)` for the duplicated fetch+mapping-refusal
  in `model_roles!`/`budgets!`; produced messages are byte-identical
  ("…: model_roles must be a mapping", "…: budgets must be a mapping") and check order
  (mapping before unknown-field/budget checks) is preserved.
- C4 REJECT: split the compound spelling predicate in `validate_root_spelling!` into a `?`
  predicate — style churn between two shapes the file already uses deliberately; no defect.
- C5 REJECT: collapse the five class-level delegators into metaprogramming — that literal
  delegation IS the public/cross-gem API surface; machinery over simplicity.
- C6 REJECT: dedupe `named = role.inspect` / message-prefix fragments across validators —
  touches pinned message construction for zero intent gain.

Applied: C1 (rename normalized_settings! → validate_normalized_settings!);
         C2 (rename credential_ref! → validate_credential_ref!);
         C3 (extract optional_section! shared by model_roles!/budgets!)
Rejected: C4 (churn, no defect); C5 (public API must stay literal); C6 (pinned bytes, zero gain)
Diff stat: 1 file changed, 15 insertions(+), 11 deletions(-) (net +4)
Commands:
- ruby -c gems/tamoz-agent-profile/lib/tamoz/agent/profile/document_validator.rb → Syntax OK
- rubocop --cache false <file> → 0 offenses (BEFORE 0; unchanged)
- ruby -Itest test/agent_profile_test.rb → 44 runs, 130 assertions, 0 failures, 0 errors
- ruby -Itest test/agent_profile_schema_seams_test.rb → 11 runs, 33 assertions, 0 failures, 0 errors
- ruby -Itest test/agent_profile_machinery_test.rb → 24 runs, 230 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none
