# Combined pass — rank 181 gems/tamoz-agent-kernel/lib/tamoz/agent/model_receipt.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (to_key: replace 3 raw NUL bytes in source with \x00 escapes — output byte-identical, file becomes readable text); C2 (validate_receipt!: collapse duplicated require_digest! branches into one guarded call — truth-table equivalent, error bytes identical)
Rejected: R1 (rename require_present!/require_digest! — vocabulary already §3-compliant, renames buy nothing on a pinned contract surface); R2 (drop redundant .freeze on %i[] STATUSES — style churn, zero value); R3 (add presence validation for graph_task in InvocationIdentity — changes accepted-input behavior of a pinned receipt shape); R4 (merge Usage.initialize's two nil-guards — they express two distinct pinned error codes, merging obfuscates the evidence contract); R5 (reformat long endless-method lines — rubocop BEFORE is 0 offenses, churn only)
Diff stat: 2 hunks — to_key line rewritten (1 del/1 ins); validate_receipt! if/elsif block → guard clause (4 del/3 ins). Net: +4/−5 lines (189 → 187). Git labels the pre-image blob binary because HEAD's version contains the raw NUL bytes this pass removes.
Commands: ruby -c → Syntax OK · rubocop --cache false BEFORE 0 offenses / AFTER 0 offenses (one transient Style/GuardClause from C2's first form was fixed by adopting the guard-clause form, not suppressed) · ruby -Itest test/agent_model_receipt_test.rb → 15 runs, 49 assertions, 0 failures, 0 errors (before AND after) · LogicalCallKey#to_key hex fingerprint identical pre/post edit (/tmp/to_key_before_181.txt == /tmp/to_key_final_181.txt)
Deviations: none
FLAG (owner decisions): The NUL separators inside LogicalCallKey#to_key are a deliberate collision-proof key format and are PRESERVED byte-for-byte (hex-verified before/after). Only the source representation changed (raw 0x00 bytes → "\x00" escapes); raw control bytes in source made `file(1)` classify the file as binary data and broke text tooling on it.

---

## Phase 1 — ANALYZE (written before any edit)

Baseline (BEFORE): `ruby -c` Syntax OK · rubocop --cache false: **0 offenses** · focused test
`test/agent_model_receipt_test.rb`: 15 runs, 49 assertions, 0 failures · to_key hex captured to
/tmp/to_key_before_181.txt.

Audit against the five criteria:

1. **Name states intent** — compliant. resolve_role / find_model_role / resolve_role_endpoint /
   verify_profile_digest / require_present! / require_digest! / validate_receipt! all read as their
   behavior; predicates end in ? (succeeded?, unknown?); no get_/set_. Nothing to rename.
2. **Length** — compliant. Largest bodies: validate_receipt! (~15 stmts), ModelReceipt#initialize
   (~14), resolve_role (~11). All under the ≤20 normal ceiling; no extraction warranted.
3. **One level of abstraction** — compliant. resolve_role orchestrates named helpers
   (verify_profile_digest → find_model_role → resolve_role_endpoint) then constructs ModelRole;
   validate_receipt! is a flat guard list.
4. **Public DSL** — compliant. Data-defined keyword constructors + two module functions read as a
   small closed surface.
5. **Step-down** — compliant already; private helpers sit one level below their callers.

### Candidates

- C1 · hygiene/readability · line 35 `LogicalCallKey#to_key` embeds three RAW U+0000 bytes between
  interpolations. Runtime value is a pinned journal dedup key (`effect_id: logical.to_key`,
  witness_verifier.rb:68, episode_model_call.rb:149) — must stay byte-identical. Replacing the raw
  source bytes with `\x00` escapes yields an identical runtime String by Ruby lexing rules.
  Confidence: provable + hex before/after check. Risk: none to consumers (output unchanged).
- C2 · duplication · lines 122–126: identical `ModelCall.require_digest!("receipt.response_digest",
  response_digest)` call in both if/elsif arms. Collapsing to `if status == :succeeded ||
  response_digest` is truth-table equivalent (short-circuit order preserved), error message bytes
  identical ("receipt/response_digest/bad_digest: #{value.inspect}"), and removes a duplicated
  field-name literal (drift risk).
- R1..R5 — see rejections above; each fails "high-confidence improvement" or touches pinned shape.

Extra rails honored: receipt shapes, digest inputs, provider-attributed fields untouched;
method-level only; no extraction into new machinery.

## Phase 2 — APPLY

C1 applied via byte-level replacement (raw 0x00 → literal `\x00` escapes inside the double-quoted
string; Ruby lexes both to the identical one-byte content). Verified: `file(1)` now reports "Ruby
script text, UTF-8 text"; `to_key` hex fingerprint byte-identical.

C2 applied as a guard clause (`return unless status == :succeeded || response_digest`) after the
plain `if` form drew one new Style/GuardClause offense; guard form matches the method's existing
raise-guard idiom, keeps error bytes identical ("receipt/response_digest/bad_digest: …"), and
restores rubocop to 0 offenses. Truth table checked exhaustively: succeeded+nil/bad raises,
succeeded+valid passes, non-succeeded+truthy validates, non-succeeded+blank skips — same as before.

No other edits. Owned file only; tests and everything else untouched.
