# Combined pass — rank 87 gems/tamoz-core/lib/tamoz/error.rb

Verdict: CLEAN

Applied: none
Rejected: C1 (unify `InterruptInNonInteractiveEpisodeError#initialize`'s `super(message || SAFE_MESSAGE)` with the inherited `Error::Metadata` fallback, as `NodeError#initialize` already does — provably byte-identical but zero value on a pinned constructor; the explicit fallback keeps the leaf self-evident without reading `Metadata`, and unifying would leave the two siblings inconsistent in the other direction); C2 (extract the repeated `&.to_s&.dup&.freeze` idiom ×4 in `NodeError#initialize` into a private coercion helper — four shallow identical lines are clearer inline; indirection exceeds the idiom's value); C3 (decompose `disclosable_message` into stage helpers — the 9-line chain IS the byte-exact disclosure contract consumed by tamoz-mcp elicitation/invocation and tamoz-agent-kernel effect_dispatcher wire payloads; every stage boundary risks divergence where it matters most: byteslice splitting a multibyte char → `.scrub("")`, strip/rstrip ordering); C4 (deduplicate restated CATEGORY/RETRYABLE/USER_VISIBLE/SAFE_MESSAGE constants among the StandardError-rooted config/state classes via a shared intermediate — would change ancestry shape that cross-gem `is_a?` checks and `public_api_test.rb` pin).

Audit notes (all five dimensions pass as-is):
- Names state intent: markers (`FatalRuntimeFailure`, `DisclosableMessage`), predicates end `?` (`retryable?`, `user_visible?`), no get_/set_, constants self-describing.
- Lengths within Q6 ceilings: longest is `NodeError#initialize` (16 lines incl. signature), then `disclosable_message` (9). One level of abstraction throughout.
- Public surface reads as a small declarative DSL: leaf classes declare CATEGORY/RETRYABLE/USER_VISIBLE/SAFE_MESSAGE; one class-level entry point `Tamoz::Error.disclosable_message(value, fallback:)`.
- Step-down holds: markers → base Error (+ private Metadata) → retryable leaves → Node family → Checkpoint family → Store family → pool/stream/circuit → StandardError-rooted config/state family → special leaf.
- Comments are sanctioned §11 "why" material only (default-deny disclosure model, DR-2 terminality, DR-4 stale-request semantics, T0.4 episode mapping) — kept.
- Contract confirmed live by repo grep: `FatalRuntimeFailure` rescued in tamoz-concurrency pool + tamoz-sqlite lease, checked in task_result; `DisclosableMessage` opted into across tamoz-mcp/errors, tamoz-agent-kernel/errors, core tool_error; names pinned in test/public_api_test.rb.

Diff stat: (no change — working tree clean for owned file)

Commands:
- `ruby -c gems/tamoz-core/lib/tamoz/error.rb` → Syntax OK
- `rubocop --cache false gems/tamoz-core/lib/tamoz/error.rb` → 0 offenses (BEFORE = AFTER = 0)
- `ruby -Itest test/agent_tool_error_recovery_test.rb` → 13 runs, 116 assertions, 0 failures, 0 errors, 0 skips
- `ruby -Itest test/public_api_test.rb` → 3 runs, 1015 assertions, 0 failures, 0 errors, 0 skips

Deviations: prompt binding said OWNED FILE=gems/tamoz-core/lib/tamoz/core/error.rb, which does not exist and never existed in git history; .review-state/queue.tsv and state.tsv authoritatively map rank 87 → gems/tamoz-core/lib/tamoz/error.rb (292 lines, matching the file audited), assigned to no other rank. Audited that file. No other path touched.

FLAG (owner decisions): none
