# Combined pass — rank 67 gems/tamoz-stream/lib/tamoz/stream/approval_relay.rb
Verdict: COMMITTED-CANDIDATES
Applied: C2 (extract duplicated "situation-runtime/approval-submission/v1\n" literal into SUBMISSION_DOMAIN beside ASSERTION_DOMAIN — same bytes, naming-level dedup); C1 (rename block param `field` → `key` in require_prompt_fields! — it shadowed the private method `field(approval, key)` used by render_prompt)
Rejected: C3 (drop provably-dead `value.nil? ||` in require_field! — nil.to_s is "" so it is redundant, BUT it edits predicate logic inside a validation primitive; this file's mandate is method-level naming/step-down only and the gain is zero); C4 (fold local `empty` var into the condition — NOT provably behavior-preserving: `[].empty?` is true while `[].to_s` is "[]" non-empty, so Array/Hash payload values would flip from raise to coerce); C5 (rename private `field` → `bounded_field` — it pairs deliberately with the raising variant `require_field!`; churn without clarity gain); C6 (kwargs-ify private build_assertion(approval, approver_id, decision, nonce) per §4 — private seam reads fine, signature churn near authority code is risk without gain); C7 (mixed fetch styles in escalation_payload — intentional required/defaulted/optional triad closed by .compact; behavior-sensitive)
Diff stat: gems/tamoz-stream/lib/tamoz/stream/approval_relay.rb | 9 +++++---- — 5 insertions(+), 4 deletions(-), net +1 (well under the ~60-line stop)
Commands: ruby -c <file> → Syntax OK · rubocop --cache false <file> → BEFORE 0 offenses / AFTER 0 offenses (no increase) · ruby -Itest test/stream_approval_relay_test.rb → 17 runs, 94 assertions, 0 failures, 0 errors (identical to pre-edit baseline) · ruby -Itest test/stream_invariants_test.rb → 9 runs, 212 assertions, 0 failures, 0 errors (identical to pre-edit baseline). Both test files construct/exercise ApprovalRelay directly; no other test touches the file beyond the class-name reference in test/public_api_test.rb.
Deviations: binding path said lib/tamox/stream/… (and the "corrected" path repeated it) — actual file is gems/tamoz-stream/lib/tamoz/stream/approval_relay.rb (tamoz), verified by ls/glob; audited and edited that file.
FLAG (owner decisions): none

## Phase 1 audit (written before any edit)

Baseline: ruby -c OK · rubocop --cache false BEFORE = 0 offenses · test/stream_approval_relay_test.rb green (17 runs, 94 assertions) · test/stream_invariants_test.rb green (9 runs, 212 assertions). Note: stream_*_test is on the known-red lane list, so both were baselined BEFORE the edit for a like-for-like comparison.

1. Name states intent — public DSL deliver/withdraw/submit_decision/escalate reads well; private helpers follow §3 vocabulary (validate_/assert_/require_/build_/render_/normalize_). Findings: C1 (shadowing block param), C2 (duplicated domain literal), rejected renames C5.
2. Short, does one thing — longest methods are initialize (~16 lines), build_assertion (15), idempotency_key (12 incl. its why-comment); all under Q6 ceilings. No extraction candidates.
3. One level of abstraction — submit_decision steps down assert_* → claim_nonce! → build_assertion → submit_assertion; deliver validates then delegates. Clean.
4. Public entry points as small DSL — consistent kwargs, no boolean params (§4 ✓); only attr_reader is relay_id.
5. Step-down — initialize validates ports, assigns, freezes; escalate = normalize → next_candidate → payload. Clean.

Authority semantics untouched: no change to assertion bytes, field order, error messages, nonce/idempotency behavior, or any signature. Both applied candidates are provably byte-identical in behavior.
