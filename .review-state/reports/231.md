# Combined pass — rank 231 gems/tamoz-graph/lib/tamoz/graph/route_planner.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (rename private `declared_routes` → `resolve_routes`, single in-file caller proven by repo-wide grep incl. send/__send__/public_send); C2 (step-down `next_frontier`: routing-mode guards extracted verbatim into `validate_routing_mode!`, `InvalidUpdateError` messages byte-identical, execution order preserved); C3 (dedupe route-target normalization + declared-membership test into `route_target` / predicate `declared_route?`; identity-or-value comparison kept verbatim)
Rejected: C4 (collapse `rescue InvalidUpdateError; raise` — NOT redundant: without it the error is double-wrapped by `rescue StandardError` into "branch X failed: InvalidUpdateError", changing message bytes); C5 (unify `equal?(START)` vs `== node` edge-scan equality — equivalence unproven across node-key types, routing decisions pinned); C6 (extract whole per-outcome body into `plan_outcome` — threads two mutable accumulators for ~8 saved lines, more machinery than payoff); C7 (restyle guard pairs to guard clauses/case — pure churn)
Diff stat: 1 file changed, 28 insertions(+), 14 deletions(-) [42 changed lines, under the ~60 stop line]
Commands:
- BEFORE: `ruby -c` → Syntax OK · `rubocop --cache false <file>` → 0 offenses
- Baseline: `ruby -Itest test/graph_execution_test.rb` → 11 runs, 43 assertions, 0F0E0S · `ruby -Itest test/graph_checkpoint_codec_test.rb` → 4 runs, 22 assertions, 0F0E0S
- AFTER: `ruby -c` → Syntax OK · `rubocop --cache false <file>` → 0 offenses (delta vs BEFORE = 0; one self-introduced Style/GuardClause on the new helper was fixed by equivalent guard restructure, not suppressed)
- AFTER tests: `ruby -Itest test/graph_execution_test.rb` → 11 runs, 43 assertions, 0F0E0S · `ruby -Itest test/graph_checkpoint_codec_test.rb` → 4 runs, 22 assertions, 0F0E0S (both match baseline exactly; execution suite asserts the routing error-message strings touched here)
Deviations: none. Public surface untouched (`initial_frontier`/`next_frontier` signatures intact incl. the `__send__(:route_planner).initial_frontier` test path). File carries recorded Metrics debt in `.rubocop_todo.yml` (untouched, forbidden); Q6 ≤20-line ceiling met by every method in the result. Behavior-preserving only: no wire/digest/ordering/approval surface involved beyond error-message bytes, which are unchanged.
FLAG (owner decisions): docs/P6_DURABLE_SESSION_RECOVERY_PLAN.md:69 names `RoutePlanner#declared_routes` (and pins line numbers) — a historical plan doc outside my ownership; it now trails this rename. No action taken; noted for the doc's owner if that plan is ever resumed.
