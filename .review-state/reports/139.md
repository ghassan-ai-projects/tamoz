# Combined pass — rank 139 gems/tamoz-stream/lib/tamoz/stream/live_learning_handlers.rb

Verdict: COMMITTED-CANDIDATES
Applied: C2 (hoist repeated `receipt.fetch("delivery_receipt")` in `withdraw_approval`
into a local; same reads, same check order, identical error bytes; drops the
re-traversal and the intermediate `message_id` variable) | Rejected: C1 (rename
`validate_and_call`: no lexicon verb covers validate-then-dispatch, alternatives hide
validation or duplicate it ×5); C3 (guard order in `resolve_approval`: normalizing
changes which StreamError fires when both faults hold — observable error bytes);
C4 (derive `intent_id` inside admit/log helpers instead of threading it: signatures
already ≤5 params, re-deriving adds redundant fetches — churn, no defect); C5
(`require_approval_ports!` bang suffix: house convention for raising guards —
`require_adapter!`, `require_phase!`, `enforce_parent_capabilities!`); C6
(`handle_admission_result` nest: already guard-style, inversion gains nothing)
Diff stat: `1 file changed, 3 insertions(+), 3 deletions(+)` (net 0 LOC)
Commands:
- BEFORE/AFTER `ruby -c gems/tamoz-stream/lib/tamoz/stream/live_learning_handlers.rb` → Syntax OK
- BEFORE/AFTER `rubocop --cache false <file>` → 0 offenses / 0 offenses (no increase)
- Baseline + post-edit `ruby -Itest test/stream_learning_loop_test.rb` →
  17 runs, 84 assertions, 0 failures, 0 errors, 0 skips (both runs identical;
  only test file referencing LiveLearningHandlers; not in SLOW_TESTS/SERIAL_TESTS)
Deviations: none. Event-type → handler wiring in `callables` untouched (ordering
pinned per binding note); public surface (`new`, `callables`) unchanged; no renames.
FLAG (owner decisions): C3 only — `resolve_approval` checks tenant before approval
ports while its two siblings check ports first. Unifying would reorder which
StreamError surfaces in the both-faults case, so it stays as recorded asymmetry.
