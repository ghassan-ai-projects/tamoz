# Combined pass — rank 72 gems/tamoz-sqlite/lib/tamoz/sqlite/comms_outbox.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (`coalesce_milestone!` returns live-row/nil instead of true/false, clearing
pre-existing Naming/PredicateMethod — sole caller line 44 proven in-file, truthiness
unchanged); C2 (rename misleading existence-probe locals `missing` → `existing` in
`claim_delivery` and `bind_journal_effect`, matching `append_delivery`'s established
name; public symbol `:missing` kept); C3 (fix pre-existing Layout/LineLength at the
`rewrite_milestone!` call by hoisting the bind array to a local — hand-written after
scoped autocorrect produced a worse layout, same binds in same order).
Rejected: C4 (MethodLength on `append_delivery` 26/20 — durable append-transaction core,
recorded cohesion exception forbids extraction, boundary stays one visible unit);
C5 (ParameterLists on `mark_delivery` [6/5] — public cross-gem signature delegated by
CommsStore, untouchable); C6 (normalize `return :conflict` to `next` inside the
bind-transaction block — verified against DatabaseKernel#transaction: a non-local return
skips `inject(:before_commit)` + COMMIT today, `next` would add both to the conflict path,
fault-injection sequence would differ ⇒ not provable byte-preserving); C7
(`milestone_request_ref` `.fetch('markup')` instead of `['markup']` — KeyError where []
yields nil if a cross-gem wire ever lacks the key, presence unprovable here); C8 (split
`milestone_rows`' default-SQL-clause parameter — reshaping inside the protected milestone
seam for a cosmetic win); C9 (`statuses.map { '?' }` placeholder build — no defect).

## Phase 1 candidate list was written before any edit (see history below); Phase 2 applied C1–C3 only.

Diff stat: 1 file changed, 10 insertions(+), 8 deletions(-) (net +2)
Commands:
- BEFORE `ruby -c` → Syntax OK; `rubocop --cache false` → 4 offenses (MethodLength 41,
  ParameterLists 209, PredicateMethod 253, LineLength 287)
- AFTER `ruby -c` → Syntax OK; `rubocop --cache false` → 2 offenses (only the two
  untouchable ones remain; count decreased, nothing suppressed)
- `ruby -Itest test/sqlite_comms_store_test.rb` → 42 runs, 219 assertions, 0 failures, 0 errors
- `ruby -Itest test/progress_projection_test.rb` → 12 runs, 360 assertions, 0 failures, 0 errors
- `ruby -Itest test/delivery_drainer_test.rb` → 10 runs, 50 assertions, 0 failures, 0 errors
Deviations: scoped autocorrect attempt for C3 initially raised offenses 4→6; reverted that
hunk and applied an equivalent manual fix instead. No other deviations.
FLAG (owner decisions): none

---

# Phase 1 — ANALYZE (candidate list, written before any edit)

BEFORE state: `ruby -c` OK; `rubocop --cache false` = 4 offenses
(Metrics/MethodLength 41 [26/20], Metrics/ParameterLists 209 [6/5],
Naming/PredicateMethod 253, Layout/LineLength 287 [182/120], last one autocorrectable).

File audit against rules 1–5: public entry points (`append_delivery`, `claim_delivery`,
`reserve_delivery_slot`, `release_delivery_claim`, `mark_delivery_send_started`,
`reconcile_expired_deliveries`, `defer_delivery`, `bind_journal_effect`, `outbox_rows`,
`mark_delivery`, `resolve_delivery`) read as a small DSL of one-transaction verbs, each
delegated verbatim by `CommsStore` (cross-gem API — untouchable). Vocabulary conforms to
§3/§3.1 (`validate_rate!` is a true shape check; `milestone_bound_reached?` is a real
predicate). Recorded cohesion exception (durable outbox transaction core) honored:
transaction boundaries stay visible and untouched; method-level naming/step-down only,
no extraction from transaction bodies.

- C1 — `coalesce_milestone!` returns literal `true`/`false`, which trips pre-existing
  Naming/PredicateMethod (line 253) while its single call site (`next :coalesced if …`)
  only tests truthiness. Return the live row (or nil via bare `return`) instead.
  Provable in-file: grep shows the only caller is line 44 of this same file; truthiness
  is unchanged (hash truthy / nil falsy). Clears one pre-existing offense without
  suppression. LOW RISK → APPLY (revert if the cop disagrees). [applied; cop cleared]
- C2 — locals named `missing` hold an EXISTENCE PROBE that is truthy when the row was
  FOUND (`claim_delivery` line 79 `SELECT 1…`; `bind_journal_effect` line 176
  `SELECT effect_key…`). The name states the opposite of what holds. `append_delivery`
  already names the identical probe `existing` (line 49). Rename the two locals
  `missing` → `existing` (`next :missing unless existing` keeps the public result
  symbol). Purely lexical, zero behavior change. LOW RISK → APPLY.
- C3 — pre-existing autocorrectable Layout/LineLength at 287 (182 chars: the
  `rewrite_milestone!` bind array). Mechanical reformat of one line, no semantic
  content. LOW RISK → APPLY (autocorrect first, manual equivalent after it misfired).
- C4 — Metrics/MethodLength on `append_delivery` (26/20): REJECT — body is the durable
  append transaction core; recorded exception forbids extraction, boundary must remain
  visible as one unit. Pre-existing accepted debt.
- C5 — Metrics/ParameterLists on `mark_delivery` (6 incl. `receipt:`): REJECT — public
  cross-gem signature delegated by `CommsStore#mark_delivery`; untouchable.
- C6 — normalize `return :conflict` (line 181, inside the bind transaction block) to
  `next`: REJECT — verified against DatabaseKernel#transaction: `result = yield tx` then
  `inject(:before_commit)` then COMMIT; today a non-local `return` skips both injection
  points, `next` would add them to the conflict path and commit an empty IMMEDIATE tx.
  Fault-injection call sequence would differ ⇒ cannot prove byte-preserving; rails say
  leave transaction-boundary flow alone.
- C7 — make `milestone_request_ref` use `.fetch('markup')` instead of `['markup']`:
  REJECT — fetch raises KeyError where `[]` yields nil if a wire ever lacks the key;
  wires are built cross-gem (tamoz-comms gateway/sink), key presence not provable
  repo-wide from here.
- C8 — split `milestone_rows(txn, surface_id, conversation_id, request_ref,
  status_clause = '')` to drop the default-SQL-clause parameter: REJECT — extraction/
  reshaping inside the milestone seam the recorded exception protects; both callers are
  in-file but the win is cosmetic while risk lands on the coalesce path.
- C9 — `statuses.map { '?' }.join(', ')` in `outbox_rows`: no defect; placeholder build
  is local, obvious, and rubocop-clean. No change.
