# Combined pass — rank 57 gems/tamoz-agent-improvement/lib/tamoz/agent/improvement/promotion.rb

Verdict: COMMITTED-CANDIDATES

## Phase 1 — candidates (written before any edit)

Baseline: `ruby -c` OK; `rubocop --cache false` 0 offenses (BEFORE=0). File carries
recorded `.rubocop_todo.yml` Exclude debt for Metrics/AbcSize, Metrics/CyclomaticComplexity,
Metrics/MethodLength — which is why `promote` (~39 body lines) and `rollback` (~59) pass today.
TODO file is forbidden to this pass, so debt removal is flagged, not done.

- C1 — `#promote` step-down: the `@registry.record(...)` call + result literal (18 lines)
  sit one abstraction level below the gate sequence. Extract verbatim into private
  `record_promotion`. Pure code motion: same order, same kwargs/values (`body` passed for the
  seal), same returned hash bytes. Behavior-preserving by construction; public kwargs API of
  `#promote` untouched (only caller repo-wide incl. send/__send__/public_send grep:
  test/improvement_candidate_test.rb).
- C2 — `#promote` step-down: inline decision refusal (`unless decision.fetch("passed") … end`)
  is policy detail between two guard calls. Extract into private `assert_decision_passed!`
  with byte-identical raise message. Same position in the sequence ⇒ same first-error behavior.
- C3 — `#rollback` step-down: ~59-line body vs hard ceiling 30; needs two extractions
  (refusal chain + record tail), ≈80 changed lines alone. REJECTED under this pass's
  ~60-changed-line rail once C1+C2 are applied; left as candidate for escalation.
- C4 — §3.1 verb-family renames of private guards (`assert_evidence_resolves!`→`verify_*`,
  `assert_forward!`/`assert_one_live_heuristic!`→`enforce_*`). REJECTED: the surrounding
  pipeline lexicon is consistently `assert_*!` as the raise-guard spelling
  (`EvaluationReport.assert_human_gate!`, `Heuristic#assert_bounded!`,
  `Provenance#assert_complete!` — all external). Renaming only this file's privates would ADD
  a second spelling, not remove one.
- C5 — flip the double negative in `successor_version`; dedupe the rpartition/ordinal grammar
  shared with `assert_forward!`. REJECTED: churn without intent gain; each site states the
  version grammar where it uses it.
- C6 — comment reduction. REJECTED: existing comments are §11 "why" material (seal-vs-MAC
  rationale, DR-1/invariant references, serialization notes), not narration.

Applied: C2 (extract assert_decision_passed!), C1 (extract record_promotion)
Rejected: C3 (budget rail ~60 changed lines; candidate for two-agent pipeline); C4 (breaks local assert_* lexicon consistency); C5 (churn without intent gain); C6 (comments are §11 why-material)
Diff stat: gems/tamoz-agent-improvement/lib/tamoz/agent/improvement/promotion.rb | 54 ++--- : 31 insertions(+), 23 deletions(-) (inside the ~60-line rail)
Commands:
- ruby -c promotion.rb → Syntax OK
- rubocop --cache false promotion.rb → BEFORE 0 offenses / AFTER 0 offenses (a transient
  Metrics/ClassLength 251/250 from my own net lines was fixed by compacting my call-site and
  def signature to single lines ≤120 cols, not suppressed)
- grep -l "Promotion" test/*.rb → healing_failure_contract_test.rb, healing_matrix_test.rb,
  improvement_candidate_test.rb; only improvement_candidate_test.rb instantiates
  Improvement::Promotion (the other two match Healing::PromotionGate/SelfPromotionError)
- ruby -Itest test/improvement_candidate_test.rb → 14 runs, 284 assertions, 0 failures,
  0 errors, 0 skips
Deviations: no commits by me (per brief), but parallel clean-code-loop committers active in
this tree swept my finished owned-file edit into their commit 7577f93
("refactor(tamoz-agent-improvement): clean-code pass on promotion.rb") at 15:26:10 — verified
that commit contains ONLY promotion.rb with exactly my 31/23 diff; working tree == HEAD ==
gated content.
FLAG (owner decisions): promotion.rb now plausibly clears its recorded TODO debt for
Metrics/MethodLength on #promote (#rollback still over); .rubocop_todo.yml is out of my
ownership — quality-program owner may regenerate. C3 (rollback step-down, ~59-line body vs
hard ceiling 30) recommended for the two-agent pipeline.

## Notes

- Policy binding honored: method-level naming/step-down only; promotion criteria semantics
  (gate order, decision refusal, provenance binding, liveness count, forward-only versions)
  untouched — C1/C2 move statements verbatim without reordering.
- Error/message bytes preserved: raise strings moved character-for-character.
