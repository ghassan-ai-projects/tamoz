# Combined pass — rank 95 gems/tamoz-sqlite/lib/tamoz/sqlite/comms_store_rows.rb
Verdict: COMMITTED-CANDIDATES
Applied: C1 (outbox_binds reuses wire_time_ms for expires_at instead of duplicating its body inline); C2 (drop redundant .freeze on Regexp literal REQUEST_REF_PATTERN — regex literals are frozen on the pinned Ruby 3.3, verified empirically; also clears the file's one pre-existing Style/RedundantFreeze offense)
Rejected: C3 (use wire_time_ms inside binding_binds/route_binds for fetch'd required values — diverges on the error path when a key is present but non-string: Time.parse raises TypeError vs wire_time_ms returning nil, and error/message bytes are untouchable); C4 (rename open_reservations to state it returns a slot total — callers live in read-only comms_outbox.rb/comms_store.rb, rename not provable in-file); C5 (drop unused _now params of binding_binds/route_binds — external callers in read-only files pass now positionally); C6 (simplify the BACKEND_TIME_SQL heredoc-squash idiom — pinned SQL bytes sent to sqlite, rewrite risks whitespace drift for zero gain)
Diff stat: gems/tamoz-sqlite/lib/tamoz/sqlite/comms_store_rows.rb | 4 ++-- (2 insertions, 2 deletions; net 0 LOC)
Commands: ruby -c → Syntax OK · rubocop --cache false <file> → BEFORE 1 offense / AFTER 0 offenses · ruby -Itest test/comms_lifecycle_test.rb → 9 runs, 68 assertions, 0 failures, 0 errors · ruby -Itest test/comms_admission_test.rb → 15 runs, 51 assertions, 0 failures, 0 errors · ruby -Itest test/progress_projection_test.rb (outbox append path through outbox_binds) → 12 runs, 360 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

## Candidate audit (written before any edit)

Module context: shared row-shape/SQL surface included by comms_store.rb, comms_outbox.rb,
comms_routes.rb; recorded cohesion exception applies (durable transaction-core family) —
method-level naming/step-down only, no extraction. Row shapes and wire bytes pinned.

- C1 APPLY — `outbox_binds` line `delivery_wire['expires_at'] && now_ms(Time.parse(delivery_wire['expires_at']))` is character-for-character the body of `wire_time_ms` defined 54 lines up. Replacing the inline copy with `wire_time_ms(delivery_wire['expires_at'])` preserves the nil short-circuit exactly.
- C2 APPLY — `REQUEST_REF_PATTERN = /.../.freeze`: RuboCop flags Style/RedundantFreeze; `/x/.frozen? == true` on Ruby 3.3.11 (verified), so `.freeze` is a same-object no-op.
- C3 REJECT — see above; both reads are equally clear at call level.
- C4 REJECT — cross-file rename, forbidden scope.
- C5 REJECT — signature change with external positional callers.
- C6 REJECT — working pinned construct, churn risk.
- Names audited against §3 vocabulary: predicates end `?` (`identical_inbound_row?`), mutations bang (`insert_inbound!`, `record_inbound_conflict!`, `upsert_*!`), no get_/set_, bind builders share the `_binds` suffix; all methods ≤20 lines; comments are §11 why-comments (invariants 1/57, plan 02) — kept.
