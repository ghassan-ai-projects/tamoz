# Fix report — rank 30 gems/tamoz-agent-profile/lib/tamoz/agent/profile.rb

Applied: C1 (`from_authority` split into `enforce_authority_shape!` + `build_synthetic_document`; validator sequence and all message bytes verbatim, shape step returns the digest); C2 (`validate_schema!` version refusals extracted as `enforce_schema_version!`, placed at the same call site); C3 (`load` adoption gate extracted as `enforce_activated!`, AdoptionError message byte-identical); C4 (snapshot literal now calls `pinned_model_roles`; transform body moved verbatim); C5 (double-negative guard replaced by `refuse_evidence_only_activation!(path, env:) unless suggestion`; evidence-only message byte-identical); C6 (safe_parse docstring corrected — tags/alias-budget/duplicate-key claims belong to YamlScanner, verified against yaml_scanner.rb; comment-only) | Rejected: FLAG-1 owner-only (`hash.delete("adoption")` left in place per binding — suspected legacy tolerance, removal changes behavior, OWNER decision); FLAG-2 owner-only (boolean keyword switches `open_verified(permissions:)` / `load_document(suggestion:)` / `preview(suggestion:)` are cross-gem CLI/registry/evals surface — within_file: no)
Diff stat: gems/tamoz-agent-profile/lib/tamoz/agent/profile.rb | 124 ++++++++++++--------- | 1 file changed, 74 insertions(+), 50 deletions(-) → net +24
Commands:
1. ruby -c <file> before=Syntax OK / after=Syntax OK — pass
2. rubocop --cache false BEFORE = "1 file inspected, no offenses detected" (baseline 0); mid-edit my new lines introduced 42 offenses (Lint/IneffectiveAccessModifier ×41 from an instance `private` followed by singleton defs + Lint/UselessConstantScoping ×2 + Style/GuardClause ×1) — all FIXED in code, none suppressed, no todo edits; AFTER = no offenses detected (0 = baseline, count did not increase) — pass
3a. ruby -Itest test/agent_profile_machinery_test.rb — 24 runs, 230 assertions, 0 failures, 0 errors — pass
3b. ruby -Itest test/agent_profile_test.rb — 44 runs, 130 assertions, 0 failures, 0 errors — pass
3c. ruby -Itest test/agent_cli_profile_test.rb — 10 runs, 84 assertions, 0 failures, 0 errors — pass
   Note: the first machinery run errored 18/24 while a PARALLEL fixer's edit to gems/tamoz-evals/lib/tamoz/evals/benchmark/openclaw_comms_fixture.rb was mid-flight in the shared tree (broke test_helper's require for every suite; file not mine, not touched by me). Clean on rerun once that fixer's edit settled.
4. Extra equivalence evidence (not a gate): A/B probe of HEAD copy vs edited file through 19 refusal/order cases (from_authority mapping/unknown/missing/digest/refusal-order/deep-validator chain, validate_schema! empty/no-profile/version string/newer/older/refusal-order/unknown-profile-field, load_document suggestion refusal, preview allowance, bad-version-through-file) + canonical_digest — outputs byte-identical after normalizing mktmpdir paths (/tmp/tamoz_profile_probe.rb, HEAD copy under /tmp/tamoz-probe-head/, repo untouched by it)
Deviations: C4 applied as ONE helper instead of the proposed two-method split (`pinned_model_roles` alone — simple-over-complicated; the merge line still shows name-only recording), and it is a public instance method because this class has no instance-private convention and a `private` marker poisoned every following `def self.` with Lint/IneffectiveAccessModifier; `enforce_schema_version!` tail uses `return if version == SCHEMA_VERSION` guard (Style/GuardClause after extraction; message bytes unchanged); P17 egress comment moved with its code into `build_synthetic_document` and tightened to three lines. Owner notes: `hash.delete("adoption")` present and untouched (suspected pre-AdoptionRegistry legacy tolerance — candidate for fail-closed removal, owner call); boolean keyword flags remain owner-scope. Working tree carries only my owned file's change from me; other modified files (jcs.rb, openclaw_comms_fixture.rb, sqlite_convergence_probe.rb, verifier.rb, executor.rb) belong to parallel fixers.
