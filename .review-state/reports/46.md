# Fix report — rank 46 gems/tamoz-evals/lib/tamoz/evals/benchmark/scenario_driver.rb
Applied: C1 (step-down `call`: extracted `oracle_arguments`, `scored_result`, plus `prepare_run`/`drive_scenario` after first cut still breached AbcSize; all three Metrics disables DELETED, not suppressed); C2 (initialize: single conditional push of :worker_until_effect!, :worker_subprocess!, :prepare_changes! via existing `restart_scenario?`); C3 (`materialize_fixture` → `prepare_fixture`, split into `write_status_fixture` vs `prepare_restart_fixture`; deletion timing and `'restart_fixture_already_exists'` bytes unchanged); C4 (`restart_task` emits `JSON.generate(definition.fetch('setup').fetch('fixture'))` — byte-identical, RESTART_FIXTURE insertion order matches old manual key list); C5 (`restart_fixture_owned?` keeps only the content comparison — digest conjunct was implied; ownership-marker comment trimmed to one line); C6 (`receipt_digest` reduced to the explicit-key fallback chain; output unwrap + sha256 scrape moved to `output_digest`; short-circuit semantics preserved); C7 (shared private `OracleResult` module — `result(metrics:, hard_zero:, reasons:)` envelope + `hard_zero_reason(name, status)` — included in both oracle singleton classes; emitted hashes/reason arrays byte-identical incl. ordering)
Rejected: none of C1–C7 | Flags kept (owner decisions, NOT applied): `.implemented?`/`.definitions` zero-caller removal; `.oracle` module-function misname; file-size split.
Flag proof (own repo-wide scan, live tree only, `.worktrees/` copies excluded): `implemented?` and plural `.definitions` have ZERO callers in gems/, apps/, bin/, lib/, test/; no `send`/`public_send`/string-name variants; `Harness::SQLiteScenarioDriver` is an unrelated class (no delegation). Singular `.definition` IS called (internal initialize + scenario_driver_test.rb:187); `.oracle` IS called by tests (scenario_driver_test.rb ×5) so renaming it reaches outside my file.
Diff stat: 1 file changed, 89 insertions(+), 65 deletions(-)
Commands:
- `ruby -c <owned>` → Syntax OK (before and after)
- `rubocop --cache false <owned>` BEFORE → 0 offenses; AFTER → 0 offenses (gate holds; the ~1000-offense repo-wide legacy debt does not touch this file)
- `ruby gems/tamoz-evals/test/scenario_driver_test.rb` → 10 runs, 39 assertions, 0 failures, 0 errors, 0 skips (covers both oracles' envelopes/reason strings incl. failed/unknown paths, receipt sha256 scraping, restart fixture reuse/delete timing, unknown-fixture + symlink blocking with exact reason bytes, exact create_file argument JSON in enqueued task, adapter seam requirements)
- Not run by design: root sqlite_raw_oracle / convergence probe / sqlite_scenario_driver lanes are on the known-red SLOW_TESTS list; no other live code references ScenarioDriver.
Deviations:
1. C1 required two extra extractions beyond the candidate text (`prepare_run`, `drive_scenario`) because the proposed shape measured Metrics/AbcSize 22.11/20 on `call`; fixed structurally, never suppressed. Sequence, guard/passthrough, rescue, and all message bytes preserved.
2. Working tree contains pre-existing modifications to other files (docs/README.md, session_adaptive.rb, session_context_controls.rb, scoreboard.rb, server_config.rb) made before this session started — untouched, not mine; my changes are confined to the owned file.
3. Net LOC +24 (437 → 461): cost of the shared OracleResult module and named steps; ClassLength/file split remains flagged out-of-scope per binding.
