# Combined pass — rank 145 gems/tamoz-concurrency/lib/tamoz/concurrency/drain.rb

Verdict: COMMITTED-CANDIDATES
Applied: C1 (`depths` step-down: delegate to existing `lane_depths` instead of duplicating the projection); C2 (extract `validate_configuration!(limits, batch_size, interval)` from `initialize` — verbatim code motion, exception order/messages byte-identical)
Rejected: C3 (`take_batch` raw `@closed` reads → `drain_closed?`: cosmetic swap of unambiguous ivar reads inside the lock owner; style churn, no intent gain); C4 (delete `drain_closed?`/`drain_disabled?` as in-file-uncalled: they ARE the documented subclass contract — AsyncExporter#record/#health L36–37/55, Journal#accept_or_drop/#health L236–237/205, test Collector#store L27 — removal breaks cross-gem API); C5 (rename accept/shift_lane/lane_depths/synchronize/disable_drain!: out-of-file subclass callers, cross-gem API untouchable); C6 (close's `rescue StandardError → nil`: deliberate join-grace swallow, error semantics pinned); C7 (restructure drain_loop's double `handle_loop_error` path when the default re-raises: concurrency/error ordering explicitly pinned; loop already reads take→deliver→account); C8 (merge flush/close wait loops: different contracts — bounded drain vs broadcast+join-with-grace; a mode flag would violate §4 no-boolean-params)
Diff stat: 1 file changed, 16 insertions(+), 12 deletions(-)
Commands: ruby -c → OK; rubocop --cache false → BEFORE 0 offenses / AFTER 0; ruby -Itest test/concurrency_drain_test.rb → 7 runs, 36 assertions, 0 failures, 0 errors; ruby -Itest test/public_api_test.rb → 3 runs, 1015 assertions, 0 failures, 0 errors; ruby -Itest test/observability_runtime_test.rb → 12 runs, 49 assertions, 0 failures, 0 errors; ruby -Itest test/otel_test.rb → 6 runs, 17 assertions, 0 failures, 0 errors
Deviations: none
FLAG (owner decisions): none

## Analyzer candidate list (written before any edit)

Audit basis: repo-wide caller map — `Drain` is subclassed cross-gem by
`OTel::AsyncExporter` (tamoz-otel) and `Observability::Journal` (tamoz-observability)
plus `test/concurrency_drain_test.rb::Collector`; the subclass-facing private contract
in active use is `synchronize`, `accept`, `drain_closed?`, `drain_disabled?`,
`lane_depths`, `shift_lane`, `monotonic_now`, `disable_drain!`, `batch_size` — none
renamable or removable in-file. Public surface pinned by `test/public_api_test.rb`
(`"Tamoz::Concurrency::Drain" => {}`). CODING_STANDARD §3/§3.1/§4/§6/§11. Binding note:
concurrency/error ordering pinned; method-level naming/step-down only.
BEFORE state: `ruby -c` OK; `rubocop --cache false` = 0 offenses.

- C1 [APPLY] `depths` (L45–47) duplicates `lane_depths`'s body verbatim
  (`@queues.transform_values(&:length)` twice, both under the same mutex discipline).
  Delegate: `synchronize { lane_depths }`. Same expression, same lock, same return —
  removes the one duplicated state projection and gives the private helper its
  base-class caller.
- C2 [APPLY] `initialize` (L13–37, ~22 code lines > 20 normal ceiling) mixes levels:
  lane-key normalization, three ordered validation guards, state wiring, thread spawn.
  Move the three unless/raise blocks verbatim into private
  `validate_configuration!(limits, batch_size, interval)` (validate_ per §3.1: bad
  input named by field; matches repo idiom, e.g. Journal#validate!). Raises stay in
  order lanes → batch_size → interval with byte-identical messages; pure motion, no
  lock interaction, called between normalization and assignment.
- C3 [REJECT] take_batch L144/L147: replace raw `@closed` with `drain_closed?`.
- C4 [REJECT] Delete drain_closed?/drain_disabled? (zero in-file callers).
- C5 [REJECT] Rename any subclass-contract helper (accept, shift_lane, …).
- C6 [REJECT] Touch close() error swallowing.
- C7 [REJECT] Restructure drain_loop rescue/ensure ordering.
- C8 [REJECT] Merge flush/close waiting logic behind a flag.
